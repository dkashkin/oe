{"id": "2e14b42d-7601-45f3-ac18-2857b928d0fc", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "cf24f5ae-8c77-4508-807b-541dfb2753b7", "generation": 5, "timestamp": 1772755701.0358148, "iteration_found": 134, "metrics": {"score": 0.9784956, "combined_score": 0.9784956, "compile_success": 1.0, "correctness": 1, "performance_score": 0.966786, "adaptability_score": 0.99606, "avg_time": 0.0034355}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          if (a[i] < a[i - 1]) {\n              int v = a[i], j = i;\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\nwith:\n  // Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertio...\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      int i = 1;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i < n) quicksort(arr, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i == n) return;\n      if (i == 1) {\n          while (i < n && arr[i - 1] >= arr[i]) ++i;\n          if (i == n) {\n              for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n              return;\n          }\n      }\n      quicksort(arr, 0, n - 1);\n  }", "parent_metrics": {"score": 0.9635662, "combined_score": 0.9635662, "compile_success": 1.0, "correctness": 1, "performance_score": 0.944007, "adaptability_score": 0.992905, "avg_time": 0.0059314}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9636\n- Feature coordinates: score=0.96, performance_score=0.94, adaptability_score=0.99\n- Focus areas: - Fitness declined: 0.9785 \u2192 0.9636. Consider revising recent changes.\n- Exploring score=0.96, performance_score=0.94, adaptability_score=0.99 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00100393, 0.0106753, 0.000492751, 0.00546911, 0.00092816, 0.0241228, 0.00026487, 0.00502074, 0.0009264, 0.0104099]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          if (a[i] < a[i - 1]) {\n              int v = a[i], j = i;\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      int i = 1;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i < n) quicksort(arr, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i == n) return;\n      if (i == 1) {\n          while (i < n && arr[i - 1] >= arr[i]) ++i;\n          if (i == n) {\n              for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n              return;\n          }\n      }\n      quicksort(arr, 0, n - 1);\n  }\n- Metrics: score: 0.9785, combined_score: 0.9785, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9668, adaptability_score: 0.9961, avg_time: 0.0034\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertio...\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i == n) return;\n  ... (9 more lines)\nwith:\n  // Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertio...\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 18) {\n          int lt = l, gt = r, i = l, m = l + (r - l) / 2, p;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; i++) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && a[i - 1] <= a[i]) i++;\n      if (i == n) return;\n  ... (9 more lines)\n- Metrics: score: 0.9785, combined_score: 0.9785, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9668, adaptability_score: 0.9960, avg_time: 0.0034\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i == n) return;\n      if (i == 1) {\n  ... (8 more lines)\n- Metrics: score: 0.9787, combined_score: 0.9787, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9671, adaptability_score: 0.9961, avg_time: 0.0034\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9789)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9789), Performs well on combined_score (0.9789), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9674), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 2 (Score: 0.9787)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Compact, efficient, and pattern-adaptive\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9787), Performs well on combined_score (0.9787), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9671), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 3 (Score: 0.9785)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 18) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2, p;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; i++) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && a[i - 1] <= a[i]) i++;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && a[i - 1] >= a[i]) i++;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(a[l++], a[r--]);\n            return;\n        }\n    }\n    quicksort(a, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9785), Performs well on combined_score (0.9785), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9668), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0034)\n\n### Program 4 (Score: 0.9785)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9785), Performs well on combined_score (0.9785), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9668), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9784)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9784)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort with adaptive pre-checks and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9785)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 24) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2, p;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; i++) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && a[i - 1] <= a[i]) i++;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && a[i - 1] >= a[i]) i++;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(a[l++], a[r--]);\n            return;\n        }\n    }\n    quicksort(a, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9634, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Hybrid 3-way QuickSort: Optimized with pre-scan and tuned thresholds\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i < n) quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9785, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 18) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2, p;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; i++) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && a[i - 1] <= a[i]) i++;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && a[i - 1] >= a[i]) i++;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(a[l++], a[r--]);\n            return;\n        }\n    }\n    quicksort(a, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9789, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9630, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 24) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    quicksort(arr, 0, n - 1);\n    for (int i = 1; i < n; i++) {\n        int v = arr[i], j = i;\n        while (j > 0 && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n        arr[j] = v;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        if (a[i] < a[i - 1]) {\n            int v = a[i], j = i;\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    int i = 1;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i < n) quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness declined because it simplified the `adaptive_sort` logic, removing the reverse-order pre-scan that significantly boosts performance on descending data patterns. To restore and improve the fitness score, I will re-integrate the reverse-scan logic into `adaptive_sort` and refine the `quicksort` implementation to match the most successful high-performing programs (like Program 1), which use a more efficient variable declaration pattern and a consistent 3-way partitioning threshold.\n\nThese changes should specifically improve the `adaptability_score` while maintaining high performance across various data distributions.\n\n<<<<<<< SEARCH\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        if (a[i] < a[i - 1]) {\n            int v = a[i], j = i;\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n=======\n// Optimized Hybrid 3-way QuickSort: Median-of-three, Dijkstra partitioning, and inlined insertion sort\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int lt = l, gt = r, i = l, m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    int i = 1;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i < n) quicksort(arr, 0, n - 1);\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    if (i == 1) {\n        while (i < n && arr[i - 1] >= arr[i]) ++i;\n        if (i == n) {\n            for (int l = 0, r = n - 1; l < r; ) std::swap(arr[l++], arr[r--]);\n            return;\n        }\n    }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00098761, 0.0102173, 0.00050936, 0.00557687, 1.293e-05, 0.00012866, 0.00029237, 0.00525687, 0.00092635, 0.0104466], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}