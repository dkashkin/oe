{"id": "04856440-9454-496e-9429-35362c44775a", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Compact, efficient, and pattern-adaptive\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "157d94d9-e2ba-40cb-8ddd-981de4d0c212", "generation": 5, "timestamp": 1772751686.8764677, "iteration_found": 34, "metrics": {"score": 0.9629521999999999, "combined_score": 0.9629521999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.943173, "adaptability_score": 0.992621, "avg_time": 0.00602507}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partition, and guarded insertion sort\n  void insertion_sort(std::vector<int>& a, int l, int r) {\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      insertion_sort(a, l, r);\n  }\n  \n  ... (4 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Optimized Hybrid 3-way QuickSort: Compact, efficient, and pattern-adaptive\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n  }\n  // EVOLVE-BLOCK-END", "parent_metrics": {"score": 0.9561672000000001, "combined_score": 0.9561672000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.933318, "adaptability_score": 0.990441, "avg_time": 0.00714457}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9562\n- Feature coordinates: score=0.96, performance_score=0.93, adaptability_score=0.99\n- Focus areas: - Fitness unchanged at 0.9562\n- Exploring score=0.96, performance_score=0.93, adaptability_score=0.99 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00101855, 0.0112297, 0.00054779, 0.00621935, 0.00117607, 0.0335145, 0.00030465, 0.00550017, 0.0009871, 0.0109478]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Optimized Hybrid 3-way QuickSort: Compact, efficient, and adaptive\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 16) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n  }\n  // EVOLVE-BLOCK-END\nwith:\n  // EVOLVE-BLOCK-START\n  // Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partition, and guarded insertion sort\n  void insertion_sort(std::vector<int>& a, int l, int r) {\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          if (v < a[j - 1]) {\n              while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n              a[j] = v;\n          }\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 20) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      insertion_sort(a, l, r);\n  }\n  \n  ... (4 more lines)\n- Metrics: score: 0.9562, combined_score: 0.9562, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9333, adaptability_score: 0.9904, avg_time: 0.0071\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Hybrid 3-way QuickSort: Optimizes for duplicates, nearly sorted, and small arrays\n  void insertion_sort(std::vector<int>& a, int l, int r) {\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 16) {\n          int m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m], lt = l, gt = r, i = l;\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      insertion_sort(a, l, r);\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  // Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 24) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      quicksort(arr, 0, n - 1);\n      for (int i = 1; i < n; i++) {\n          int v = arr[i], j = i;\n          while (j > 0 && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n          arr[j] = v;\n      }\n  }\n- Metrics: score: 0.9630, combined_score: 0.9630, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9431, adaptability_score: 0.9929, avg_time: 0.0060\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Hybrid 3-way QuickSort: Optimizes for duplicates, nearly sorted, and small arrays\n  void insertion_sort(std::vector<int>& a, int l, int r) {\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 16) {\n          int m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m], lt = l, gt = r, i = l;\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      insertion_sort(a, l, r);\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  // Optimized Hybrid 3-way QuickSort: Compact, efficient, and adaptive\n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 16) {\n          int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m];\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n  }\n- Metrics: score: 0.9631, combined_score: 0.9631, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9433, adaptability_score: 0.9928, avg_time: 0.0060\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9636)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Optimized for performance and patterns\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9636), Performs well on combined_score (0.9636), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9440), Performs well on adaptability_score (0.9930), Performs well on avg_time (0.0059)\n\n### Program 2 (Score: 0.9631)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Compact, efficient, and adaptive\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 16) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9631), Performs well on combined_score (0.9631), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9433), Performs well on adaptability_score (0.9928), Performs well on avg_time (0.0060)\n\n### Program 3 (Score: 0.9630)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 24) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    quicksort(arr, 0, n - 1);\n    for (int i = 1; i < n; i++) {\n        int v = arr[i], j = i;\n        while (j > 0 && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n        arr[j] = v;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9630), Performs well on combined_score (0.9630), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9431), Performs well on adaptability_score (0.9929), Performs well on avg_time (0.0060)\n\n### Program 4 (Score: 0.9562)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partition, and guarded insertion sort\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9562), Performs well on combined_score (0.9562), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9333), Performs well on adaptability_score (0.9904), Performs well on avg_time (0.0071)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9631, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Compact, efficient, and adaptive\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 16) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9636, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Optimized for performance and patterns\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9630, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Compact 3-way QuickSort with a final insertion sort pass for speed and pattern adaptability\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 24) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    quicksort(arr, 0, n - 1);\n    for (int i = 1; i < n; i++) {\n        int v = arr[i], j = i;\n        while (j > 0 && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n        arr[j] = v;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partition, and guarded insertion sort\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will optimize the sorting algorithm by simplifying the insertion sort logic and refining the hybrid approach. Specifically, we will:\n1.  **Remove the branchy `if` guard** in the insertion sort pass, as it can sometimes lead to branch misprediction overhead on random data.\n2.  **Inline the insertion sort logic** into the `quicksort` function to reduce function call overhead and simplify the code, which also addresses the goal of reducing code length.\n3.  **Maintain the 3-way Dijkstra partitioning** and median-of-three pivot selection, which are excellent for handling duplicate elements and nearly-sorted patterns (high adaptability).\n4.  **Keep the threshold at 20**, which was found to be highly effective in the best-performing programs (e.g., Program 1 with a score of 0.9636).\n\nThese changes aim to boost the `performance_score` while maintaining the high `adaptability_score` of 0.99.\n\n<<<<<<< SEARCH\n// EVOLVE-BLOCK-START\n// Refined Hybrid 3-way QuickSort: Median-of-three, Dijkstra partition, and guarded insertion sort\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        if (v < a[j - 1]) {\n            while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n            a[j] = v;\n        }\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n=======\n// EVOLVE-BLOCK-START\n// Optimized Hybrid 3-way QuickSort: Compact, efficient, and pattern-adaptive\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 20) {\n        int m = l + (r - l) / 2, lt = l, gt = r, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m];\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00098741, 0.0103251, 0.00050102, 0.00547834, 0.00089866, 0.0253552, 0.00031562, 0.00514564, 0.00091731, 0.0103264], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}