{"id": "a6bf3015-cd5e-4c75-9ad0-fae593bfc2d0", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = static_cast<int>(arr.size());\n    if (n <= 1) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0, j = n - 1; i < j; ++i, --j) std::swap(arr[i], arr[j]);\n        return;\n    }\n    if (n < 64) {\n        insertion_sort_range(arr, 0, n - 1);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n\n// Implement the single-argument version expected by headers\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        \n        // Optimize recursion: recurse on smaller interval first, loop on larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n    int m = low;\n    for (int i = low + 1; i <= high; ++i) if (arr[i] < arr[m]) m = i;\n    std::swap(arr[low], arr[m]);\n    for (int i = low + 2; i <= high; ++i) {\n        int k = arr[i], j = i - 1;\n        while (arr[j] > k) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "8ebc2a86-2edd-4fa8-8f0a-90de411b4168", "generation": 3, "timestamp": 1772751245.4566875, "iteration_found": 21, "metrics": {"score": 0.9281446000000001, "combined_score": 0.9281446000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.895097, "adaptability_score": 0.977716, "avg_time": 0.0117197}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) return;\n      // For small arrays, use insertion sort directly for better performance\n      if (arr.size() < 48) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = static_cast<int>(arr.size());\n      if (n <= 1) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0, j = n - 1; i < j; ++i, --j) std::swap(arr[i], arr[j]);\n          return;\n      }\n      if (n < 64) {\n          insertion_sort_range(arr, 0, n - 1);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n          if (high - low < 20) {\n              insertion_sort_range(arr, low, high);\n              break;\n          }\n  \n          int p = partition(arr, low, high);\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) {\n              insertion_sort_range(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      // Median-of-3 pivot selection\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n  \n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high];\n  \n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              i++;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nChange 4: Replace:\n  void insertion_sort_range(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i];\n          int j = i - 1;\n          while (j >= low && arr[j] > key) {\n              arr[j + 1] = arr[j];\n              --j;\n          }\n          arr[j + 1] = key;\n      }\n  }\nwith:\n  void insertion_sort_range(std::vector<int>& arr, int low, int high) {\n      if (low >= high) return;\n      int m = low;\n      for (int i = low + 1; i <= high; ++i) if (arr[i] < arr[m]) m = i;\n      std::swap(arr[low], arr[m]);\n      for (int i = low + 2; i <= high; ++i) {\n          int k = arr[i], j = i - 1;\n          while (arr[j] > k) {\n              arr[j + 1] = arr[j];\n              --j;\n          }\n          arr[j + 1] = k;\n      }\n  }", "parent_metrics": {"score": 0.9253586, "combined_score": 0.9253586, "compile_success": 1.0, "correctness": 1, "performance_score": 0.890351, "adaptability_score": 0.97787, "avg_time": 0.0123152}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9254\n- Feature coordinates: score=0.93, performance_score=0.89, adaptability_score=0.98\n- Focus areas: - Fitness improved: 0.0000 \u2192 0.9254\n- Exploring score=0.93, performance_score=0.89, adaptability_score=0.98 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00096306, 0.0108749, 0.0005377, 0.00596632, 0.00046942, 0.00555844, 0.00838431, 0.0792129, 0.00087541, 0.01031]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 15) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\nChange 2: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nChange 3: Replace:\n  // Helper function to detect if array is nearly sorted\n  bool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n      if (arr.size() <= 1) {\n          return true;\n      }\n      int inversions = 0;\n      double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n      for (size_t i = 0; i < arr.size() - 1; ++i) {\n          for (size_t j = i + 1; j < arr.size(); ++j) {\n              if (arr[i] > arr[j]) {\n                  ++inversions;\n                  if (static_cast<double>(inversions) > max_inversions) {\n                      return false;\n                  }\n              }\n          }\n      }\n      return true;\n  }\n  \n  // Helper function for insertion sort (useful for small arrays)\n  void insertion_sort(std::vector<int>& arr) {\n      for (size_t i = 1; i < arr.size(); ++i) {\n          int j = static_cast<int>(i);\n          while (j > 0 && arr[j - 1] > arr[j]) {\n              std::swap(arr[j], arr[j - 1]);\n              --j;\n          }\n      }\n  }\nwith:\n  void insertion_sort(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i], j = i - 1;\n          while (j >= low && arr[j] > key) {\n              arr[j + 1] = arr[j];\n              --j;\n          }\n          arr[j + 1] = key;\n      }\n  }\n- Metrics: score: 0.0000, compile_success: 0.0000, correctness: 0.0000, performance_score: 0.0000, adaptability_score: 0.0000\n- Outcome: Regression in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 15) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n  ... (15 more lines)\nwith:\n  // Forward declarations\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  void insertion_sort_range(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) return;\n      // For small arrays, use insertion sort directly for better performance\n      if (arr.size() < 48) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  // Implement the single-argument version expected by headers\n  void insertion_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n          if (high - low < 20) {\n              insertion_sort_range(arr, low, high);\n              break;\n          }\n  \n  ... (45 more lines)\n- Metrics: score: 0.9254, combined_score: 0.9254, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8904, adaptability_score: 0.9779, avg_time: 0.0123\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 15) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n  ... (15 more lines)\nwith:\n  // Forward declarations for internal helper functions\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  void insertion_sort_range(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) return;\n  \n      // Adaptability: Linear-time scan for sorted or reverse-sorted data\n      bool is_sorted = true, is_reverse = true;\n      for (size_t i = 1; i < arr.size(); ++i) {\n          if (arr[i] < arr[i - 1]) is_sorted = false;\n          if (arr[i] > arr[i - 1]) is_reverse = false;\n      }\n  \n      if (is_sorted) return;\n      if (is_reverse) {\n          size_t i = 0, j = arr.size() - 1;\n          while (i < j) std::swap(arr[i++], arr[j--]);\n          return;\n      }\n  \n      // Performance: Small array cutoff directly to insertion sort\n      if (arr.size() < 56) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n          return;\n      }\n  \n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  ... (70 more lines)\n- Metrics: score: 0.9266, combined_score: 0.9266, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8926, adaptability_score: 0.9775, avg_time: 0.0120\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9266)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations for internal helper functions\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n\n    // Adaptability: Linear-time scan for sorted or reverse-sorted data\n    bool is_sorted = true, is_reverse = true;\n    for (size_t i = 1; i < arr.size(); ++i) {\n        if (arr[i] < arr[i - 1]) is_sorted = false;\n        if (arr[i] > arr[i - 1]) is_reverse = false;\n    }\n\n    if (is_sorted) return;\n    if (is_reverse) {\n        size_t i = 0, j = arr.size() - 1;\n        while (i < j) std::swap(arr[i++], arr[j--]);\n        return;\n    }\n\n    // Performance: Small array cutoff directly to insertion sort\n    if (arr.size() < 56) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\n// Fixed signature for public-facing insertion sort\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays\n        if (high - low < 22) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n\n        int p = partition(arr, low, high);\n\n        // Tail call optimization: recurse on the smaller partition, loop on the larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    // Median-of-three pivot selection\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    int i = low;\n    \n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i++], arr[j]);\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n\n    // Optimization: find min element and place at the start as a sentinel\n    int min_idx = low;\n    for (int i = low + 1; i <= high; ++i) {\n        if (arr[i] < arr[min_idx]) min_idx = i;\n    }\n    std::swap(arr[low], arr[min_idx]);\n\n    // Sentinel allows skipping 'j >= low' check in the inner loop\n    for (int i = low + 2; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9266), Performs well on combined_score (0.9266), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8926), Performs well on adaptability_score (0.9775), Performs well on avg_time (0.0120)\n\n### Program 2 (Score: 0.9254)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n    // For small arrays, use insertion sort directly for better performance\n    if (arr.size() < 48) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\n// Implement the single-argument version expected by headers\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n        if (high - low < 20) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        \n        int p = partition(arr, low, high);\n        \n        // Optimize recursion: recurse on smaller interval first, loop on larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    // Median-of-3 pivot selection\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    \n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            i++;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9254), Performs well on combined_score (0.9254), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8904), Performs well on adaptability_score (0.9779), Performs well on avg_time (0.0123)\n\n### Program 3 (Score: 0.0000)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 15) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i], j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.0000), Performs well on compile_success (0.0000), Performs well on correctness (0.0000), Performs well on performance_score (0.0000), Performs well on adaptability_score (0.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9266, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations for internal helper functions\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n\n    // Adaptability: Linear-time scan for sorted or reverse-sorted data\n    bool is_sorted = true, is_reverse = true;\n    for (size_t i = 1; i < arr.size(); ++i) {\n        if (arr[i] < arr[i - 1]) is_sorted = false;\n        if (arr[i] > arr[i - 1]) is_reverse = false;\n    }\n\n    if (is_sorted) return;\n    if (is_reverse) {\n        size_t i = 0, j = arr.size() - 1;\n        while (i < j) std::swap(arr[i++], arr[j--]);\n        return;\n    }\n\n    // Performance: Small array cutoff directly to insertion sort\n    if (arr.size() < 56) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\n// Fixed signature for public-facing insertion sort\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays\n        if (high - low < 22) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n\n        int p = partition(arr, low, high);\n\n        // Tail call optimization: recurse on the smaller partition, loop on the larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    // Median-of-three pivot selection\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    int i = low;\n    \n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i++], arr[j]);\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n\n    // Optimization: find min element and place at the start as a sentinel\n    int min_idx = low;\n    for (int i = low + 1; i <= high; ++i) {\n        if (arr[i] < arr[min_idx]) min_idx = i;\n    }\n    std::swap(arr[low], arr[min_idx]);\n\n    // Sentinel allows skipping 'j >= low' check in the inner loop\n    for (int i = low + 2; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 15) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i], j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n    // For small arrays, use insertion sort directly for better performance\n    if (arr.size() < 48) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\n// Implement the single-argument version expected by headers\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n        if (high - low < 20) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        \n        int p = partition(arr, low, high);\n        \n        // Optimize recursion: recurse on smaller interval first, loop on larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    // Median-of-3 pivot selection\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    \n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            i++;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the sorting algorithm's performance and adaptability, I've implemented several key optimizations inspired by high-performing hybrid sorting techniques.\n\n### Key Improvements:\n1.  **Linear-Time Monotonicity Check**: Added an $O(n)$ pre-scan in `adaptive_sort` to detect if the data is already sorted or reverse-sorted. This provides a significant boost to the `adaptability_score` for structured datasets and avoids unnecessary $O(n \\log n)$ processing.\n2.  **Sentinel-Based Insertion Sort**: Enhanced `insertion_sort_range` by first finding the minimum element and placing it at the start. This \"sentinel\" removes the need for the boundary check (`j >= low`) in the inner loop, which reduces the number of instructions per iteration and improves performance on small subarrays.\n3.  **Threshold Tuning**: Adjusted the thresholds for transitioning between algorithms. The initial cutoff for small arrays is now 64, and the recursive quicksort cutoff is 24, which aligns better with modern CPU cache behaviors and the overhead of recursive calls.\n4.  **Loop Refinement**: Compacted the `partition` and `adaptive_sort` logic to reduce code complexity while maintaining readability and performance.\n\nThese changes collectively target a higher `performance_score` through micro-optimizations and a higher `adaptability_score` through early-exit patterns for common data distributions.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n    // For small arrays, use insertion sort directly for better performance\n    if (arr.size() < 48) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = static_cast<int>(arr.size());\n    if (n <= 1) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0, j = n - 1; i < j; ++i, --j) std::swap(arr[i], arr[j]);\n        return;\n    }\n    if (n < 64) {\n        insertion_sort_range(arr, 0, n - 1);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n        if (high - low < 20) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        \n        int p = partition(arr, low, high);\n=======\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    // Median-of-3 pivot selection\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    \n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            i++;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n=======\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n=======\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n    int m = low;\n    for (int i = low + 1; i <= high; ++i) if (arr[i] < arr[m]) m = i;\n    std::swap(arr[low], arr[m]);\n    for (int i = low + 2; i <= high; ++i) {\n        int k = arr[i], j = i - 1;\n        while (arr[j] > k) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = k;\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00097904, 0.0110139, 0.00059059, 0.0058219, 1.866e-05, 0.00018679, 0.00829397, 0.0789409, 0.00084193, 0.0105091], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}