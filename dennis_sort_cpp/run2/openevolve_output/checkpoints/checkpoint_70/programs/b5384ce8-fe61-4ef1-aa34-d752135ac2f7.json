{"id": "b5384ce8-fe61-4ef1-aa34-d752135ac2f7", "code": "// Hybrid Adaptive Quick-Insertion Sort\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n-1);\n    else quicksort(a, 0, n-1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (high - low > 24) {\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        int v = a[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n    if (low < high) insertion_sort_range(a, low, high);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "c200f75c-03df-44b6-98b2-6bf33737fd80", "generation": 4, "timestamp": 1772752526.1265543, "iteration_found": 53, "metrics": {"score": 0.9780932, "combined_score": 0.9780932, "compile_success": 1.0, "correctness": 1, "performance_score": 0.966178, "adaptability_score": 0.995966, "avg_time": 0.00350055}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  \n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  ... (32 more lines)\nwith:\n  // Hybrid Adaptive Quick-Insertion Sort\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int l, int h);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i-1]) s = 0;\n          if (a[i] > a[i-1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n-1);\n      else quicksort(a, 0, n-1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (high - low > 24) {\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          int v = a[m], lt = low, gt = high, i = low;\n  ... (22 more lines)", "parent_metrics": {"score": 0.9775398, "combined_score": 0.9775398, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965279, "adaptability_score": 0.995931, "avg_time": 0.00359704}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9775\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9775 \u2192 0.9775\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00096559, 0.010202, 0.0005754, 0.00660432, 1.87e-05, 0.00024586, 0.000300829, 0.00522447, 0.00092113, 0.0109121]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]);\n          return;\n      }\n      if (n < 32) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void insertion_sort(std::vector<int>& a) {\n      insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\nwith:\n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\nChange 3: Replace:\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n          else { quicksort(a, gt + 1, high); high = lt - 1; }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n          else { quicksort(a, gt + 1, high); high = lt - 1; }\n      }\n  }\nChange 4: Replace:\n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; ++i) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; ++i) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; --j; }\n          a[j + 1] = k;\n      }\n  }\nwith:\n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = k;\n      }\n  }\n- Metrics: score: 0.9775, combined_score: 0.9775, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9652, adaptability_score: 0.9958, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Forward declarations\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  void insertion_sort_range(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) return;\n      // For small arrays, use insertion sort directly for better performance\n      if (arr.size() < 48) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  // Implement the single-argument version expected by headers\n  void insertion_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) {\n          insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\nwith:\n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\nChange 3: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n          if (high - low < 20) {\n              insertion_sort_range(arr, low, high);\n              break;\n          }\n  \n          int p = partition(arr, low, high);\n  \n          // Optimize recursion: recurse on smaller interval first, loop on larger\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      // Median-of-3 pivot selection\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n  \n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high];\n  ... (23 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n          else { quicksort(a, gt + 1, high); high = lt - 1; }\n      }\n  }\n  \n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = k;\n      }\n  }\n- Metrics: score: 0.9775, combined_score: 0.9775, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9653, adaptability_score: 0.9959, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n <= 1) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0, j = n - 1; i < j; ++i, --j) std::swap(a[i], a[j]);\n          return;\n      }\n      if (n < 64) { insertion_sort_range(a, 0, n - 1); return; }\n      quicksort(a, 0, n - 1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]);\n          return;\n      }\n      if (n < 32) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n- Metrics: score: 0.9776, combined_score: 0.9776, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9654, adaptability_score: 0.9958, avg_time: 0.0036\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9786)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9786), Performs well on combined_score (0.9786), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9670), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 2 (Score: 0.9776)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]);\n        return;\n    }\n    if (n < 32) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; ++i) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; --j; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9776), Performs well on combined_score (0.9776), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9654), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.9775)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9775), Performs well on combined_score (0.9775), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9653), Performs well on adaptability_score (0.9959), Performs well on avg_time (0.0036)\n\n### Program 4 (Score: 0.9775)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9775), Performs well on combined_score (0.9775), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9652), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0036)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9767)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9770)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 48) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9756)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n <= 1) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0, j = n - 1; i < j; ++i, --j) std::swap(a[i], a[j]);\n        return;\n    }\n    if (n < 64) { insertion_sort_range(a, 0, n - 1); return; }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; ++i) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; --j; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9767, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9254, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\n// Forward declarations\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) return;\n    // For small arrays, use insertion sort directly for better performance\n    if (arr.size() < 48) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\n// Implement the single-argument version expected by headers\nvoid insertion_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        insertion_sort_range(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        // Cutoff to insertion sort for small subarrays (threshold tuned for performance)\n        if (high - low < 20) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        \n        int p = partition(arr, low, high);\n        \n        // Optimize recursion: recurse on smaller interval first, loop on larger\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    // Median-of-3 pivot selection\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    \n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high];\n    \n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            i++;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9786, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9770, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 48) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following changes optimize the hybrid adaptive sorting algorithm to improve its performance score and significantly reduce the code length (addressing the recommendation for simplification). \n\nThe improvements include:\n1.  **Redundant Swap Removal**: In `quicksort`, the pivot is used directly from the median calculation rather than being swapped into `a[low]`, saving several operations in each recursive call.\n2.  **Tail-Recursion Optimization**: The `quicksort` structure is streamlined to prioritize the smaller partition for recursion, while using a more robust while-loop for the larger partition.\n3.  **Threshold Refinement**: The transition to `insertion_sort_range` within `quicksort` is moved out of the main loop to reduce branching overhead.\n4.  **Code Length Simplification**: Comments and redundant braces have been pruned to create a more compact and maintainable implementation while preserving the core adaptive logic.\n\nThe adaptability score is maintained at 1.00 as the algorithm still detects pre-sorted and reverse-sorted data and handles duplicates efficiently with 3-way partitioning.\n\n<<<<<<< SEARCH\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n=======\n// Hybrid Adaptive Quick-Insertion Sort\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n-1);\n    else quicksort(a, 0, n-1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (high - low > 24) {\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        int v = a[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n    if (low < high) insertion_sort_range(a, low, high);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00100252, 0.0104433, 0.00052229, 0.00588921, 1.872e-05, 0.00018744, 0.00026542, 0.00504015, 0.00090317, 0.0107333], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}