{"id": "710ca768-7212-48cf-a1d3-9f267e584fda", "code": "// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h, p;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (r > p) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "c6a6d204-fc39-4fbc-9fe7-c5e5fe0b8be1", "generation": 5, "timestamp": 1772754077.6531324, "iteration_found": 94, "metrics": {"score": 0.9778866000000002, "combined_score": 0.9778866000000002, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965887, "adaptability_score": 0.995886, "avg_time": 0.00353178}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 24) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              int r = a[j];\n              if (r < p) { a[j++] = a[i]; a[i++] = r; }\n              else if (p < r) { a[j] = a[k]; a[k--] = r; }\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; i++) {\n          int v = a[i], j = i - 1;\n          while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 32) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h, p;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          p = a[m];\n          while (j <= k) {\n              int r = a[j];\n              if (r < p) { a[j++] = a[i]; a[i++] = r; }\n              else if (r > p) { a[j] = a[k]; a[k--] = r; }\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n      for (int i = l + 2; i <= h; ++i) {\n          int v = a[i], j = i - 1;\n          while (a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n      }\n  }\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool asc = 1, dsc = 1;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) asc = 0;\n          else if (arr[i] > arr[i - 1]) dsc = 0;\n          if (!asc && !dsc) break;\n      }\n      if (asc) return;\n      if (dsc) {\n          for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size(), i = 1;\n      if (n < 2) return;\n      for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n      if (i == n) return;\n      for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n      if (i == n) {\n          for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }", "parent_metrics": {"score": 0.9782862, "combined_score": 0.9782862, "compile_success": 1.0, "correctness": 1, "performance_score": 0.966475, "adaptability_score": 0.996003, "avg_time": 0.00346884}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9783\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9784 \u2192 0.9783. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00098154, 0.0104345, 0.00048041, 0.00556547, 1.874e-05, 0.00024972, 0.00027554, 0.00515827, 0.00090681, 0.0106174]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  \n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 32) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              if (a[j] < p) std::swap(a[i++], a[j++]);\n              else if (a[j] > p) std::swap(a[j], a[k--]);\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; i++) {\n          int v = a[i], j = i - 1;\n          while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n  ... (18 more lines)\nwith:\n  // Adaptive Hybrid Sort: 3-way Quicksort + Insertion Sort\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 24) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              int r = a[j];\n              if (r < p) { a[j++] = a[i]; a[i++] = r; }\n              else if (p < r) { a[j] = a[k]; a[k--] = r; }\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; i++) {\n          int v = a[i], j = i - 1;\n          while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool asc = 1, dsc = 1;\n      for (int i = 1; i < n; i++) {\n  ... (12 more lines)\n- Metrics: score: 0.9784, combined_score: 0.9784, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9666, adaptability_score: 0.9960, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              for (int i = l + 1; i <= h; i++) {\n                  int v = a[i], j = i - 1;\n                  while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                  a[j + 1] = v;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int v = a[m];\n          while (j <= k) {\n              if (a[j] < v) std::swap(a[i++], a[j++]);\n              else if (a[j] > v) std::swap(a[j], a[k--]);\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool asc = true, dsc = true;\n      for (int i = 1; i < n; i++) {\n  ... (10 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 24) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              int r = a[j];\n              if (r < p) { a[j++] = a[i]; a[i++] = r; }\n              else if (r > p) { a[j] = a[k]; a[k--] = r; }\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool asc = true, dsc = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) asc = false;\n          if (arr[i] > arr[i - 1]) dsc = false;\n  ... (9 more lines)\n- Metrics: score: 0.9784, combined_score: 0.9784, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9667, adaptability_score: 0.9960, avg_time: 0.0034\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  \n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 32) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              if (a[j] < p) std::swap(a[i++], a[j++]);\n              else if (a[j] > p) std::swap(a[j], a[k--]);\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; i++) {\n          int v = a[i], j = i - 1;\n          while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n  ... (2 more lines)\nwith:\n  // Hybrid Adaptive 3-way Sort\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 32) {\n          int m = l + (h - l) / 2, i = l, j = l, k = h;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (j <= k) {\n              int r = a[j];\n              if (r < p) { a[j++] = a[i]; a[i++] = r; }\n              else if (p < r) { a[j] = a[k]; a[k--] = r; }\n              else j++;\n          }\n          if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n          else { quicksort(a, k + 1, h); h = i - 1; }\n      }\n      for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n      for (int i = l + 2; i <= h; ++i) {\n          int v = a[i], j = i - 1;\n          while (a[j] > v) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = v;\n      }\n  }\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool asc = 1, dsc = 1;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) asc = 0;\n          if (arr[i] > arr[i - 1]) dsc = 0;\n          if (!asc && !dsc) break;\n      }\n      if (asc) return;\n      if (dsc) {\n          for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n      } else quicksort(arr, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size(), i = 1;\n      if (n < 2) return;\n      for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n      if (i == n) return;\n      for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n      if (i == n) {\n          for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\n- Metrics: score: 0.9786, combined_score: 0.9786, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9669, adaptability_score: 0.9961, avg_time: 0.0034\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9789)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    int i = 1;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9789), Performs well on combined_score (0.9789), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9673), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 2 (Score: 0.9786)\n```unknown\n// Hybrid Adaptive 3-way Sort\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size(), i = 1;\n    if (n < 2) return;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9786), Performs well on combined_score (0.9786), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9669), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 3 (Score: 0.9784)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (r > p) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = true, dsc = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = false;\n        if (arr[i] > arr[i - 1]) dsc = false;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9784), Performs well on combined_score (0.9784), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9667), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0034)\n\n### Program 4 (Score: 0.9784)\n```unknown\n// Adaptive Hybrid Sort: 3-way Quicksort + Insertion Sort\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9784), Performs well on combined_score (0.9784), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9666), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0035)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9783)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    int i = 1;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9783)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        else if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9783)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9784, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (r > p) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = true, dsc = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = false;\n        if (arr[i] > arr[i - 1]) dsc = false;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9783, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.1220, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\n// Helper function to detect if array is nearly sorted\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() <= 1) {\n        return true;\n    }\n    int inversions = 0;\n    double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n    for (size_t i = 0; i < arr.size() - 1; ++i) {\n        for (size_t j = i + 1; j < arr.size(); ++j) {\n            if (arr[i] > arr[j]) {\n                ++inversions;\n                if (static_cast<double>(inversions) > max_inversions) {\n                    return false;\n                }\n            }\n        }\n    }\n    return true;\n}\n\n// Helper function for insertion sort (useful for small arrays)\nvoid insertion_sort(std::vector<int>& arr) {\n    for (size_t i = 1; i < arr.size(); ++i) {\n        int j = static_cast<int>(i);\n        while (j > 0 && arr[j - 1] > arr[j]) {\n            std::swap(arr[j], arr[j - 1]);\n            --j;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9789, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    int i = 1;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        else if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, this update implements several refinements to the hybrid sorting algorithm:\n\n1.  **Quicksort Threshold**: The threshold for switching from quicksort to insertion sort is increased from 24 to 32, which often performs better on modern architectures by leveraging larger insertion sort runs.\n2.  **Sentinel Insertion Sort**: The insertion sort implementation is optimized by first identifying the minimum element in the subarray and placing it at the front. This acts as a sentinel, allowing the inner loop to skip the bounds check (`j >= l`), significantly improving the performance of the final sorting stage.\n3.  **Refined Adaptive Detection**: The `adaptive_sort` function is streamlined to use two separate scans for ascending and descending patterns. This approach is more concise and slightly more efficient for pre-sorted and reverse-sorted data.\n4.  **Conciseness**: Minor syntax adjustments (like declaring multiple variables on one line) were made to reduce the code length closer to the 500-character target without sacrificing performance.\n\nThese changes are based on high-performing patterns observed in the solution space, particularly aimed at increasing the `performance_score` while maintaining the high `adaptability_score`.\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 24) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (p < r) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; i++) {\n        int v = a[i], j = i - 1;\n        while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 32) {\n        int m = l + (h - l) / 2, i = l, j = l, k = h, p;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        p = a[m];\n        while (j <= k) {\n            int r = a[j];\n            if (r < p) { a[j++] = a[i]; a[i++] = r; }\n            else if (r > p) { a[j] = a[k]; a[k--] = r; }\n            else j++;\n        }\n        if (i - l < h - k) { quicksort(a, l, i - 1); l = k + 1; }\n        else { quicksort(a, k + 1, h); h = i - 1; }\n    }\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[l]) std::swap(a[i], a[l]);\n    for (int i = l + 2; i <= h; ++i) {\n        int v = a[i], j = i - 1;\n        while (a[j] > v) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = v;\n    }\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool asc = 1, dsc = 1;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) asc = 0;\n        else if (arr[i] > arr[i - 1]) dsc = 0;\n        if (!asc && !dsc) break;\n    }\n    if (asc) return;\n    if (dsc) {\n        for (int i = 0; i < n / 2; i++) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    for (; i < n; ++i) if (arr[i] < arr[i - 1]) break;\n    if (i == n) return;\n    for (i = 1; i < n; ++i) if (arr[i] > arr[i - 1]) break;\n    if (i == n) {\n        for (int k = 0; k < n / 2; ++k) std::swap(arr[k], arr[n - 1 - k]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00096036, 0.0109801, 0.00053064, 0.00573682, 1.294e-05, 0.0001301, 0.00027723, 0.00513021, 0.000907491, 0.010652], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}