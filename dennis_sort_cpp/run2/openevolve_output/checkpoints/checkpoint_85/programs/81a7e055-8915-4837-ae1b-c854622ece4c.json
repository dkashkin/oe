{"id": "81a7e055-8915-4837-ae1b-c854622ece4c", "code": "// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort(a, 0, (int)a.size() - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "5492f5f0-70a6-4358-b52c-4c3b00a53b92", "generation": 3, "timestamp": 1772752245.7804658, "iteration_found": 48, "metrics": {"score": 0.9756416, "combined_score": 0.9756416, "compile_success": 1.0, "correctness": 1, "performance_score": 0.962252, "adaptability_score": 0.995726, "avg_time": 0.00392284}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (arr.size() < 32 || is_nearly_sorted(arr, 0.1)) {\n          insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n      } else {\n          quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n  ... (28 more lines)\nwith:\n  void quicksort(std::vector<int>&, int, int);\n  void insertion_sort(std::vector<int>&, int, int);\n  int partition(std::vector<int>&, int, int);\n  bool is_nearly_sorted(const std::vector<int>&, double);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n          insertion_sort(arr, 0, (int)arr.size() - 1);\n      } else {\n          quicksort(arr, 0, (int)arr.size() - 1);\n      }\n  }\n  \n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              insertion_sort(arr, l, h);\n              return;\n          }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) {\n              quicksort(arr, l, p);\n              l = p + 1;\n          } else {\n              quicksort(arr, p + 1, h);\n              h = p;\n          }\n      }\n  }\n  ... (33 more lines)", "parent_metrics": {"score": 0.0, "compile_success": 0.0, "correctness": 0.0, "performance_score": 0.0, "adaptability_score": 0.0}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: score=0.00, performance_score=0.00, adaptability_score=0.00\n- Focus areas: - Fitness declined: 0.9754 \u2192 0.0000. Consider revising recent changes.\n- Exploring score=0.00, performance_score=0.00, adaptability_score=0.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### error\n```\nCompilation failed\n```\n\n### stderr\n```\nIn file included from /var/tmp/tmpob014h0a/main.cpp:17:\n/var/tmp/tmpob014h0a/sort_impl.cpp: In function \u2018void adaptive_sort(std::vector<int>&)\u2019:\n/var/tmp/tmpob014h0a/sort_impl.cpp:6:64: error: too many arguments to function \u2018void insertion_sort(std::vector<int>&)\u2019\n    6 |         insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n      |                                                                ^\n/var/tmp/tmpob014h0a/main.cpp:14:6: note: declared here\n   14 | void insertion_sort(std::vector<int>& arr);\n      |      ^~~~~~~~~~~~~~\nIn file included from /var/tmp/tmpob014h0a/main.cpp:17:\n/var/tmp/tmpob014h0a/sort_impl.cpp: In function \u2018void quicksort(std::vector<int>&, int, int)\u2019:\n/var/tmp/tmpob014h0a/sort_impl.cpp:15:42: error: too many arguments to function \u2018void insertion_sort(std::vector<int>&)\u2019\n   15 |             insertion_sort(arr, low, high);\n      |                                          ^\n/var/tmp/tmpob014h0a/main.cpp:14:6: note: declared here\n   14 | void insertion_sort(std::vector<int>& arr);\n      |      ^~~~~~~~~~~~~~\n\n```\n\n### stdout\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  bool is_nearly_sorted(const std::vector<int>& a, double t) {\n      int v = 0, m = (int)(a.size() * t);\n      for (size_t i = 0; i + 1 < a.size(); ++i)\n          if (a[i] > a[i + 1] && ++v > m) return false;\n      return true;\n  }\nwith:\n  bool is_nearly_sorted(const std::vector<int>& a, double t) {\n      int v = 0, m = (int)(a.size() * t);\n      if (m > 128) m = 128; // Cap inversions to ensure Insertion Sort safety\n      for (size_t i = 0; i + 1 < a.size(); ++i)\n          if (a[i] > a[i + 1] && ++v > m) return false;\n      return true;\n  }\nChange 2: 'void insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }' to ''\n- Metrics: score: 0.9754, combined_score: 0.9754, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9618, adaptability_score: 0.9957, avg_time: 0.0040\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (arr.size() < 32 || is_nearly_sorted(arr, 0.1)) {\n          insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n      } else {\n          quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>&, int, int);\n  void insertion_sort(std::vector<int>&, int, int);\n  int partition(std::vector<int>&, int, int);\n  bool is_nearly_sorted(const std::vector<int>&, double);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n          insertion_sort(arr, 0, (int)arr.size() - 1);\n      } else {\n          quicksort(arr, 0, (int)arr.size() - 1);\n      }\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(arr, l, h); return; }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n          else { quicksort(arr, p + 1, h); h = p; }\n      }\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\nChange 3: Replace:\n  bool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n      if (arr.size() < 2) return true;\n      int inv = 0;\n      for (size_t i = 0; i < arr.size() - 1; ++i) if (arr[i] > arr[i + 1]) inv++;\n      return static_cast<double>(inv) / arr.size() < threshold;\n  }\n  \n  void insertion_sort(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i];\n          int j = i;\n          while (j > low && arr[j - 1] > key) {\n              arr[j] = arr[j - 1];\n              --j;\n          }\n          arr[j] = key;\n      }\n  }\nwith:\n  bool is_nearly_sorted(const std::vector<int>& a, double t) {\n      int v = 0, m = (int)(a.size() * t);\n      for (size_t i = 0; i + 1 < a.size(); ++i)\n          if (a[i] > a[i + 1] && ++v > m) return false;\n      return true;\n  }\n  \n  void insertion_sort(std::vector<int>& a, int l, int h) {\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  void insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n- Metrics: score: 0.9755, combined_score: 0.9755, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9620, adaptability_score: 0.9957, avg_time: 0.0040\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(arr, l, h); return; }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n          else { quicksort(arr, p + 1, h); h = p; }\n      }\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\nChange 2: Replace:\n  bool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n      int inv = 0, lim = (int)(arr.size() * threshold);\n      for (size_t i = 0; i + 1 < arr.size(); ++i)\n          if (arr[i] > arr[i + 1]) if (++inv > lim) return false;\n      return true;\n  }\n  \n  void insertion_sort(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int v = arr[i], j = i;\n          while (j > low && arr[j - 1] > v) {\n              arr[j] = arr[j - 1];\n              --j;\n          }\n          arr[j] = v;\n      }\n  }\n  \n  void insertion_sort(std::vector<int>& arr) {\n      insertion_sort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  bool is_nearly_sorted(const std::vector<int>& a, double t) {\n      int v = 0, m = (int)(a.size() * t);\n      for (size_t i = 0; i + 1 < a.size(); ++i)\n          if (a[i] > a[i + 1] && ++v > m) return false;\n      return true;\n  }\n  \n  void insertion_sort(std::vector<int>& a, int l, int h) {\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  void insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n- Metrics: score: 0.9760, combined_score: 0.9760, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9628, adaptability_score: 0.9958, avg_time: 0.0039\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9761)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double t) {\n    int v = 0, m = (int)(arr.size() * t);\n    for (size_t i = 0; i + 1 < arr.size(); ++i)\n        if (arr[i] > arr[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9761), Performs well on combined_score (0.9761), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9629), Performs well on adaptability_score (0.9959), Performs well on avg_time (0.0038)\n\n### Program 2 (Score: 0.9760)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9760), Performs well on combined_score (0.9760), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9628), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0039)\n\n### Program 3 (Score: 0.9755)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9755), Performs well on combined_score (0.9755), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9620), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0040)\n\n### Program 4 (Score: 0.9754)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    if (m > 128) m = 128; // Cap inversions to ensure Insertion Sort safety\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\n\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9754), Performs well on combined_score (0.9754), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9618), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0040)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9250)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    int inv = 0, lim = (int)(arr.size() * threshold);\n    for (size_t i = 0; i + 1 < arr.size(); ++i)\n        if (arr[i] > arr[i + 1]) if (++inv > lim) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int v = arr[i], j = i;\n        while (j > low && arr[j - 1] > v) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9219)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    int inv = 0, lim = (int)(arr.size() * threshold);\n    for (size_t i = 0; i + 1 < arr.size(); ++i) {\n        if (arr[i] > arr[i + 1]) {\n            if (++inv > lim) return false;\n        }\n    }\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i;\n        while (j > low && arr[j - 1] > key) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = key;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9752)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9760, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9250, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    int inv = 0, lim = (int)(arr.size() * threshold);\n    for (size_t i = 0; i + 1 < arr.size(); ++i)\n        if (arr[i] > arr[i + 1]) if (++inv > lim) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int v = arr[i], j = i;\n        while (j > low && arr[j - 1] > v) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9754, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    if (m > 128) m = 128; // Cap inversions to ensure Insertion Sort safety\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\n\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9761, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double t) {\n    int v = 0, m = (int)(arr.size() * t);\n    for (size_t i = 0; i + 1 < arr.size(); ++i)\n        if (arr[i] > arr[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 32 || is_nearly_sorted(arr, 0.1)) {\n        insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n    } else {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() < 2) return true;\n    int inv = 0;\n    for (size_t i = 0; i < arr.size() - 1; ++i) if (arr[i] > arr[i + 1]) inv++;\n    return static_cast<double>(inv) / arr.size() < threshold;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i;\n        while (j > low && arr[j - 1] > key) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I will restore the high-performing features from previous successful programs while fixing the compilation error. The strategy involves:\n\n1.  **Fixing Compilation**: Adding forward declarations for `quicksort`, `insertion_sort`, `partition`, and `is_nearly_sorted` at the top of the file. This resolves the visibility issue where `adaptive_sort` could not see the 3-argument `insertion_sort` or matched it incorrectly against the 1-argument declaration from the header.\n2.  **Optimizing Partition**: Replacing the current Lomuto partition scheme with the more efficient Hoare partition scheme using the median-of-three pivot selection. This handles duplicates better and performs fewer swaps on average.\n3.  **Optimizing Adaptive Logic**: Using an early-exit optimized `is_nearly_sorted` function that aborts scanning once the inversion threshold is exceeded, minimizing overhead for unsorted arrays. The threshold is set to 0.05 (5%) based on previous high-performing results.\n4.  **Optimizing Quicksort**: Implementing tail-recursion optimization and switching to insertion sort for small subarrays (size < 20).\n5.  **Completeness**: Providing the implementation for the 1-argument `insertion_sort` required by the external declaration, forwarding it to the 3-argument implementation.\n\nThis approach combines structural fixes with algorithmic optimizations that have historically yielded the highest scores (approx 0.9761).\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 32 || is_nearly_sorted(arr, 0.1)) {\n        insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n    } else {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() < 2) return true;\n    int inv = 0;\n    for (size_t i = 0; i < arr.size() - 1; ++i) if (arr[i] > arr[i + 1]) inv++;\n    return static_cast<double>(inv) / arr.size() < threshold;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i;\n        while (j > low && arr[j - 1] > key) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = key;\n    }\n}\n=======\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort(a, 0, (int)a.size() - 1);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00105512, 0.0119424, 0.00029481, 0.00365803, 0.00017102, 0.00229234, 0.00050603, 0.00747556, 0.0009415, 0.0108915], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}