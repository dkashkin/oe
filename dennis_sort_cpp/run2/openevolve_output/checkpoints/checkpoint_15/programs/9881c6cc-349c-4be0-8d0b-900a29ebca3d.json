{"id": "9881c6cc-349c-4be0-8d0b-900a29ebca3d", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 15) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i], j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "a36d5734-f0b2-43d9-afef-1f782d6afa7e", "generation": 1, "timestamp": 1772750581.5406275, "iteration_found": 2, "metrics": {"score": 0.0, "compile_success": 0.0, "correctness": 0.0, "performance_score": 0.0, "adaptability_score": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 15) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\nChange 2: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nChange 3: Replace:\n  // Helper function to detect if array is nearly sorted\n  bool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n      if (arr.size() <= 1) {\n          return true;\n      }\n      int inversions = 0;\n      double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n      for (size_t i = 0; i < arr.size() - 1; ++i) {\n          for (size_t j = i + 1; j < arr.size(); ++j) {\n              if (arr[i] > arr[j]) {\n                  ++inversions;\n                  if (static_cast<double>(inversions) > max_inversions) {\n                      return false;\n                  }\n              }\n          }\n      }\n      return true;\n  }\n  \n  // Helper function for insertion sort (useful for small arrays)\n  void insertion_sort(std::vector<int>& arr) {\n      for (size_t i = 1; i < arr.size(); ++i) {\n          int j = static_cast<int>(i);\n          while (j > 0 && arr[j - 1] > arr[j]) {\n              std::swap(arr[j], arr[j - 1]);\n              --j;\n          }\n      }\n  }\nwith:\n  void insertion_sort(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i], j = i - 1;\n          while (j >= low && arr[j] > key) {\n              arr[j + 1] = arr[j];\n              --j;\n          }\n          arr[j + 1] = key;\n      }\n  }", "parent_metrics": {"score": 0.12204554000000001, "combined_score": 0.12204554000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.0689559, "adaptability_score": 0.20168, "avg_time": 1.3502}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.1220\n- Feature coordinates: score=0.12, performance_score=0.07, adaptability_score=0.20\n- Focus areas: - Fitness unchanged at 0.1220\n- Exploring score=0.12, performance_score=0.07, adaptability_score=0.20 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: score: 0.1220, combined_score: 0.1220, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.0690, adaptability_score: 0.2017, avg_time: 1.3502\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.1220)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\n// Helper function to detect if array is nearly sorted\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() <= 1) {\n        return true;\n    }\n    int inversions = 0;\n    double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n    for (size_t i = 0; i < arr.size() - 1; ++i) {\n        for (size_t j = i + 1; j < arr.size(); ++j) {\n            if (arr[i] > arr[j]) {\n                ++inversions;\n                if (static_cast<double>(inversions) > max_inversions) {\n                    return false;\n                }\n            }\n        }\n    }\n    return true;\n}\n\n// Helper function for insertion sort (useful for small arrays)\nvoid insertion_sort(std::vector<int>& arr) {\n    for (size_t i = 1; i < arr.size(); ++i) {\n        int j = static_cast<int>(i);\n        while (j > 0 && arr[j - 1] > arr[j]) {\n            std::swap(arr[j], arr[j - 1]);\n            --j;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.1220), Performs well on combined_score (0.1220), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.0690), Performs well on adaptability_score (0.2017), Performs well on avg_time (1.3502)\n\n\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\n// Helper function to detect if array is nearly sorted\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() <= 1) {\n        return true;\n    }\n    int inversions = 0;\n    double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n    for (size_t i = 0; i < arr.size() - 1; ++i) {\n        for (size_t j = i + 1; j < arr.size(); ++j) {\n            if (arr[i] > arr[j]) {\n                ++inversions;\n                if (static_cast<double>(inversions) > max_inversions) {\n                    return false;\n                }\n            }\n        }\n    }\n    return true;\n}\n\n// Helper function for insertion sort (useful for small arrays)\nvoid insertion_sort(std::vector<int>& arr) {\n    for (size_t i = 1; i < arr.size(); ++i) {\n        int j = static_cast<int>(i);\n        while (j > 0 && arr[j - 1] > arr[j]) {\n            std::swap(arr[j], arr[j - 1]);\n            --j;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, performance, and adaptability while reducing code length, I have implemented a hybrid sorting strategy. This approach uses **Median-of-Three** pivot selection to increase robustness against sorted/adversarial data, adds a **small-array cutoff** that switches to **Insertion Sort** for better cache performance on small ranges, and employs **Tail Recursion Optimization** to reduce stack depth. I have also removed the computationally expensive (and unused) $O(N^2)$ `is_nearly_sorted` function to significantly clean up the codebase.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 15) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n=======\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n    std::swap(arr[mid], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\n// Helper function to detect if array is nearly sorted\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() <= 1) {\n        return true;\n    }\n    int inversions = 0;\n    double max_inversions = (static_cast<double>(arr.size()) * (arr.size() - 1) / 2.0) * threshold;\n    for (size_t i = 0; i < arr.size() - 1; ++i) {\n        for (size_t j = i + 1; j < arr.size(); ++j) {\n            if (arr[i] > arr[j]) {\n                ++inversions;\n                if (static_cast<double>(inversions) > max_inversions) {\n                    return false;\n                }\n            }\n        }\n    }\n    return true;\n}\n\n// Helper function for insertion sort (useful for small arrays)\nvoid insertion_sort(std::vector<int>& arr) {\n    for (size_t i = 1; i < arr.size(); ++i) {\n        int j = static_cast<int>(i);\n        while (j > 0 && arr[j - 1] > arr[j]) {\n            std::swap(arr[j], arr[j - 1]);\n            --j;\n        }\n    }\n}\n=======\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i], j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            --j;\n        }\n        arr[j + 1] = key;\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"error\": \"Compilation failed\", \"stderr\": \"In file included from /var/tmp/tmpt1iws9cw/main.cpp:17:\\n/var/tmp/tmpt1iws9cw/sort_impl.cpp: In function \\u2018void quicksort(std::vector<int>&, int, int)\\u2019:\\n/var/tmp/tmpt1iws9cw/sort_impl.cpp:19:42: error: too many arguments to function \\u2018void insertion_sort(std::vector<int>&)\\u2019\\n   19 |             insertion_sort(arr, low, high);\\n      |                                          ^\\n/var/tmp/tmpt1iws9cw/main.cpp:14:6: note: declared here\\n   14 | void insertion_sort(std::vector<int>& arr);\\n      |      ^~~~~~~~~~~~~~\\n\", \"stdout\": \"\"}", "artifact_dir": null, "embedding": null}