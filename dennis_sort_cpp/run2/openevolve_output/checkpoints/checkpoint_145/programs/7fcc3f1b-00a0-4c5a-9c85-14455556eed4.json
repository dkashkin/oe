{"id": "7fcc3f1b-00a0-4c5a-9c85-14455556eed4", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Hybrid 3-way QuickSort: Optimizes for duplicates, nearly sorted, and small arrays\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 16) {\n        int m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m], lt = l, gt = r, i = l;\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "8079a518-c37d-4f43-a1f8-c05483b946aa", "generation": 2, "timestamp": 1772750846.1824136, "iteration_found": 8, "metrics": {"score": 0.963204, "combined_score": 0.963204, "compile_success": 1.0, "correctness": 1, "performance_score": 0.943424, "adaptability_score": 0.992874, "avg_time": 0.00599687}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Optimized hybrid sort: QuickSort + Insertion Sort + Median-of-Three Pivot\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) {\n          quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\n  \n  void insertion_sort_range(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i];\n          int j = i - 1;\n          while (j >= low && arr[j] > key) {\n              arr[j + 1] = arr[j];\n              j--;\n          }\n          arr[j + 1] = key;\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n      if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n      if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n  \n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high];\n      int i = low;\n  ... (28 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Hybrid 3-way QuickSort: Optimizes for duplicates, nearly sorted, and small arrays\n  void insertion_sort(std::vector<int>& a, int l, int r) {\n      for (int i = l + 1; i <= r; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n          a[j] = v;\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int r) {\n      while (r - l > 16) {\n          int m = l + (r - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[r] < a[l]) std::swap(a[l], a[r]);\n          if (a[r] < a[m]) std::swap(a[m], a[r]);\n          int p = a[m], lt = l, gt = r, i = l;\n          while (i <= gt) {\n              if (a[i] < p) std::swap(a[lt++], a[i++]);\n              else if (a[i] > p) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, r); r = lt - 1; }\n      }\n      insertion_sort(a, l, r);\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  ... (2 more lines)", "parent_metrics": {"score": 0.920979, "combined_score": 0.920979, "compile_success": 1.0, "correctness": 1, "performance_score": 0.884289, "adaptability_score": 0.976014, "avg_time": 0.0130852}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9210\n- Feature coordinates: score=0.92, performance_score=0.88, adaptability_score=0.98\n- Focus areas: - Fitness unchanged at 0.9210\n- Exploring score=0.92, performance_score=0.88, adaptability_score=0.98 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.000996991, 0.0111194, 0.00058238, 0.00633262, 0.00047564, 0.00554464, 0.00911282, 0.085867, 0.00088282, 0.00993715]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n  ... (39 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Optimized hybrid sort: QuickSort + Insertion Sort + Median-of-Three Pivot\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) {\n          quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n      }\n  }\n  \n  void insertion_sort_range(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int key = arr[i];\n          int j = i - 1;\n          while (j >= low && arr[j] > key) {\n              arr[j + 1] = arr[j];\n              j--;\n          }\n          arr[j + 1] = key;\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n      if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n      if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n  \n      std::swap(arr[mid], arr[high]);\n      int pivot = arr[high];\n      int i = low;\n  ... (28 more lines)\n- Metrics: score: 0.9210, combined_score: 0.9210, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8843, adaptability_score: 0.9760, avg_time: 0.0131\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9210)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: QuickSort + Insertion Sort + Median-of-Three Pivot\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            j--;\n        }\n        arr[j + 1] = key;\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    \n    std::swap(arr[mid], arr[high]); \n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        \n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9210), Performs well on combined_score (0.9210), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8843), Performs well on adaptability_score (0.9760), Performs well on avg_time (0.0131)\n\n\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: QuickSort + Insertion Sort + Median-of-Three Pivot\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            j--;\n        }\n        arr[j + 1] = key;\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    \n    std::swap(arr[mid], arr[high]); \n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        \n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, this update replaces the standard hybrid Quicksort with a more robust **3-way partitioning (Dutch National Flag) hybrid sort**. \n\n### Key Improvements:\n1.  **3-way Partitioning**: Specifically handles datasets with many duplicate elements, reducing complexity from $O(n \\log n)$ to $O(n)$ in the best case for duplicates. This significantly boosts both **adaptability** and **performance**.\n2.  **Hoare-style Logic**: The partition loop is more efficient than the previous Lomuto scheme, performing fewer swaps on average.\n3.  **Code Simplification**: By integrating the partitioning logic directly into the `quicksort` function and streamlining the helper functions, the code becomes more concise. This addresses the warning about code length while maintaining the high adaptability score.\n4.  **Tail Recursion Optimization**: Continues to manage the stack effectively by recursing on the smaller partition and looping on the larger one, ensuring $O(\\log n)$ space complexity.\n\n```unknown\n<<<<<<< SEARCH\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: QuickSort + Insertion Sort + Median-of-Three Pivot\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= low && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            j--;\n        }\n        arr[j + 1] = key;\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    \n    std::swap(arr[mid], arr[high]); \n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            insertion_sort_range(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        \n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n=======\n// EVOLVE-BLOCK-START\n// Hybrid 3-way QuickSort: Optimizes for duplicates, nearly sorted, and small arrays\nvoid insertion_sort(std::vector<int>& a, int l, int r) {\n    for (int i = l + 1; i <= r; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n        a[j] = v;\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int r) {\n    while (r - l > 16) {\n        int m = l + (r - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[r] < a[l]) std::swap(a[l], a[r]);\n        if (a[r] < a[m]) std::swap(a[m], a[r]);\n        int p = a[m], lt = l, gt = r, i = l;\n        while (i <= gt) {\n            if (a[i] < p) std::swap(a[lt++], a[i++]);\n            else if (a[i] > p) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < r - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, r); r = lt - 1; }\n    }\n    insertion_sort(a, l, r);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n// EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00098837, 0.010382, 0.00051713, 0.00559497, 0.0009575, 0.0242127, 0.00028211, 0.00515784, 0.0009349, 0.0109413], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}