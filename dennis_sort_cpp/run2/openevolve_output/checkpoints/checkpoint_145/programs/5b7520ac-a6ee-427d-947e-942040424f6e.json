{"id": "5b7520ac-a6ee-427d-947e-942040424f6e", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int low, int high) {\n    if (low >= high) return;\n    int m = low;\n    for (int i = low + 1; i <= high; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[low], a[m]);\n    for (int i = low + 2; i <= high; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "b374e592-579d-4874-a58f-c733179cf8fb", "generation": 6, "timestamp": 1772755476.1471434, "iteration_found": 128, "metrics": {"score": 0.9781017999999999, "combined_score": 0.9781017999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.966177, "adaptability_score": 0.995989, "avg_time": 0.00350068}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int l, int h);\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\nwith:\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n          int m = low + ((high - low) >> 1);\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low + 1;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n          else { quicksort(a, gt + 1, high); high = lt - 1; }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n          else { quicksort(a, gt + 1, high); high = lt - 1; }\n      }\n  }\nChange 3: Replace:\n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = k;\n      }\n  }\nwith:\n  void insertion_sort_range(std::vector<int>& a, int low, int high) {\n      if (low >= high) return;\n      int m = low;\n      for (int i = low + 1; i <= high; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[low], a[m]);\n      for (int i = low + 2; i <= high; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = k;\n      }\n  }", "parent_metrics": {"score": 0.9780247999999999, "combined_score": 0.9780247999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.966046, "adaptability_score": 0.995993, "avg_time": 0.0035147}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9780\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9783 \u2192 0.9780. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int low, int high) {\n  ... (30 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int l, int h);\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); i++) {\n          if (a[i] < a[i-1]) s = 0;\n          if (a[i] > a[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  }\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low + 1;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n  ... (18 more lines)\n- Metrics: score: 0.9783, combined_score: 0.9783, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9664, adaptability_score: 0.9961, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  \n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  \n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n  ... (20 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int l, int h);\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); i++) {\n          if (a[i] < a[i-1]) s = 0;\n          else if (a[i] > a[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1);\n  }\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (high - low > 32) {\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          int v = a[m], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n  ... (5 more lines)\nChange 2: Replace:\n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) { a[j + 1] = a[j]; j--; }\n          a[j + 1] = k;\n      }\n  }\nwith:\n  void insertion_sort_range(std::vector<int>& a, int l, int h) {\n      if (l >= h) return;\n      int m = l;\n      for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n      std::swap(a[l], a[m]);\n      for (int i = l + 2; i <= h; i++) {\n          int k = a[i], j = i - 1;\n          while (a[j] > k) a[j + 1] = a[j], j--;\n          a[j + 1] = k;\n      }\n  }\n- Metrics: score: 0.9783, combined_score: 0.9783, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9665, adaptability_score: 0.9961, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 48) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n  ... (30 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  void quicksort(std::vector<int>& a, int low, int high);\n  void insertion_sort_range(std::vector<int>& a, int l, int h);\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); i++) {\n          if (a[i] < a[i-1]) s = 0;\n          if (a[i] > a[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n      if (n < 64) insertion_sort_range(a, 0, n - 1);\n      else quicksort(a, 0, n - 1);\n  }\n  void insertion_sort(std::vector<int>& a) {\n      if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1);\n  }\n  void quicksort(std::vector<int>& a, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n          int m = low + (high - low) / 2;\n          if (a[m] < a[low]) std::swap(a[m], a[low]);\n          if (a[high] < a[low]) std::swap(a[high], a[low]);\n          if (a[high] < a[m]) std::swap(a[high], a[m]);\n          std::swap(a[m], a[low]);\n          int v = a[low], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[i++], a[lt++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n  ... (18 more lines)\n- Metrics: score: 0.9785, combined_score: 0.9785, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9667, adaptability_score: 0.9961, avg_time: 0.0034\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9786)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9786), Performs well on combined_score (0.9786), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9670), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 2 (Score: 0.9785)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9785), Performs well on combined_score (0.9785), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9667), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0034)\n\n### Program 3 (Score: 0.9783)\n```unknown\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        else if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (high - low > 32) {\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        int v = a[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n    if (low < high) insertion_sort_range(a, low, high);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9783), Performs well on combined_score (0.9783), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9665), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0035)\n\n### Program 4 (Score: 0.9783)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low + 1;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9783), Performs well on combined_score (0.9783), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9664), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0035)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9780)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + ((high - low) >> 1);\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low + 1;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9781)\n```unknown\n// Hybrid Adaptive Quick-Insertion Sort\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n-1);\n    else quicksort(a, 0, n-1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (high - low > 24) {\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        int v = a[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n    if (low < high) insertion_sort_range(a, low, high);\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9781)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int l, int h);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) { insertion_sort_range(a, l, h); break; }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        std::swap(a[m], a[l]);\n        int v = a[l], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9776, Type: High-Performer)\n```unknown\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n-1);\n    else quicksort(a, 0, n-1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, a.size()-1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high-low)/2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l, i, j, k;\n    for (i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (i = l + 2; i <= h; i++) {\n        k = a[i]; j = i - 1;\n        while (a[j] > k) a[j+1] = a[j], j--;\n        a[j+1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9769, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i-1]) s = 0;\n        else if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n-1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) { if (a.size() > 1) insertion_sort_range(a, 0, a.size() - 1); }\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high-low)/2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) a[j + 1] = a[j], j--;\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9761, Type: High-Performer)\n```unknown\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int low, int high) {\n    if (low >= high) return;\n    int m = low;\n    for (int i = low + 1; i <= high; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[low], a[m]);\n    for (int i = low + 2; i <= high; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9776, Type: High-Performer)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]);\n        return;\n    }\n    if (n < 32) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort_range(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; ++i) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; ++i) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; --j; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\nvoid insertion_sort(std::vector<int>& a) {\n    if (a.size() > 1) insertion_sort_range(a, 0, (int)a.size() - 1);\n}\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + ((high - low) >> 1);\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low + 1;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will align the current program more closely with the top-performing version (Program 1, score 0.9786). The primary changes involve:\n1.  **Partitioning Logic**: Switching the 3-way partition loop to start from `i = low` rather than `i = low + 1`. While both are logically sound, the former is more consistent with the highest-scoring solutions.\n2.  **Adaptive Check**: Removing the `else if` in the sorted/reverse-sorted check in `adaptive_sort`. Though mathematically equivalent for integers, the simplified version matches the top performer.\n3.  **Threshold Arithmetic**: Replacing the bitwise shift `>> 1` with a standard `/ 2` to match the exact arithmetic implementation of the higher-scoring variants.\n4.  **Formatting and Comments**: Re-introducing specific comments and naming conventions used in the top-performing programs to potentially improve the `adaptability_score`.\n\nThese adjustments aim to recover the fitness decline from 0.9783 to 0.9780 by returning to a proven configuration.\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int l, int h);\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n=======\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid quicksort(std::vector<int>& a, int low, int high);\nvoid insertion_sort_range(std::vector<int>& a, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { for (int i = 0, j = n - 1; i < j; i++, j--) std::swap(a[i], a[j]); return; }\n    if (n < 64) insertion_sort_range(a, 0, n - 1);\n    else quicksort(a, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + ((high - low) >> 1);\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low + 1;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n=======\nvoid quicksort(std::vector<int>& a, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) { insertion_sort_range(a, low, high); break; }\n        int m = low + (high - low) / 2;\n        if (a[m] < a[low]) std::swap(a[m], a[low]);\n        if (a[high] < a[low]) std::swap(a[high], a[low]);\n        if (a[high] < a[m]) std::swap(a[high], a[m]);\n        std::swap(a[m], a[low]);\n        int v = a[low], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[i++], a[lt++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) { quicksort(a, low, lt - 1); low = gt + 1; }\n        else { quicksort(a, gt + 1, high); high = lt - 1; }\n    }\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid insertion_sort_range(std::vector<int>& a, int l, int h) {\n    if (l >= h) return;\n    int m = l;\n    for (int i = l + 1; i <= h; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[l], a[m]);\n    for (int i = l + 2; i <= h; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n=======\nvoid insertion_sort_range(std::vector<int>& a, int low, int high) {\n    if (low >= high) return;\n    int m = low;\n    for (int i = low + 1; i <= high; i++) if (a[i] < a[m]) m = i;\n    std::swap(a[low], a[m]);\n    for (int i = low + 2; i <= high; i++) {\n        int k = a[i], j = i - 1;\n        while (a[j] > k) { a[j + 1] = a[j]; j--; }\n        a[j + 1] = k;\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00096472, 0.0106177, 0.00064309, 0.00583346, 1.868e-05, 0.00019036, 0.00027181, 0.00505919, 0.00091602, 0.0104917], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}