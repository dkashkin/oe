{"id": "cee22961-db0a-4602-9e31-bcff69392aa7", "code": "// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(a, l, h); return; }\n        int p = partition(a, l, h);\n        if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n        else { quicksort(a, p + 1, h); h = p; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), v = 0, m = (n / 20 > 64) ? 64 : n / 20;\n    if (n < 64) { if (n > 1) insertion_sort(a, 0, n - 1); return; }\n    for (int i = 1; i < n; i++)\n        if (a[i - 1] > a[i] && ++v > m) { quicksort(a, 0, n - 1); return; }\n    insertion_sort(a, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n", "changes_description": null, "language": "unknown", "parent_id": "2dfc492b-1575-43ad-9ba0-c63c46fce08c", "generation": 5, "timestamp": 1772755342.976267, "iteration_found": 125, "metrics": {"score": 0.9769142, "combined_score": 0.9769142, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964263, "adaptability_score": 0.995891, "avg_time": 0.00370619}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>&, int, int);\n  void insertion_sort(std::vector<int>&, int, int);\n  int partition(std::vector<int>&, int, int);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      if (n > 64) {\n          int v = 0, m = n / 20;\n          for (int i = 0; i < n - 1; ++i)\n              if (arr[i] > arr[i + 1] && ++v > m) { quicksort(arr, 0, n - 1); return; }\n      }\n      insertion_sort(arr, 0, n - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (h - l > 16) {\n          int p = partition(arr, l, h);\n          if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n          else { quicksort(arr, p + 1, h); h = p; }\n      }\n      insertion_sort(arr, l, h);\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n  ... (15 more lines)\nwith:\n  void insertion_sort(std::vector<int>& a, int l, int h) {\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(a, l, h); return; }\n          int p = partition(a, l, h);\n          if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n          else { quicksort(a, p + 1, h); h = p; }\n      }\n  }\n  ... (8 more lines)", "parent_metrics": {"score": 0.9752373999999999, "combined_score": 0.9752373999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.961609, "adaptability_score": 0.99568, "avg_time": 0.00399233}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9752\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9757 \u2192 0.9752. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00104842, 0.0120247, 0.00031633, 0.0039619, 0.00018369, 0.00227602, 0.00051578, 0.00750586, 0.00098553, 0.0111051]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size(), v = 0;\n      if (n < 64) { if (n > 1) insertion_sort(arr, 0, n - 1); return; }\n      for (int i = 1; i < n; i++)\n          if (arr[i-1] > arr[i] && ++v > n / 20) { quicksort(arr, 0, n - 1); return; }\n      insertion_sort(arr, 0, n - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(arr, l, h); return; }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n          else { quicksort(arr, p + 1, h); h = p; }\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(), v = 0, m = n / 20;\n      if (n < 64) { if (n > 1) insertion_sort(a, 0, n - 1); return; }\n      if (m > 64) m = 64;\n      for (int i = 1; i < n; i++)\n          if (a[i - 1] > a[i] && ++v > m) { quicksort(a, 0, n - 1); return; }\n      insertion_sort(a, 0, n - 1);\n  }\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(a, l, h); return; }\n          int p = partition(a, l, h);\n          if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n          else { quicksort(a, p + 1, h); h = p; }\n      }\n  }\nChange 2: Replace:\n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\n  \n  \n  \n  void insertion_sort(std::vector<int>& a, int l, int h) {\nwith:\n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\n  void insertion_sort(std::vector<int>& a, int l, int h) {\n- Metrics: score: 0.9757, combined_score: 0.9757, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9624, adaptability_score: 0.9957, avg_time: 0.0039\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>&, int, int);\n  void insertion_sort(std::vector<int>&, int, int);\n  int partition(std::vector<int>&, int, int);\n  bool is_nearly_sorted(const std::vector<int>&, double);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n          insertion_sort(arr, 0, (int)arr.size() - 1);\n      } else {\n          quicksort(arr, 0, (int)arr.size() - 1);\n      }\n  }\n  \n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              insertion_sort(arr, l, h);\n              return;\n          }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) {\n              quicksort(arr, l, p);\n              l = p + 1;\n          } else {\n              quicksort(arr, p + 1, h);\n              h = p;\n          }\n      }\n  }\n  ... (33 more lines)\nwith:\n  void insertion_sort(std::vector<int>& a, int l, int h) {\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (h - l > 20) {\n          int p = partition(a, l, h);\n          if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n          else { quicksort(a, p + 1, h); h = p; }\n      }\n      insertion_sort(a, l, h);\n  }\n  ... (13 more lines)\n- Metrics: score: 0.9760, combined_score: 0.9760, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9628, adaptability_score: 0.9957, avg_time: 0.0039\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 24) {\n              insertion_sort(arr, low, high);\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) { insertion_sort(arr, l, h); return; }\n          int p = partition(arr, l, h);\n          if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n          else { quicksort(arr, p + 1, h); h = p; }\n      }\n  }\n  \n  int partition(std::vector<int>& a, int l, int h) {\n      int m = l + (h - l) / 2;\n      if (a[m] < a[l]) std::swap(a[m], a[l]);\n      if (a[h] < a[l]) std::swap(a[h], a[l]);\n      if (a[h] < a[m]) std::swap(a[h], a[m]);\n      int p = a[m], i = l - 1, j = h + 1;\n      while (1) {\n          while (a[++i] < p);\n          while (a[--j] > p);\n          if (i >= j) return j;\n          std::swap(a[i], a[j]);\n      }\n  }\nChange 2: Replace:\n  bool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n      int inv = 0, lim = (int)(arr.size() * threshold);\n      for (size_t i = 0; i + 1 < arr.size(); ++i)\n          if (arr[i] > arr[i + 1]) if (++inv > lim) return false;\n      return true;\n  }\n  \n  void insertion_sort(std::vector<int>& arr, int low, int high) {\n      for (int i = low + 1; i <= high; ++i) {\n          int v = arr[i], j = i;\n          while (j > low && arr[j - 1] > v) {\n              arr[j] = arr[j - 1];\n              --j;\n          }\n          arr[j] = v;\n      }\n  }\n  \n  void insertion_sort(std::vector<int>& arr) {\n      insertion_sort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  bool is_nearly_sorted(const std::vector<int>& a, double t) {\n      int v = 0, m = (int)(a.size() * t);\n      for (size_t i = 0; i + 1 < a.size(); ++i)\n          if (a[i] > a[i + 1] && ++v > m) return false;\n      return true;\n  }\n  \n  void insertion_sort(std::vector<int>& a, int l, int h) {\n      for (int i = l + 1; i <= h; ++i) {\n          int v = a[i], j = i;\n          while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n          a[j] = v;\n      }\n  }\n  \n  void insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n- Metrics: score: 0.9760, combined_score: 0.9760, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9628, adaptability_score: 0.9958, avg_time: 0.0039\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9764)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(a, l, h); return; }\n        int p = partition(a, l, h);\n        if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n        else { quicksort(a, p + 1, h); h = p; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), v = 0, m = (n / 20 > 64) ? 64 : n / 20;\n    if (n < 64) { if (n > 1) insertion_sort(a, 0, n - 1); return; }\n    for (int i = 1; i < n; i++)\n        if (a[i - 1] > a[i] && ++v > m) { quicksort(a, 0, n - 1); return; }\n    insertion_sort(a, 0, n - 1);\n}\n\n\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9764), Performs well on combined_score (0.9764), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9635), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0038)\n\n### Program 2 (Score: 0.9760)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9760), Performs well on combined_score (0.9760), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9628), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0039)\n\n### Program 3 (Score: 0.9760)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 20) {\n        int p = partition(a, l, h);\n        if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n        else { quicksort(a, p + 1, h); h = p; }\n    }\n    insertion_sort(a, l, h);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size(), v = 0;\n    if (n < 2) return;\n    if (n > 64) {\n        for (int i = 1; i < n; i++)\n            if (arr[i - 1] > arr[i] && ++v > n / 20) {\n                quicksort(arr, 0, n - 1);\n                return;\n            }\n    }\n    insertion_sort(arr, 0, n - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9760), Performs well on combined_score (0.9760), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9628), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0039)\n\n### Program 4 (Score: 0.9757)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), v = 0, m = n / 20;\n    if (n < 64) { if (n > 1) insertion_sort(a, 0, n - 1); return; }\n    if (m > 64) m = 64;\n    for (int i = 1; i < n; i++)\n        if (a[i - 1] > a[i] && ++v > m) { quicksort(a, 0, n - 1); return; }\n    insertion_sort(a, 0, n - 1);\n}\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(a, l, h); return; }\n        int p = partition(a, l, h);\n        if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n        else { quicksort(a, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\n\n// EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on score (0.9757), Performs well on combined_score (0.9757), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9624), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0039)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9756)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort(a, 0, (int)a.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9757)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 64) {\n        if (n > 1) insertion_sort(arr, 0, n - 1);\n        return;\n    }\n    int lim = n / 20, v = 0;\n    for (int i = 0; i < n - 1; ++i) {\n        if (arr[i] > arr[i + 1] && ++v > lim) {\n            quicksort(arr, 0, n - 1);\n            return;\n        }\n    }\n    insertion_sort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            insertion_sort(arr, l, h);\n            return;\n        }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) {\n            quicksort(arr, l, p);\n            l = p + 1;\n        } else {\n            quicksort(arr, p + 1, h);\n            h = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) {\n    insertion_sort(a, 0, (int)a.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9756)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\n\n// EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 32 || is_nearly_sorted(arr, 0.1)) {\n        insertion_sort(arr, 0, static_cast<int>(arr.size()) - 1);\n    } else {\n        quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    if (arr.size() < 2) return true;\n    int inv = 0;\n    for (size_t i = 0; i < arr.size() - 1; ++i) if (arr[i] > arr[i + 1]) inv++;\n    return static_cast<double>(inv) / arr.size() < threshold;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i;\n        while (j > low && arr[j - 1] > key) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = key;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9219, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 24) {\n            insertion_sort(arr, low, high);\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n\nbool is_nearly_sorted(const std::vector<int>& arr, double threshold) {\n    int inv = 0, lim = (int)(arr.size() * threshold);\n    for (size_t i = 0; i + 1 < arr.size(); ++i) {\n        if (arr[i] > arr[i + 1]) {\n            if (++inv > lim) return false;\n        }\n    }\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& arr, int low, int high) {\n    for (int i = low + 1; i <= high; ++i) {\n        int key = arr[i];\n        int j = i;\n        while (j > low && arr[j - 1] > key) {\n            arr[j] = arr[j - 1];\n            --j;\n        }\n        arr[j] = key;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& arr) {\n    insertion_sort(arr, 0, (int)arr.size() - 1);\n}\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9760, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9752, Type: High-Performer)\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\nbool is_nearly_sorted(const std::vector<int>&, double);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (arr.size() < 64 || is_nearly_sorted(arr, 0.05)) {\n        insertion_sort(arr, 0, (int)arr.size() - 1);\n    } else {\n        quicksort(arr, 0, (int)arr.size() - 1);\n    }\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(arr, l, h); return; }\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nbool is_nearly_sorted(const std::vector<int>& a, double t) {\n    int v = 0, m = (int)(a.size() * t);\n    for (size_t i = 0; i + 1 < a.size(); ++i)\n        if (a[i] > a[i + 1] && ++v > m) return false;\n    return true;\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a) { insertion_sort(a, 0, (int)a.size() - 1); }\n// EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Hybrid Adaptive Sort Implementation\n// EVOLVE-BLOCK-START\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    if (n > 64) {\n        int v = 0, m = n / 20;\n        for (int i = 0; i < n - 1; ++i)\n            if (arr[i] > arr[i + 1] && ++v > m) { quicksort(arr, 0, n - 1); return; }\n    }\n    insertion_sort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (h - l > 16) {\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n    insertion_sort(arr, l, h);\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n// EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will refine the `adaptive_sort` and `quicksort` logic based on the top-performing program (Program 1, score 0.9764). \n\n### Key Improvements:\n1.  **Capped Adaptive Threshold**: In `adaptive_sort`, we now cap the inversion threshold `m` at 64. This prevents the initial scan from being too lenient on very large arrays, switching to quicksort more quickly when a clear pattern of unsortedness is detected.\n2.  **Optimized Quicksort Switch**: We updated `quicksort` to switch to `insertion_sort` immediately when a sub-array is smaller than 20 elements, rather than waiting until the end of the partitioning loop. This reduces overhead for small sub-arrays.\n3.  **Function Reordering**: We reordered the functions to define `insertion_sort` and `partition` before they are used in `quicksort` and `adaptive_sort`. This allows us to remove forward declarations, simplifying the code and slightly reducing character count.\n4.  **Streamlined Logic**: We slightly adjusted the `adaptive_sort` structure to be more concise while maintaining its robustness for both sorted and random data.\n\n```cpp\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>&, int, int);\nvoid insertion_sort(std::vector<int>&, int, int);\nint partition(std::vector<int>&, int, int);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    if (n > 64) {\n        int v = 0, m = n / 20;\n        for (int i = 0; i < n - 1; ++i)\n            if (arr[i] > arr[i + 1] && ++v > m) { quicksort(arr, 0, n - 1); return; }\n    }\n    insertion_sort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n    while (h - l > 16) {\n        int p = partition(arr, l, h);\n        if (p - l < h - p) { quicksort(arr, l, p); l = p + 1; }\n        else { quicksort(arr, p + 1, h); h = p; }\n    }\n    insertion_sort(arr, l, h);\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n=======\nvoid insertion_sort(std::vector<int>& a, int l, int h) {\n    for (int i = l + 1; i <= h; ++i) {\n        int v = a[i], j = i;\n        while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n        a[j] = v;\n    }\n}\n\nint partition(std::vector<int>& a, int l, int h) {\n    int m = l + (h - l) / 2;\n    if (a[m] < a[l]) std::swap(a[m], a[l]);\n    if (a[h] < a[l]) std::swap(a[h], a[l]);\n    if (a[h] < a[m]) std::swap(a[h], a[m]);\n    int p = a[m], i = l - 1, j = h + 1;\n    while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) return j;\n        std::swap(a[i], a[j]);\n    }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) { insertion_sort(a, l, h); return; }\n        int p = partition(a, l, h);\n        if (p - l < h - p) { quicksort(a, l, p); l = p + 1; }\n        else { quicksort(a, p + 1, h); h = p; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(), v = 0, m = (n / 20 > 64) ? 64 : n / 20;\n    if (n < 64) { if (n > 1) insertion_sort(a, 0, n - 1); return; }\n    for (int i = 1; i < n; i++)\n        if (a[i - 1] > a[i] && ++v > m) { quicksort(a, 0, n - 1); return; }\n    insertion_sort(a, 0, n - 1);\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00103511, 0.0116289, 0.00026984, 0.00329082, 0.0001442, 0.00186372, 0.00048554, 0.00722484, 0.00094264, 0.0101763], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}