{"id": "48a4f41d-d2ac-495f-82be-15838cf2e3d6", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (arr.back() <= arr.front() && std::is_sorted(it, arr.end())) {\n        std::rotate(arr.begin(), it, arr.end());\n    } else if ((size_t)(it - arr.begin()) > 8 && std::is_sorted(it, arr.end())) {\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "7d877691-4584-41c1-b0fc-5e1488a48ca4", "generation": 4, "timestamp": 1773771051.7364662, "iteration_found": 86, "metrics": {"score": 0.9768146, "combined_score": 0.9768146, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964313, "adaptability_score": 0.995567, "avg_time": 0.00370078}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  #include <vector>\n  #include <algorithm>\n  #include <functional>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (std::is_sorted(arr.begin(), arr.end())) return;\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      auto it = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n      auto split = it + 1;\n      if (std::is_sorted(split, arr.end())) {\n          if (arr.back() <= arr.front()) std::rotate(arr.begin(), split, arr.end());\n          else std::inplace_merge(arr.begin(), split, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (arr.back() <= arr.front() && std::is_sorted(it, arr.end())) {\n          std::rotate(arr.begin(), it, arr.end());\n      } else if ((size_t)(it - arr.begin()) > 8 && std::is_sorted(it, arr.end())) {\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }", "parent_metrics": {"score": 0.97672, "combined_score": 0.97672, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964142, "adaptability_score": 0.995587, "avg_time": 0.00371911}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9767\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9770 \u2192 0.9767. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.000961731, 0.010973, 0.0002958, 0.00350111, 1.399e-05, 0.00014069, 0.00045465, 0.00671199, 0.000960551, 0.0101141, 0.00111803, 0.0135359, 1.239e-05, 0.00012329, 0.00068585, 0.00654849, 0.00095488, 0.0113046, 0.0004272, 0.0055439]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Adapt to reverse-sorted input patterns with an O(N) check\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Fallback to standard introsort for random or complex data\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Use a single-pass scan to check for ascending and descending patterns.\n      // This reduces the overhead for special cases and random data.\n      bool ascending = true;\n      bool descending = true;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) {\n              ascending = false;\n              if (!descending) break;\n          } else if (arr[i] > arr[i - 1]) {\n              descending = false;\n              if (!ascending) break;\n          }\n      }\n  \n      if (ascending) return;\n      if (descending) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // For unsorted data, fall back to std::sort (introsort).\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9770, combined_score: 0.9770, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9646, adaptability_score: 0.9956, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Adapt to reverse-sorted input patterns\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) scan to find the first break in ascending order\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // Detect reverse-sorted patterns: O(N) scan only if it's actually reverse-sorted\n      // For random data, this fails in O(1).\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Catch rotated sorted arrays or two sorted segments (e.g., single insertions).\n      // The threshold ensures we don't perform an O(N) scan on random data.\n      if ((it - arr.begin()) > 32 || (it - arr.begin()) > (n >> 3)) {\n          if (std::is_sorted(it, arr.end())) {\n              if (arr.back() <= arr.front()) {\n                  std::rotate(arr.begin(), it, arr.end());\n              } else {\n                  std::inplace_merge(arr.begin(), it, arr.end());\n              }\n              return;\n          }\n      }\n  \n      // Fallback to optimized introsort for general unsorted/random data\n      std::sort(arr.begin(), arr.end());\n  ... (1 more lines)\n- Metrics: score: 0.9771, combined_score: 0.9771, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9647, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  #include <vector>\n  #include <algorithm>\n  #include <functional>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Detect the first point where ascending order is broken\n      auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n      if (drop == arr.end()) return; // Already sorted\n  \n      // Detect the first point where descending order is broken\n      auto rise = std::adjacent_find(arr.begin(), arr.end(), std::less<int>());\n      if (rise == arr.end()) {\n          std::reverse(arr.begin(), arr.end()); // Reverse sorted\n          return;\n      }\n  \n      // Adaptive Check: If there is exactly one 'drop' and the remainder is sorted,\n      // we have two sorted runs (common in rotated arrays or single-element insertions).\n      auto second_run_start = std::next(drop);\n      if (std::is_sorted(second_run_start, arr.end())) {\n          std::inplace_merge(arr.begin(), second_run_start, arr.end());\n      } else {\n          // Fallback to optimized introsort for general unsorted data\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9771, combined_score: 0.9771, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9648, adaptability_score: 0.9957, avg_time: 0.0036\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first point where ascending order is broken\n    auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (drop == arr.end()) return; // Already sorted\n\n    // Detect the first point where descending order is broken\n    auto rise = std::adjacent_find(arr.begin(), arr.end(), std::less<int>());\n    if (rise == arr.end()) {\n        std::reverse(arr.begin(), arr.end()); // Reverse sorted\n        return;\n    }\n\n    // Adaptive Check: If there is exactly one 'drop' and the remainder is sorted,\n    // we have two sorted runs (common in rotated arrays or single-element insertions).\n    auto second_run_start = std::next(drop);\n    if (std::is_sorted(second_run_start, arr.end())) {\n        std::inplace_merge(arr.begin(), second_run_start, arr.end());\n    } else {\n        // Fallback to optimized introsort for general unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9771), Performs well on combined_score (0.9771), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) scan to find the first break in ascending order\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // Detect reverse-sorted patterns: O(N) scan only if it's actually reverse-sorted\n    // For random data, this fails in O(1).\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Catch rotated sorted arrays or two sorted segments (e.g., single insertions).\n    // The threshold ensures we don't perform an O(N) scan on random data.\n    if ((it - arr.begin()) > 32 || (it - arr.begin()) > (n >> 3)) {\n        if (std::is_sorted(it, arr.end())) {\n            if (arr.back() <= arr.front()) {\n                std::rotate(arr.begin(), it, arr.end());\n            } else {\n                std::inplace_merge(arr.begin(), it, arr.end());\n            }\n            return;\n        }\n    }\n\n    // Fallback to optimized introsort for general unsorted/random data\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9771), Performs well on combined_score (0.9771), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9647), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n### Program 4 (Score: 0.9770)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Use a single-pass scan to check for ascending and descending patterns.\n    // This reduces the overhead for special cases and random data.\n    bool ascending = true;\n    bool descending = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            ascending = false;\n            if (!descending) break;\n        } else if (arr[i] > arr[i - 1]) {\n            descending = false;\n            if (!ascending) break;\n        }\n    }\n\n    if (ascending) return;\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For unsorted data, fall back to std::sort (introsort).\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9770), Performs well on combined_score (0.9770), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9646), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0037)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n\n    // Check for ascending segments\n    int drop_count = 0;\n    size_t drop_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            drop_count++;\n            drop_index = i;\n            if (drop_count > 1) break;\n        }\n    }\n\n    if (drop_count == 0) return; // Already sorted\n    \n    if (drop_count == 1) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n        }\n        return;\n    }\n\n    // Check for descending segments\n    int rise_count = 0;\n    size_t rise_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] > arr[i - 1]) {\n            rise_count++;\n            rise_index = i;\n            if (rise_count > 1) break;\n        }\n    }\n\n    if (rise_count == 0) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    if (rise_count == 1) {\n        std::reverse(arr.begin(), arr.begin() + rise_index);\n        std::reverse(arr.begin() + rise_index, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + rise_index, arr.end());\n        return;\n    }\n\n    // Fallback to standard sort\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n\n    // Detect first drop; O(N) check for sorted data\n    auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (drop == arr.end()) return;\n\n    // Check for reverse-sorted pattern\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Check for two sorted runs (rotated arrays or single insertions)\n    auto split = drop + 1;\n    if (std::is_sorted(split, arr.end())) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), split, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), split, arr.end());\n        }\n        return;\n    }\n\n    // Fallback for general unsorted data\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns with an O(N) check\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    // Fallback to standard introsort for random or complex data\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9769, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::is_sorted(it, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n        else std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9767, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto d = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (d == arr.end()) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    auto s = std::next(d);\n    if (std::is_sorted(s, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), s, arr.end());\n        else std::inplace_merge(arr.begin(), s, arr.end());\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9767, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first point where the ascending order is broken\n    // This serves as an O(N) check for already-sorted data\n    auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (drop == arr.end()) return;\n\n    // Detect if the array is reverse-sorted\n    // Using rbegin/rend allows the compiler to potentially use SIMD optimizations for is_sorted\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Adaptive Check: If there is only one \"drop\" and the rest of the array is sorted,\n    // we have two sorted runs. This is common in rotated arrays or single-element shifts.\n    auto split = drop + 1;\n    if (std::is_sorted(split, arr.end())) {\n        // If the array is a pure rotation (e.g., [4, 5, 1, 2, 3]), std::rotate is O(N) and in-place.\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), split, arr.end());\n        } else {\n            // If it's two runs (e.g., [1, 2, 5, 6, 3, 4]), std::inplace_merge is the efficient choice.\n            std::inplace_merge(arr.begin(), split, arr.end());\n        }\n        return;\n    }\n\n    // Fallback to optimized introsort for general unsorted or complex data\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9766, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect initial monotone patterns (ascending or descending)\n    bool ascending = true;\n    bool descending = true;\n    size_t split = 0;\n    \n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            if (ascending) {\n                ascending = false;\n                split = i;\n            }\n        }\n        if (arr[i] > arr[i - 1]) descending = false;\n        \n        // If the array is neither fully ascending nor fully descending\n        if (!ascending && !descending) {\n            // Adaptive check: if we have a substantial sorted prefix, \n            // check if the remainder is also sorted (two-run pattern)\n            if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n            } else {\n                // Fallback to introsort for random or complex data\n                std::sort(arr.begin(), arr.end());\n            }\n            return;\n        }\n    }\n\n    // If the loop finishes, the array is entirely monotone\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (std::is_sorted(arr.begin(), arr.end())) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    auto it = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    auto split = it + 1;\n    if (std::is_sorted(split, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), split, arr.end());\n        else std::inplace_merge(arr.begin(), split, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will simplify the algorithm to reduce character count and eliminate redundant data scans. The current implementation performs multiple overlapping O(N) passes (e.g., `is_sorted`, then `adjacent_find`, then `is_sorted` on a suffix). \n\nThe revised implementation uses `std::is_sorted_until` to find the first point where the ascending order is broken in a single pass. It then uses cheap hints (`arr.back() <= arr.front()`) and thresholds to decide whether to check for more complex patterns like rotated arrays or merged sorted runs, protecting the performance on random data. This approach maximizes adaptability while maintaining high performance.\n\n<<<<<<< SEARCH\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (std::is_sorted(arr.begin(), arr.end())) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    auto it = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    auto split = it + 1;\n    if (std::is_sorted(split, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), split, arr.end());\n        else std::inplace_merge(arr.begin(), split, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n=======\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (arr.back() <= arr.front() && std::is_sorted(it, arr.end())) {\n        std::rotate(arr.begin(), it, arr.end());\n    } else if ((size_t)(it - arr.begin()) > 8 && std::is_sorted(it, arr.end())) {\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000952069, 0.0107715, 0.00030286, 0.00343833, 1.629e-05, 0.000169, 0.00047649, 0.00683616, 0.00091841, 0.0107038, 0.00106162, 0.0134392, 1.248e-05, 0.00012335, 0.00071835, 0.00555725, 0.000966709, 0.0115467, 0.00038628, 0.00561863], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}