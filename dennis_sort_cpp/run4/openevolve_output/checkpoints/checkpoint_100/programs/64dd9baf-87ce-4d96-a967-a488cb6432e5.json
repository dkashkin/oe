{"id": "64dd9baf-87ce-4d96-a967-a488cb6432e5", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n        return;\n    }\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n            *j = k;\n        }\n    } else std::sort(b, e);\n}", "changes_description": null, "language": "unknown", "parent_id": "29bdcb59-8737-4ccc-91ab-2a15684d7b75", "generation": 7, "timestamp": 1773769287.3301396, "iteration_found": 43, "metrics": {"score": 0.9771496000000001, "combined_score": 0.9771496000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964824, "adaptability_score": 0.995638, "avg_time": 0.00364587}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if (e - it < 32) {\n          for (; it != e; ++it) {\n              int k = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > k) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = k;\n          }\n      } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else {\n          std::sort(b, e);\n      }\nwith:\n  if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n          return;\n      }\n  \n      if (e - it < 24) {\n          for (; it != e; ++it) {\n              int k = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n              *j = k;\n          }\n      } else std::sort(b, e);", "parent_metrics": {"score": 0.9770768000000001, "combined_score": 0.9770768000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9647, "adaptability_score": 0.995642, "avg_time": 0.00365914}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9771\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9771 \u2192 0.9771. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.000967309, 0.010966, 0.00029687, 0.00340382, 1.887e-05, 0.0001879, 0.00046713, 0.006777, 0.00088934, 0.00985572, 0.0010443, 0.0132556, 8.18e-06, 8.133e-05, 0.00071681, 0.00623717, 0.000940109, 0.0112939, 0.00037794, 0.00539754]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // If only a few elements are unsorted at the end, use insertion sort\n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      auto b = arr.begin(), e = arr.end();\n      if (e - b < 2) return;\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 24) {\n          for (; it != e; ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else {\n          std::sort(b, e);\n      }\n  }\n- Metrics: score: 0.9771, combined_score: 0.9771, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9648, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // If only a few elements are unsorted at the end, use insertion sort\n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9649, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // If only a few elements are unsorted at the end, use insertion sort\n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9649, adaptability_score: 0.9957, avg_time: 0.0036\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Identify sorted prefix; skip forward if already sorted\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // Check for reverse-sorted data\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Use optimized Insertion Sort for small arrays or fall back to Introsort\n    if (n < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9649), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9649), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9649), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 4 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9771), Performs well on combined_score (0.9771), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 32) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9770)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto b = arr.begin(), e = arr.end(), it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // If only a few elements are unsorted at the end, use insertion sort\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Identify sorted prefix; skip forward if already sorted\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // Check for reverse-sorted data\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Use optimized Insertion Sort for small arrays or fall back to Introsort\n    if (n < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9771, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // If only a few elements are unsorted at the end, use insertion sort\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9770, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto b = arr.begin(), e = arr.end(), it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 32) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score and address the decline seen in recent iterations, I have refined the adaptive sorting strategy to better handle reverse-sorted and nearly-sorted data while keeping the code concise. \n\nKey improvements include:\n1.  **Prioritizing the Reverse-Sorted Check**: By moving the reverse-sorted check to before the small-tail insertion sort, we ensure that reverse-sorted arrays of all sizes (even small ones) are handled in $O(N)$ time with a simple reverse operation, rather than an $O(N^2)$ insertion sort.\n2.  **Optimized Threshold**: I've adjusted the insertion sort threshold to 24, which aligns with the highest-performing versions (0.9772) found in the history.\n3.  **Conciseness**: The code has been simplified to reduce character count while maintaining clarity, addressing the system's suggestion to keep the code under 500 characters.\n4.  **Adaptability**: The logic remains robust for various distributions, including large arrays with sorted prefixes, by using `is_sorted_until` and only performing the insertion sort on the unsorted tail.\n\n```cpp\n<<<<<<< SEARCH\n    if (e - it < 32) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n=======\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n        return;\n    }\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n            *j = k;\n        }\n    } else std::sort(b, e);\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.0009556, 0.0106772, 0.00029235, 0.00345794, 1.409e-05, 0.00012886, 0.00045423, 0.00657866, 0.00091205, 0.0103521, 0.00103338, 0.0130704, 7.54e-06, 7.485e-05, 0.00051428, 0.00625437, 0.00095228, 0.0113653, 0.00037022, 0.00545171], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}