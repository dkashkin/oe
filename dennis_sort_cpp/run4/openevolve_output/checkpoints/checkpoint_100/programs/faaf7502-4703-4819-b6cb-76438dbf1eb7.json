{"id": "faaf7502-4703-4819-b6cb-76438dbf1eb7", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto a = std::is_sorted_until(b, e);\n    auto d = std::is_sorted_until(b, e, [](int x, int y){ return x > y; });\n    if (a == e) return;\n    if (d == e) { std::reverse(b, e); return; }\n\n    auto it = a;\n    if (d > a) { std::reverse(b, d); it = d; }\n\n    if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int x, int y){ return x > y; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else std::sort(b, e);\n}", "changes_description": null, "language": "unknown", "parent_id": "162d2b6f-6e7d-4849-a565-8f46912ef6ea", "generation": 6, "timestamp": 1773771481.4106803, "iteration_found": 99, "metrics": {"score": 0.9810078, "combined_score": 0.9810078, "compile_success": 1.0, "correctness": 1, "performance_score": 0.970943, "adaptability_score": 0.996105, "avg_time": 0.00299268}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      auto b = arr.begin(), e = arr.end();\n      if (e - b < 2) return;\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 24) {\n          for (; it != e; ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else {\n          std::sort(b, e);\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      auto b = arr.begin(), e = arr.end();\n      auto a = std::is_sorted_until(b, e);\n      auto d = std::is_sorted_until(b, e, [](int x, int y){ return x > y; });\n      if (a == e) return;\n      if (d == e) { std::reverse(b, e); return; }\n  \n      auto it = a;\n      if (d > a) { std::reverse(b, d); it = d; }\n  \n      if (e - it < 48 || it - b > (e - it)) {\n          if (std::is_sorted(it, e, [](int x, int y){ return x > y; })) std::reverse(it, e);\n          else if (!std::is_sorted(it, e)) std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else std::sort(b, e);\n  }", "parent_metrics": {"score": 0.977142, "combined_score": 0.977142, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964818, "adaptability_score": 0.995628, "avg_time": 0.00364654}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9771\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9772 \u2192 0.9771. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00097013, 0.010935, 0.00029032, 0.00343266, 1.45e-05, 0.00013534, 0.00045069, 0.00678808, 0.00090549, 0.0101227, 0.00104524, 0.0134006, 8.88e-06, 8.827e-05, 0.00051896, 0.00600057, 0.00097151, 0.011047, 0.00037381, 0.00543121]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  auto b = arr.begin(), e = arr.end();\n      if (e - b < 2) return;\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 32) {\n          for (; it != e; ++it) {\n              int k = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > k) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = k;\n          }\n      } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else {\n          std::sort(b, e);\n      }\nwith:\n  auto b = arr.begin(), e = arr.end();\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 24) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (std::is_sorted(it, e)) {\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9650, adaptability_score: 0.9957, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  if (e - it < 24) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (std::is_sorted(it, e)) {\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\nwith:\n  if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (e - it < 48 || it - b > (e - it)) {\n          if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n          else if (!std::is_sorted(it, e)) std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\n- Metrics: score: 0.9809, combined_score: 0.9809, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9708, adaptability_score: 0.9961, avg_time: 0.0030\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      auto b = arr.begin(), e = arr.end();\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 24) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (std::is_sorted(it, e)) {\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      auto b = arr.begin(), e = arr.end();\n      auto a = std::is_sorted_until(b, e);\n      auto d = std::is_sorted_until(b, e, [](int x, int y){ return x > y; });\n      if (a == e) return;\n      if (d == e) { std::reverse(b, e); return; }\n      auto it = a;\n      if (d > a) { std::reverse(b, d); it = d; }\n      if (e - it < 48 || it - b > (e - it)) {\n          if (std::is_sorted(it, e, [](int x, int y){ return x > y; })) std::reverse(it, e);\n          else if (!std::is_sorted(it, e)) std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else std::sort(b, e);\n  }\n- Metrics: score: 0.9812, combined_score: 0.9812, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9712, adaptability_score: 0.9961, avg_time: 0.0030\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9817)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto a = std::is_sorted_until(b, e);\n    auto d = std::is_sorted_until(b, e, [](int a, int b){ return a > b; });\n    if (a == e) return;\n    if (d == e) { std::reverse(b, e); return; }\n    auto it = a;\n    if (d > a) { std::reverse(b, d); it = d; }\n    if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9817), Performs well on combined_score (0.9817), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9720), Performs well on adaptability_score (0.9963), Performs well on avg_time (0.0029)\n\n### Program 2 (Score: 0.9812)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto a = std::is_sorted_until(b, e);\n    auto d = std::is_sorted_until(b, e, [](int x, int y){ return x > y; });\n    if (a == e) return;\n    if (d == e) { std::reverse(b, e); return; }\n    auto it = a;\n    if (d > a) { std::reverse(b, d); it = d; }\n    if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int x, int y){ return x > y; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9812), Performs well on combined_score (0.9812), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9712), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0030)\n\n### Program 3 (Score: 0.9809)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9809), Performs well on combined_score (0.9809), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9708), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0030)\n\n### Program 4 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9650), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n        return;\n    }\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n            *j = k;\n        }\n    } else std::sort(b, e);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9771, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // If only a few elements are unsorted at the end, use insertion sort\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9767, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 32) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score and performance while maintaining a high adaptability score, I have revised the sorting logic to better detect both ascending and descending runs. The current program's detection of reverse-sorted arrays was limited to the entire array; the new version efficiently detects descending prefixes and suffixes, reversing them as needed.\n\nKey improvements:\n1.  **Dual Run Detection**: We now use `std::is_sorted_until` to find both the longest ascending and descending prefixes.\n2.  **Optimized Merging**: Instead of defaulting to a full `std::sort` when a prefix is already sorted, we use `std::inplace_merge` if the sorted part is significantly larger than the unsorted part or if the unsorted part is small.\n3.  **Heuristic Hybridization**: We use a threshold (48 elements) to decide between merging and sorting, which is a common optimization in adaptive algorithms like Timsort.\n4.  **Pattern Preservation**: The logic now specifically checks if the unsorted suffix is itself descending or already sorted before performing a merge, further reducing unnecessary work.\n\nThese changes target the `performance_score` and `adaptability_score` by specializing for common data patterns while remaining efficient for general cases.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto a = std::is_sorted_until(b, e);\n    auto d = std::is_sorted_until(b, e, [](int x, int y){ return x > y; });\n    if (a == e) return;\n    if (d == e) { std::reverse(b, e); return; }\n\n    auto it = a;\n    if (d > a) { std::reverse(b, d); it = d; }\n\n    if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int x, int y){ return x > y; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else std::sort(b, e);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00095972, 0.0106728, 0.00029834, 0.00334843, 1.3971e-05, 0.000140249, 0.000445671, 0.0068012, 0.00088366, 0.0102417, 3.743e-05, 0.00032772, 1.238e-05, 0.00012328, 0.00054772, 0.00682701, 0.0009519, 0.0113244, 0.00039816, 0.00549787], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}