{"id": "8b4463c7-8782-43ec-a74d-eacbd1f74339", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != it && s[-1] <= s[0]) --s;\n        if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "adb8d046-c260-4eca-a830-60fffae6c119", "generation": 8, "timestamp": 1773769412.89798, "iteration_found": 46, "metrics": {"score": 0.9813953999999999, "combined_score": 0.9813953999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.971471, "adaptability_score": 0.996282, "avg_time": 0.00293668}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  const size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          auto s = e - 1;\n          while (s != b && s[-1] <= s[0]) --s;\n          if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n              std::sort(b, s);\n              std::inplace_merge(b, s, e);\n          } else std::sort(b, e);\n      }\nwith:\n  size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          auto s = e - 1;\n          while (s != it && s[-1] <= s[0]) --s;\n          if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n              std::sort(b, s);\n              std::inplace_merge(b, s, e);\n          } else std::sort(b, e);\n      }", "parent_metrics": {"score": 0.9819370000000001, "combined_score": 0.9819370000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.972295, "adaptability_score": 0.9964, "avg_time": 0.00284947}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9819\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9815 \u2192 0.9819\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.000969639, 0.0104766, 0.00029425, 0.00334224, 1.51e-05, 0.00015338, 0.000448851, 0.00680312, 0.000705231, 0.00774742, 9.307e-05, 0.00102451, 6.55e-06, 6.477e-05, 0.000757329, 0.00618934, 0.00094492, 0.0111431, 0.00039383, 0.00541613]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  auto s = e - 1;\n      while (s != b && s[-1] <= s[0]) --s;\n      ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 8;\n      if (p >= q && (p > m || e - it < 128)) {\nwith:\n  auto s = e - 1;\n      while (s != it && s[-1] <= s[0]) --s;\n      ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 4;\n      if (p >= q && (p > m || e - it < 128)) {\n- Metrics: score: 0.9815, combined_score: 0.9815, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9717, adaptability_score: 0.9963, avg_time: 0.0029\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          auto s = e - 1;\n          while (s != b && s[-1] <= s[0]) --s;\n          if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n              std::sort(b, s);\n              std::inplace_merge(b, s, e);\n          } else std::sort(b, e);\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      auto s = e - 1;\n      while (s != b && s[-1] <= s[0]) --s;\n      ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 8;\n      if (p >= q && (p > m || e - it < 128)) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (q > m || s - b < 128) {\n          std::sort(b, s);\n          std::inplace_merge(b, s, e);\n      } else std::sort(b, e);\n  }\n- Metrics: score: 0.9817, combined_score: 0.9817, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9719, adaptability_score: 0.9963, avg_time: 0.0029\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      auto s = e - 1;\n      while (s != b && s[-1] <= s[0]) --s;\n      ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 8;\n      if (p >= q && (p > m || e - it < 128)) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (q > m || s - b < 128) {\n          std::sort(b, s);\n          std::inplace_merge(b, s, e);\n      } else std::sort(b, e);\nwith:\n  size_t n = arr.size();\n      auto b = arr.begin(), e = arr.end();\n      if (n < 64) { std::sort(b, e); return; }\n      auto it = std::is_sorted_until(b, e), r = b + 1;\n      while (r != e && r[-1] >= r[0]) ++r;\n      if (r > it) { std::reverse(b, r); it = r; }\n      if (it == e) return;\n      if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          auto s = e - 1;\n          while (s != b && s[-1] <= s[0]) --s;\n          if (it >= s) {\n              std::inplace_merge(b, it, e);\n          } else if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n              std::sort(b, s);\n              std::inplace_merge(b, s, e);\n          } else std::sort(b, e);\n      }\n- Metrics: score: 0.9818, combined_score: 0.9818, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9720, adaptability_score: 0.9964, avg_time: 0.0029\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9819)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != b && s[-1] <= s[0]) --s;\n        if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9819), Performs well on combined_score (0.9819), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9723), Performs well on adaptability_score (0.9964), Performs well on avg_time (0.0028)\n\n### Program 2 (Score: 0.9818)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != b && s[-1] <= s[0]) --s;\n        if (it >= s) {\n            std::inplace_merge(b, it, e);\n        } else if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9818), Performs well on combined_score (0.9818), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9720), Performs well on adaptability_score (0.9964), Performs well on avg_time (0.0029)\n\n### Program 3 (Score: 0.9817)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    auto s = e - 1;\n    while (s != b && s[-1] <= s[0]) --s;\n    ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 8;\n    if (p >= q && (p > m || e - it < 128)) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (q > m || s - b < 128) {\n        std::sort(b, s);\n        std::inplace_merge(b, s, e);\n    } else std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9817), Performs well on combined_score (0.9817), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9719), Performs well on adaptability_score (0.9963), Performs well on avg_time (0.0029)\n\n### Program 4 (Score: 0.9815)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    auto s = e - 1;\n    while (s != it && s[-1] <= s[0]) --s;\n    ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 4;\n    if (p >= q && (p > m || e - it < 128)) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (q > m || s - b < 128) {\n        std::sort(b, s);\n        std::inplace_merge(b, s, e);\n    } else std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9815), Performs well on combined_score (0.9815), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9717), Performs well on adaptability_score (0.9963), Performs well on avg_time (0.0029)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9817, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    auto s = e - 1;\n    while (s != b && s[-1] <= s[0]) --s;\n    ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 8;\n    if (p >= q && (p > m || e - it < 128)) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (q > m || s - b < 128) {\n        std::sort(b, s);\n        std::inplace_merge(b, s, e);\n    } else std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9815, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    auto s = e - 1;\n    while (s != it && s[-1] <= s[0]) --s;\n    ptrdiff_t p = it - b, q = e - s, m = (ptrdiff_t)n / 4;\n    if (p >= q && (p > m || e - it < 128)) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (q > m || s - b < 128) {\n        std::sort(b, s);\n        std::inplace_merge(b, s, e);\n    } else std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9818, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != b && s[-1] <= s[0]) --s;\n        if (it >= s) {\n            std::inplace_merge(b, it, e);\n        } else if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != b && s[-1] <= s[0]) --s;\n        if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we focus on two main areas: optimizing the performance for nearly-sorted data and simplifying the code to address the length warning.\n\n### Key Improvements:\n1.  **Suffix Scan Optimization**: In the suffix search, we now stop scanning at `it` (the end of the sorted prefix) instead of scanning all the way to the beginning of the array. This avoids redundant checks on elements already known to be part of the sorted prefix.\n2.  **Code Simplification**: We slightly compressed the logic and removed the `const` qualifier on `n` to keep the code more concise while maintaining the same robust thresholds for adaptation.\n3.  **Efficiency**: By stopping the suffix scan early, we improve the performance on arrays that consist of two sorted segments (like `[prefix] [suffix]`), which is a common pattern in nearly-sorted data.\n\nThe logic continues to handle small arrays, sorted/reverse-sorted prefixes, and sorted suffixes, falling back to `std::sort` only when no significant sorted portions are found.\n\n```cpp\n<<<<<<< SEARCH\n    const size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != b && s[-1] <= s[0]) --s;\n        if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n=======\n    size_t n = arr.size();\n    auto b = arr.begin(), e = arr.end();\n    if (n < 64) { std::sort(b, e); return; }\n    auto it = std::is_sorted_until(b, e), r = b + 1;\n    while (r != e && r[-1] >= r[0]) ++r;\n    if (r > it) { std::reverse(b, r); it = r; }\n    if (it == e) return;\n    if (it - b > (ptrdiff_t)n / 4 || e - it < 128) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        auto s = e - 1;\n        while (s != it && s[-1] <= s[0]) --s;\n        if (e - s > (ptrdiff_t)n / 4 || s - b < 128) {\n            std::sort(b, s);\n            std::inplace_merge(b, s, e);\n        } else std::sort(b, e);\n    }\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00097104, 0.0109558, 0.00034059, 0.00352624, 1.526e-05, 0.00017322, 0.00044459, 0.00687283, 0.000722069, 0.0078517, 9.632e-05, 0.0010893, 7e-06, 7.646e-05, 0.00045432, 0.00727302, 0.00094216, 0.0110795, 0.00039227, 0.00544997], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}