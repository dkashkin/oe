{"id": "a7d3926d-f437-41ea-b400-cc7e872a82a0", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect initial monotone patterns (ascending or descending)\n    bool ascending = true;\n    bool descending = true;\n    size_t split = 0;\n    \n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            if (ascending) {\n                ascending = false;\n                split = i;\n            }\n        }\n        if (arr[i] > arr[i - 1]) descending = false;\n        \n        // If the array is neither fully ascending nor fully descending\n        if (!ascending && !descending) {\n            // Adaptive check: if we have a substantial sorted prefix, \n            // check if the remainder is also sorted (two-run pattern)\n            if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n            } else {\n                // Fallback to introsort for random or complex data\n                std::sort(arr.begin(), arr.end());\n            }\n            return;\n        }\n    }\n\n    // If the loop finishes, the array is entirely monotone\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "6821cc26-3d48-4bc3-8bfe-66fa60be53ef", "generation": 2, "timestamp": 1773768389.9793127, "iteration_found": 20, "metrics": {"score": 0.9766034, "combined_score": 0.9766034, "compile_success": 1.0, "correctness": 1, "performance_score": 0.963933, "adaptability_score": 0.995609, "avg_time": 0.0037417}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Detect initial monotone patterns (ascending or descending)\n      bool ascending = true;\n      bool descending = true;\n      size_t split = 0;\n  \n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) {\n              if (ascending) {\n                  ascending = false;\n                  split = i;\n              }\n          }\n          if (arr[i] > arr[i - 1]) descending = false;\n  \n          // If the array is neither fully ascending nor fully descending\n          if (!ascending && !descending) {\n              // Adaptive check: if we have a substantial sorted prefix,\n              // check if the remainder is also sorted (two-run pattern)\n              if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                  std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n              } else {\n                  // Fallback to introsort for random or complex data\n                  std::sort(arr.begin(), arr.end());\n              }\n              return;\n          }\n  ... (7 more lines)", "parent_metrics": {"score": 0.9760588, "combined_score": 0.9760588, "compile_success": 1.0, "correctness": 1, "performance_score": 0.962916, "adaptability_score": 0.995773, "avg_time": 0.0038512}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9761\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.0000 \u2192 0.9761\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n\n## Last Execution Output\n\n### times\n```\n[0.00102521, 0.0107296, 0.00028883, 0.00336309, 0.00014076, 0.00171669, 0.000448229, 0.00670915, 0.00091091, 0.0104556, 0.00103265, 0.0129231, 0.00017782, 0.00211785, 0.000659009, 0.00615651, 0.00095663, 0.0113512, 0.000363251, 0.00549789]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Unknown changes\n- Metrics: score: 0.0000, compile_success: 0.0000, correctness: 0.0000, performance_score: 0.0000, adaptability_score: 0.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9761, combined_score: 0.9761, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9629, adaptability_score: 0.9958, avg_time: 0.0039\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Adapt to reverse-sorted input patterns\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Check for ascending segments\n      int drop_count = 0;\n      size_t drop_index = 0;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) {\n              drop_count++;\n              drop_index = i;\n              if (drop_count > 1) break;\n          }\n      }\n  \n      if (drop_count == 0) return; // Already sorted\n  \n      if (drop_count == 1) {\n          if (arr.back() <= arr.front()) {\n              std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n          } else {\n              std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n          }\n          return;\n      }\n  \n      // Check for descending segments\n  ... (25 more lines)\n- Metrics: score: 0.9769, combined_score: 0.9769, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9645, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n\n    // Check for ascending segments\n    int drop_count = 0;\n    size_t drop_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            drop_count++;\n            drop_index = i;\n            if (drop_count > 1) break;\n        }\n    }\n\n    if (drop_count == 0) return; // Already sorted\n    \n    if (drop_count == 1) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n        }\n        return;\n    }\n\n    // Check for descending segments\n    int rise_count = 0;\n    size_t rise_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] > arr[i - 1]) {\n            rise_count++;\n            rise_index = i;\n            if (rise_count > 1) break;\n        }\n    }\n\n    if (rise_count == 0) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    if (rise_count == 1) {\n        std::reverse(arr.begin(), arr.begin() + rise_index);\n        std::reverse(arr.begin() + rise_index, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + rise_index, arr.end());\n        return;\n    }\n\n    // Fallback to standard sort\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9769), Performs well on combined_score (0.9769), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9645), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n### Program 3 (Score: 0.9761)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9761), Performs well on combined_score (0.9761), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9629), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0039)\n\n### Program 4 (Score: 0.0000)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.0000), Performs well on compile_success (0.0000), Performs well on correctness (0.0000), Performs well on performance_score (0.0000), Performs well on adaptability_score (0.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9769, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n\n    // Check for ascending segments\n    int drop_count = 0;\n    size_t drop_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            drop_count++;\n            drop_index = i;\n            if (drop_count > 1) break;\n        }\n    }\n\n    if (drop_count == 0) return; // Already sorted\n    \n    if (drop_count == 1) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n        }\n        return;\n    }\n\n    // Check for descending segments\n    int rise_count = 0;\n    size_t rise_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] > arr[i - 1]) {\n            rise_count++;\n            rise_index = i;\n            if (rise_count > 1) break;\n        }\n    }\n\n    if (rise_count == 0) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    if (rise_count == 1) {\n        std::reverse(arr.begin(), arr.begin() + rise_index);\n        std::reverse(arr.begin() + rise_index, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + rise_index, arr.end());\n        return;\n    }\n\n    // Fallback to standard sort\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["This improvement enhances the sorting algorithm's adaptivity by detecting monotone runs (ascending or descending) and common \"nearly sorted\" patterns like two concatenated sorted segments. The implementation uses a single-pass scan for initial pattern detection, making it more efficient than multiple passes. For random data, it exits early with minimal overhead and falls back to `std::sort`, while for special cases, it leverages highly efficient $O(N)$ operations like `std::reverse` and `std::inplace_merge`.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect initial monotone patterns (ascending or descending)\n    bool ascending = true;\n    bool descending = true;\n    size_t split = 0;\n    \n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            if (ascending) {\n                ascending = false;\n                split = i;\n            }\n        }\n        if (arr[i] > arr[i - 1]) descending = false;\n        \n        // If the array is neither fully ascending nor fully descending\n        if (!ascending && !descending) {\n            // Adaptive check: if we have a substantial sorted prefix, \n            // check if the remainder is also sorted (two-run pattern)\n            if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n            } else {\n                // Fallback to introsort for random or complex data\n                std::sort(arr.begin(), arr.end());\n            }\n            return;\n        }\n    }\n\n    // If the loop finishes, the array is entirely monotone\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000958719, 0.0106195, 0.00028976, 0.0034693, 2.504e-05, 0.00025082, 0.00045131, 0.00666823, 0.0009002, 0.00990905, 0.0011286, 0.0132294, 2.051e-05, 0.00021339, 0.00063535, 0.00775359, 0.000954569, 0.011451, 0.00038294, 0.00552275], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}