{"id": "bdcd473e-bd4a-4d2e-a9dc-22a8be5149a4", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    if (arr.front() > arr.back() && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 4) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "a0bbb178-23f1-438b-9ca0-b9f9cbfa1958", "generation": 4, "timestamp": 1773769405.660831, "iteration_found": 45, "metrics": {"score": 0.9767258, "combined_score": 0.9767258, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964137, "adaptability_score": 0.995609, "avg_time": 0.00371966}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\nwith:\n  if (arr.front() > arr.back() && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (static_cast<size_t>(arr.end() - it) < (n >> 4) + 8) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }", "parent_metrics": {"score": 0.9770574000000001, "combined_score": 0.9770574000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964655, "adaptability_score": 0.995661, "avg_time": 0.00366401}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9771\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9770 \u2192 0.9771\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.0009945, 0.0105942, 0.00028945, 0.0033877, 1.869e-05, 0.00022587, 0.000453329, 0.00667097, 0.00092819, 0.0103929, 0.00105684, 0.0130974, 7.58e-06, 0.00010855, 0.00075351, 0.00635203, 0.00095293, 0.0112426, 0.00037275, 0.00538014]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      if (std::is_sorted(arr.begin(), arr.end())) return;\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Check for a sorted prefix and reuse the scan result\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // Optimization: If the unsorted suffix is small, sort it and merge\n      if (static_cast<size_t>(arr.end() - it) < (n / 8)) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          // Still check for reverse-sorted if suffix isn't nearly sorted\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          // Fall back to general sort for random distributions\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9770, combined_score: 0.9770, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9646, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (std::is_sorted(it, arr.end())) {\n          if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n          else std::inplace_merge(arr.begin(), it, arr.end());\n      } else if (static_cast<size_t>(arr.end() - it) < (n >> 3)) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9771, combined_score: 0.9771, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9647, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int l, int r) {\n      if (r - l < 16) {\n          std::sort(arr.begin() + l, arr.begin() + r + 1);\n          return;\n      }\n      int i = l, j = r, p = arr[l + (r - l) / 2];\n      while (i <= j) {\n          while (arr[i] < p) i++;\n          while (arr[j] > p) j--;\n          if (i <= j) std::swap(arr[i++], arr[j--]);\n      }\n      if (l < j) quicksort(arr, l, j);\n      if (i < r) quicksort(arr, i, r);\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1 && !std::is_sorted(arr.begin(), arr.end()))\n          quicksort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (std::is_sorted(it, arr.end())) {\n          if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n          else std::inplace_merge(arr.begin(), it, arr.end());\n      } else if (static_cast<size_t>(arr.end() - it) < (n >> 3)) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9648, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9773)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (std::is_sorted(arr.begin(), arr.end())) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9773), Performs well on combined_score (0.9773), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9650), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::is_sorted(it, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n        else std::inplace_merge(arr.begin(), it, arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 3)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9771), Performs well on combined_score (0.9771), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9647), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n### Program 4 (Score: 0.9770)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    \n    // Check for a sorted prefix and reuse the scan result\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    \n    // Optimization: If the unsorted suffix is small, sort it and merge\n    if (static_cast<size_t>(arr.end() - it) < (n / 8)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        // Still check for reverse-sorted if suffix isn't nearly sorted\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fall back to general sort for random distributions\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9770), Performs well on combined_score (0.9770), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9646), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::is_sorted(it, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n        else std::inplace_merge(arr.begin(), it, arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    if (arr.front() <= arr.back()) {\n        auto it = std::is_sorted_until(arr.begin(), arr.end());\n        if (it == arr.end()) return;\n        if (static_cast<size_t>(arr.end() - it) < (n / 8 + 8)) {\n            std::sort(it, arr.end());\n            std::inplace_merge(arr.begin(), it, arr.end());\n            return;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::is_sorted(it, arr.end())) {\n        if (arr.back() <= arr.front()) std::rotate(arr.begin(), it, arr.end());\n        else std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9766, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    const int f = arr.front();\n    const int b = arr.back();\n\n    // Use a heuristic based on the first and last elements to decide the check strategy.\n    // This allows identifying already-sorted or reverse-sorted arrays in a single pass\n    // while bailing out early to std::sort for random data.\n    if (f < b) {\n        // If the array is sorted, it must be in ascending order.\n        if (std::is_sorted(arr.begin(), arr.end())) return;\n    } else if (f > b) {\n        // If the array is sorted, it must be in descending order.\n        if (std::is_sorted(arr.rbegin(), arr.rend())) {\n            std::reverse(arr.begin(), arr.end());\n            return;\n        }\n    } else {\n        // If front == back, it can only be sorted if all elements are identical.\n        if (std::is_sorted(arr.begin(), arr.end())) return;\n    }\n\n    // Default to highly optimized std::sort for random or nearly-sorted data.\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9768, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = true, d = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) a = false;\n        else if (arr[i] > arr[i - 1]) d = false;\n        if (!a && !d) {\n            std::sort(arr.begin(), arr.end());\n            return;\n        }\n    }\n    if (!a) std::reverse(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9770, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    \n    // Check for a sorted prefix and reuse the scan result\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    \n    // Optimization: If the unsorted suffix is small, sort it and merge\n    if (static_cast<size_t>(arr.end() - it) < (n / 8)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        // Still check for reverse-sorted if suffix isn't nearly sorted\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fall back to general sort for random distributions\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9773, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    if (std::is_sorted(arr.begin(), arr.end())) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current `adaptive_sort` program already performs well, but its performance score can be improved by reducing redundant operations for random distributions and fine-tuning the adaptive threshold.\n\n### Key Changes:\n1.  **Redundant Scan Guard**: Introduced a `arr.front() > arr.back()` check before performing a full reverse-sorted scan. This prevents an unnecessary $O(n)$ pass for random data where the descending-sort property cannot possibly hold.\n2.  **Adaptive Threshold Tuning**: Adjusted the \"nearly sorted\" suffix threshold from `n/8 + 8` to `n/16 + 8`. Modern `std::sort` implementations are highly optimized for partially sorted data; using `std::inplace_merge` is only beneficial when the sorted prefix is exceptionally large. Reducing this threshold allows the more optimized `std::sort` to handle a wider range of cases while keeping the adaptive merge for truly nearly-sorted arrays.\n3.  **Logical Refinement**: The structure still leverages `std::is_sorted_until` to catch fully sorted arrays in a single pass, maintaining the high adaptability score (1.00) while shaving off execution time on non-ideal data.\n\n```cpp\n<<<<<<< SEARCH\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 3) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n=======\n    if (arr.front() > arr.back() && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (static_cast<size_t>(arr.end() - it) < (n >> 4) + 8) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.000961391, 0.0107033, 0.00029165, 0.00346243, 1.402e-05, 0.00014068, 0.00046651, 0.00687746, 0.00092489, 0.0100599, 0.0010503, 0.0132535, 8.88e-06, 9.212e-05, 0.00078074, 0.00708606, 0.0009719, 0.011416, 0.00039048, 0.00544099], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}