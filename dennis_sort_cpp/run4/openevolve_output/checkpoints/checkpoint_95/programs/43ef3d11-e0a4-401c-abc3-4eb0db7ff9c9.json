{"id": "43ef3d11-e0a4-401c-abc3-4eb0db7ff9c9", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "0b82aa22-8da8-4e29-a913-5cff527f1d6c", "generation": 7, "timestamp": 1773771105.369198, "iteration_found": 88, "metrics": {"score": 0.9809026, "combined_score": 0.9809026, "compile_success": 1.0, "correctness": 1, "performance_score": 0.970783, "adaptability_score": 0.996082, "avg_time": 0.00300963}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if (e - it < 24) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (std::is_sorted(it, e)) {\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\nwith:\n  if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (e - it < 48 || it - b > (e - it)) {\n          if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n          else if (!std::is_sorted(it, e)) std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }", "parent_metrics": {"score": 0.9765516, "combined_score": 0.9765516, "compile_success": 1.0, "correctness": 1, "performance_score": 0.963862, "adaptability_score": 0.995586, "avg_time": 0.00374929}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9766\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9772 \u2192 0.9766. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00098989, 0.0108143, 0.00033216, 0.00357199, 2.038e-05, 0.0002147, 0.00048433, 0.00683341, 0.00093543, 0.0102303, 0.00104356, 0.0132821, 9.81e-06, 9.763e-05, 0.00075547, 0.00697819, 0.00094298, 0.0115212, 0.00041736, 0.00551058]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // If only a few elements are unsorted at the end, use insertion sort\n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9649, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      // If only a few elements are unsorted at the end, use insertion sort\n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (arr.end() - it < 24) {\n          for (; it != arr.end(); ++it) {\n              int key = *it;\n              auto j = it;\n              while (j > arr.begin() && *(j - 1) > key) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = key;\n          }\n      } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9649, adaptability_score: 0.9957, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  auto b = arr.begin(), e = arr.end();\n      if (e - b < 2) return;\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 32) {\n          for (; it != e; ++it) {\n              int k = *it;\n              auto j = it;\n              while (j > b && *(j - 1) > k) {\n                  *j = *(j - 1);\n                  --j;\n              }\n              *j = k;\n          }\n      } else if (*it < *b && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else {\n          std::sort(b, e);\n      }\nwith:\n  auto b = arr.begin(), e = arr.end();\n      auto it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      if (e - it < 24) {\n          std::sort(it, e);\n          std::inplace_merge(b, it, e);\n      } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(b, e);\n      } else if (std::is_sorted(it, e)) {\n          std::inplace_merge(b, it, e);\n      } else {\n          std::sort(b, e);\n      }\n- Metrics: score: 0.9772, combined_score: 0.9772, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9650, adaptability_score: 0.9957, avg_time: 0.0036\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9812)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9812), Performs well on combined_score (0.9812), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9713), Performs well on adaptability_score (0.9962), Performs well on avg_time (0.0030)\n\n### Program 2 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9650), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9649), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0036)\n\n### Program 4 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9649), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    // If only a few elements are unsorted at the end, use insertion sort\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (arr[0] >= arr[1] && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n        return;\n    }\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n            *j = k;\n        }\n    } else std::sort(b, e);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9812, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9770, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto b = arr.begin(), e = arr.end(), it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = k;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9771, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    if (e - b < 2) return;\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n        return;\n    }\n\n    if (e - it < 24) {\n        for (; it != e; ++it) {\n            int k = *it;\n            auto j = it;\n            while (j > b && *(j - 1) > k) { *j = *(j - 1); --j; }\n            *j = k;\n        }\n    } else std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9767, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (arr.end() - it < 24) {\n        for (; it != arr.end(); ++it) {\n            int key = *it;\n            auto j = it;\n            while (j > arr.begin() && *(j - 1) > key) {\n                *j = *(j - 1);\n                --j;\n            }\n            *j = key;\n        }\n    } else if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    auto b = arr.begin(), e = arr.end();\n    auto it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    if (e - it < 24) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness declined due to a lower threshold for merging and an inefficient check for reverse-sorted arrays. By increasing the threshold for merging sorted runs (from 24 to 48) and prioritizing the detection of fully descending arrays, we can significantly improve performance. The following changes adapt the logic from the highest-performing version in the history (Score: 0.9812), which uses a more aggressive merging strategy when a significant sorted prefix is already present.\n\nThe `it - b > (e - it)` condition is particularly effective because it identifies when the majority of the array is already sorted, making an $O(N)$ `std::inplace_merge` more efficient than a full $O(N \\log N)$ `std::sort`.\n\n<<<<<<< SEARCH\n    if (e - it < 24) {\n        std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else if (it == b + 1 && std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (std::is_sorted(it, e)) {\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n=======\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(b, e);\n    } else if (e - it < 48 || it - b > (e - it)) {\n        if (std::is_sorted(it, e, [](int a, int b){ return a > b; })) std::reverse(it, e);\n        else if (!std::is_sorted(it, e)) std::sort(it, e);\n        std::inplace_merge(b, it, e);\n    } else {\n        std::sort(b, e);\n    }\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00096846, 0.0108781, 0.00029128, 0.003386, 1.511e-05, 0.00015178, 0.00049288, 0.00677291, 0.00093535, 0.0102147, 3.761e-05, 0.00034573, 7.03e-06, 6.943e-05, 0.0006644, 0.00661166, 0.00093771, 0.0115379, 0.00040158, 0.00547312], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}