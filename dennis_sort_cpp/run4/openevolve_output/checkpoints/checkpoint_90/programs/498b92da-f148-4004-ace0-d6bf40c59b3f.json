{"id": "498b92da-f148-4004-ace0-d6bf40c59b3f", "code": "#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first point where ascending order is broken\n    auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (drop == arr.end()) return; // Already sorted\n\n    // Detect the first point where descending order is broken\n    auto rise = std::adjacent_find(arr.begin(), arr.end(), std::less<int>());\n    if (rise == arr.end()) {\n        std::reverse(arr.begin(), arr.end()); // Reverse sorted\n        return;\n    }\n\n    // Adaptive Check: If there is exactly one 'drop' and the remainder is sorted,\n    // we have two sorted runs (common in rotated arrays or single-element insertions).\n    auto second_run_start = std::next(drop);\n    if (std::is_sorted(second_run_start, arr.end())) {\n        std::inplace_merge(arr.begin(), second_run_start, arr.end());\n    } else {\n        // Fallback to optimized introsort for general unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "a53b6114-328c-4f33-9632-e29bdf09432d", "generation": 1, "timestamp": 1773768456.529905, "iteration_found": 25, "metrics": {"score": 0.9771424, "combined_score": 0.9771424, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964798, "adaptability_score": 0.995659, "avg_time": 0.00364869}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  #include <vector>\n  #include <algorithm>\n  #include <functional>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Detect the first point where ascending order is broken\n      auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n      if (drop == arr.end()) return; // Already sorted\n  \n      // Detect the first point where descending order is broken\n      auto rise = std::adjacent_find(arr.begin(), arr.end(), std::less<int>());\n      if (rise == arr.end()) {\n          std::reverse(arr.begin(), arr.end()); // Reverse sorted\n          return;\n      }\n  \n      // Adaptive Check: If there is exactly one 'drop' and the remainder is sorted,\n      // we have two sorted runs (common in rotated arrays or single-element insertions).\n      auto second_run_start = std::next(drop);\n      if (std::is_sorted(second_run_start, arr.end())) {\n          std::inplace_merge(arr.begin(), second_run_start, arr.end());\n      } else {\n          // Fallback to optimized introsort for general unsorted data\n          std::sort(arr.begin(), arr.end());\n      }\n  }", "parent_metrics": {"score": 0.0, "compile_success": 0.0, "correctness": 0.0, "performance_score": 0.0, "adaptability_score": 0.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: score=0.00, performance_score=0.00, adaptability_score=0.00\n- Focus areas: - Fitness declined: 0.9761 \u2192 0.0000. Consider revising recent changes.\n- Exploring score=0.00, performance_score=0.00, adaptability_score=0.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9761, combined_score: 0.9761, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9629, adaptability_score: 0.9958, avg_time: 0.0039\n- Outcome: Improvement in all metrics\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Detect initial monotone patterns (ascending or descending)\n      bool ascending = true;\n      bool descending = true;\n      size_t split = 0;\n  \n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) {\n              if (ascending) {\n                  ascending = false;\n                  split = i;\n              }\n          }\n          if (arr[i] > arr[i - 1]) descending = false;\n  \n          // If the array is neither fully ascending nor fully descending\n          if (!ascending && !descending) {\n              // Adaptive check: if we have a substantial sorted prefix,\n              // check if the remainder is also sorted (two-run pattern)\n              if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                  std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n              } else {\n                  // Fallback to introsort for random or complex data\n                  std::sort(arr.begin(), arr.end());\n              }\n              return;\n          }\n  ... (7 more lines)\n- Metrics: score: 0.9766, combined_score: 0.9766, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9639, adaptability_score: 0.9956, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Adapt to reverse-sorted input patterns\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Check for ascending segments\n      int drop_count = 0;\n      size_t drop_index = 0;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) {\n              drop_count++;\n              drop_index = i;\n              if (drop_count > 1) break;\n          }\n      }\n  \n      if (drop_count == 0) return; // Already sorted\n  \n      if (drop_count == 1) {\n          if (arr.back() <= arr.front()) {\n              std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n          } else {\n              std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n          }\n          return;\n      }\n  \n      // Check for descending segments\n  ... (25 more lines)\n- Metrics: score: 0.9769, combined_score: 0.9769, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9645, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9772)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9772), Performs well on combined_score (0.9772), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9648), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n\n    // Check for ascending segments\n    int drop_count = 0;\n    size_t drop_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            drop_count++;\n            drop_index = i;\n            if (drop_count > 1) break;\n        }\n    }\n\n    if (drop_count == 0) return; // Already sorted\n    \n    if (drop_count == 1) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n        }\n        return;\n    }\n\n    // Check for descending segments\n    int rise_count = 0;\n    size_t rise_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] > arr[i - 1]) {\n            rise_count++;\n            rise_index = i;\n            if (rise_count > 1) break;\n        }\n    }\n\n    if (rise_count == 0) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    if (rise_count == 1) {\n        std::reverse(arr.begin(), arr.begin() + rise_index);\n        std::reverse(arr.begin() + rise_index, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + rise_index, arr.end());\n        return;\n    }\n\n    // Fallback to standard sort\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nKey features: Performs well on score (0.9769), Performs well on combined_score (0.9769), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9645), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n### Program 3 (Score: 0.9766)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect initial monotone patterns (ascending or descending)\n    bool ascending = true;\n    bool descending = true;\n    size_t split = 0;\n    \n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            if (ascending) {\n                ascending = false;\n                split = i;\n            }\n        }\n        if (arr[i] > arr[i - 1]) descending = false;\n        \n        // If the array is neither fully ascending nor fully descending\n        if (!ascending && !descending) {\n            // Adaptive check: if we have a substantial sorted prefix, \n            // check if the remainder is also sorted (two-run pattern)\n            if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n            } else {\n                // Fallback to introsort for random or complex data\n                std::sort(arr.begin(), arr.end());\n            }\n            return;\n        }\n    }\n\n    // If the loop finishes, the array is entirely monotone\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9766), Performs well on combined_score (0.9766), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9639), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0037)\n\n### Program 4 (Score: 0.9761)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9761), Performs well on combined_score (0.9761), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9629), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0039)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.0000)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to compile_success\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9772, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2 || std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Adapt to reverse-sorted input patterns\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9769, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n\n    // Check for ascending segments\n    int drop_count = 0;\n    size_t drop_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            drop_count++;\n            drop_index = i;\n            if (drop_count > 1) break;\n        }\n    }\n\n    if (drop_count == 0) return; // Already sorted\n    \n    if (drop_count == 1) {\n        if (arr.back() <= arr.front()) {\n            std::rotate(arr.begin(), arr.begin() + drop_index, arr.end());\n        } else {\n            std::inplace_merge(arr.begin(), arr.begin() + drop_index, arr.end());\n        }\n        return;\n    }\n\n    // Check for descending segments\n    int rise_count = 0;\n    size_t rise_index = 0;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] > arr[i - 1]) {\n            rise_count++;\n            rise_index = i;\n            if (rise_count > 1) break;\n        }\n    }\n\n    if (rise_count == 0) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    if (rise_count == 1) {\n        std::reverse(arr.begin(), arr.begin() + rise_index);\n        std::reverse(arr.begin() + rise_index, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + rise_index, arr.end());\n        return;\n    }\n\n    // Fallback to standard sort\n    std::sort(arr.begin(), arr.end());\n}\n\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9766, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect initial monotone patterns (ascending or descending)\n    bool ascending = true;\n    bool descending = true;\n    size_t split = 0;\n    \n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) {\n            if (ascending) {\n                ascending = false;\n                split = i;\n            }\n        }\n        if (arr[i] > arr[i - 1]) descending = false;\n        \n        // If the array is neither fully ascending nor fully descending\n        if (!ascending && !descending) {\n            // Adaptive check: if we have a substantial sorted prefix, \n            // check if the remainder is also sorted (two-run pattern)\n            if (split > n / 8 && std::is_sorted(arr.begin() + split, arr.end())) {\n                std::inplace_merge(arr.begin(), arr.begin() + split, arr.end());\n            } else {\n                // Fallback to introsort for random or complex data\n                std::sort(arr.begin(), arr.end());\n            }\n            return;\n        }\n    }\n\n    // If the loop finishes, the array is entirely monotone\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9761, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we need to address the fact that the current manual Quicksort implementation (Attempt 4/D1) is likely failing due to missing headers (`<vector>`, `<algorithm>`) and inefficient pivot selection. \n\nThe following improvements replace the manual implementation with a highly adaptive hybrid approach:\n1.  **Add Essential Headers**: Including `<vector>`, `<algorithm>`, and `<functional>` to allow use of standard library utilities.\n2.  **Early Exit for Sorted Data**: Using `std::adjacent_find` to detect if the array is already sorted in $O(N)$ time.\n3.  **Handle Reverse-Sorted Data**: Using a single pass to detect if the array is strictly descending, then using `std::reverse`.\n4.  **Detect and Merge Sorted Runs**: Identifying cases where the array consists of two sorted runs (e.g., rotated arrays or a single misplaced element) and using `std::inplace_merge` to fix them in $O(N)$ time.\n5.  **Robust Fallback**: Using `std::sort` (Introsort) for general cases, which provides $O(N \\log N)$ worst-case performance and is highly optimized.\n\nThis approach combines the best of the previous high-performing attempts while being more comprehensive and efficient.\n\n<<<<<<< SEARCH\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n=======\n#include <vector>\n#include <algorithm>\n#include <functional>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first point where ascending order is broken\n    auto drop = std::adjacent_find(arr.begin(), arr.end(), std::greater<int>());\n    if (drop == arr.end()) return; // Already sorted\n\n    // Detect the first point where descending order is broken\n    auto rise = std::adjacent_find(arr.begin(), arr.end(), std::less<int>());\n    if (rise == arr.end()) {\n        std::reverse(arr.begin(), arr.end()); // Reverse sorted\n        return;\n    }\n\n    // Adaptive Check: If there is exactly one 'drop' and the remainder is sorted,\n    // we have two sorted runs (common in rotated arrays or single-element insertions).\n    auto second_run_start = std::next(drop);\n    if (std::is_sorted(second_run_start, arr.end())) {\n        std::inplace_merge(arr.begin(), second_run_start, arr.end());\n    } else {\n        // Fallback to optimized introsort for general unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00095612, 0.0107882, 0.00029356, 0.00345679, 2.941e-05, 0.00016577, 0.00046118, 0.00672769, 0.00091584, 0.0104657, 0.00102537, 0.0129901, 1.235e-05, 0.00012335, 0.00063209, 0.00554802, 0.00104521, 0.0112814, 0.00042016, 0.00563545], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}