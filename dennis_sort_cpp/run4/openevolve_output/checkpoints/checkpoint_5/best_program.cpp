#include <vector>
#include <algorithm>

void adaptive_sort(std::vector<int>& arr) {
    // Utilize the standard library's highly optimized hybrid sorting algorithm (Introsort)
    std::sort(arr.begin(), arr.end());
}