{"id": "ca7da1fc-524f-4f82-be31-2585b03bb327", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "f1842f63-029f-4c45-8cb7-ce4a42eece98", "generation": 5, "timestamp": 1773805484.145913, "iteration_found": 225, "metrics": {"score": 0.9970038, "combined_score": 0.9970038, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995551, "adaptability_score": 0.999183, "avg_time": 0.000446884}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      if (n > 64) {\n          int mn = arr[0], mx = arr[0];\n          for (size_t i = 1; i < n; ++i) {\n              if (arr[i] < mn) mn = arr[i];\n              else if (arr[i] > mx) mx = arr[i];\n          }\n          const long long r = (long long)mx - mn;\n          if (r < 2000000 && r < (long long)n * 20) {\n              std::vector<int> c(r + 1, 0);\n              for (int x : arr) c[x - mn]++;\n              auto it = arr.begin();\n              for (int i = 0; i <= (int)r; ++i)\n                  if (int cnt = c[i]) {\n                      std::fill_n(it, cnt, i + mn);\n                      it += cnt;\n                  }\n              return;\n  ... (6 more lines)\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }", "parent_metrics": {"score": 0.996781, "combined_score": 0.996781, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995181, "adaptability_score": 0.999181, "avg_time": 0.000484282}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9968\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9973 \u2192 0.9968. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00017111, 0.00058152, 6.615e-05, 0.00070962, 1.051e-05, 0.00010266, 2.776e-05, 0.00028587, 0.00016308, 0.00064848, 4.697e-05, 0.00049029, 1.391e-05, 0.00013869, 5.82e-05, 0.00065844, 0.00102504, 0.00382985, 0.00014838, 0.00050912]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nwith:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nChange 2: Replace:\n  int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          mn = std::min(mn, arr[i]);\n          mx = std::max(mx, arr[i]);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 20) {\nwith:\n  int mn = arr[0], mx = arr[0];\n      for (int x : arr) {\n          mn = std::min(mn, x);\n          mx = std::max(mx, x);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9968, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    int a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    \n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 24LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9972, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n\n    const long long r = (long long)mx - mn;\n    if (n > 64 && r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int mn = arr[0], mx = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            if (arr[i] < mn) mn = arr[i];\n            else if (arr[i] > mx) mx = arr[i];\n        }\n        const long long r = (long long)mx - mn;\n        if (r < 2000000 && r < (long long)n * 20) {\n            std::vector<int> c(r + 1, 0);\n            for (int x : arr) c[x - mn]++;\n            auto it = arr.begin();\n            for (int i = 0; i <= (int)r; ++i)\n                if (int cnt = c[i]) {\n                    std::fill_n(it, cnt, i + mn);\n                    it += cnt;\n                }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program was slightly underperforming compared to previous versions. The primary improvements made are:\n1.  **Optimized the sortedness check**: Added `else if` and replaced the logical OR (`||`) with a bitwise OR (`|`) to potentially improve branch handling and avoid redundant checks.\n2.  **Streamlined the Counting Sort criteria**: Removed the arbitrary `n > 64` threshold and increased the density factor from `n * 20` to `n * 32`. This allows Counting Sort to be used in more situations where it is likely to outperform the standard sort.\n3.  **Cleaned up min/max finding**: Used a range-based for loop with `std::min` and `std::max` for conciseness and clarity, while ensuring the range calculation is safe from overflow.\n4.  **Simplified the Counting Sort implementation**: Used the return value of `std::fill_n` to update the iterator, resulting in more compact and readable code.\n\nThese changes aim to restore and exceed the previous high fitness score of 0.9974.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int mn = arr[0], mx = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            if (arr[i] < mn) mn = arr[i];\n            else if (arr[i] > mx) mx = arr[i];\n        }\n        const long long r = (long long)mx - mn;\n        if (r < 2000000 && r < (long long)n * 20) {\n            std::vector<int> c(r + 1, 0);\n            for (int x : arr) c[x - mn]++;\n            auto it = arr.begin();\n            for (int i = 0; i <= (int)r; ++i)\n                if (int cnt = c[i]) {\n                    std::fill_n(it, cnt, i + mn);\n                    it += cnt;\n                }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000156409, 0.000416371, 5.3929e-05, 0.0006176, 1.291e-05, 0.00012861, 1.642e-05, 0.00017492, 0.00014785, 0.00065107, 4.797e-05, 0.000423, 1.698e-05, 0.00017373, 4.959e-05, 0.00050072, 0.00102804, 0.00382354, 0.00012426, 0.00037377], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}