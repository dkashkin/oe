{"id": "e945e572-4307-43cf-9ca4-72899c2a0288", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    \n    // Check if already sorted\n    if (std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Check if reverse sorted\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fallback for random or mostly unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "b02350fa-308e-4abf-80ff-b6673f623fa7", "generation": 3, "timestamp": 1773798469.8614361, "iteration_found": 7, "metrics": {"score": 0.9771108000000001, "combined_score": 0.9771108000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964764, "adaptability_score": 0.995631, "avg_time": 0.00365234}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (!std::is_sorted(arr.begin(), arr.end())) {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      // Check if already sorted\n      if (std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Check if reverse sorted\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          // Fallback for random or mostly unsorted data\n          std::sort(arr.begin(), arr.end());\n      }\n  }", "parent_metrics": {"score": 0.9766598, "combined_score": 0.9766598, "compile_success": 1.0, "correctness": 1, "performance_score": 0.963961, "adaptability_score": 0.995708, "avg_time": 0.00373865}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9767\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness unchanged at 0.9767\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n\n## Last Execution Output\n\n### times\n```\n[0.00095603, 0.010636, 0.00029411, 0.00343669, 0.000146391, 0.00173419, 0.000532669, 0.00702621, 0.000902779, 0.00996984, 0.00104815, 0.013049, 7.52e-06, 7.942e-05, 0.000342551, 0.00619783, 0.000966279, 0.0113433, 0.00038104, 0.0057229]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1 && !std::is_sorted(arr.begin(), arr.end()))\n          quicksort(arr, 0, (int)arr.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pi = partition(arr, low, high);\n          if (pi > low) quicksort(arr, low, pi - 1);\n          quicksort(arr, pi + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      std::swap(arr[low + (high - low) / 2], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j)\n          if (arr[j] <= pivot) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (!std::is_sorted(arr.begin(), arr.end())) {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\n- Metrics: score: 0.9767, combined_score: 0.9767, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9640, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9767)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (!std::is_sorted(arr.begin(), arr.end())) {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9767), Performs well on combined_score (0.9767), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9640), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (!std::is_sorted(arr.begin(), arr.end())) {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I have enhanced the `adaptive_sort` function to more effectively handle reverse-sorted arrays, which is a common distribution that can be optimized.\n\nWhile the previous version handled already-sorted arrays in $O(N)$ time using `std::is_sorted`, it defaulted to `std::sort` ($O(N \\log N)$) for reverse-sorted data. The new implementation adds a check for the reverse-sorted case using `std::is_sorted(arr.rbegin(), arr.rend())`, which allows it to use `std::reverse` ($O(N)$) for such cases.\n\nKey aspects of this improvement:\n1.  **Efficiency for Random Data**: For random data, both `std::is_sorted` checks (forward and reverse) will fail almost immediately (typically after 1 or 2 comparisons), resulting in $O(1)$ overhead before falling back to `std::sort`.\n2.  **Efficiency for Specific Distributions**: Already-sorted arrays are handled in one $O(N)$ pass, and reverse-sorted arrays are handled in two $O(N)$ passes (one to detect and one to reverse). This is significantly faster than the $O(N \\log N)$ complexity of a standard sort.\n3.  **Adaptability**: By explicitly handling another common data pattern, the algorithm becomes more \"adaptive,\" which should improve both the performance and adaptability metrics.\n\n```cpp\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (!std::is_sorted(arr.begin(), arr.end())) {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    \n    // Check if already sorted\n    if (std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // Check if reverse sorted\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fallback for random or mostly unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.000968499, 0.0107169, 0.00029598, 0.0035391, 1.446e-05, 0.00014579, 0.00045975, 0.00672004, 0.00092178, 0.0105068, 0.00104124, 0.013248, 7.52e-06, 7.473e-05, 0.00052887, 0.00593412, 0.00094804, 0.0111423, 0.00038932, 0.00544371], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}