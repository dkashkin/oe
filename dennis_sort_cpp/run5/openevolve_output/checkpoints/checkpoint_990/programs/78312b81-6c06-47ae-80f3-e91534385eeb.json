{"id": "78312b81-6c06-47ae-80f3-e91534385eeb", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(); if (n < 2) return;\n    int *p = v.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        int* it = p;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}", "changes_description": null, "language": "unknown", "parent_id": "5822e710-c92c-4908-992c-46a5f6b6cf2f", "generation": 9, "timestamp": 1773826804.427556, "iteration_found": 893, "metrics": {"score": 0.9974486, "combined_score": 0.9974486, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996217, "adaptability_score": 0.999296, "avg_time": 0.000379736}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i] <= arr[i - 1]) ++i;\n      if (arr[0] > arr[i - 1]) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i - 1] <= arr[i]) ++i;\n      if (i == n) return;\n      int mn = arr[0], mx = mn;\n      for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i - 1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      int n = v.size(); if (n < 2) return;\n      int *p = v.data(), i = 1;\n      while (i < n && p[i] <= p[i-1]) ++i;\n      if (p[0] > p[i-1]) std::reverse(p, p + i);\n      while (i < n && p[i-1] <= p[i]) ++i;\n      if (i == n) return;\n      int mn = p[0], mx = mn;\n      for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          int* it = p;\n          for (int j = 0; j <= r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(p + i, p + n);\n          if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n      } else std::sort(p, p + n);\n  }", "parent_metrics": {"score": 0.9973824, "combined_score": 0.9973824, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996124, "adaptability_score": 0.99927, "avg_time": 0.000389077}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9974\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9974 \u2192 0.9974. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00015351, 0.00042938, 4.65e-05, 0.00050267, 7.32e-06, 7.225e-05, 1.702e-05, 0.00015261, 0.00013693, 0.00049317, 3.005e-05, 0.00032261, 5.48e-06, 4.657e-05, 4.333e-05, 0.00045481, 0.00095996, 0.0033958, 0.00011247, 0.0003991]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  while (i < n && arr[i] == arr[i-1]) i++;\n      if (i < n && arr[i] < arr[i-1]) {\n          while (i < n && arr[i] <= arr[i-1]) i++;\n          std::reverse(arr.begin(), arr.begin() + i);\n      }\n      while (i < n && arr[i-1] <= arr[i]) i++;\nwith:\n  while (i < n && arr[i] <= arr[i-1]) ++i;\n      if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i-1] <= arr[i]) ++i;\nChange 2: 'if (r < 1000000 && r < n * 48LL) {' to 'if (r < 2000000 && r < n * 32LL) {'\nChange 3: Replace:\n  if (i * 3 > n || n - i < 64) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\nwith:\n  if (i * 4 > n || n - i < 80) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  int i = 1;\n      while (i < n && arr[i] == arr[i-1]) i++;\n      if (i < n && arr[i] < arr[i-1]) {\n          while (i < n && arr[i] <= arr[i-1]) i++;\n          std::reverse(arr.begin(), arr.begin() + i);\n      }\n      while (i < n && arr[i-1] <= arr[i]) i++;\nwith:\n  int i = 1;\n      while (i < n && arr[i] <= arr[i-1]) ++i;\n      if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i-1] <= arr[i]) ++i;\nChange 2: Replace:\n  int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\nwith:\n  int mn = arr[0], mx = mn;\n      for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& v) {\n      int n = v.size(), i = 1; if (n < 2) return;\n      while (i < n && v[i] <= v[i-1]) ++i;\n      if (v[0] > v[i-1]) std::reverse(v.begin(), v.begin() + i);\n      while (i < n && v[i-1] <= v[i]) ++i;\n      if (i == n) return;\n      int mn = v[0], mx = mn;\n      for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(v.begin() + i, v.end());\n          if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n      } else std::sort(v.begin(), v.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      int n = v.size(); if (n < 2) return;\n      int *p = v.data(), i = 1;\n      while (i < n && p[i] <= p[i-1]) ++i;\n      if (p[0] > p[i-1]) std::reverse(p, p + i);\n      while (i < n && p[i-1] <= p[i]) ++i;\n      if (i == n) return;\n      int mn = p[0], mx = mn;\n      for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          int* it = p;\n          for (int j = 0; j <= r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(p + i, p + n);\n          if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n      } else std::sort(p, p + n);\n  }\n- Metrics: score: 0.9975, combined_score: 0.9975, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] <= v[i-1]) ++i;\n    if (v[0] > v[i-1]) std::reverse(v.begin(), v.begin() + i);\n    while (i < n && v[i-1] <= v[i]) ++i;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(); if (n < 2) return;\n    int *p = v.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        int* it = p;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    int i = 1;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int v = c[j]) it = std::fill_n(it, v, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int *p = arr.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* q = p;\n        for (int j = 0; j <= (int)r; ++j) if (int k = c[j]) q = std::fill_n(q, k, j + mn);\n    } else if (i * 4 > n || n - i < 80) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] <= v[i-1]) ++i;\n    if (v[0] > v[i-1]) std::reverse(v.begin(), v.begin() + i);\n    while (i < n && v[i-1] <= v[i]) ++i;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9974, Type: Migrant)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int *p = arr.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* q = p;\n        for (int j = 0; j <= (int)r; ++j) if (int k = c[j]) q = std::fill_n(q, k, j + mn);\n    } else if (i * 4 > n || n - i < 80) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int *p = arr.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int *q = p;\n        for (int j = 0; j <= (int)r; ++j) if (int k = c[j]) q = std::fill_n(q, k, j + mn);\n    } else if (i * 4 > n || n - i < 64) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int i = 1;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i - 1]) ++i;\n    if (arr[0] > arr[i - 1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i - 1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score and restore it to its peak (0.9975), we will shift the current implementation toward the successful pointer-based approach of Program 2. Using raw pointers often results in slightly better performance and code density by avoiding iterator overhead and simplifying function calls. \n\nAdditionally, we'll refine the prefix-sorted detection and the counting sort implementation to be more compact, which helps address the suggestion to simplify the code length.\n\n### Reasoning:\n1.  **Raw Pointers:** Using `int *p = v.data()` and applying `std::sort`, `std::reverse`, and `std::inplace_merge` to the pointer range improves micro-optimization across different compilers.\n2.  **Compact Counting Sort:** Reconstructing the array using `it = std::fill_n(it, k, j + mn)` with pointers is both efficient and concise.\n3.  **Refined Logic Flow:** Maintaining the `i * 4 > n || n - i < 80` heuristic for merging ensures that we only perform expensive `inplace_merge` operations when a significant portion of the array is already sorted.\n4.  **Consolidation:** Variable names and loop structures are tightened to reduce the character count while preserving the high-performance AVX2 pragmas.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i - 1]) ++i;\n    if (arr[0] > arr[i - 1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i - 1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i - 1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(); if (n < 2) return;\n    int *p = v.data(), i = 1;\n    while (i < n && p[i] <= p[i-1]) ++i;\n    if (p[0] > p[i-1]) std::reverse(p, p + i);\n    while (i < n && p[i-1] <= p[i]) ++i;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        int* it = p;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00016914, 0.00040659, 4.673e-05, 0.00048529, 9.63e-06, 9.48e-05, 1.5709e-05, 0.00014062, 0.00013722, 0.00048632, 2.9691e-05, 0.000307629, 4.66e-06, 4.642e-05, 4.297e-05, 0.0004355, 0.0009843, 0.00326733, 9.655e-05, 0.00038763], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}