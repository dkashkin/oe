{"id": "6ffa6e62-3b10-410b-ac83-673c60301e06", "code": "#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = false;\n        else if (a[i] > a[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}", "changes_description": null, "language": "unknown", "parent_id": "7d31dacc-c191-4791-a0f2-0070e0045857", "generation": 5, "timestamp": 1773803740.2217395, "iteration_found": 172, "metrics": {"score": 0.9797758, "combined_score": 0.9797758, "compile_success": 1.0, "correctness": 1, "performance_score": 0.968969, "adaptability_score": 0.995986, "avg_time": 0.00320248}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void qs(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n          else { qs(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n  ... (10 more lines)\nwith:\n  void qs(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (true) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n          else { qs(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = (int)a.size();\n  ... (10 more lines)", "parent_metrics": {"score": 0.9797378000000001, "combined_score": 0.9797378000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.968897, "adaptability_score": 0.995999, "avg_time": 0.00321019}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9797\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9800 \u2192 0.9797. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n      }\n      if (s) return;\n      if (r) { std::reverse(a.begin(), a.end()); return; }\n      qs(a, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (a[i] < a[i-1]) s = 0;\n          else if (a[i] > a[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) { std::reverse(a.begin(), a.end()); return; }\n      qs(a, 0, n - 1);\n  }\n- Metrics: score: 0.9800, combined_score: 0.9800, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9693, adaptability_score: 0.9960, avg_time: 0.0032\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = (int)a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n      }\n      if (s) return;\n      if (r) { std::reverse(a.begin(), a.end()); return; }\n      quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n  ... (5 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }\n- Metrics: score: 0.9801, combined_score: 0.9801, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9694, adaptability_score: 0.9961, avg_time: 0.0032\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  int partition(std::vector<int>& arr, int low, int high);\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (arr[i] < arr[i - 1]) s = false;\n          else if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p; }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n          while (arr[--j] > p);\n          if (i >= j) return j;\n          std::swap(arr[i], arr[j]);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int v = arr[i], j = i;\n                  if (v < arr[j - 1]) {\n                      while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                      arr[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n          if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n          if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n          int p = arr[m], i = low - 1, j = high + 1;\n          while (true) {\n              while (arr[++i] < p);\n              while (arr[--j] > p);\n              if (i >= j) break;\n              std::swap(arr[i], arr[j]);\n          }\n          if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n          else { quicksort(arr, j + 1, high); high = j; }\n      }\n  }\n- Metrics: score: 0.9802, combined_score: 0.9802, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9696, adaptability_score: 0.9961, avg_time: 0.0031\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9802)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9802), Performs well on combined_score (0.9802), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9696), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0031)\n\n### Program 2 (Score: 0.9802)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9802), Performs well on combined_score (0.9802), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9696), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0031)\n\n### Program 3 (Score: 0.9801)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9801), Performs well on combined_score (0.9801), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9694), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0032)\n\n### Program 4 (Score: 0.9800)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i-1]) s = 0;\n        else if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9800), Performs well on combined_score (0.9800), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9693), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0032)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9798)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9798)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = false;\n        else if (a[i] > a[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9800)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9796, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9802, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9796, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i];\n                if (v < arr[i - 1]) {\n                    int j = i - 1;\n                    while (j >= low && arr[j] > v) { arr[j + 1] = arr[j]; j--; }\n                    arr[j + 1] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9773, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I have analyzed the previous attempts and the top-performing programs. The decline from 0.9800 to 0.9797 seems to be related to very minor formatting and initialization changes. I will revert the quicksort partitioning structure to match the most successful version (0.9802), specifically by moving the initialization of the partition indices and using `while (true)` for the main partition loop. Additionally, I will refine the `adaptive_sort` function to use explicit boolean constants and ensure the insertion sort remains highly efficient for nearly-sorted data.\n\nThese changes focus on maximizing performance while maintaining the adaptability and simplicity scores by keeping the code concise and robust.\n\n<<<<<<< SEARCH\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n=======\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = false;\n        else if (a[i] > a[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000962251, 0.0115893, 0.000271169, 0.00295799, 2.683e-05, 0.00027667, 0.00047951, 0.00682143, 0.000895289, 0.0103508, 0.00065487, 0.00812375, 1.543e-05, 0.00013057, 0.00017734, 0.00238725, 0.00096401, 0.0114815, 0.00037385, 0.00510981], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}