{"id": "eaefdab5-4918-4c8b-81fa-796560207bbc", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    auto p = std::minmax_element(arr.begin(), arr.end());\n    int mn = *p.first, mx = *p.second;\n    const long long r = (long long)mx - mn;\n\n    if (n > 64 && r < 4000000 && r < (long long)n * 40) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "339120b0-659b-4708-9e42-70517a480bda", "generation": 4, "timestamp": 1773799042.8236156, "iteration_found": 28, "metrics": {"score": 0.9960758, "combined_score": 0.9960758, "compile_success": 1.0, "correctness": 1, "performance_score": 0.994059, "adaptability_score": 0.999101, "avg_time": 0.000597671}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) detection of pre-sorted or reverse-sorted patterns\n      // This reliably breaks in O(1) for random data\n      bool asc = true, desc = true;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Density check: calculate range to determine if Counting Sort is viable\n      int min_v = arr[0], max_v = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          min_v = std::min(min_v, arr[i]);\n          max_v = std::max(max_v, arr[i]);\n      }\n  \n      const long long range = (long long)max_v - min_v;\n      // Use Counting Sort for dense distributions (memory-safe threshold of ~8MB)\n      if (range < 2000000 && range < (long long)n * 20) {\n          std::vector<int> counts(range + 1, 0);\n          for (int x : arr) {\n  ... (15 more lines)\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      auto p = std::minmax_element(arr.begin(), arr.end());\n      int mn = *p.first, mx = *p.second;\n      const long long r = (long long)mx - mn;\n  \n      if (n > 64 && r < 4000000 && r < (long long)n * 40) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }", "parent_metrics": {"score": 0.99728, "combined_score": 0.99728, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995962, "adaptability_score": 0.999257, "avg_time": 0.00040542}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9973\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9755 \u2192 0.9973\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.0001579, 0.00043325, 5.293e-05, 0.00051797, 1.374e-05, 0.00010956, 1.676e-05, 0.0001409, 0.00014756, 0.00055047, 3.336e-05, 0.00043637, 1.393e-05, 0.00013949, 3.766e-05, 0.00039952, 0.0009596, 0.00347549, 0.00010907, 0.00036288]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      // std::sort implements Introsort, an adaptive hybrid sorting algorithm\n      // that starts with quicksort and switches to heapsort/insertion sort\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9755, combined_score: 0.9755, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9621, adaptability_score: 0.9957, avg_time: 0.0039\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or strictly reverse-sorted inputs\n      // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n      bool asc = true;\n      bool desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!asc && !desc) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Attempt Counting Sort for inputs with a dense distribution\n      if (n >= 128) {\n          int min_val = arr[0];\n          int max_val = arr[0];\n          // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n          for (int i = 1; i < n; ++i) {\n              min_val = std::min(min_val, arr[i]);\n              max_val = std::max(max_val, arr[i]);\n          }\n  \n  ... (24 more lines)\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const int n = arr.size();\n      if (n < 2) return;\n  \n      bool asc = true, desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n      if (asc) return;\n      if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      if (n > 64) {\n          int min_val = arr[0], max_val = arr[0];\n          for (int i = 1; i < n; ++i) {\n              min_val = std::min(min_val, arr[i]);\n              max_val = std::max(max_val, arr[i]);\n          }\n          const long long range = (long long)max_val - min_val;\n          if (range <= 4194304 && range <= (long long)n * 32) {\n              std::vector<int> counts(range + 1, 0);\n              for (int x : arr) counts[x - min_val]++;\n              int idx = 0;\n              for (int i = 0; i <= (int)range; ++i) {\n                  if (int c = counts[i]) {\n                      std::fill_n(arr.begin() + idx, c, i + min_val);\n                      idx += c;\n                  }\n              }\n  ... (5 more lines)\n- Metrics: score: 0.9969, combined_score: 0.9969, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9955, adaptability_score: 0.9991, avg_time: 0.0005\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or reverse-sorted patterns\n      bool asc = true;\n      bool desc = true;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // For dense distributions, Counting Sort provides O(N) performance\n      if (n > 64) {\n          int min_v = arr[0];\n          int max_v = arr[0];\n          for (size_t i = 1; i < n; ++i) {\n              int val = arr[i];\n  ... (25 more lines)\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) detection of pre-sorted or reverse-sorted patterns\n    // This reliably breaks in O(1) for random data\n    bool asc = true, desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Density check: calculate range to determine if Counting Sort is viable\n    int min_v = arr[0], max_v = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        min_v = std::min(min_v, arr[i]);\n        max_v = std::max(max_v, arr[i]);\n    }\n\n    const long long range = (long long)max_v - min_v;\n    // Use Counting Sort for dense distributions (memory-safe threshold of ~8MB)\n    if (range < 2000000 && range < (long long)n * 20) {\n        std::vector<int> counts(range + 1, 0);\n        for (int x : arr) {\n            counts[x - min_v]++;\n        }\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)range; ++i) {\n            if (int c = counts[i]) {\n                std::fill_n(it, c, i + min_v);\n                it += c;\n            }\n        }\n        return;\n    }\n\n    // Fallback to Introsort for sparse distributions\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9969)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    bool asc = true, desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int min_val = arr[0], max_val = arr[0];\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n        const long long range = (long long)max_val - min_val;\n        if (range <= 4194304 && range <= (long long)n * 32) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_val]++;\n            int idx = 0;\n            for (int i = 0; i <= (int)range; ++i) {\n                if (int c = counts[i]) {\n                    std::fill_n(arr.begin() + idx, c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9969), Performs well on combined_score (0.9969), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9955), Performs well on adaptability_score (0.9991), Performs well on avg_time (0.0005)\n\n### Program 4 (Score: 0.9755)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9755), Performs well on combined_score (0.9755), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9621), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0039)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9755, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9969, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    bool asc = true, desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int min_val = arr[0], max_val = arr[0];\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n        const long long range = (long long)max_val - min_val;\n        if (range <= 4194304 && range <= (long long)n * 32) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_val]++;\n            int idx = 0;\n            for (int i = 0; i <= (int)range; ++i) {\n                if (int c = counts[i]) {\n                    std::fill_n(arr.begin() + idx, c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) detection of pre-sorted or reverse-sorted patterns\n    // This reliably breaks in O(1) for random data\n    bool asc = true, desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Density check: calculate range to determine if Counting Sort is viable\n    int min_v = arr[0], max_v = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        min_v = std::min(min_v, arr[i]);\n        max_v = std::max(max_v, arr[i]);\n    }\n\n    const long long range = (long long)max_v - min_v;\n    // Use Counting Sort for dense distributions (memory-safe threshold of ~8MB)\n    if (range < 2000000 && range < (long long)n * 20) {\n        std::vector<int> counts(range + 1, 0);\n        for (int x : arr) {\n            counts[x - min_v]++;\n        }\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)range; ++i) {\n            if (int c = counts[i]) {\n                std::fill_n(it, c, i + min_v);\n                it += c;\n            }\n        }\n        return;\n    }\n\n    // Fallback to Introsort for sparse distributions\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the program's fitness while exploring the diversity space and addressing the code length suggestion, I will simplify the adaptive sorting implementation. \n\nThe primary changes are:\n1.  **Code Simplification**: Removed verbose comments and used more concise variable names to bring the code closer to the 500-character target, which can improve the overall fitness if length is a metric.\n2.  **Efficiency Improvement**: Replaced the manual min/max calculation pass with `std::minmax_element`. Modern STL implementations of `minmax_element` process elements in pairs, performing exactly `3n/2` comparisons, which is often more efficient than separate passes or manual loops.\n3.  **Adaptive Threshold Adjustment**: Increased the Counting Sort memory threshold to 4 million entries (approximately 16MB) and the density factor to 40. This allows the O(N) counting sort to trigger on a wider variety of inputs, potentially improving performance on sparse-but-frequent distributions.\n4.  **Safeguard for Small N**: Added a `n > 64` check for the counting sort logic to ensure that for very small arrays, where `std::sort` (optimized with insertion sort) is already optimal, we avoid the overhead of vector allocation and zero-initialization.\n\n```cpp\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) detection of pre-sorted or reverse-sorted patterns\n    // This reliably breaks in O(1) for random data\n    bool asc = true, desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Density check: calculate range to determine if Counting Sort is viable\n    int min_v = arr[0], max_v = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        min_v = std::min(min_v, arr[i]);\n        max_v = std::max(max_v, arr[i]);\n    }\n\n    const long long range = (long long)max_v - min_v;\n    // Use Counting Sort for dense distributions (memory-safe threshold of ~8MB)\n    if (range < 2000000 && range < (long long)n * 20) {\n        std::vector<int> counts(range + 1, 0);\n        for (int x : arr) {\n            counts[x - min_v]++;\n        }\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)range; ++i) {\n            if (int c = counts[i]) {\n                std::fill_n(it, c, i + min_v);\n                it += c;\n            }\n        }\n        return;\n    }\n\n    // Fallback to Introsort for sparse distributions\n    std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    auto p = std::minmax_element(arr.begin(), arr.end());\n    int mn = *p.first, mx = *p.second;\n    const long long r = (long long)mx - mn;\n\n    if (n > 64 && r < 4000000 && r < (long long)n * 40) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00021612, 0.00107833, 7.2191e-05, 0.0007191, 1.04e-05, 0.00010238, 8.058e-05, 0.0007672, 0.000188349, 0.00149663, 6.566e-05, 0.00054752, 1.238e-05, 0.0001233, 5.537e-05, 0.00066804, 0.00101102, 0.00407975, 0.00012454, 0.00053456], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}