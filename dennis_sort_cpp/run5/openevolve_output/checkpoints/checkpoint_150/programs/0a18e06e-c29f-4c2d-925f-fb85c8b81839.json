{"id": "0a18e06e-c29f-4c2d-925f-fb85c8b81839", "code": "#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                while (j >= l && a[j] > k) a[j + 1] = a[j--];\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int v = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < v);\n            while (a[--j] > v);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "5f1d867f-e732-4853-955e-0b749e4b071f", "generation": 2, "timestamp": 1773798752.2306442, "iteration_found": 18, "metrics": {"score": 0.0, "combined_score": 0.0, "compile_success": 1.0, "correctness": 0, "performance_score": 0.969283, "adaptability_score": 0.996113, "avg_time": 0.00316907}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n; ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { std::reverse(a.begin(), a.end()); return; }\n      quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n  ... (5 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 24) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int k = a[i], j = i - 1;\n                  while (j >= l && a[j] > k) a[j + 1] = a[j--];\n                  a[j + 1] = k;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int v = a[m], i = l - 1, j = h + 1;\n          while (true) {\n              while (a[++i] < v);\n              while (a[--j] > v);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }", "parent_metrics": {"score": 0.9774858000000001, "combined_score": 0.9774858000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965423, "adaptability_score": 0.99558, "avg_time": 0.00358152}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9775\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.7033 \u2192 0.9775\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00112768, 0.0122268, 0.00031189, 0.00366497, 2.208e-05, 0.00022121, 0.00058126, 0.00784615, 0.00100194, 0.0116218, 0.0007584, 0.00885098, 1.3731e-05, 0.000136369, 0.000232591, 0.00280071, 0.00106232, 0.0129109, 0.00044747, 0.00579115]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  int partition(std::vector<int>& arr, int low, int high);\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      bool sorted = true;\n      for (int i = 1; i < (int)arr.size(); ++i)\n          if (arr[i] < arr[i-1]) { sorted = false; break; }\n      if (!sorted) quicksort(arr, 0, (int)arr.size() - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                  arr[j+1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p - 1; }\n      }\n  }\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j)\n          if (arr[j] < pivot) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\n- Metrics: score: 0.7033, combined_score: 0.7033, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.6249, adaptability_score: 0.8210, avg_time: 0.0600\n- Outcome: Improvement in all metrics\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 3: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p - 1; }\n      }\n  }\nChange 4: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int p = arr[high], i = low;\n      for (int j = low; j < high; j++)\n          if (arr[j] < p) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\n- Metrics: score: 0.7040, combined_score: 0.7040, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.6259, adaptability_score: 0.8212, avg_time: 0.0598\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n          while (arr[--j] > p);\n          if (i >= j) return j;\n          std::swap(arr[i], arr[j]);\n      }\n  }\n- Metrics: score: 0.9775, combined_score: 0.9775, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9654, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9777)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nKey features: Performs well on score (0.9777), Performs well on combined_score (0.9777), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9657), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9775)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nKey features: Performs well on score (0.9775), Performs well on combined_score (0.9775), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9654), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 3 (Score: 0.7040)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int p = arr[high], i = low;\n    for (int j = low; j < high; j++)\n        if (arr[j] < p) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.7040), Performs well on combined_score (0.7040), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.6259), Performs well on adaptability_score (0.8212), Performs well on avg_time (0.0598)\n\n### Program 4 (Score: 0.7033)\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    bool sorted = true;\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i-1]) { sorted = false; break; }\n    if (!sorted) quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j)\n        if (arr[j] < pivot) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.7033), Performs well on combined_score (0.7033), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.6249), Performs well on adaptability_score (0.8210), Performs well on avg_time (0.0600)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.0000)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to compile_success\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7040, Type: Alternative)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int p = arr[high], i = low;\n    for (int j = low; j < high; j++)\n        if (arr[j] < p) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], Mixed iteration strategies\n\n### Inspiration 2 (Score: 0.7033, Type: Alternative)\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    bool sorted = true;\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i-1]) { sorted = false; break; }\n    if (!sorted) quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j)\n        if (arr[j] < pivot) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], Mixed iteration strategies\n\n### Inspiration 3 (Score: 0.9777, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following improvements aim to boost the fitness score by optimizing the performance of the sorting process and simplifying the code structure to address the code length feedback.\n\n### Key Changes:\n1.  **Early Exit for Adaptive Scan**: The `adaptive_sort` scan now breaks early if it detects that the data is neither sorted nor reverse-sorted, reducing the constant overhead of the initial scan on random data.\n2.  **Inlined Partition Logic**: The `partition` function has been integrated into the `quicksort` function. This reduces function call overhead and shortens the overall program length, which is beneficial for the simplicity goal.\n3.  **Threshold Optimization**: The insertion sort threshold has been increased from 16 to 24. This often provides a better balance between quicksort's overhead and insertion sort's efficiency in the base cases.\n4.  **Tighter Logic**: Minor micro-optimizations in the insertion sort loop and Boolean logic in the adaptive scan provide cleaner, faster execution.\n\n```cpp\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    quicksort(a, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                while (j >= l && a[j] > k) a[j + 1] = a[j--];\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int v = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < v);\n            while (a[--j] > v);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.000931891, 0.0110419, 0.0002711, 0.00311142, 1.887e-05, 0.00018923, 0.00051442, 0.00701948, 0.00087801, 0.00989017, 0.00070216, 0.00823766, 1.854e-05, 0.00024634, 0.00021105, 0.00275323, 0.000904999, 0.0111021, 0.000401159, 0.00493754], \"all_correct\": false, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}