{"id": "ccc242da-4a05-4de2-b0f9-224234a62123", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) a = 0; else if (v[i] > v[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v + n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; else if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "59f5b818-ec87-4cbf-b54d-6054942402f0", "generation": 14, "timestamp": 1773810102.85188, "iteration_found": 385, "metrics": {"score": 0.9969816, "combined_score": 0.9969816, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995456, "adaptability_score": 0.99927, "avg_time": 0.000456458}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  int mn = v[0], mx = v[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n      } else std::sort(v, v + n);\nwith:\n  int mn = v[0], mx = v[0];\n      for (int x : arr) { if (x < mn) mn = x; else if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());", "parent_metrics": {"score": 0.997318, "combined_score": 0.997318, "compile_success": 1.0, "correctness": 1, "performance_score": 0.99602, "adaptability_score": 0.999265, "avg_time": 0.000399636}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9973\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9974 \u2192 0.9973. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  int mn=v[0], mx=v[0];\n      for (int i=1; i<n; ++i) { mn=std::min(mn, v[i]); mx=std::max(mx, v[i]); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int i=0; i<n; ++i) c[v[i]-mn]++;\n          int* p = v;\n          for (int i=0; i<=r; ++i)\n              if (int k=c[i]) p = std::fill_n(p, k, i+mn);\n          return;\n      }\nwith:\n  int mn = v[0], mx = v[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  for (int i = 1; i < n; ++i) {\n          if (v[i-1] > v[i]) a = 0;\n          else if (v[i-1] < v[i]) d = 0;\n          if (!(a | d)) break;\n      }\nwith:\n  for (int i = 1; i < n; ++i) {\n          if (v[i] < v[i-1]) a = 0;\n          else if (v[i] > v[i-1]) d = 0;\n          if (!(a | d)) break;\n      }\nChange 2: Replace:\n  int mn = v[0], mx = v[0];\n      for (int i = 1; i < n; ++i) {\n          int x = v[i]; if (x < mn) mn = x; else if (x > mx) mx = x;\n      }\nwith:\n  int mn = v[0], mx = v[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\nChange 3: Replace:\n  if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int i = 0; i < n; ++i) c[v[i] - mn]++;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) v = std::fill_n(v, k, i + mn);\n          return;\n      }\n      std::sort(v, v + n);\nwith:\n  if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  int mn = p[0], mx = p[0];\n      for (int i=0; i<n; ++i) { mn = std::min(mn, p[i]); mx = std::max(mx, p[i]); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int i=0; i<n; ++i) c[p[i] - mn]++;\n          for (int i=0; i<=r; ++i)\n              if (int k=c[i]) p = std::fill_n(p, k, i+mn);\n          return;\n      }\n      std::sort(p, p+n);\nwith:\n  int mn = p[0], mx = p[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i=0; i<=r; ++i) if (int k=c[i]) { std::fill_n(p, k, i+mn); p+=k; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* p = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (p[i]<p[i-1]) a=0; else if (p[i]>p[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(p, p+n); return; }\n    int mn = p[0], mx = p[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) a = 0;\n        else if (v[i] > v[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(v, v + n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), s = 0;\n    if (n < 2) return;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] > arr[i-1]) s |= 1;\n        else if (arr[i] < arr[i-1]) s |= 2;\n        if (s == 3) break;\n    }\n    if (!(s & 2)) return;\n    if (!(s & 1)) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i) if (c[i]) it = std::fill_n(it, c[i], i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) a = 0; else if (v[i] > v[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v + n); return; }\n    int mn = v[0], mx = v[0];\n    for (int i = 1; i < n; ++i) { mn = std::min(mn, v[i]); mx = std::max(mx, v[i]); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int i = 0; i < n; ++i) c[v[i] - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) { std::fill_n(p, k, i + mn); p += k; }\n    } else std::sort(v, v + n);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(); if (n < 2) return;\n    int *p = v.data(), mn = p[0], mx = p[0]; bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (p[i] < p[i-1]) a = 0; else if (p[i] > p[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return; if (d) { std::reverse(p, p + n); return; }\n    for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n    } else std::sort(p, p + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9972, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 4000000 && r < n * 64LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(v, v+n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) a = 0; else if (v[i] > v[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v + n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n    } else std::sort(v, v + n);\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I will refine the `adaptive_sort` implementation by optimizing the min/max calculation and restructuring the counting sort logic to match the higher-performing versions from the evolution history. Specifically, I'll use a manual `if-else` min/max check which is generally faster than calling `std::min` and `std::max` repeatedly, and I will restore the `if/return` structure and use `std::sort(arr.begin(), arr.end())` which were consistent elements in the versions that achieved the 0.9974 score.\n\n<<<<<<< SEARCH\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n    } else std::sort(v, v + n);\n=======\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; else if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00017421, 0.00055431, 6.12e-05, 0.0006365, 1.044e-05, 0.00010445, 3.049e-05, 0.00029266, 0.00015749, 0.00065494, 5.105e-05, 0.00051679, 1.555e-05, 0.00017323, 5.902e-05, 0.00060077, 0.00098225, 0.0034088, 0.00011508, 0.00052993], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}