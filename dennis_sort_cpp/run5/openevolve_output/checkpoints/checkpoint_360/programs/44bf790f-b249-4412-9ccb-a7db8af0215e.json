{"id": "44bf790f-b249-4412-9ccb-a7db8af0215e", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or strictly reverse-sorted inputs\n    // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n    bool asc = true;\n    bool desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!asc && !desc) break;\n    }\n    \n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Attempt Counting Sort for inputs with a dense distribution\n    if (n >= 128) {\n        int min_val = arr[0];\n        int max_val = arr[0];\n        // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n\n        long long range = (long long)max_val - (long long)min_val;\n        \n        // If density is sufficient, solve O(N log N) into strict branchless O(N)\n        // Hard-cap memory allocation overhead to 8MB max (2 million int array)\n        if (range <= 2000000 && range <= (long long)n * 10) {\n            std::vector<int> counts(range + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                counts[(long long)arr[i] - min_val]++;\n            }\n            int idx = 0;\n            for (int i = 0; i <= range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill(arr.begin() + idx, arr.begin() + idx + c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "efc8181c-13ba-4de8-a31d-54a3254f3bf2", "generation": 3, "timestamp": 1773798618.5167716, "iteration_found": 13, "metrics": {"score": 0.9972734000000001, "combined_score": 0.9972734000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995959, "adaptability_score": 0.999245, "avg_time": 0.000405699}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or strictly reverse-sorted inputs\n      // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n      bool asc = true;\n      bool desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!asc && !desc) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Attempt Counting Sort for inputs with a dense distribution\n      if (n >= 128) {\n          int min_val = arr[0];\n          int max_val = arr[0];\n          // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n  ... (29 more lines)", "parent_metrics": {"score": 0.9755244, "combined_score": 0.9755244, "compile_success": 1.0, "correctness": 1, "performance_score": 0.96207, "adaptability_score": 0.995706, "avg_time": 0.00394253}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9755\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness unchanged at 0.9755\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n\n## Last Execution Output\n\n### times\n```\n[0.00105252, 0.0109993, 0.00030115, 0.00351836, 0.000148109, 0.00171086, 0.00045261, 0.00672687, 0.00091736, 0.0106271, 0.00101855, 0.0130394, 0.000200209, 0.00225818, 0.0006678, 0.00700471, 0.000986489, 0.0114016, 0.00040736, 0.00541201]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      // std::sort implements Introsort, an adaptive hybrid sorting algorithm\n      // that starts with quicksort and switches to heapsort/insertion sort\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9755, combined_score: 0.9755, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9621, adaptability_score: 0.9957, avg_time: 0.0039\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      // Array of size 0 or 1 is already sorted by definition\n      if (arr.size() < 2) return;\n  \n      // O(N) check - instantly breaks early to O(1) on randomized data\n      if (std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // O(N) check - captures worst-case scenarios and resolves in O(N)\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Fallback to highly-optimized introsort for random/unpredictable distributions\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9766, combined_score: 0.9766, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9640, adaptability_score: 0.9955, avg_time: 0.0037\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9766)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    // Array of size 0 or 1 is already sorted by definition\n    if (arr.size() < 2) return;\n    \n    // O(N) check - instantly breaks early to O(1) on randomized data\n    if (std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // O(N) check - captures worst-case scenarios and resolves in O(N)\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    // Fallback to highly-optimized introsort for random/unpredictable distributions\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9766), Performs well on combined_score (0.9766), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9640), Performs well on adaptability_score (0.9955), Performs well on avg_time (0.0037)\n\n### Program 2 (Score: 0.9755)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9755), Performs well on combined_score (0.9755), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9621), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0039)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9766, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    // Array of size 0 or 1 is already sorted by definition\n    if (arr.size() < 2) return;\n    \n    // O(N) check - instantly breaks early to O(1) on randomized data\n    if (std::is_sorted(arr.begin(), arr.end())) {\n        return;\n    }\n    \n    // O(N) check - captures worst-case scenarios and resolves in O(N)\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n    \n    // Fallback to highly-optimized introsort for random/unpredictable distributions\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["An excellent approach to improving the fitness score for this adaptive sorting algorithm is to truly make it **adaptive** to the input characteristics, rather than relying solely on `std::sort` (which relies primarily on quicksort/introsort). \n\nBy identifying specific, common input distributions, we can process them in `O(N)` time instead of `O(N log N)`. The modifications below implement a set of smart, low-overhead heuristics.\n\n### Key Optimizations added:\n1. **Hardware Vectorization**: Added AVX2/BMI2 pragmas to ensure integer min/max and filling operations are heavily vectorized by the compiler.\n2. **`O(N)` Fast-path for Sorted/Reverse Data**: On completely random data this loop breaks after 2 or 3 elements, causing near zero overhead. On sorted/reverse-sorted datasets, it catches the pattern immediately and bypasses sorting logic to conclude in `O(1)` or `O(N)`.\n3. **`O(N)` Adaptive Counting Sort**: QuickSort takes `O(N log N)`. If the density of numbers is high (e.g. `Max Value - Min Value` isn't exponentially larger than `N`), we can resolve sorting with an allocation of up to ~8MB, transforming an `O(N log N)` branching operation into a branch-free `O(N)` memory frequency plot (Counting Sort).\n4. **Graceful Fallback**: If it's a completely unpredictable distribution with a massive integer range, it naturally falls back to heavily optimized C++ Introsort (`std::sort`). \n\n<<<<<<< SEARCH\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n=======\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or strictly reverse-sorted inputs\n    // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n    bool asc = true;\n    bool desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!asc && !desc) break;\n    }\n    \n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Attempt Counting Sort for inputs with a dense distribution\n    if (n >= 128) {\n        int min_val = arr[0];\n        int max_val = arr[0];\n        // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n\n        long long range = (long long)max_val - (long long)min_val;\n        \n        // If density is sufficient, solve O(N log N) into strict branchless O(N)\n        // Hard-cap memory allocation overhead to 8MB max (2 million int array)\n        if (range <= 2000000 && range <= (long long)n * 10) {\n            std::vector<int> counts(range + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                counts[(long long)arr[i] - min_val]++;\n            }\n            int idx = 0;\n            for (int i = 0; i <= range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill(arr.begin() + idx, arr.begin() + idx + c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00015814, 0.00045826, 5.924e-05, 0.00057302, 1.296e-05, 0.00012539, 1.406e-05, 0.0001179, 0.00014124, 0.00049306, 3.398e-05, 0.00033455, 1.4e-05, 0.00013913, 4.193e-05, 0.00042724, 0.00097584, 0.00352731, 0.00011816, 0.00034858], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}