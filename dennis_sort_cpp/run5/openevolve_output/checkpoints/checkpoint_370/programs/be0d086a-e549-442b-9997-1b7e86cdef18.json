{"id": "be0d086a-e549-442b-9997-1b7e86cdef18", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    long long r = 1LL * mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "78fc41dc-cc09-4637-8329-cf8fd022cd16", "generation": 6, "timestamp": 1773804076.3636668, "iteration_found": 167, "metrics": {"score": 0.9970294, "combined_score": 0.9970294, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995525, "adaptability_score": 0.999286, "avg_time": 0.000449494}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < mn) mn = arr[i];\n          else if (arr[i] > mx) mx = arr[i];\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 18) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) {\n          if (x < mn) mn = x;\n          else if (x > mx) mx = x;\n      }\n      long long r = 1LL * mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }", "parent_metrics": {"score": 0.9968682, "combined_score": 0.9968682, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995289, "adaptability_score": 0.999237, "avg_time": 0.00047332}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9969\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9973 \u2192 0.9969. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00018352, 0.0005956, 6.928e-05, 0.00060449, 1.202e-05, 0.00011792, 3.204e-05, 0.00029598, 0.000162569, 0.00069864, 5.08e-05, 0.000512, 1.851e-05, 0.00026136, 5.835e-05, 0.00060193, 0.00097193, 0.00358598, 0.00012545, 0.00050804]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nwith:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nChange 2: Replace:\n  int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          mn = std::min(mn, arr[i]);\n          mx = std::max(mx, arr[i]);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 20) {\nwith:\n  int mn = arr[0], mx = arr[0];\n      for (int x : arr) {\n          mn = std::min(mn, x);\n          mx = std::max(mx, x);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9965, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        int x = arr[i];\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9969, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    \n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9968, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    int a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    \n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 24LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < mn) mn = arr[i];\n        else if (arr[i] > mx) mx = arr[i];\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I will restore the counting sort threshold from `n * 18` to `n * 32`, which was used in higher-performing versions (0.9974). Additionally, I'll simplify the code by using a range-based `for` loop for finding the minimum and maximum values and micro-optimize the monotonicity check and range calculations. These changes also help in reducing the code length below the 500-character recommendation.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < mn) mn = arr[i];\n        else if (arr[i] > mx) mx = arr[i];\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    long long r = 1LL * mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00016794, 0.00058054, 6.356e-05, 0.00059864, 1.047e-05, 0.00010251, 3.338e-05, 0.00030992, 0.0001545, 0.00065182, 4.902e-05, 0.00049163, 1.546e-05, 0.00015505, 5.863e-05, 0.000577, 0.00100426, 0.0033295, 0.0001256, 0.000510449], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}