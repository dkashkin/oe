{"id": "2d88d2e1-d290-49ea-9160-b6558d8b1ffa", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    bool asc = true, desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int min_val = arr[0], max_val = arr[0];\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n        const long long range = (long long)max_val - min_val;\n        if (range <= 4194304 && range <= (long long)n * 32) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_val]++;\n            int idx = 0;\n            for (int i = 0; i <= (int)range; ++i) {\n                if (int c = counts[i]) {\n                    std::fill_n(arr.begin() + idx, c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "44bf790f-b249-4412-9ccb-a7db8af0215e", "generation": 4, "timestamp": 1773798793.9317887, "iteration_found": 20, "metrics": {"score": 0.9968958, "combined_score": 0.9968958, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995457, "adaptability_score": 0.999054, "avg_time": 0.000456361}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or strictly reverse-sorted inputs\n      // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n      bool asc = true;\n      bool desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!asc && !desc) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Attempt Counting Sort for inputs with a dense distribution\n      if (n >= 128) {\n          int min_val = arr[0];\n          int max_val = arr[0];\n          // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n          for (int i = 1; i < n; ++i) {\n              min_val = std::min(min_val, arr[i]);\n              max_val = std::max(max_val, arr[i]);\n          }\n  \n  ... (24 more lines)\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const int n = arr.size();\n      if (n < 2) return;\n  \n      bool asc = true, desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n      if (asc) return;\n      if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      if (n > 64) {\n          int min_val = arr[0], max_val = arr[0];\n          for (int i = 1; i < n; ++i) {\n              min_val = std::min(min_val, arr[i]);\n              max_val = std::max(max_val, arr[i]);\n          }\n          const long long range = (long long)max_val - min_val;\n          if (range <= 4194304 && range <= (long long)n * 32) {\n              std::vector<int> counts(range + 1, 0);\n              for (int x : arr) counts[x - min_val]++;\n              int idx = 0;\n              for (int i = 0; i <= (int)range; ++i) {\n                  if (int c = counts[i]) {\n                      std::fill_n(arr.begin() + idx, c, i + min_val);\n                      idx += c;\n                  }\n              }\n  ... (5 more lines)", "parent_metrics": {"score": 0.9972734000000001, "combined_score": 0.9972734000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995959, "adaptability_score": 0.999245, "avg_time": 0.000405699}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9973\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9755 \u2192 0.9973\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00015814, 0.00045826, 5.924e-05, 0.00057302, 1.296e-05, 0.00012539, 1.406e-05, 0.0001179, 0.00014124, 0.00049306, 3.398e-05, 0.00033455, 1.4e-05, 0.00013913, 4.193e-05, 0.00042724, 0.00097584, 0.00352731, 0.00011816, 0.00034858]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      // std::sort implements Introsort, an adaptive hybrid sorting algorithm\n      // that starts with quicksort and switches to heapsort/insertion sort\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9755, combined_score: 0.9755, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9621, adaptability_score: 0.9957, avg_time: 0.0039\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or reverse-sorted patterns\n      bool asc = true;\n      bool desc = true;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // For dense distributions, Counting Sort provides O(N) performance\n      if (n > 64) {\n          int min_v = arr[0];\n          int max_v = arr[0];\n          for (size_t i = 1; i < n; ++i) {\n              int val = arr[i];\n  ... (25 more lines)\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or strictly reverse-sorted inputs\n      // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n      bool asc = true;\n      bool desc = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!asc && !desc) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // Attempt Counting Sort for inputs with a dense distribution\n      if (n >= 128) {\n          int min_val = arr[0];\n          int max_val = arr[0];\n          // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n  ... (29 more lines)\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or strictly reverse-sorted inputs\n    // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n    bool asc = true;\n    bool desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!asc && !desc) break;\n    }\n    \n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Attempt Counting Sort for inputs with a dense distribution\n    if (n >= 128) {\n        int min_val = arr[0];\n        int max_val = arr[0];\n        // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n\n        long long range = (long long)max_val - (long long)min_val;\n        \n        // If density is sufficient, solve O(N log N) into strict branchless O(N)\n        // Hard-cap memory allocation overhead to 8MB max (2 million int array)\n        if (range <= 2000000 && range <= (long long)n * 10) {\n            std::vector<int> counts(range + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                counts[(long long)arr[i] - min_val]++;\n            }\n            int idx = 0;\n            for (int i = 0; i <= range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill(arr.begin() + idx, arr.begin() + idx + c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9755)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9755), Performs well on combined_score (0.9755), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9621), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0039)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9755, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or strictly reverse-sorted inputs\n    // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n    bool asc = true;\n    bool desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!asc && !desc) break;\n    }\n    \n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Attempt Counting Sort for inputs with a dense distribution\n    if (n >= 128) {\n        int min_val = arr[0];\n        int max_val = arr[0];\n        // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n\n        long long range = (long long)max_val - (long long)min_val;\n        \n        // If density is sufficient, solve O(N log N) into strict branchless O(N)\n        // Hard-cap memory allocation overhead to 8MB max (2 million int array)\n        if (range <= 2000000 && range <= (long long)n * 10) {\n            std::vector<int> counts(range + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                counts[(long long)arr[i] - min_val]++;\n            }\n            int idx = 0;\n            for (int i = 0; i <= range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill(arr.begin() + idx, arr.begin() + idx + c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program is already highly efficient, but we can improve its performance and adaptability by refining the thresholds for the counting sort fallback and simplifying the logic to reduce overhead and code length. \n\nKey improvements:\n1.  **Increased Range Threshold**: The counting sort range is increased from 2 million to approximately 4.2 million (2^22), allowing it to handle a wider variety of dense distributions.\n2.  **Increased Density Threshold**: The density requirement is increased from `10 * n` to `32 * n`, taking advantage of the fact that counting sort is significantly faster than $O(N \\log N)$ even with more sparse data.\n3.  **Refined Logic and Compactness**: The code is simplified by removing redundant checks and comments, and by using more idiomatic C++ constructs like `std::fill_n` and range-based for-loops, which can also help the compiler generate better code.\n4.  **Lower Guard for Small Arrays**: Lowered the `n` guard for counting sort from 128 to 64 to benefit mid-sized arrays with small ranges.\n\nThese changes collectively aim to reduce the average execution time (specifically targeting the outliers in the test cases) and improve the overall fitness score.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or strictly reverse-sorted inputs\n    // On random data, this reliably breaks in ~2.5 iterations (O(1) overhead)\n    bool asc = true;\n    bool desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!asc && !desc) break;\n    }\n    \n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Attempt Counting Sort for inputs with a dense distribution\n    if (n >= 128) {\n        int min_val = arr[0];\n        int max_val = arr[0];\n        // std::min/max vectorize extremely well to vminps/vmaxps under AVX2\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n\n        long long range = (long long)max_val - (long long)min_val;\n        \n        // If density is sufficient, solve O(N log N) into strict branchless O(N)\n        // Hard-cap memory allocation overhead to 8MB max (2 million int array)\n        if (range <= 2000000 && range <= (long long)n * 10) {\n            std::vector<int> counts(range + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                counts[(long long)arr[i] - min_val]++;\n            }\n            int idx = 0;\n            for (int i = 0; i <= range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill(arr.begin() + idx, arr.begin() + idx + c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    bool asc = true, desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int min_val = arr[0], max_val = arr[0];\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n        const long long range = (long long)max_val - min_val;\n        if (range <= 4194304 && range <= (long long)n * 32) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_val]++;\n            int idx = 0;\n            for (int i = 0; i <= (int)range; ++i) {\n                if (int c = counts[i]) {\n                    std::fill_n(arr.begin() + idx, c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000161371, 0.000427589, 5.3471e-05, 0.00054232, 1.1951e-05, 0.00013988, 1.654e-05, 0.00014218, 0.00014562, 0.0005412, 3.6891e-05, 0.00036678, 1.3929e-05, 0.000138811, 4.5069e-05, 0.00046072, 0.000951939, 0.00444941, 0.00010777, 0.00037377], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}