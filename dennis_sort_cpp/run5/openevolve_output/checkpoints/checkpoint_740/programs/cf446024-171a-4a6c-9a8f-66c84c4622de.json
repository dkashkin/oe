{"id": "cf446024-171a-4a6c-9a8f-66c84c4622de", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 48) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "3e6b20d2-45ce-446e-98d3-ab4b886afb61", "generation": 10, "timestamp": 1773820835.8329468, "iteration_found": 722, "metrics": {"score": 0.9973898000000001, "combined_score": 0.9973898000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996135, "adaptability_score": 0.999272, "avg_time": 0.000387999}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      size_t n = arr.size(), i = 1;\n      if (n < 2) return;\n      while (i < n && arr[i] == arr[i-1]) i++;\n      if (i < n && arr[i] < arr[i-1]) {\n          while (i < n && arr[i] <= arr[i-1]) i++;\n          std::reverse(arr.begin(), arr.begin() + i);\n      }\n      while (i < n && arr[i-1] <= arr[i]) i++;\n      if (i == n) return;\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) it = std::fill_n(it, cnt, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1; if (n < 2) return;\n      while (i < n && v[i] == v[i-1]) i++;\n      if (i < n && v[i] < v[i-1]) {\n          while (i < n && v[i] <= v[i-1]) i++;\n          std::reverse(v.begin(), v.begin() + i);\n      }\n      while (i < n && v[i-1] <= v[i]) i++;\n      if (i == n) return;\n      int mn = v[0], mx = v[0];\n      for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 48) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(v.begin() + i, v.end());\n          if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n      } else std::sort(v.begin(), v.end());\n  }", "parent_metrics": {"score": 0.9973858, "combined_score": 0.9973858, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996125, "adaptability_score": 0.999277, "avg_time": 0.000388979}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9974\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9974 \u2192 0.9974. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00015673, 0.00043018, 4.765e-05, 0.00048671, 1.1871e-05, 0.00011756, 1.5809e-05, 0.000139571, 0.000141571, 0.000498389, 3.093e-05, 0.00033222, 6.25e-06, 6.177e-05, 4.319e-05, 0.00045882, 0.00095706, 0.00337026, 0.00010056, 0.00037247]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  while (i < n && arr[i] == arr[i-1]) i++;\n      if (i < n && arr[i] < arr[i-1]) {\n          while (i < n && arr[i] <= arr[i-1]) i++;\n          std::reverse(arr.begin(), arr.begin() + i);\n      }\n      while (i < n && arr[i-1] <= arr[i]) i++;\nwith:\n  while (i < n && arr[i] <= arr[i-1]) ++i;\n      if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i-1] <= arr[i]) ++i;\nChange 2: 'if (r < 1000000 && r < n * 48LL) {' to 'if (r < 2000000 && r < n * 32LL) {'\nChange 3: Replace:\n  if (i * 3 > n || n - i < 64) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\nwith:\n  if (i * 4 > n || n - i < 80) {\n          std::sort(arr.begin() + i, arr.end());\n          if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  int i = 1;\n      while (i < n && arr[i] == arr[i-1]) i++;\n      if (i < n && arr[i] < arr[i-1]) {\n          while (i < n && arr[i] <= arr[i-1]) i++;\n          std::reverse(arr.begin(), arr.begin() + i);\n      }\n      while (i < n && arr[i-1] <= arr[i]) i++;\nwith:\n  int i = 1;\n      while (i < n && arr[i] <= arr[i-1]) ++i;\n      if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i-1] <= arr[i]) ++i;\nChange 2: Replace:\n  int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\nwith:\n  int mn = arr[0], mx = mn;\n      for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1;\n      if (n < 2) return;\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1; if (n < 2) return;\nChange 2: Replace:\n  int mn = v[0], mx = mn;\n      for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n          return;\n      }\nwith:\n  int mn = v[0], mx = v[0];\n      for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n          return;\n      }\n- Metrics: score: 0.9975, combined_score: 0.9975, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9963, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9963), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9963), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    int i = 1;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int v = c[j]) it = std::fill_n(it, v, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] <= v[i-1]) ++i;\n    if (v[0] > v[i-1]) std::reverse(v.begin(), v.begin() + i);\n    while (i < n && v[i-1] <= v[i]) ++i;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] <= arr[i-1]) ++i;\n    if (arr[0] > arr[i-1]) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) ++i;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) mn = std::min(mn, x), mx = std::max(mx, x);\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = mn;\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 48) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j) if (int cnt = c[j]) it = std::fill_n(it, cnt, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] == arr[i-1]) i++;\n    if (i < n && arr[i] < arr[i-1]) {\n        while (i < n && arr[i] <= arr[i-1]) i++;\n        std::reverse(arr.begin(), arr.begin() + i);\n    }\n    while (i < n && arr[i-1] <= arr[i]) i++;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) it = std::fill_n(it, cnt, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Based on the performance history and the specific requirements of the adaptive sorting task, I have refined the sorting algorithm to improve its efficiency. The following changes incorporate a more optimized min/max search pattern (which is more amenable to compiler vectorization), increases the threshold for counting sort slightly to handle a wider range of cases efficiently, and consolidates the code to address the character count recommendation while maintaining high performance and adaptability.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && arr[i] == arr[i-1]) i++;\n    if (i < n && arr[i] < arr[i-1]) {\n        while (i < n && arr[i] <= arr[i-1]) i++;\n        std::reverse(arr.begin(), arr.begin() + i);\n    }\n    while (i < n && arr[i-1] <= arr[i]) i++;\n    if (i == n) return;\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) it = std::fill_n(it, cnt, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(arr.begin() + i, arr.end());\n        if (arr[i] < arr[i-1]) std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 48) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int k = c[j]) it = std::fill_n(it, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00015601, 0.00041449, 5.041e-05, 0.00046303, 1.039e-05, 0.00010253, 1.683e-05, 0.00014367, 0.00013921, 0.00048903, 3.359e-05, 0.00030466, 5.51e-06, 5.885e-05, 4.387e-05, 0.0004992, 0.00098488, 0.00338644, 9.608e-05, 0.000361309], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}