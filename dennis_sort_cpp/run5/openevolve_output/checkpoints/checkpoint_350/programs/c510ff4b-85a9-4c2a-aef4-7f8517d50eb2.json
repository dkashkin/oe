{"id": "c510ff4b-85a9-4c2a-aef4-7f8517d50eb2", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "8c053a6f-4a8c-46f8-8a0b-45691becfaf0", "generation": 8, "timestamp": 1773806089.8286793, "iteration_found": 247, "metrics": {"score": 0.9973268, "combined_score": 0.9973268, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996036, "adaptability_score": 0.999263, "avg_time": 0.000397999}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }\nwith:\n  const size_t n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n          return;\n      }", "parent_metrics": {"score": 0.9973460000000001, "combined_score": 0.9973460000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.99605, "adaptability_score": 0.99929, "avg_time": 0.000396581}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9973\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9974 \u2192 0.9973. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < mn) mn = arr[i];\n          else if (arr[i] > mx) mx = arr[i];\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 18) {\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 24) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool a = 1, d = 1;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a || d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n  \n      int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < mn) mn = arr[i];\n          else if (arr[i] > mx) mx = arr[i];\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 18) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) a = 0;\n          else if (arr[i] > arr[i-1]) d = 0;\n          if (!(a | d)) break;\n      }\n      if (a) return;\n      if (d) { std::reverse(arr.begin(), arr.end()); return; }\n      int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\n  }\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9961, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) it = std::fill_n(it, cnt, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9961), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9968, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        if (arr[i] > arr[i-1]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int mn = arr[0], mx = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            if (arr[i] < mn) mn = arr[i];\n            else if (arr[i] > mx) mx = arr[i];\n        }\n        const long long r = (long long)mx - mn;\n        if (r < 2000000 && r < (long long)n * 20) {\n            std::vector<int> c(r + 1, 0);\n            for (int x : arr) c[x - mn]++;\n            auto it = arr.begin();\n            for (int i = 0; i <= (int)r; ++i)\n                if (int cnt = c[i]) {\n                    std::fill_n(it, cnt, i + mn);\n                    it += cnt;\n                }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9968, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    int a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        if (x < mn) mn = x;\n        else if (x > mx) mx = x;\n    }\n    \n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 24LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9973, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following improvements focus on recovering and potentially exceeding the previous high fitness of 0.9974. The changes restore the more effective range density multiplier (24) and optimize the data pattern check and type consistency. \n\nKey changes:\n1.  **Consistent Types**: Reverting back to `size_t` for the array size and indices to ensure optimal interaction with the `std::vector` and the compiler's range-based optimizations.\n2.  **Density Multiplier**: Adjusting the counting sort threshold from `32` back to `24`, which was used in the top-performing versions. This better balances the trade-off between the $O(N+R)$ counting sort and $O(N \\log N)$ introsort.\n3.  **Pattern Check Loop**: Using a more compact `if` structure in the ascending/descending check that avoids `else if` branching and utilizes bitwise OR for the early exit condition.\n4.  **Counting Sort Efficiency**: Using `it = std::fill_n(...)` which is a concise and efficient way to update the iterator while filling the array.\n\n```cpp\n<<<<<<< SEARCH\n    int n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) a = 0;\n        else if (arr[i] > arr[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n=======\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 24) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) it = std::fill_n(it, v, i + mn);\n        return;\n    }\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00016071, 0.00042415, 4.752e-05, 0.00046434, 1.1969e-05, 0.000102311, 1.8371e-05, 0.000145669, 0.00013868, 0.00051056, 3.4871e-05, 0.00035286, 1.392e-05, 0.0001439, 4.524e-05, 0.000455349, 0.000947971, 0.00345169, 0.00011759, 0.0003723], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}