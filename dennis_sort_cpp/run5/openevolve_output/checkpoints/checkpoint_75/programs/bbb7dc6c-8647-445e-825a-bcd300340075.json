{"id": "bbb7dc6c-8647-445e-825a-bcd300340075", "code": "#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i];\n                if (v < arr[i - 1]) {\n                    int j = i - 1;\n                    while (j >= low && arr[j] > v) { arr[j + 1] = arr[j]; j--; }\n                    arr[j + 1] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "5f1d867f-e732-4853-955e-0b749e4b071f", "generation": 2, "timestamp": 1773800025.5665567, "iteration_found": 61, "metrics": {"score": 0.9795692, "combined_score": 0.9795692, "compile_success": 1.0, "correctness": 1, "performance_score": 0.968666, "adaptability_score": 0.995924, "avg_time": 0.00323481}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; ++i) {\n          if (arr[i] < arr[i - 1]) s = false;\n          else if (arr[i] > arr[i - 1]) r = false;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n  ... (5 more lines)\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int v = arr[i];\n                  if (v < arr[i - 1]) {\n                      int j = i - 1;\n                      while (j >= low && arr[j] > v) { arr[j + 1] = arr[j]; j--; }\n                      arr[j + 1] = v;\n                  }\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n          if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n          if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n          int p = arr[m], i = low - 1, j = high + 1;\n          while (true) {\n              while (arr[++i] < p);\n              while (arr[--j] > p);\n              if (i >= j) break;\n              std::swap(arr[i], arr[j]);\n          }\n          if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n          else { quicksort(arr, j + 1, high); high = j; }\n      }\n  }", "parent_metrics": {"score": 0.9774858000000001, "combined_score": 0.9774858000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965423, "adaptability_score": 0.99558, "avg_time": 0.00358152}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9775\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9774 \u2192 0.9775\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00112768, 0.0122268, 0.00031189, 0.00366497, 2.208e-05, 0.00022121, 0.00058126, 0.00784615, 0.00100194, 0.0116218, 0.0007584, 0.00885098, 1.3731e-05, 0.000136369, 0.000232591, 0.00280071, 0.00106232, 0.0129109, 0.00044747, 0.00579115]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      bool sorted = true;\n      for (int i = 1; i < (int)arr.size(); ++i)\n          if (arr[i] < arr[i-1]) { sorted = false; break; }\n      if (!sorted) quicksort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                  arr[j+1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p - 1; }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p; }\n      }\n  }\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int pivot = arr[high], i = low;\n      for (int j = low; j < high; ++j)\n          if (arr[j] < pivot) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n          while (arr[--j] > p);\n          if (i >= j) return j;\n          std::swap(arr[i], arr[j]);\n      }\n  }\n- Metrics: score: 0.9774, combined_score: 0.9774, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9652, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  #include <vector>\n  #include <algorithm>\n  \n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n          while (arr[--j] > p);\n          if (i >= j) return j;\n          std::swap(arr[i], arr[j]);\n      }\n  }\n- Metrics: score: 0.9775, combined_score: 0.9775, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9654, adaptability_score: 0.9956, avg_time: 0.0036\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n          if (!s && !r) break;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p - 1; }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      std::swap(arr[m], arr[high]);\n      int p = arr[high], i = low;\n      for (int j = low; j < high; j++)\n          if (arr[j] < p) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n          if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n          if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n          int p = arr[m], i = low - 1, j = high + 1;\n          while (true) {\n              while (arr[++i] < p);\n              while (arr[--j] > p);\n              if (i >= j) break;\n              std::swap(arr[i], arr[j]);\n          }\n          if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n          else { quicksort(arr, j + 1, high); high = j; }\n      }\n  }\n- Metrics: score: 0.9782, combined_score: 0.9782, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9666, adaptability_score: 0.9958, avg_time: 0.0035\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9797)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i];\n                if (v < arr[i - 1]) {\n                    int j = i - 1;\n                    while (j >= low && arr[j] > v) { arr[j + 1] = arr[j]; j--; }\n                    arr[j + 1] = v;\n                }\n            }\n            return;\n        }\n        int mid = low + (high - low) / 2;\n        if (arr[mid] < arr[low]) std::swap(arr[mid], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[mid]) std::swap(arr[high], arr[mid]);\n        int p = arr[mid], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9797), Performs well on combined_score (0.9797), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9688), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0032)\n\n### Program 2 (Score: 0.9782)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9782), Performs well on combined_score (0.9782), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9666), Performs well on adaptability_score (0.9958), Performs well on avg_time (0.0035)\n\n### Program 3 (Score: 0.9775)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nKey features: Performs well on score (0.9775), Performs well on combined_score (0.9775), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9654), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n### Program 4 (Score: 0.9774)\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nKey features: Performs well on score (0.9774), Performs well on combined_score (0.9774), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9652), Performs well on adaptability_score (0.9956), Performs well on avg_time (0.0036)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9773)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.7033)\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    bool sorted = true;\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i-1]) { sorted = false; break; }\n    if (!sorted) quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int pivot = arr[high], i = low;\n    for (int j = low; j < high; ++j)\n        if (arr[j] < pivot) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.7040)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p - 1); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p - 1; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    std::swap(arr[m], arr[high]);\n    int p = arr[high], i = low;\n    for (int j = low; j < high; j++)\n        if (arr[j] < p) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9782, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9773, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9774, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score and streamline the codebase, I have implemented several optimizations inspired by the highest-performing versions in the evolution history.\n\n### Key Improvements:\n1.  **Adaptive Sort Early Exit:** Added an early exit condition `if (!s && !r) break;` in the sorted/reverse-sorted check. This avoids unnecessary scans for randomly ordered data.\n2.  **Inlined Partitioning:** Inlined the Hoare partition logic directly into the `quicksort` function. This reduces function call overhead and helps simplify the overall structure.\n3.  **Increased Insertion Sort Threshold:** Raised the threshold for switching to insertion sort from 16 to 32, which is generally more efficient for modern CPU architectures.\n4.  **Optimized Insertion Sort:** Added a pre-check (`if (v < arr[i - 1])`) inside the insertion sort loop to skip elements that are already in the correct position relative to their immediate predecessor.\n5.  **Refined Median-of-Three:** Integrated the median-of-three pivot selection directly before the partitioning loop for better cache locality.\n\nThese changes consolidate the code, potentially reducing the overall character count, while targeting a higher performance and adaptability score.\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high);\nint partition(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n=======\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n        if (!s && !r) break;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n=======\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i];\n                if (v < arr[i - 1]) {\n                    int j = i - 1;\n                    while (j >= low && arr[j] > v) { arr[j + 1] = arr[j]; j--; }\n                    arr[j + 1] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.0009376, 0.0111452, 0.00023184, 0.0030426, 2.049e-05, 0.00021507, 0.00049792, 0.00691113, 0.00085301, 0.0111859, 0.00064382, 0.00797898, 1.852e-05, 0.0002464, 0.00016781, 0.00239749, 0.00093525, 0.0117772, 0.00034881, 0.00514115], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}