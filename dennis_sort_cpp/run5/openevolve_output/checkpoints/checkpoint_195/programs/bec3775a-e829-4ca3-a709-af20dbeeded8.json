{"id": "bec3775a-e829-4ca3-a709-af20dbeeded8", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "1b6d0c4b-c773-4cc5-acb2-01147fad8af3", "generation": 5, "timestamp": 1773800376.9172413, "iteration_found": 70, "metrics": {"score": 0.9968242, "combined_score": 0.9968242, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995373, "adaptability_score": 0.999001, "avg_time": 0.000464852}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          mn = std::min(mn, arr[i]);\n          mx = std::max(mx, arr[i]);\n      }\n  \n      const long long r = (long long)mx - mn;\n      if (n > 64 && r < 2000000 && r < (long long)n * 18) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) {\n                  std::fill_n(it, cnt, i + mn);\n                  it += cnt;\n              }\n          return;\n      }\nwith:\n  int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          mn = std::min(mn, arr[i]);\n          mx = std::max(mx, arr[i]);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 20) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n          return;\n      }", "parent_metrics": {"score": 0.997155, "combined_score": 0.997155, "compile_success": 1.0, "correctness": 1, "performance_score": 0.995773, "adaptability_score": 0.999228, "avg_time": 0.000424519}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9972\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9972 \u2192 0.9972. Consider revising recent changes.\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00017318, 0.00042895, 6.162e-05, 0.00059014, 1.071e-05, 0.00010846, 1.674e-05, 0.00015851, 0.00014543, 0.00054359, 3.69e-05, 0.00038578, 1.392e-05, 0.00013874, 5.1051e-05, 0.00053974, 0.000962159, 0.00361574, 0.00013847, 0.00037054]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          else if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nwith:\n  for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) a = 0;\n          if (arr[i-1] < arr[i]) d = 0;\n          if (!(a || d)) break;\n      }\nChange 2: Replace:\n  if (n > 64) {\n          int mn = arr[0], mx = arr[0];\n          for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n          const long long r = (long long)mx - mn;\n          if (r < 2000000 && r < (long long)n * 20) {\n              std::vector<int> c(r + 1, 0);\n              for (int x : arr) c[x - mn]++;\n              auto it = arr.begin();\n              for (int i = 0; i <= (int)r; ++i)\n                  if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n              return;\n          }\n      }\nwith:\n  int mn = arr[0], mx = mn;\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 20) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n          return;\n      }\n- Metrics: score: 0.9972, combined_score: 0.9972, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9959, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  if (n > 64) {\n          int mn = arr[0], mx = arr[0];\n          for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n          const long long r = (long long)mx - mn;\n          if (r < 2000000 && r < (long long)n * 20) {\n              std::vector<int> c(r + 1, 0);\n              for (int x : arr) c[x - mn]++;\n              auto it = arr.begin();\n              for (int i = 0; i <= (int)r; ++i)\n                  if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n              return;\n          }\n      }\nwith:\n  int mn = arr[0], mx = arr[0];\n      for (size_t i = 1; i < n; ++i) {\n          mn = std::min(mn, arr[i]);\n          mx = std::max(mx, arr[i]);\n      }\n      const long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 20) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          auto it = arr.begin();\n          for (int i = 0; i <= (int)r; ++i)\n              if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n          return;\n      }\n- Metrics: score: 0.9972, combined_score: 0.9972, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9959, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      std::sort(arr.begin(), arr.end());\n  }\nwith:\n  #pragma GCC optimize(\"O3,unroll-loops\")\n  #pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n  #include <vector>\n  #include <algorithm>\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      // Fast O(N) check for sorted or reverse-sorted patterns\n      bool asc = true;\n      bool desc = true;\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i-1] > arr[i]) asc = false;\n          if (arr[i-1] < arr[i]) desc = false;\n          if (!(asc || desc)) break;\n      }\n  \n      if (asc) return;\n      if (desc) {\n          std::reverse(arr.begin(), arr.end());\n          return;\n      }\n  \n      // For dense distributions, Counting Sort provides O(N) performance\n      if (n > 64) {\n          int min_v = arr[0];\n          int max_v = arr[0];\n          for (size_t i = 1; i < n; ++i) {\n              int val = arr[i];\n  ... (25 more lines)\n- Metrics: score: 0.9973, combined_score: 0.9973, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9960, adaptability_score: 0.9992, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) detection of pre-sorted or reverse-sorted patterns\n    // This reliably breaks in O(1) for random data\n    bool asc = true, desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // Density check: calculate range to determine if Counting Sort is viable\n    int min_v = arr[0], max_v = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        min_v = std::min(min_v, arr[i]);\n        max_v = std::max(max_v, arr[i]);\n    }\n\n    const long long range = (long long)max_v - min_v;\n    // Use Counting Sort for dense distributions (memory-safe threshold of ~8MB)\n    if (range < 2000000 && range < (long long)n * 20) {\n        std::vector<int> counts(range + 1, 0);\n        for (int x : arr) {\n            counts[x - min_v]++;\n        }\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)range; ++i) {\n            if (int c = counts[i]) {\n                std::fill_n(it, c, i + min_v);\n                it += c;\n            }\n        }\n        return;\n    }\n\n    // Fallback to Introsort for sparse distributions\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9973)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Fast O(N) check for sorted or reverse-sorted patterns\n    bool asc = true;\n    bool desc = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n\n    if (asc) return;\n    if (desc) {\n        std::reverse(arr.begin(), arr.end());\n        return;\n    }\n\n    // For dense distributions, Counting Sort provides O(N) performance\n    if (n > 64) {\n        int min_v = arr[0];\n        int max_v = arr[0];\n        for (size_t i = 1; i < n; ++i) {\n            int val = arr[i];\n            if (val < min_v) min_v = val;\n            if (val > max_v) max_v = val;\n        }\n\n        const long long range = (long long)max_v - (long long)min_v;\n        // Memory-safe threshold: 2 million entries (~8MB) and sufficient density\n        if (range < 2000000 && range < (long long)n * 15) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_v]++;\n            \n            auto it = arr.begin();\n            for (int i = 0; i <= (int)range; ++i) {\n                int c = counts[i];\n                if (c > 0) {\n                    std::fill_n(it, c, i + min_v);\n                    it += c;\n                }\n            }\n            return;\n        }\n    }\n\n    // Fallback to highly-optimized introsort\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9973), Performs well on combined_score (0.9973), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9960), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9972)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9972), Performs well on combined_score (0.9972), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9959), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9972)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) { std::fill_n(it, v, i + mn); it += v; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9972), Performs well on combined_score (0.9972), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9959), Performs well on adaptability_score (0.9992), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9972)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = mn;\n    for (int x : arr) {\n        mn = std::min(mn, x);\n        mx = std::max(mx, x);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int v = c[i]) {\n                std::fill_n(it, v, i + mn);\n                it += v;\n            }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9972)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n\n    const long long r = (long long)mx - mn;\n    if (n > 64 && r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9971)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        else if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int mn = arr[0], mx = arr[0];\n        for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n        const long long r = (long long)mx - mn;\n        if (r < 2000000 && r < (long long)n * 20) {\n            std::vector<int> c(r + 1, 0);\n            for (int x : arr) c[x - mn]++;\n            auto it = arr.begin();\n            for (int i = 0; i <= (int)r; ++i)\n                if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9961, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    auto p = std::minmax_element(arr.begin(), arr.end());\n    int mn = *p.first, mx = *p.second;\n    const long long r = (long long)mx - mn;\n\n    if (n > 64 && r < 4000000 && r < (long long)n * 40) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9969, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const int n = arr.size();\n    if (n < 2) return;\n\n    bool asc = true, desc = true;\n    for (int i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) asc = false;\n        if (arr[i-1] < arr[i]) desc = false;\n        if (!(asc || desc)) break;\n    }\n    if (asc) return;\n    if (desc) { std::reverse(arr.begin(), arr.end()); return; }\n\n    if (n > 64) {\n        int min_val = arr[0], max_val = arr[0];\n        for (int i = 1; i < n; ++i) {\n            min_val = std::min(min_val, arr[i]);\n            max_val = std::max(max_val, arr[i]);\n        }\n        const long long range = (long long)max_val - min_val;\n        if (range <= 4194304 && range <= (long long)n * 32) {\n            std::vector<int> counts(range + 1, 0);\n            for (int x : arr) counts[x - min_val]++;\n            int idx = 0;\n            for (int i = 0; i <= (int)range; ++i) {\n                if (int c = counts[i]) {\n                    std::fill_n(arr.begin() + idx, c, i + min_val);\n                    idx += c;\n                }\n            }\n            return;\n        }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9755, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9971, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n\n    const long long r = (long long)mx - mn;\n    if (n > 64 && r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> counts(r + 1, 0);\n        for (int x : arr) counts[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int c = counts[i]) { std::fill_n(it, c, i + mn); it += c; }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    bool a = 1, d = 1;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i-1] > arr[i]) a = 0;\n        if (arr[i-1] < arr[i]) d = 0;\n        if (!(a || d)) break;\n    }\n    if (a) return;\n    if (d) { std::reverse(arr.begin(), arr.end()); return; }\n\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n\n    const long long r = (long long)mx - mn;\n    if (n > 64 && r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness is nearly optimal, but there's room for improvement in `performance_score` and code length. I will simplify the density check and the min/max finding block to match the logic of the top-performing program (Program 1), which has a slightly higher fitness score. I'll also remove the `n > 64` threshold as it is redundant when combined with the density threshold `r < (long long)n * 20`, and condense the code to address the length concern mentioned in the focus areas.\n\n<<<<<<< SEARCH\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n\n    const long long r = (long long)mx - mn;\n    if (n > 64 && r < 2000000 && r < (long long)n * 18) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) {\n                std::fill_n(it, cnt, i + mn);\n                it += cnt;\n            }\n        return;\n    }\n=======\n    int mn = arr[0], mx = arr[0];\n    for (size_t i = 1; i < n; ++i) {\n        mn = std::min(mn, arr[i]);\n        mx = std::max(mx, arr[i]);\n    }\n    const long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 20) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        auto it = arr.begin();\n        for (int i = 0; i <= (int)r; ++i)\n            if (int cnt = c[i]) { std::fill_n(it, cnt, i + mn); it += cnt; }\n        return;\n    }\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.0001583, 0.00043016, 4.635e-05, 0.00049668, 1.039e-05, 0.00010221, 1.638e-05, 0.00014567, 0.00014221, 0.00062212, 3.745e-05, 0.00035492, 1.393e-05, 0.00014465, 3.808e-05, 0.000386049, 0.0009399, 0.00469696, 0.00013558, 0.00037906], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}