{"id": "6837eb34-3b12-47b5-adff-df288c54f551", "code": "#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1, *p = v.data(); if (n < 2) return;\n    while (i < n && p[i] == p[i-1]) i++;\n    if (i < n && p[i] < p[i-1]) {\n        while (i < n && p[i] <= p[i-1]) i++;\n        std::reverse(p, p + i);\n    }\n    while (i < n && p[i] >= p[i-1]) i++;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 20LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 64) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}", "changes_description": null, "language": "unknown", "parent_id": "65aaf9a7-b5e2-4e31-af92-a3579f91b35c", "generation": 9, "timestamp": 1773820603.9538953, "iteration_found": 716, "metrics": {"score": 0.9974336, "combined_score": 0.9974336, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996194, "adaptability_score": 0.999293, "avg_time": 0.000382016}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1; if (n < 2) return;\n      while (i < n && v[i] == v[i-1]) i++;\n      if (i < n && v[i] < v[i-1]) {\n          while (i < n && v[i] <= v[i-1]) i++;\n          std::reverse(v.begin(), v.begin() + i);\n      }\n      while (i < n && v[i-1] <= v[i]) i++;\n      if (i == n) return;\n      int mn = v[0], mx = v[0];\n      for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(v.begin() + i, v.end());\n          if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n      } else std::sort(v.begin(), v.end());\n  }\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      int n = v.size(), i = 1, *p = v.data(); if (n < 2) return;\n      while (i < n && p[i] == p[i-1]) i++;\n      if (i < n && p[i] < p[i-1]) {\n          while (i < n && p[i] <= p[i-1]) i++;\n          std::reverse(p, p + i);\n      }\n      while (i < n && p[i] >= p[i-1]) i++;\n      if (i == n) return;\n      int mn = p[0], mx = mn;\n      for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 20LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 64) {\n          std::sort(p + i, p + n);\n          if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n      } else std::sort(p, p + n);\n  }", "parent_metrics": {"score": 0.9974874, "combined_score": 0.9974874, "compile_success": 1.0, "correctness": 1, "performance_score": 0.996283, "adaptability_score": 0.999294, "avg_time": 0.000373071}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9975\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9974 \u2192 0.9975\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  int n = arr.size(); if (n < 2) return;\n      int* v = arr.data(); bool a=1, d=1;\n      for (int i=1; i<n; ++i) {\n          if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n          if (!(a|d)) break;\n      }\n      if (a) return; if (d) { std::reverse(v, v+n); return; }\n      int mn = v[0], mx = v[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\nwith:\n  int n = arr.size(); if (n < 2) return;\n      int* v = arr.data(); int i = 1;\n      while (i < n && v[i] == v[i-1]) i++;\n      if (i < n && v[i] < v[i-1]) {\n          while (i < n && v[i] <= v[i-1]) i++;\n          std::reverse(v, v + i);\n      }\n      while (i < n && v[i] >= v[i-1]) i++;\n      if (i == n) return;\n      int mn = v[0], mx = v[0];\n      for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n          return;\n      }\n      if (i * 4 > n || n - i < 80) {\n          std::sort(v + i, v + n);\n          if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n      } else std::sort(v, v + n);\n- Metrics: score: 0.9974, combined_score: 0.9974, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9962, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  int* v = arr.data(); bool a=1, d=1;\n      for (int i=1; i<n; ++i) {\n          if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n          if (!(a|d)) break;\n      }\n      if (a) return; if (d) { std::reverse(v, v+n); return; }\n      int mn = v[0], mx = v[0];\n      for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\nwith:\n  int* v = arr.data(); int i = 1;\n      while (i < n && v[i] == v[i-1]) i++;\n      if (i < n && v[i] < v[i-1]) {\n          while (i < n && v[i] <= v[i-1]) i++;\n          std::reverse(v, v + i);\n      }\n      while (i < n && v[i] >= v[i-1]) i++;\n      if (i == n) return;\n      int mn = v[0], mx = v[0];\n      for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\nChange 2: Replace:\n  if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int i = 0; i <= r; ++i)\n              if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n          return;\n      }\n      std::sort(arr.begin(), arr.end());\nwith:\n  if (r < 2000000 && r < n * 32LL) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : arr) c[x - mn]++;\n          int* p = v;\n          for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n          return;\n      }\n      if (4LL * i > n || n - i < 80) {\n          std::sort(v + i, v + n);\n          if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n      } else std::sort(v, v + n);\n- Metrics: score: 0.9975, combined_score: 0.9975, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9963, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1;\n      if (n < 2) return;\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1; if (n < 2) return;\nChange 2: Replace:\n  int mn = v[0], mx = mn;\n      for (int x : v) { mn = std::min(mn, x); mx = std::max(mx, x); }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n          return;\n      }\nwith:\n  int mn = v[0], mx = v[0];\n      for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n      long long r = (long long)mx - mn;\n      if (r < 2000000 && r < (long long)n * 32) {\n          std::vector<int> c(r + 1, 0);\n          for (int x : v) c[x - mn]++;\n          auto it = v.begin();\n          for (int j = 0; j <= (int)r; ++j)\n              if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n          return;\n      }\n- Metrics: score: 0.9975, combined_score: 0.9975, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9963, adaptability_score: 0.9993, avg_time: 0.0004\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9963), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 2 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9963), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 3 (Score: 0.9975)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); int i = 1;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v, v + i);\n    }\n    while (i < n && v[i] >= v[i-1]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (4LL * i > n || n - i < 80) {\n        std::sort(v + i, v + n);\n        if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n    } else std::sort(v, v + n);\n}\n```\nKey features: Performs well on score (0.9975), Performs well on combined_score (0.9975), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9963), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n### Program 4 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); int i = 1;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v, v + i);\n    }\n    while (i < n && v[i] >= v[i-1]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v + i, v + n);\n        if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n    } else std::sort(v, v + n);\n}\n```\nKey features: Performs well on score (0.9974), Performs well on combined_score (0.9974), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9962), Performs well on adaptability_score (0.9993), Performs well on avg_time (0.0004)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); int s = 0;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) s |= 2;\n        else if (v[i] > v[i-1]) s |= 1;\n        if (s == 3) break;\n    }\n    if (s < 2) return;\n    if (s == 2) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i=0; i<=r; ++i) if (int k=c[i]) p = std::fill_n(p, k, i+mn);\n        return;\n    }\n    std::sort(v, v + n);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); int i = 1;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v, v + i);\n    }\n    while (i < n && v[i] >= v[i-1]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (4LL * i > n || n - i < 80) {\n        std::sort(v + i, v + n);\n        if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n    } else std::sort(v, v + n);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9974)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* p = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (p[i]<p[i-1]) a=0; else if (p[i]>p[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(p, p+n); return; }\n    int mn = p[0], mx = p[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) v = std::fill_n(v, k, i + mn);\n        return;\n    }\n    std::sort(v, v + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); int i = 1;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v, v + i);\n    }\n    while (i < n && v[i] >= v[i-1]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (4LL * i > n || n - i < 80) {\n        std::sort(v + i, v + n);\n        if (v[i] < v[i-1]) std::inplace_merge(v, v + i, v + n);\n    } else std::sort(v, v + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a = 1, d = 1;\n    for (int i = 1; i < n; ++i) {\n        if (v[i] < v[i-1]) a = 0; else if (v[i] > v[i-1]) d = 0;\n        if (!(a | d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v + n); return; }\n    int mn = v[0], mx = v[0];\n    for (int i = 1; i < n; ++i) { mn = std::min(mn, v[i]); mx = std::max(mx, v[i]); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int i = 0; i < n; ++i) c[v[i] - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i) if (int k = c[i]) { std::fill_n(p, k, i + mn); p += k; }\n    } else std::sort(v, v + n);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9974, Type: High-Performer)\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size(); if (n < 2) return;\n    int* v = arr.data(); bool a=1, d=1;\n    for (int i=1; i<n; ++i) {\n        if (v[i]<v[i-1]) a=0; else if (v[i]>v[i-1]) d=0;\n        if (!(a|d)) break;\n    }\n    if (a) return; if (d) { std::reverse(v, v+n); return; }\n    int mn = v[0], mx = v[0];\n    for (int x : arr) { mn = std::min(mn, x); mx = std::max(mx, x); }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 32LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : arr) c[x - mn]++;\n        int* p = v;\n        for (int i = 0; i <= r; ++i)\n            if (int k = c[i]) p = std::fill_n(p, k, i + mn);\n        return;\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#pragma GCC optimize(\"O3,unroll-loops\")\n#pragma GCC target(\"avx2,bmi,bmi2,lzcnt,popcnt\")\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program is already highly optimized, but it can be further improved by refining the adaptive sorting logic and simplifying the implementation to reduce its overall size. The following changes streamline the prefix-detection logic, use pointers for more concise and potentially faster array access, and adjust the counting sort heuristic to be more effective on a wider range of distributions.\n\n### Key Improvements:\n1.  **Prefix Detection:** Simplified the logic that identifies and handles ascending or descending prefixes.\n2.  **Pointer-Based Access:** Used raw pointers (`v.data()`) to simplify pointer arithmetic and potentially help the compiler with vectorization.\n3.  **Heuristic Tuning:** Adjusted the counting sort threshold from `32n` to `20n` to better balance the $O(N+R)$ complexity of counting sort against the $O(N \\log N)$ complexity of comparison-based sorting.\n4.  **Code Simplification:** Reduced the overall length of the function to address the 500-character suggestion while maintaining high performance.\n5.  **Merge Junction Check:** Retained the junction check `if (p[i] < p[i-1])` to skip the relatively expensive `std::inplace_merge` when the prefix and the newly sorted portion are already perfectly ordered.\n\n```cpp\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1; if (n < 2) return;\n    while (i < n && v[i] == v[i-1]) i++;\n    if (i < n && v[i] < v[i-1]) {\n        while (i < n && v[i] <= v[i-1]) i++;\n        std::reverse(v.begin(), v.begin() + i);\n    }\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    int mn = v[0], mx = v[0];\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < (long long)n * 32) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        auto it = v.begin();\n        for (int j = 0; j <= (int)r; ++j)\n            if (int cnt = c[j]) { std::fill_n(it, cnt, j + mn); it += cnt; }\n        return;\n    }\n    if (i * 4 > n || n - i < 80) {\n        std::sort(v.begin() + i, v.end());\n        if (v[i] < v[i-1]) std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n=======\nvoid adaptive_sort(std::vector<int>& v) {\n    int n = v.size(), i = 1, *p = v.data(); if (n < 2) return;\n    while (i < n && p[i] == p[i-1]) i++;\n    if (i < n && p[i] < p[i-1]) {\n        while (i < n && p[i] <= p[i-1]) i++;\n        std::reverse(p, p + i);\n    }\n    while (i < n && p[i] >= p[i-1]) i++;\n    if (i == n) return;\n    int mn = p[0], mx = mn;\n    for (int x : v) { if (x < mn) mn = x; if (x > mx) mx = x; }\n    long long r = (long long)mx - mn;\n    if (r < 2000000 && r < n * 20LL) {\n        std::vector<int> c(r + 1, 0);\n        for (int x : v) c[x - mn]++;\n        for (int j = 0; j <= r; ++j) if (int k = c[j]) p = std::fill_n(p, k, j + mn);\n        return;\n    }\n    if (i * 4 > n || n - i < 64) {\n        std::sort(p + i, p + n);\n        if (p[i] < p[i-1]) std::inplace_merge(p, p + i, p + n);\n    } else std::sort(p, p + n);\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00015276, 0.00041148, 4.581e-05, 0.0004577, 1.048e-05, 0.00010833, 1.704e-05, 0.000150411, 0.000137889, 0.00051918, 3.2029e-05, 0.00032038, 4.709e-06, 4.6469e-05, 4.34e-05, 0.00043964, 0.00101695, 0.00327421, 9.594e-05, 0.00035551], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}