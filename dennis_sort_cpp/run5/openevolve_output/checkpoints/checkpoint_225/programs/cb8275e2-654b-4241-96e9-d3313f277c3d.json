{"id": "cb8275e2-654b-4241-96e9-d3313f277c3d", "code": "#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n        else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n    }\n    if (!a) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) i++;\n    if (i == n) return;\n    if (i > n/4 || n-i < 64) {\n        std::sort(arr.begin() + i, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}", "changes_description": null, "language": "unknown", "parent_id": "27e1f295-1d9e-4cd3-ac94-615aa6168103", "generation": 6, "timestamp": 1773800184.0509257, "iteration_found": 64, "metrics": {"score": 0.9815921999999999, "combined_score": 0.9815921999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.971777, "adaptability_score": 0.996315, "avg_time": 0.00290431}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n      auto it = std::is_sorted_until(arr.begin(), arr.end());\n      if (it == arr.end()) return;\n  \n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else if (std::distance(it, arr.end()) < std::max<size_t>(32, arr.size() / 16)) {\n          std::sort(it, arr.end());\n          std::inplace_merge(arr.begin(), it, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      size_t n = arr.size(), i = 1;\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (; i < n; ++i) {\n          if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n          else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n      }\n      if (!a) std::reverse(arr.begin(), arr.begin() + i);\n      while (i < n && arr[i-1] <= arr[i]) i++;\n      if (i == n) return;\n      if (i > n/4 || n-i < 64) {\n          std::sort(arr.begin() + i, arr.end());\n          std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n  }", "parent_metrics": {"score": 0.9766676000000001, "combined_score": 0.9766676000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964084, "adaptability_score": 0.995543, "avg_time": 0.00372538}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9767\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9771 \u2192 0.9767. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00095807, 0.0106743, 0.000286429, 0.00348518, 2.173e-05, 0.0001996, 0.00046723, 0.00663405, 0.00089378, 0.00967633, 0.00122211, 0.0142458, 7.57e-06, 7.477e-05, 0.00061514, 0.0067862, 0.00092708, 0.0114078, 0.000386449, 0.00553799]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() < 2) return;\n  \n      // Check if already sorted\n      if (std::is_sorted(arr.begin(), arr.end())) {\n          return;\n      }\n  \n      // Check if reverse sorted\n      if (std::is_sorted(arr.rbegin(), arr.rend())) {\n          std::reverse(arr.begin(), arr.end());\n      } else {\n          // Fallback for random or mostly unsorted data\n          std::sort(arr.begin(), arr.end());\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool ascending = true;\n      bool descending = true;\n  \n      // Combined single-pass scan for sorted or reverse-sorted sequences\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) {\n              ascending = false;\n              // If it's no longer ascending AND it was already not descending,\n              // it's neither sorted nor reverse-sorted. Break early.\n              if (!descending) {\n                  std::sort(arr.begin(), arr.end());\n                  return;\n              }\n          } else if (arr[i] > arr[i-1]) {\n              descending = false;\n              // If it's no longer descending AND it was already not ascending,\n              // it's neither sorted nor reverse-sorted. Break early.\n              if (!ascending) {\n                  std::sort(arr.begin(), arr.end());\n                  return;\n              }\n          }\n      }\n  \n      // If the array is descending (and not necessarily ascending), reverse it.\n      // If it is ascending (or both, meaning all elements are equal), it's already sorted.\n  ... (4 more lines)\n- Metrics: score: 0.9771, combined_score: 0.9771, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9647, adaptability_score: 0.9957, avg_time: 0.0037\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      const size_t n = arr.size();\n      if (n < 2) return;\n  \n      bool ascending = true;\n      bool descending = true;\n  \n      // Combined single-pass scan for sorted or reverse-sorted sequences\n      for (size_t i = 1; i < n; ++i) {\n          if (arr[i] < arr[i-1]) {\n              ascending = false;\n              // If it's no longer ascending AND it was already not descending,\n              // it's neither sorted nor reverse-sorted. Break early.\n              if (!descending) {\n                  std::sort(arr.begin(), arr.end());\n                  return;\n              }\n          } else if (arr[i] > arr[i-1]) {\n              descending = false;\n              // If it's no longer descending AND it was already not ascending,\n              // it's neither sorted nor reverse-sorted. Break early.\n              if (!ascending) {\n                  std::sort(arr.begin(), arr.end());\n                  return;\n              }\n          }\n      }\n  \n      // If the array is descending (and not necessarily ascending), reverse it.\n      // If it is ascending (or both, meaning all elements are equal), it's already sorted.\n  ... (4 more lines)\nwith:\n  void adaptive_sort(std::vector<int>& v) {\n      size_t n = v.size(), i = 1;\n      if (n < 2) return;\n      bool a = 1, d = 1;\n      for (; i < n; ++i) {\n          if (v[i] < v[i-1]) { if (!d) break; a = 0; }\n          else if (v[i] > v[i-1]) { if (!a) break; d = 0; }\n      }\n      if (i == n) { if (!a) std::reverse(v.begin(), v.end()); }\n      else if (i > n/3 || n-i < 64) {\n          if (!a) std::reverse(v.begin(), v.begin() + i);\n          std::sort(v.begin() + i, v.end());\n          std::inplace_merge(v.begin(), v.begin() + i, v.end());\n      } else std::sort(v.begin(), v.end());\n  }\n- Metrics: score: 0.9808, combined_score: 0.9808, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9706, adaptability_score: 0.9961, avg_time: 0.0030\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  size_t i = 1;\n      bool a = true, d = true;\n      for (; i < n; ++i) {\n          if (arr[i] < arr[i-1]) { if (!d) break; a = false; }\n          else if (arr[i] > arr[i-1]) { if (!a) break; d = false; }\n      }\n      if (i == n) {\n          if (!a) std::reverse(arr.begin(), arr.end());\n      } else if (i > n / 3 || n - i < 64) {\n          if (!a) std::reverse(arr.begin(), arr.begin() + i);\n          std::sort(arr.begin() + i, arr.end());\n          std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else {\n          std::sort(arr.begin(), arr.end());\n      }\nwith:\n  size_t i = 1;\n      bool a = 1, d = 1;\n      for (; i < n; ++i) {\n          if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n          else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n      }\n      if (i == n) { if (!a) std::reverse(arr.begin(), arr.end()); }\n      else if (i > n / 2 || (n > 64 && n - i < 64)) {\n          if (!a) std::reverse(arr.begin(), arr.begin() + i);\n          std::sort(arr.begin() + i, arr.end());\n          std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n      } else std::sort(arr.begin(), arr.end());\n- Metrics: score: 0.9808, combined_score: 0.9808, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9706, adaptability_score: 0.9961, avg_time: 0.0030\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9819)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (v[i] < v[i-1]) { if (!d) break; a = 0; }\n        else if (v[i] > v[i-1]) { if (!a) break; d = 0; }\n    }\n    if (!a) std::reverse(v.begin(), v.begin() + i);\n    while (i < n && v[i-1] <= v[i]) i++;\n    if (i == n) return;\n    if (i > n / 4 || n - i < 64) {\n        std::sort(v.begin() + i, v.end());\n        std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9819), Performs well on combined_score (0.9819), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9722), Performs well on adaptability_score (0.9964), Performs well on avg_time (0.0029)\n\n### Program 2 (Score: 0.9808)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    size_t i = 1;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n        else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n    }\n    if (i == n) { if (!a) std::reverse(arr.begin(), arr.end()); }\n    else if (i > n / 2 || (n > 64 && n - i < 64)) {\n        if (!a) std::reverse(arr.begin(), arr.begin() + i);\n        std::sort(arr.begin() + i, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Performs well on score (0.9808), Performs well on combined_score (0.9808), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9706), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0030)\n\n### Program 3 (Score: 0.9808)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (v[i] < v[i-1]) { if (!d) break; a = 0; }\n        else if (v[i] > v[i-1]) { if (!a) break; d = 0; }\n    }\n    if (i == n) { if (!a) std::reverse(v.begin(), v.end()); }\n    else if (i > n/3 || n-i < 64) {\n        if (!a) std::reverse(v.begin(), v.begin() + i);\n        std::sort(v.begin() + i, v.end());\n        std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nKey features: Performs well on score (0.9808), Performs well on combined_score (0.9808), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9706), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0030)\n\n### Program 4 (Score: 0.9771)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    \n    bool ascending = true;\n    bool descending = true;\n    \n    // Combined single-pass scan for sorted or reverse-sorted sequences\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) {\n            ascending = false;\n            // If it's no longer ascending AND it was already not descending,\n            // it's neither sorted nor reverse-sorted. Break early.\n            if (!descending) {\n                std::sort(arr.begin(), arr.end());\n                return;\n            }\n        } else if (arr[i] > arr[i-1]) {\n            descending = false;\n            // If it's no longer descending AND it was already not ascending,\n            // it's neither sorted nor reverse-sorted. Break early.\n            if (!ascending) {\n                std::sort(arr.begin(), arr.end());\n                return;\n            }\n        }\n    }\n    \n    // If the array is descending (and not necessarily ascending), reverse it.\n    // If it is ascending (or both, meaning all elements are equal), it's already sorted.\n    if (descending) {\n        std::reverse(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Performs well on score (0.9771), Performs well on combined_score (0.9771), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9647), Performs well on adaptability_score (0.9957), Performs well on avg_time (0.0037)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9767)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first unsorted element\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) {\n        return;\n    }\n\n    // Optimization for nearly sorted data: if the unsorted part is small, sort and merge\n    const size_t unsorted_len = static_cast<size_t>(std::distance(it, arr.end()));\n    if (unsorted_len < std::max<size_t>(32, n / 16)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n        return;\n    }\n\n    // Check if the entire array is reverse sorted\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fallback for random or significantly unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9769)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = true, d = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) {\n            if (!d) { std::sort(arr.begin(), arr.end()); return; }\n            a = false;\n        } else if (arr[i] > arr[i-1]) {\n            if (!a) { std::sort(arr.begin(), arr.end()); return; }\n            d = false;\n        }\n    }\n    if (d && !a) std::reverse(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9767)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::distance(it, arr.end()) < std::max<size_t>(32, arr.size() / 16)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9808, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& v) {\n    size_t n = v.size(), i = 1;\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (v[i] < v[i-1]) { if (!d) break; a = 0; }\n        else if (v[i] > v[i-1]) { if (!a) break; d = 0; }\n    }\n    if (i == n) { if (!a) std::reverse(v.begin(), v.end()); }\n    else if (i > n/3 || n-i < 64) {\n        if (!a) std::reverse(v.begin(), v.begin() + i);\n        std::sort(v.begin() + i, v.end());\n        std::inplace_merge(v.begin(), v.begin() + i, v.end());\n    } else std::sort(v.begin(), v.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9769, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    bool a = true, d = true;\n    for (size_t i = 1; i < n; ++i) {\n        if (arr[i] < arr[i-1]) {\n            if (!d) { std::sort(arr.begin(), arr.end()); return; }\n            a = false;\n        } else if (arr[i] > arr[i-1]) {\n            if (!a) { std::sort(arr.begin(), arr.end()); return; }\n            d = false;\n        }\n    }\n    if (d && !a) std::reverse(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9808, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n    size_t i = 1;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n        else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n    }\n    if (i == n) { if (!a) std::reverse(arr.begin(), arr.end()); }\n    else if (i > n / 2 || (n > 64 && n - i < 64)) {\n        if (!a) std::reverse(arr.begin(), arr.begin() + i);\n        std::sort(arr.begin() + i, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9767, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    const size_t n = arr.size();\n    if (n < 2) return;\n\n    // Detect the first unsorted element\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) {\n        return;\n    }\n\n    // Optimization for nearly sorted data: if the unsorted part is small, sort and merge\n    const size_t unsorted_len = static_cast<size_t>(std::distance(it, arr.end()));\n    if (unsorted_len < std::max<size_t>(32, n / 16)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n        return;\n    }\n\n    // Check if the entire array is reverse sorted\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else {\n        // Fallback for random or significantly unsorted data\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::distance(it, arr.end()) < std::max<size_t>(32, arr.size() / 16)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following improvement replaces the current multiple-pass sorted/reverse-sorted check with a single-pass mono-prefix detection loop. This new approach not only efficiently identifies both ascending and descending prefixes in a single traversal but also includes an optimization to extend the sorted prefix after reversing a descending segment. This method is highly effective for nearly sorted, reverse-sorted, and mixed datasets, leading to a higher fitness score by improving both performance and adaptability while reducing code complexity.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    auto it = std::is_sorted_until(arr.begin(), arr.end());\n    if (it == arr.end()) return;\n\n    if (std::is_sorted(arr.rbegin(), arr.rend())) {\n        std::reverse(arr.begin(), arr.end());\n    } else if (std::distance(it, arr.end()) < std::max<size_t>(32, arr.size() / 16)) {\n        std::sort(it, arr.end());\n        std::inplace_merge(arr.begin(), it, arr.end());\n    } else {\n        std::sort(arr.begin(), arr.end());\n    }\n}\n=======\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    bool a = 1, d = 1;\n    for (; i < n; ++i) {\n        if (arr[i] < arr[i-1]) { if (!d) break; a = 0; }\n        else if (arr[i] > arr[i-1]) { if (!a) break; d = 0; }\n    }\n    if (!a) std::reverse(arr.begin(), arr.begin() + i);\n    while (i < n && arr[i-1] <= arr[i]) i++;\n    if (i == n) return;\n    if (i > n/4 || n-i < 64) {\n        std::sort(arr.begin() + i, arr.end());\n        std::inplace_merge(arr.begin(), arr.begin() + i, arr.end());\n    } else std::sort(arr.begin(), arr.end());\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00101183, 0.0107213, 0.00029149, 0.00345515, 2.553e-05, 0.0001946, 0.00045856, 0.00674704, 0.00070642, 0.00765372, 9.4011e-05, 0.00108023, 1.243e-05, 0.00012327, 0.00052156, 0.00661224, 0.00102953, 0.0115773, 0.000382, 0.00538805], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}