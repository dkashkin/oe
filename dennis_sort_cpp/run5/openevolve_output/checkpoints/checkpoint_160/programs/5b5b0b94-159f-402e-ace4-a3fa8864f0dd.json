{"id": "5b5b0b94-159f-402e-ace4-a3fa8864f0dd", "code": "#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "c55ddf81-ff79-4564-bb70-ccbd070756cb", "generation": 3, "timestamp": 1773802587.2074971, "iteration_found": 140, "metrics": {"score": 0.980184, "combined_score": 0.980184, "compile_success": 1.0, "correctness": 1, "performance_score": 0.969574, "adaptability_score": 0.996099, "avg_time": 0.00313813}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  int partition(std::vector<int>& arr, int low, int high);\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (arr[i] < arr[i - 1]) s = false;\n          else if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n          else { quicksort(arr, p + 1, high); high = p; }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n          while (arr[--j] > p);\n          if (i >= j) return j;\n          std::swap(arr[i], arr[j]);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 32) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int v = arr[i], j = i;\n                  if (v < arr[j - 1]) {\n                      while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                      arr[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n          if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n          if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n          int p = arr[m], i = low - 1, j = high + 1;\n          while (true) {\n              while (arr[++i] < p);\n              while (arr[--j] > p);\n              if (i >= j) break;\n              std::swap(arr[i], arr[j]);\n          }\n          if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n          else { quicksort(arr, j + 1, high); high = j; }\n      }\n  }", "parent_metrics": {"score": 0.9773554, "combined_score": 0.9773554, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965221, "adaptability_score": 0.995557, "avg_time": 0.0036032}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9774\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9798 \u2192 0.9774. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00110189, 0.0124793, 0.00036965, 0.00384704, 2.057e-05, 0.00020582, 0.0005653, 0.00780643, 0.0009629, 0.0116155, 0.00074587, 0.00880482, 1.283e-05, 0.00012664, 0.00022176, 0.00281374, 0.00106891, 0.0128864, 0.00044437, 0.00596427]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n  ... (10 more lines)\nwith:\n  void qs(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (true) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n          else { qs(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = (int)a.size();\n  ... (10 more lines)\n- Metrics: score: 0.9798, combined_score: 0.9798, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9690, adaptability_score: 0.9960, avg_time: 0.0032\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void qs(int* a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n          else { qs(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n  ... (11 more lines)\nwith:\n  void qs(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n          else { qs(a, j + 1, h); h = j; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n  ... (10 more lines)\n- Metrics: score: 0.9800, combined_score: 0.9800, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9693, adaptability_score: 0.9960, avg_time: 0.0032\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high);\n  int partition(std::vector<int>& arr, int low, int high);\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      int n = (int)arr.size();\n      if (n < 2) return;\n      bool s = true, r = true;\n      for (int i = 1; i < n; i++) {\n          if (arr[i] < arr[i - 1]) s = false;\n          if (arr[i] > arr[i - 1]) r = false;\n      }\n      if (s) return;\n      if (r) { std::reverse(arr.begin(), arr.end()); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h);\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = (int)a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (a[i] < a[i - 1]) s = 0;\n          else if (a[i] > a[i - 1]) r = 0;\n      }\n      if (s) return;\n      if (r) { std::reverse(a.begin(), a.end()); return; }\n      quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 16) {\n              for (int i = low + 1; i <= high; i++) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int m = low + (high - low) / 2;\n      if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n      if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n      if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n      int p = arr[m];\n      int i = low - 1, j = high + 1;\n      while (true) {\n          while (arr[++i] < p);\n  ... (5 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 32) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  if (v < a[j - 1]) {\n                      while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                      a[j] = v;\n                  }\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m];\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }\n- Metrics: score: 0.9801, combined_score: 0.9801, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9694, adaptability_score: 0.9961, avg_time: 0.0032\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9802)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9802), Performs well on combined_score (0.9802), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9696), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0031)\n\n### Program 2 (Score: 0.9801)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& a, int l, int h);\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9801), Performs well on combined_score (0.9801), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9694), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0032)\n\n### Program 3 (Score: 0.9800)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9800), Performs well on combined_score (0.9800), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9693), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0032)\n\n### Program 4 (Score: 0.9798)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = false;\n        else if (a[i] > a[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9798), Performs well on combined_score (0.9798), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9690), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0032)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9797)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; --j; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, i = l - 1, j = h + 1;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    quicksort(a, 0, n - 1);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9797)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9797)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = 0;\n        else if (arr[i] > arr[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; --j; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (1) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9788, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(int* a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    int s = 1, r = 1;\n    int* a = arr.data();\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a, a + n); return; }\n    qs(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9800, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = 0;\n        else if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9798, Type: High-Performer)\n```unknown\n#include <vector>\n#include <algorithm>\n\nvoid qs(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 32) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                if (v < a[j - 1]) {\n                    while (j > l && a[j - 1] > v) { a[j] = a[j - 1]; j--; }\n                    a[j] = v;\n                }\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (true) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { qs(a, l, j); l = j + 1; }\n        else { qs(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = (int)a.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i - 1]) s = false;\n        else if (a[i] > a[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(a.begin(), a.end()); return; }\n    qs(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <vector>\n#include <algorithm>\n\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will optimize the adaptive check at the beginning of the algorithm and inline the partitioning logic to reduce function call overhead. We also increase the insertion sort threshold to 32, which has shown better performance in previous high-scoring versions.\n\n<<<<<<< SEARCH\nint partition(std::vector<int>& arr, int low, int high);\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < arr[i - 1]) s = false;\n        if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n=======\nvoid quicksort(std::vector<int>& arr, int low, int high);\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = (int)arr.size();\n    if (n < 2) return;\n    bool s = true, r = true;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = false;\n        else if (arr[i] > arr[i - 1]) r = false;\n    }\n    if (s) return;\n    if (r) { std::reverse(arr.begin(), arr.end()); return; }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 16) {\n            for (int i = low + 1; i <= high; i++) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) { quicksort(arr, low, p); low = p + 1; }\n        else { quicksort(arr, p + 1, high); high = p; }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int m = low + (high - low) / 2;\n    if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n    if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n    if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n    int p = arr[m];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) return j;\n        std::swap(arr[i], arr[j]);\n    }\n}\n=======\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 32) {\n            for (int i = low + 1; i <= high; ++i) {\n                int v = arr[i], j = i;\n                if (v < arr[j - 1]) {\n                    while (j > low && arr[j - 1] > v) { arr[j] = arr[j - 1]; j--; }\n                    arr[j] = v;\n                }\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[m], arr[low]);\n        if (arr[high] < arr[low]) std::swap(arr[high], arr[low]);\n        if (arr[high] < arr[m]) std::swap(arr[high], arr[m]);\n        int p = arr[m], i = low - 1, j = high + 1;\n        while (true) {\n            while (arr[++i] < p);\n            while (arr[--j] > p);\n            if (i >= j) break;\n            std::swap(arr[i], arr[j]);\n        }\n        if (j - low < high - j) { quicksort(arr, low, j); low = j + 1; }\n        else { quicksort(arr, j + 1, high); high = j; }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.0009993, 0.0114843, 0.00025316, 0.00284787, 2.501e-05, 0.00025472, 0.00044662, 0.00726536, 0.00092881, 0.00939092, 0.000675851, 0.0078131, 1.236e-05, 0.00012323, 0.00015842, 0.00250754, 0.00094469, 0.0112329, 0.00035116, 0.00504727], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}