{"id": "20b22c23-136e-4dd1-9d67-4c745d64dc96", "code": "void quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) a[j + 1] = a[j], j--;\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}", "changes_description": null, "language": "unknown", "parent_id": "a08274fa-3a5a-4fb0-97af-bb14cc42297b", "generation": 2, "timestamp": 1773763933.4350011, "iteration_found": 20, "metrics": {"score": 0.9780556, "combined_score": 0.9780556, "compile_success": 1.0, "correctness": 1, "performance_score": 0.96611, "adaptability_score": 0.995974, "avg_time": 0.00350786}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 20) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[low], arr[m]);\n          if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n          if (arr[high] < arr[m]) std::swap(arr[m], arr[high]);\n          int v = arr[m], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (arr[i] < v) std::swap(arr[lt++], arr[i++]);\n              else if (arr[i] > v) std::swap(arr[i], arr[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) {\n              quicksort(arr, low, lt - 1);\n              low = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, high);\n              high = lt - 1;\n          }\n      }\n  }\n  \n  ... (15 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 24) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i - 1;\n                  while (j >= l && a[j] > v) a[j + 1] = a[j], j--;\n                  a[j + 1] = v;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          int v = a[m], lt = l, gt = h, i = l;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); i++) {\n  ... (10 more lines)", "parent_metrics": {"score": 0.9775394, "combined_score": 0.9775394, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965309, "adaptability_score": 0.995885, "avg_time": 0.00359377}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9775\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.1215 \u2192 0.9775\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00100399, 0.0105964, 0.00056893, 0.00604687, 2.484e-05, 0.000254, 0.00030483, 0.00510129, 0.00096332, 0.0110732]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Unknown changes\n- Metrics: score: 0.1215, combined_score: 0.1215, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.0687, adaptability_score: 0.2008, avg_time: 1.3561\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (arr[i] < arr[i-1]) s = 0;\n          if (arr[i] > arr[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 20) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                  arr[j+1] = k;\n              }\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\nChange 3: Replace:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n      if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n      if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n      std::swap(arr[mid], arr[high]);\n      int v = arr[high], i = low;\n      for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\n- Metrics: score: 0.9280, combined_score: 0.9280, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8948, adaptability_score: 0.9777, avg_time: 0.0118\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 20) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                  arr[j + 1] = k;\n              }\n              return;\n          }\n          int m = low + (high - low) / 2;\n          if (arr[m] < arr[low]) std::swap(arr[low], arr[m]);\n          if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n          if (arr[high] < arr[m]) std::swap(arr[m], arr[high]);\n          int v = arr[m], lt = low, gt = high, i = low;\n          while (i <= gt) {\n              if (arr[i] < v) std::swap(arr[lt++], arr[i++]);\n              else if (arr[i] > v) std::swap(arr[i], arr[gt--]);\n              else i++;\n          }\n          if (lt - low < high - gt) {\n              quicksort(arr, low, lt - 1);\n              low = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, high);\n              high = lt - 1;\n          }\n      }\n  }\n  \n  ... (15 more lines)\n- Metrics: score: 0.9775, combined_score: 0.9775, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9653, adaptability_score: 0.9959, avg_time: 0.0036\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9775)\n```unknown\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[low], arr[m]);\n        if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n        if (arr[high] < arr[m]) std::swap(arr[m], arr[high]);\n        int v = arr[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (arr[i] < v) std::swap(arr[lt++], arr[i++]);\n            else if (arr[i] > v) std::swap(arr[i], arr[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) {\n            quicksort(arr, low, lt - 1);\n            low = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, high);\n            high = lt - 1;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = 0;\n        if (arr[i] > arr[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9775), Performs well on combined_score (0.9775), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9653), Performs well on adaptability_score (0.9959), Performs well on avg_time (0.0036)\n\n### Program 2 (Score: 0.9280)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i-1]) s = 0;\n        if (arr[i] > arr[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    std::swap(arr[mid], arr[high]);\n    int v = arr[high], i = low;\n    for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.9280), Performs well on combined_score (0.9280), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8948), Performs well on adaptability_score (0.9777), Performs well on avg_time (0.0118)\n\n### Program 3 (Score: 0.1215)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.1215), Performs well on combined_score (0.1215), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.0687), Performs well on adaptability_score (0.2008), Performs well on avg_time (1.3561)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9280, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i-1]) s = 0;\n        if (arr[i] > arr[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    std::swap(arr[mid], arr[high]);\n    int v = arr[high], i = low;\n    for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.1215, Type: Exploratory)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[low], arr[m]);\n        if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n        if (arr[high] < arr[m]) std::swap(arr[m], arr[high]);\n        int v = arr[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (arr[i] < v) std::swap(arr[lt++], arr[i++]);\n            else if (arr[i] > v) std::swap(arr[i], arr[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) {\n            quicksort(arr, low, lt - 1);\n            low = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, high);\n            high = lt - 1;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = 0;\n        if (arr[i] > arr[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program implements a highly effective hybrid sorting algorithm using a 3-way quicksort (Dijkstra's Dutch National Flag) combined with insertion sort for small subarrays and an initial linear pass to detect already sorted or reverse-sorted sequences.\n\nTo improve the **FITNESS SCORE**, the following changes focus on:\n1.  **Code Simplification**: Reducing the character count (addressing the hint that the code exceeds 500 characters) by shortening variable names and condensing the logic while maintaining the algorithm's robustness.\n2.  **Performance Tuning**: Increasing the insertion sort threshold from 20 to 24, which often yields better results in modern architectures by reducing recursion depth.\n3.  **Maintaining Adaptability**: Keeping the 3-way partitioning scheme, which is essential for maintaining a high adaptability score when dealing with datasets containing many duplicate keys.\n\nThe logic remains robust against worst-case scenarios (like sorted, reverse-sorted, and many-duplicates) while becoming more compact.\n\n```cpp\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j + 1] = arr[j]; j--; }\n                arr[j + 1] = k;\n            }\n            return;\n        }\n        int m = low + (high - low) / 2;\n        if (arr[m] < arr[low]) std::swap(arr[low], arr[m]);\n        if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n        if (arr[high] < arr[m]) std::swap(arr[m], arr[high]);\n        int v = arr[m], lt = low, gt = high, i = low;\n        while (i <= gt) {\n            if (arr[i] < v) std::swap(arr[lt++], arr[i++]);\n            else if (arr[i] > v) std::swap(arr[i], arr[gt--]);\n            else i++;\n        }\n        if (lt - low < high - gt) {\n            quicksort(arr, low, lt - 1);\n            low = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, high);\n            high = lt - 1;\n        }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i - 1]) s = 0;\n        if (arr[i] > arr[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) a[j + 1] = a[j], j--;\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00099241, 0.0104645, 0.00051266, 0.00563372, 2.491e-05, 0.00024862, 0.00027861, 0.00517653, 0.00097244, 0.0107742], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}