{"id": "2feb99be-ba75-42b0-8eb2-3ecb05e8e448", "code": "void adaptive_sort(std::vector<int>& a) {\n  for (int i = 1; i < (int)a.size(); ++i)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1; i <= h; ++i) {\n        int k = a[i], j = i - 1;\n        for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n        a[j + 1] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}", "changes_description": null, "language": "unknown", "parent_id": "184c2da0-f836-4253-baa6-ef36120cf68f", "generation": 6, "timestamp": 1773766820.782052, "iteration_found": 85, "metrics": {"score": 0.9776754000000001, "combined_score": 0.9776754000000001, "compile_success": 1.0, "correctness": 1, "performance_score": 0.965415, "adaptability_score": 0.996066, "avg_time": 0.00358241}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      for (int i = 1; i < n; i++)\n          if (arr[i] < arr[i - 1]) { quicksort(arr, 0, n - 1); return; }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n    for (int i = 1; i < (int)a.size(); ++i)\n      if (a[i] < a[i - 1]) return quicksort(a, 0, (int)a.size() - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 15) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int k = a[i], j = i - 1;\n                  while (j >= l && a[j] > k) { a[j + 1] = a[j]; --j; }\n                  a[j + 1] = k;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n      if (h - l < 15) {\n        for (int i = l + 1; i <= h; ++i) {\n          int k = a[i], j = i - 1;\n          for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n          a[j + 1] = k;\n        }\n        return;\n      }\n      int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n      while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) break;\n        std::swap(a[i], a[j]);\n      }\n      if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n      else { quicksort(a, j + 1, h); h = j; }\n    }\n  }", "parent_metrics": {"score": 0.9767972, "combined_score": 0.9767972, "compile_success": 1.0, "correctness": 1, "performance_score": 0.964094, "adaptability_score": 0.995852, "avg_time": 0.0037243}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9768\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9781 \u2192 0.9768. Consider revising recent changes.\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00101396, 0.0113239, 0.0002772, 0.0033215, 0.00015582, 0.00192108, 0.00045634, 0.00690985, 0.00094413, 0.0109193]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      for (int i = 1; i < (int)arr.size(); ++i)\n          if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    for (int i = 1; i < n; i++)\n      if (a[i] < a[i - 1]) return quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n      if (h - l < 15) {\n          for (int i = l + 1; i <= h; ++i) {\n              int k = arr[i], j = i - 1;\n              for (; j >= l && arr[j] > k; --j) arr[j + 1] = arr[j];\n              arr[j + 1] = k;\n          }\n          return;\n      }\n      int p = partition(arr, l, h);\n      quicksort(arr, l, p - 1);\n      quicksort(arr, p + 1, h);\n  }\n  \n  int partition(std::vector<int>& arr, int l, int h) {\n      std::swap(arr[l + (h - l) / 2], arr[h]);\n      int p = arr[h], i = l;\n      for (int j = l; j < h; ++j)\n          if (arr[j] < p) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[h]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n      if (h - l < 16) {\n        for (int i = l + 1, j, k; i <= h; ++i) {\n          for (k = a[i], j = i; j > l && a[j - 1] > k; --j) a[j] = a[j - 1];\n          a[j] = k;\n        }\n        return;\n      }\n      int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n      while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) break;\n        std::swap(a[i], a[j]);\n      }\n      if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n      else { quicksort(a, j + 1, h); h = j; }\n    }\n  }\n- Metrics: score: 0.9781, combined_score: 0.9781, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9660, adaptability_score: 0.9961, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      for (int i = 1; i < (int)arr.size(); ++i)\n          if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 16) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int k = a[i], j = i - 1;\n                  for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n                  a[j + 1] = k;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2;\n          if (a[m] < a[l]) std::swap(a[m], a[l]);\n          if (a[h] < a[l]) std::swap(a[h], a[l]);\n          if (a[h] < a[m]) std::swap(a[h], a[m]);\n          int p = a[m], i = l - 1, j = h + 1;\n          while (1) {\n              while (a[++i] < p);\n              while (a[--j] > p);\n              if (i >= j) break;\n              std::swap(a[i], a[j]);\n          }\n          if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n          else { quicksort(a, j + 1, h); h = j; }\n      }\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    for (int i = 1; i < n; i++)\n      if (arr[i] < arr[i - 1]) return quicksort(arr, 0, n - 1);\n  }\n  \n  void quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n      if (h - l < 15) {\n        for (int i = l + 1, j, k; i <= h; i++) {\n          for (k = a[i], j = i; j > l && a[j - 1] > k; j--) a[j] = a[j - 1];\n          a[j] = k;\n        }\n        return;\n      }\n      int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n      while (1) {\n        while (a[++i] < p);\n        while (a[--j] > p);\n        if (i >= j) break;\n        std::swap(a[i], a[j]);\n      }\n      if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n      else { quicksort(a, j + 1, h); h = j; }\n    }\n  }\n- Metrics: score: 0.9781, combined_score: 0.9781, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9661, adaptability_score: 0.9961, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      for (int i = 1; i < (int)arr.size(); ++i)\n          if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    for (int i = 1; i < n; i++)\n      if (arr[i] < arr[i - 1]) return quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n      if (h - l < 15) {\n          for (int i = l + 1; i <= h; ++i) {\n              int k = arr[i], j = i - 1;\n              for (; j >= l && arr[j] > k; --j) arr[j + 1] = arr[j];\n              arr[j + 1] = k;\n          }\n          return;\n      }\n      int p = partition(arr, l, h);\n      quicksort(arr, l, p - 1);\n      quicksort(arr, p + 1, h);\n  }\n  \n  int partition(std::vector<int>& arr, int l, int h) {\n      std::swap(arr[l + (h - l) / 2], arr[h]);\n      int p = arr[h], i = l;\n      for (int j = l; j < h; ++j)\n          if (arr[j] < p) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[h]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& arr, int l, int h) {\n    while (l < h) {\n      if (h - l < 15) {\n        for (int i = l + 1, j, k; i <= h; i++) {\n          for (k = arr[i], j = i; j > l && arr[j - 1] > k; j--) arr[j] = arr[j - 1];\n          arr[j] = k;\n        }\n        return;\n      }\n      int i = l - 1, j = h + 1, p = arr[l + (h - l) / 2];\n      while (1) {\n        while (arr[++i] < p);\n        while (arr[--j] > p);\n        if (i >= j) break;\n        std::swap(arr[i], arr[j]);\n      }\n      if (j - l < h - j) { quicksort(arr, l, j); l = j + 1; }\n      else { quicksort(arr, j + 1, h); h = j; }\n    }\n  }\n- Metrics: score: 0.9782, combined_score: 0.9782, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9662, adaptability_score: 0.9962, avg_time: 0.0035\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9782)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 15) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\nKey features: Performs well on score (0.9782), Performs well on combined_score (0.9782), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9663), Performs well on adaptability_score (0.9962), Performs well on avg_time (0.0035)\n\n### Program 2 (Score: 0.9782)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n  int n = arr.size();\n  for (int i = 1; i < n; i++)\n    if (arr[i] < arr[i - 1]) return quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1, j, k; i <= h; i++) {\n        for (k = arr[i], j = i; j > l && arr[j - 1] > k; j--) arr[j] = arr[j - 1];\n        arr[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = arr[l + (h - l) / 2];\n    while (1) {\n      while (arr[++i] < p);\n      while (arr[--j] > p);\n      if (i >= j) break;\n      std::swap(arr[i], arr[j]);\n    }\n    if (j - l < h - j) { quicksort(arr, l, j); l = j + 1; }\n    else { quicksort(arr, j + 1, h); h = j; }\n  }\n}\n```\nKey features: Performs well on score (0.9782), Performs well on combined_score (0.9782), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9662), Performs well on adaptability_score (0.9962), Performs well on avg_time (0.0035)\n\n### Program 3 (Score: 0.9781)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n  int n = arr.size();\n  for (int i = 1; i < n; i++)\n    if (arr[i] < arr[i - 1]) return quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1, j, k; i <= h; i++) {\n        for (k = a[i], j = i; j > l && a[j - 1] > k; j--) a[j] = a[j - 1];\n        a[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nKey features: Performs well on score (0.9781), Performs well on combined_score (0.9781), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9661), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0035)\n\n### Program 4 (Score: 0.9781)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n  int n = a.size();\n  for (int i = 1; i < n; i++)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 16) {\n      for (int i = l + 1, j, k; i <= h; ++i) {\n        for (k = a[i], j = i; j > l && a[j - 1] > k; --j) a[j] = a[j - 1];\n        a[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nKey features: Performs well on score (0.9781), Performs well on combined_score (0.9781), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9660), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0035)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9779)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n  for (int i = 1; i < (int)a.size(); ++i)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, (int)a.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1, j, k; i <= h; ++i) {\n        for (k = a[i], j = i - 1; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n        a[j + 1] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    for (;;) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9779)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (h - l > 15) {\n        int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n        for (;;) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    for (int i = 1; i < n; ++i) {\n        if (a[i] < a[i - 1]) {\n            quicksort(a, 0, n - 1);\n            for (int i = 1, j, k; i < n; ++i) {\n                k = a[i]; j = i - 1;\n                while (j >= 0 && a[j] > k) { a[j + 1] = a[j]; --j; }\n                a[j + 1] = k;\n            }\n            return;\n        }\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9779)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n  int n = a.size();\n  for (int i = 1; i < n; ++i)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1, j, k; i <= h; ++i) {\n        for (k = a[i], j = i; j > l && a[j - 1] > k; --j) a[j] = a[j - 1];\n        a[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9773, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n  int n = a.size();\n  for (int i = 1; i < n; ++i)\n    if (a[i] < a[i-1]) return quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 16) {\n      for (int i = l + 1, j, k; i <= h; ++i) {\n        k = a[i]; j = i;\n        while (j > l && a[j-1] > k) { a[j] = a[j-1]; --j; }\n        a[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9776, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n  int n = a.size();\n  for (int i = 1; i < n; i++)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1, j, k; i <= h; ++i) {\n        for (k = a[i], j = i; j > l && a[j - 1] > k; --j) a[j] = a[j - 1];\n        a[j] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9782, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 15) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9769, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    for (int i = 1; i < (int)arr.size(); ++i)\n        if (arr[i] < arr[i - 1]) return quicksort(arr, 0, (int)arr.size() - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 16) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    for (int i = 1; i < n; i++)\n        if (arr[i] < arr[i - 1]) { quicksort(arr, 0, n - 1); return; }\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 15) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                while (j >= l && a[j] > k) { a[j + 1] = a[j]; --j; }\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will simplify the `quicksort` logic by removing the median-of-three pivot selection, which adds unnecessary overhead for the test cases, and by condensing the insertion sort loop. These changes aim to revert the recent fitness decline and reduce the overall code length, which currently exceeds the recommended 500 characters. We'll use the efficient Hoare partition scheme with a middle-element pivot and tail recursion optimization, which previously yielded a higher fitness score of 0.9782.\n\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    for (int i = 1; i < n; i++)\n        if (arr[i] < arr[i - 1]) { quicksort(arr, 0, n - 1); return; }\n}\n=======\nvoid adaptive_sort(std::vector<int>& a) {\n  for (int i = 1; i < (int)a.size(); ++i)\n    if (a[i] < a[i - 1]) return quicksort(a, 0, (int)a.size() - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 15) {\n            for (int i = l + 1; i <= h; ++i) {\n                int k = a[i], j = i - 1;\n                while (j >= l && a[j] > k) { a[j + 1] = a[j]; --j; }\n                a[j + 1] = k;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[m], a[l]);\n        if (a[h] < a[l]) std::swap(a[h], a[l]);\n        if (a[h] < a[m]) std::swap(a[h], a[m]);\n        int p = a[m], i = l - 1, j = h + 1;\n        while (1) {\n            while (a[++i] < p);\n            while (a[--j] > p);\n            if (i >= j) break;\n            std::swap(a[i], a[j]);\n        }\n        if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n        else { quicksort(a, j + 1, h); h = j; }\n    }\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h) {\n  while (l < h) {\n    if (h - l < 15) {\n      for (int i = l + 1; i <= h; ++i) {\n        int k = a[i], j = i - 1;\n        for (; j >= l && a[j] > k; --j) a[j + 1] = a[j];\n        a[j + 1] = k;\n      }\n      return;\n    }\n    int i = l - 1, j = h + 1, p = a[l + (h - l) / 2];\n    while (1) {\n      while (a[++i] < p);\n      while (a[--j] > p);\n      if (i >= j) break;\n      std::swap(a[i], a[j]);\n    }\n    if (j - l < h - j) { quicksort(a, l, j); l = j + 1; }\n    else { quicksort(a, j + 1, h); h = j; }\n  }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.0010135, 0.0111072, 0.00027525, 0.00318243, 0.00015718, 0.00189715, 0.00050048, 0.00685299, 0.00093682, 0.00990111], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}