{"id": "99b9c167-26e8-4b67-9616-949056e4c8e8", "code": "#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    int *b = arr.data(), *e = b + arr.size();\n    int *it = std::is_sorted_until(b, e);\n    if (it == e) return;\n    if (b[0] == it[-1] && std::is_sorted(it - 1, e, [](int x, int y){return x > y;})) {\n        std::reverse(b, e);\n        return;\n    }\n    std::sort(b, e);\n}", "changes_description": null, "language": "unknown", "parent_id": "812d869e-9bd2-4876-809f-d274319eb111", "generation": 7, "timestamp": 1773765617.4474134, "iteration_found": 55, "metrics": {"score": 0.9792576, "combined_score": 0.9792576, "compile_success": 1.0, "correctness": 1, "performance_score": 0.96803, "adaptability_score": 0.996099, "avg_time": 0.00330257}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  size_t n = arr.size();\n      if (n < 2) return;\n      int* b = arr.data();\n      int* e = b + n;\n  \n      // Fast scan for ascending order\n      int* it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      // If not ascending, check if it's descending.\n      // Use O(1) filter: a non-increasing array must satisfy b[0] >= b[n-1].\n      // Also, the prefix [b, it) must be constant (b[0] == it[-1]).\n      if (b[0] > b[n - 1] && b[0] == it[-1]) {\n          if (std::is_sorted(it - 1, e, [](int x, int y) { return x > y; })) {\n              std::reverse(b, e);\n              return;\n          }\n      }\n  \n      std::sort(b, e);\nwith:\n  if (arr.size() < 2) return;\n      int *b = arr.data(), *e = b + arr.size();\n      int *it = std::is_sorted_until(b, e);\n      if (it == e) return;\n      if (b[0] == it[-1] && std::is_sorted(it - 1, e, [](int x, int y){return x > y;})) {\n          std::reverse(b, e);\n          return;\n      }\n      std::sort(b, e);", "parent_metrics": {"score": 0.9788096, "combined_score": 0.9788096, "compile_success": 1.0, "correctness": 1, "performance_score": 0.967362, "adaptability_score": 0.995981, "avg_time": 0.00337388}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9788\n- Feature coordinates: score=0.98, performance_score=0.97, adaptability_score=1.00\n- Focus areas: - Fitness unchanged at 0.9788\n- Exploring score=0.98, performance_score=0.97, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00094056, 0.0105864, 0.00028919, 0.0034524, 2.382e-05, 0.00013863, 0.00043797, 0.00672162, 0.00087509, 0.0102732]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  size_t n = arr.size();\n      if (n < 2) return;\n      int* b = arr.data();\n      int* e = b + n;\n      int* p = b;\n  \n      while (++p < e && *p == *(p - 1));\n      if (p == e) return;\n  \n      if (*p > *(p - 1)) {\n          while (++p < e && *p >= *(p - 1));\n          if (p == e) return;\n      } else {\n          while (++p < e && *p <= *(p - 1));\n          if (p == e) { std::reverse(b, e); return; }\n      }\n  \n      std::sort(b, e);\nwith:\n  size_t n = arr.size();\n      if (n < 2) return;\n      int* b = arr.data();\n      int* e = b + n;\n  \n      // Fast scan for ascending order\n      int* it = std::is_sorted_until(b, e);\n      if (it == e) return;\n  \n      // If not ascending, check if it's descending.\n      // Use O(1) filter: a non-increasing array must satisfy b[0] >= b[n-1].\n      // Also, the prefix [b, it) must be constant (b[0] == it[-1]).\n      if (b[0] > b[n - 1] && b[0] == it[-1]) {\n          if (std::is_sorted(it - 1, e, [](int x, int y) { return x > y; })) {\n              std::reverse(b, e);\n              return;\n          }\n      }\n  \n      std::sort(b, e);\n- Metrics: score: 0.9788, combined_score: 0.9788, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9674, adaptability_score: 0.9960, avg_time: 0.0034\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  size_t n = arr.size(), i = 1;\n      if (n < 2) return;\n      const int* d = arr.data();\n      while (i < n && d[i] == d[i-1]) i++;\n      if (i == n) return;\n      if (d[i] > d[i-1]) {\n          while (i < n && d[i] >= d[i-1]) i++;\n          if (i == n) return;\n      } else {\n          while (i < n && d[i] <= d[i-1]) i++;\n          if (i == n) { std::reverse(arr.begin(), arr.end()); return; }\n      }\n      std::sort(arr.begin(), arr.end());\nwith:\n  size_t n = arr.size();\n      if (n < 2) return;\n      int* b = arr.data();\n      int* e = b + n;\n      int* p = b;\n  \n      while (++p < e && *p == *(p - 1));\n      if (p == e) return;\n  \n      if (*p > *(p - 1)) {\n          while (++p < e && *p >= *(p - 1));\n          if (p == e) return;\n      } else {\n          while (++p < e && *p <= *(p - 1));\n          if (p == e) { std::reverse(b, e); return; }\n      }\n  \n      std::sort(b, e);\n- Metrics: score: 0.9790, combined_score: 0.9790, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9676, adaptability_score: 0.9960, avg_time: 0.0033\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  size_t n = arr.size();\n      if (n < 2) return;\n      int* const b = arr.data();\n      int* const e = b + n;\n      int* p = b + 1;\n  \n      // Skip initial equal elements\n      while (p < e && *p == *(p - 1)) ++p;\n      if (p == e) return;\n  \n      if (*p > *(p - 1)) {\n          // Confirm if the rest of the array is non-decreasing\n          while (++p < e && *p >= *(p - 1));\n          if (p == e) return;\n      } else {\n          // Confirm if the rest of the array is non-increasing\n          while (++p < e && *p <= *(p - 1));\n          if (p == e) {\n              std::reverse(b, e);\n              return;\n          }\n      }\n  \n      // Standard sort for the general case\n      std::sort(b, e);\nwith:\n  if (arr.size() < 2) return;\n      int *b = arr.data(), *e = b + arr.size(), *p = b + 1;\n  \n      while (p < e && *p == *(p - 1)) ++p;\n      if (p == e) return;\n  \n      if (*p > *(p - 1)) {\n          while (++p < e && *p >= *(p - 1));\n          if (p == e) return;\n      } else {\n          while (++p < e && *p <= *(p - 1));\n          if (p == e) { std::reverse(b, e); return; }\n      }\n  \n      std::sort(b, e);\n- Metrics: score: 0.9791, combined_score: 0.9791, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9679, adaptability_score: 0.9961, avg_time: 0.0033\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9793)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* const b = arr.data();\n    int* const e = b + n;\n    int* p = b + 1;\n    \n    // Skip initial equal elements\n    while (p < e && *p == *(p - 1)) ++p;\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        // Confirm if the rest of the array is non-decreasing\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        // Confirm if the rest of the array is non-increasing\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n    \n    // Standard sort for the general case\n    std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9793), Performs well on combined_score (0.9793), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9681), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0033)\n\n### Program 2 (Score: 0.9791)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    int *b = arr.data(), *e = b + arr.size(), *p = b + 1;\n    \n    while (p < e && *p == *(p - 1)) ++p;\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) { std::reverse(b, e); return; }\n    }\n    \n    std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9791), Performs well on combined_score (0.9791), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9679), Performs well on adaptability_score (0.9961), Performs well on avg_time (0.0033)\n\n### Program 3 (Score: 0.9790)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* b = arr.data();\n    int* e = b + n;\n    int* p = b;\n    \n    while (++p < e && *p == *(p - 1));\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) { std::reverse(b, e); return; }\n    }\n    \n    std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9790), Performs well on combined_score (0.9790), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9676), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0033)\n\n### Program 4 (Score: 0.9788)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* b = arr.data();\n    int* e = b + n;\n\n    // Fast scan for ascending order\n    int* it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    // If not ascending, check if it's descending.\n    // Use O(1) filter: a non-increasing array must satisfy b[0] >= b[n-1].\n    // Also, the prefix [b, it) must be constant (b[0] == it[-1]).\n    if (b[0] > b[n - 1] && b[0] == it[-1]) {\n        if (std::is_sorted(it - 1, e, [](int x, int y) { return x > y; })) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n\n    std::sort(b, e);\n}\n```\nKey features: Performs well on score (0.9788), Performs well on combined_score (0.9788), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9674), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0034)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9787)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* const b = arr.data();\n    int* const e = b + n;\n    int* p = b + 1;\n    \n    // Skip initial equal elements\n    while (p < e && *p == *(p - 1)) ++p;\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        // Confirm if the rest of the array is non-decreasing\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        // Confirm if the rest of the array is non-increasing\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n    \n    // Standard sort for the general case\n    std::sort(b, e);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9782)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    const int* d = arr.data();\n    while (i < n && d[i] == d[i-1]) i++;\n    if (i == n) return;\n    if (d[i] > d[i-1]) {\n        while (i < n && d[i] >= d[i-1]) i++;\n        if (i == n) return;\n    } else {\n        while (i < n && d[i] <= d[i-1]) i++;\n        if (i == n) { std::reverse(arr.begin(), arr.end()); return; }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9791, Type: High-Performer)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() < 2) return;\n    int *b = arr.data(), *e = b + arr.size(), *p = b + 1;\n    \n    while (p < e && *p == *(p - 1)) ++p;\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) { std::reverse(b, e); return; }\n    }\n    \n    std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9790, Type: High-Performer)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* b = arr.data();\n    int* e = b + n;\n    int* p = b;\n    \n    while (++p < e && *p == *(p - 1));\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) { std::reverse(b, e); return; }\n    }\n    \n    std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9793, Type: High-Performer)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* const b = arr.data();\n    int* const e = b + n;\n    int* p = b + 1;\n    \n    // Skip initial equal elements\n    while (p < e && *p == *(p - 1)) ++p;\n    if (p == e) return;\n    \n    if (*p > *(p - 1)) {\n        // Confirm if the rest of the array is non-decreasing\n        while (++p < e && *p >= *(p - 1));\n        if (p == e) return;\n    } else {\n        // Confirm if the rest of the array is non-increasing\n        while (++p < e && *p <= *(p - 1));\n        if (p == e) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n    \n    // Standard sort for the general case\n    std::sort(b, e);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9782, Type: High-Performer)\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size(), i = 1;\n    if (n < 2) return;\n    const int* d = arr.data();\n    while (i < n && d[i] == d[i-1]) i++;\n    if (i == n) return;\n    if (d[i] > d[i-1]) {\n        while (i < n && d[i] >= d[i-1]) i++;\n        if (i == n) return;\n    } else {\n        while (i < n && d[i] <= d[i-1]) i++;\n        if (i == n) { std::reverse(arr.begin(), arr.end()); return; }\n    }\n    std::sort(arr.begin(), arr.end());\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n#include <algorithm>\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* b = arr.data();\n    int* e = b + n;\n\n    // Fast scan for ascending order\n    int* it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    // If not ascending, check if it's descending.\n    // Use O(1) filter: a non-increasing array must satisfy b[0] >= b[n-1].\n    // Also, the prefix [b, it) must be constant (b[0] == it[-1]).\n    if (b[0] > b[n - 1] && b[0] == it[-1]) {\n        if (std::is_sorted(it - 1, e, [](int x, int y) { return x > y; })) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n\n    std::sort(b, e);\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the program's fitness, we can simplify the adaptive logic while maintaining its high performance and adaptability. The current approach uses `std::is_sorted_until` and a complex conditional check to identify reverse-sorted sequences. We can streamline this by using a more direct check: any reverse-sorted (non-increasing) array must have a non-decreasing prefix that is entirely constant. \n\nBy comparing the first element of the array with the element where the non-decreasing scan fails, we can determine with $O(1)$ complexity if it is worth performing a full non-increasing scan. If the constant prefix condition is met, we use `std::is_sorted` with a greater-than comparator. This approach is highly efficient, reduces code complexity, and maintains the robustness of using standard library algorithms.\n\nFurthermore, we've simplified the code significantly to address the length constraint, which also helps improve its adaptability and performance scores by reducing function overhead and utilizing well-optimized library implementations.\n\n<<<<<<< SEARCH\n    size_t n = arr.size();\n    if (n < 2) return;\n    int* b = arr.data();\n    int* e = b + n;\n\n    // Fast scan for ascending order\n    int* it = std::is_sorted_until(b, e);\n    if (it == e) return;\n\n    // If not ascending, check if it's descending.\n    // Use O(1) filter: a non-increasing array must satisfy b[0] >= b[n-1].\n    // Also, the prefix [b, it) must be constant (b[0] == it[-1]).\n    if (b[0] > b[n - 1] && b[0] == it[-1]) {\n        if (std::is_sorted(it - 1, e, [](int x, int y) { return x > y; })) {\n            std::reverse(b, e);\n            return;\n        }\n    }\n\n    std::sort(b, e);\n=======\n    if (arr.size() < 2) return;\n    int *b = arr.data(), *e = b + arr.size();\n    int *it = std::is_sorted_until(b, e);\n    if (it == e) return;\n    if (b[0] == it[-1] && std::is_sorted(it - 1, e, [](int x, int y){return x > y;})) {\n        std::reverse(b, e);\n        return;\n    }\n    std::sort(b, e);\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00094802, 0.0106635, 0.0003026, 0.00351539, 2.049e-05, 0.00015186, 0.00042849, 0.0065406, 0.00088518, 0.00956962], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}