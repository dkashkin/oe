{"id": "c622217a-1341-4571-9b45-20e7da0fb828", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i)\n        if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n    if (s) return;\n    if (r) { for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n - 1 - i]); return; }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 22) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                a[j] = v;\n            } return;\n        }\n        int m = l+(h-l)/2, lt = l, gt = h, i = l, v;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) {\n                while (gt > i && a[gt] > v) --gt;\n                std::swap(a[i], a[gt--]);\n            } else ++i;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}", "changes_description": null, "language": "unknown", "parent_id": "30b47aeb-193f-4cc2-a3ac-74555649e5d7", "generation": 1, "timestamp": 1773765129.791886, "iteration_found": 43, "metrics": {"score": 0.979463, "combined_score": 0.979463, "compile_success": 1.0, "correctness": 1, "performance_score": 0.968405, "adaptability_score": 0.99605, "avg_time": 0.00326263}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i)\n          if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n      if (s) return;\n      if (r) { for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n - 1 - i]); return; }\n      quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n          if (arr[j] <= pivot) {\n              std::swap(arr[i], arr[j]);\n              ++i;\n          }\n      }\n      std::swap(arr[i], arr[high]);\n      return i;\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 22) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                  a[j] = v;\n              } return;\n          }\n          int m = l+(h-l)/2, lt = l, gt = h, i = l, v;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          v = a[m];\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) {\n                  while (gt > i && a[gt] > v) --gt;\n                  std::swap(a[i], a[gt--]);\n              } else ++i;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }", "parent_metrics": {"score": 0.1215258, "combined_score": 0.1215258, "compile_success": 1.0, "correctness": 1, "performance_score": 0.068677, "adaptability_score": 0.200799, "avg_time": 1.35609}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.1215\n- Feature coordinates: score=0.12, performance_score=0.07, adaptability_score=0.20\n- Focus areas: - Fitness declined: 0.9781 \u2192 0.1215. Consider revising recent changes.\n- Exploring score=0.12, performance_score=0.07, adaptability_score=0.20 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (arr[i] < arr[i-1]) s = 0;\n          if (arr[i] > arr[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); i++) {\n          if (a[i] < a[i - 1]) s = 0;\n          if (a[i] > a[i - 1]) r = 0;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n          return;\n      }\n      quicksort(a, 0, n - 1);\n  }\nChange 2: Replace:\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 20) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                  arr[j+1] = k;\n              }\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              high = p - 1;\n          }\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int mid = low + (high - low) / 2;\n      if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n      if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n      if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n      std::swap(arr[mid], arr[high]);\n      int v = arr[high], i = low;\n      for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n      std::swap(arr[i], arr[high]);\n  ... (2 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              for (int i = l + 1; i <= h; i++) {\n                  int v = a[i], j = i - 1;\n                  while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                  a[j + 1] = v;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, lt = l, gt = h, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          int v = a[m];\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }\n- Metrics: score: 0.9781, combined_score: 0.9781, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9662, adaptability_score: 0.9960, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  void adaptive_sort(std::vector<int>& arr) {\n      int n = arr.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (arr[i] < arr[i-1]) s = 0;\n          if (arr[i] > arr[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n          return;\n      }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      while (low < high) {\n          if (high - low < 20) {\n              for (int i = low + 1; i <= high; ++i) {\n                  int k = arr[i], j = i - 1;\n                  while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                  arr[j+1] = k;\n              }\n              break;\n          }\n          int p = partition(arr, low, high);\n          if (p - low < high - p) {\n              quicksort(arr, low, p - 1);\n              low = p + 1;\n  ... (18 more lines)\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i - 1;\n                  while (j >= l && a[j] > v) { a[j+1] = a[j]; j--; }\n                  a[j+1] = v;\n              }\n              return;\n          }\n          int m = l + (h - l) / 2, lt = l, gt = h, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          int v = a[m];\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) std::swap(a[i], a[gt--]);\n              else i++;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }\n  \n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size();\n      if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n  ... (10 more lines)\n- Metrics: score: 0.9782, combined_score: 0.9782, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9663, adaptability_score: 0.9960, avg_time: 0.0035\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 20) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                  a[j] = v;\n              }\n              return;\n          }\n          int m = l+(h-l)/2;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          int v = a[m], lt = l, gt = h, i = l;\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) {\n                  while (gt > i && a[gt] > v) --gt;\n                  std::swap(a[i], a[gt--]);\n              } else ++i;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }\nwith:\n  void quicksort(std::vector<int>& a, int l, int h) {\n      while (l < h) {\n          if (h - l < 24) {\n              for (int i = l + 1; i <= h; ++i) {\n                  int v = a[i], j = i;\n                  while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                  a[j] = v;\n              } return;\n          }\n          int m = l+(h-l)/2, lt = l, gt = h, i = l;\n          if (a[m] < a[l]) std::swap(a[l], a[m]);\n          if (a[h] < a[l]) std::swap(a[l], a[h]);\n          if (a[h] < a[m]) std::swap(a[m], a[h]);\n          int v = a[m];\n          while (i <= gt) {\n              if (a[i] < v) std::swap(a[lt++], a[i++]);\n              else if (a[i] > v) {\n                  while (gt > i && a[gt] > v) --gt;\n                  std::swap(a[i], a[gt--]);\n              } else ++i;\n          }\n          if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n          else { quicksort(a, gt + 1, h); h = lt - 1; }\n      }\n  }\nChange 2: Replace:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i) {\n          if (a[i] < a[i-1]) s = 0;\n          else if (a[i] > a[i-1]) r = 0;\n      }\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n/2; ++i) std::swap(a[i], a[n-1-i]);\n          return;\n      }\n      quicksort(a, 0, n - 1);\n  }\nwith:\n  void adaptive_sort(std::vector<int>& a) {\n      int n = a.size(); if (n < 2) return;\n      bool s = 1, r = 1;\n      for (int i = 1; i < n && (s || r); ++i)\n          if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n      if (s) return;\n      if (r) {\n          for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n-1-i]);\n          return;\n      }\n      quicksort(a, 0, n - 1);\n  }\n- Metrics: score: 0.9792, combined_score: 0.9792, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9680, adaptability_score: 0.9960, avg_time: 0.0033\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9793)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                a[j] = v;\n            }\n            return;\n        }\n        int m = l+(h-l)/2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) {\n                while (gt > i && a[gt] > v) --gt;\n                std::swap(a[i], a[gt--]);\n            } else ++i;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i-1]) s = 0;\n        else if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n/2; ++i) std::swap(a[i], a[n-1-i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9793), Performs well on combined_score (0.9793), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9682), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0033)\n\n### Program 2 (Score: 0.9792)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                a[j] = v;\n            } return;\n        }\n        int m = l+(h-l)/2, lt = l, gt = h, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) {\n                while (gt > i && a[gt] > v) --gt;\n                std::swap(a[i], a[gt--]);\n            } else ++i;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i)\n        if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n-1-i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9792), Performs well on combined_score (0.9792), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9680), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0033)\n\n### Program 3 (Score: 0.9782)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) { a[j+1] = a[j]; j--; }\n                a[j+1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, lt = l, gt = h, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (a[i] < a[i-1]) s = 0;\n        if (a[i] > a[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nKey features: Performs well on score (0.9782), Performs well on combined_score (0.9782), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9663), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0035)\n\n### Program 4 (Score: 0.9781)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; i++) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, lt = l, gt = h, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n```\nKey features: Performs well on score (0.9781), Performs well on combined_score (0.9781), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9662), Performs well on adaptability_score (0.9960), Performs well on avg_time (0.0035)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9780)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.1215)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9280)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i-1]) s = 0;\n        if (arr[i] > arr[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    std::swap(arr[mid], arr[high]);\n    int v = arr[high], i = low;\n    for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9792, Type: High-Performer)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 24) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                a[j] = v;\n            } return;\n        }\n        int m = l+(h-l)/2, lt = l, gt = h, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) {\n                while (gt > i && a[gt] > v) --gt;\n                std::swap(a[i], a[gt--]);\n            } else ++i;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i)\n        if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n-1-i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9781, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; i++) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2, lt = l, gt = h, i = l;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9780, Type: High-Performer)\n```unknown\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 20) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i - 1;\n                while (j >= l && a[j] > v) { a[j + 1] = a[j]; j--; }\n                a[j + 1] = v;\n            }\n            return;\n        }\n        int m = l + (h - l) / 2;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        int v = a[m], lt = l, gt = h, i = l;\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) std::swap(a[i], a[gt--]);\n            else i++;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); i++) {\n        if (a[i] < a[i - 1]) s = 0;\n        if (a[i] > a[i - 1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; i++) std::swap(a[i], a[n - 1 - i]);\n        return;\n    }\n    quicksort(a, 0, n - 1);\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9280, Type: High-Performer)\n```unknown\nvoid adaptive_sort(std::vector<int>& arr) {\n    int n = arr.size();\n    if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i) {\n        if (arr[i] < arr[i-1]) s = 0;\n        if (arr[i] > arr[i-1]) r = 0;\n    }\n    if (s) return;\n    if (r) {\n        for (int i = 0; i < n / 2; ++i) std::swap(arr[i], arr[n - 1 - i]);\n        return;\n    }\n    quicksort(arr, 0, n - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    while (low < high) {\n        if (high - low < 20) {\n            for (int i = low + 1; i <= high; ++i) {\n                int k = arr[i], j = i - 1;\n                while (j >= low && arr[j] > k) { arr[j+1] = arr[j]; j--; }\n                arr[j+1] = k;\n            }\n            break;\n        }\n        int p = partition(arr, low, high);\n        if (p - low < high - p) {\n            quicksort(arr, low, p - 1);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p - 1;\n        }\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int mid = low + (high - low) / 2;\n    if (arr[mid] < arr[low]) std::swap(arr[low], arr[mid]);\n    if (arr[high] < arr[low]) std::swap(arr[low], arr[high]);\n    if (arr[high] < arr[mid]) std::swap(arr[mid], arr[high]);\n    std::swap(arr[mid], arr[high]);\n    int v = arr[high], i = low;\n    for (int j = low; j < high; ++j) if (arr[j] <= v) std::swap(arr[i++], arr[j]);\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness dropped significantly (from 0.978 to 0.121) because it reverted to a basic Quicksort with Lomuto partitioning, which is highly inefficient on sorted, reverse-sorted, and redundant datasets. To restore and improve the fitness score, we must re-implement a more robust, adaptive sorting strategy.\n\nThe improvements include:\n1.  **Adaptivity**: An initial scan to detect already sorted or reverse-sorted arrays, allowing $O(n)$ time for these common cases.\n2.  **Robust Partitioning**: Utilizing 3-way partitioning (Dutch National Flag algorithm) to handle duplicate elements efficiently, preventing $O(n^2)$ performance on datasets with many identical values.\n3.  **Pivot Selection**: Median-of-three pivot selection to avoid worst-case scenarios on nearly sorted data.\n4.  **Hybrid Approach**: Falling back to insertion sort for small sub-arrays (threshold set to 22) to take advantage of its low overhead.\n5.  **Recursion Optimization**: Tail-call optimization by always recursing into the smaller partition first, ensuring $O(\\log n)$ stack space usage.\n\n```cpp\n<<<<<<< SEARCH\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n=======\nvoid adaptive_sort(std::vector<int>& a) {\n    int n = a.size(); if (n < 2) return;\n    bool s = 1, r = 1;\n    for (int i = 1; i < n && (s || r); ++i)\n        if (a[i] < a[i-1]) s = 0; else if (a[i] > a[i-1]) r = 0;\n    if (s) return;\n    if (r) { for (int i = 0; i < n / 2; ++i) std::swap(a[i], a[n - 1 - i]); return; }\n    quicksort(a, 0, n - 1);\n}\n>>>>>>> REPLACE\n<<<<<<< SEARCH\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n=======\nvoid quicksort(std::vector<int>& a, int l, int h) {\n    while (l < h) {\n        if (h - l < 22) {\n            for (int i = l + 1; i <= h; ++i) {\n                int v = a[i], j = i;\n                while (j > l && a[j-1] > v) { a[j] = a[j-1]; --j; }\n                a[j] = v;\n            } return;\n        }\n        int m = l+(h-l)/2, lt = l, gt = h, i = l, v;\n        if (a[m] < a[l]) std::swap(a[l], a[m]);\n        if (a[h] < a[l]) std::swap(a[l], a[h]);\n        if (a[h] < a[m]) std::swap(a[m], a[h]);\n        v = a[m];\n        while (i <= gt) {\n            if (a[i] < v) std::swap(a[lt++], a[i++]);\n            else if (a[i] > v) {\n                while (gt > i && a[gt] > v) --gt;\n                std::swap(a[i], a[gt--]);\n            } else ++i;\n        }\n        if (lt - l < h - gt) { quicksort(a, l, lt - 1); l = gt + 1; }\n        else { quicksort(a, gt + 1, h); h = lt - 1; }\n    }\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00101698, 0.0109347, 0.00030837, 0.00353258, 2.494e-05, 0.00024836, 0.0002986, 0.00516421, 0.000960389, 0.0101372], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}