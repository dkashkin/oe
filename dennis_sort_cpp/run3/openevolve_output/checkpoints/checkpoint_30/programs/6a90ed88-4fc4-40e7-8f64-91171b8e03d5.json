{"id": "6a90ed88-4fc4-40e7-8f64-91171b8e03d5", "code": "// 3-way QuickSort (Dutch National Flag) handles duplicates and sorted data efficiently.\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n    int i = low, j = high, k = low, pivot = arr[low + (high - low) / 2];\n    while (k <= j) {\n        if (arr[k] < pivot) std::swap(arr[i++], arr[k++]);\n        else if (arr[k] > pivot) std::swap(arr[j--], arr[k]);\n        else k++;\n    }\n    quicksort(arr, low, i - 1);\n    quicksort(arr, j + 1, high);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}", "changes_description": null, "language": "unknown", "parent_id": "30b47aeb-193f-4cc2-a3ac-74555649e5d7", "generation": 1, "timestamp": 1773763037.969534, "iteration_found": 2, "metrics": {"score": 0.9752015999999999, "combined_score": 0.9752015999999999, "compile_success": 1.0, "correctness": 1, "performance_score": 0.961298, "adaptability_score": 0.996057, "avg_time": 0.00402603}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // Adaptive Sorting Algorithm Implementation\n  // This program implements a sorting algorithm that can be evolved to adapt to different data pat...\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() <= 1) {\n          return;\n      }\n      quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }\n  \n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low < high) {\n          int pivot_index = partition(arr, low, high);\n          if (pivot_index > 0) {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  int partition(std::vector<int>& arr, int low, int high) {\n      int pivot = arr[high];\n      int i = low;\n      for (int j = low; j < high; ++j) {\n  ... (8 more lines)\nwith:\n  // 3-way QuickSort (Dutch National Flag) handles duplicates and sorted data efficiently.\n  void quicksort(std::vector<int>& arr, int low, int high) {\n      if (low >= high) return;\n      int i = low, j = high, k = low, pivot = arr[low + (high - low) / 2];\n      while (k <= j) {\n          if (arr[k] < pivot) std::swap(arr[i++], arr[k++]);\n          else if (arr[k] > pivot) std::swap(arr[j--], arr[k]);\n          else k++;\n      }\n      quicksort(arr, low, i - 1);\n      quicksort(arr, j + 1, high);\n  }\n  \n  void adaptive_sort(std::vector<int>& arr) {\n      if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n  }", "parent_metrics": {"score": 0.1215258, "combined_score": 0.1215258, "compile_success": 1.0, "correctness": 1, "performance_score": 0.068677, "adaptability_score": 0.200799, "avg_time": 1.35609}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.1215\n- Feature coordinates: score=0.12, performance_score=0.07, adaptability_score=0.20\n- Focus areas: - Fitness unchanged at 0.1215\n- Exploring score=0.12, performance_score=0.07, adaptability_score=0.20 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: score: 0.1215, combined_score: 0.1215, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.0687, adaptability_score: 0.2008, avg_time: 1.3561\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.1215)\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\nKey features: Performs well on score (0.1215), Performs well on combined_score (0.1215), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.0687), Performs well on adaptability_score (0.2008), Performs well on avg_time (1.3561)\n\n\n\n# Current Program\n```unknown\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the sorting algorithm's fitness, I have replaced the basic Lomuto quicksort with a more robust 3-way partitioning (Dutch National Flag) algorithm and changed the pivot selection to the middle element.\n\n### Key Improvements:\n1.  **3-Way Partitioning**: The original implementation was inefficient for arrays with many duplicate elements, potentially leading to $O(n^2)$ complexity. 3-way partitioning handles duplicates in linear time by grouping all elements equal to the pivot together.\n2.  **Improved Pivot Selection**: Using the middle element `arr[low + (high - low) / 2]` instead of the last element `arr[high]` significantly improves performance and adaptability for already sorted or reverse-sorted data, avoiding the $O(n^2)$ worst-case.\n3.  **Code Simplification**: I merged the partition logic into the `quicksort` function and removed redundant comments to reduce the program's length to under 500 characters, addressing the system's hint to simplify the implementation.\n4.  **Adaptability**: By handling duplicates and sorted ranges better, the algorithm becomes more robust across diverse data distributions.\n\n```unknown\n<<<<<<< SEARCH\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() <= 1) {\n        return;\n    }\n    quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pivot_index = partition(arr, low, high);\n        if (pivot_index > 0) {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nint partition(std::vector<int>& arr, int low, int high) {\n    int pivot = arr[high];\n    int i = low;\n    for (int j = low; j < high; ++j) {\n        if (arr[j] <= pivot) {\n            std::swap(arr[i], arr[j]);\n            ++i;\n        }\n    }\n    std::swap(arr[i], arr[high]);\n    return i;\n}\n=======\n// 3-way QuickSort (Dutch National Flag) handles duplicates and sorted data efficiently.\nvoid quicksort(std::vector<int>& arr, int low, int high) {\n    if (low >= high) return;\n    int i = low, j = high, k = low, pivot = arr[low + (high - low) / 2];\n    while (k <= j) {\n        if (arr[k] < pivot) std::swap(arr[i++], arr[k++]);\n        else if (arr[k] > pivot) std::swap(arr[j--], arr[k]);\n        else k++;\n    }\n    quicksort(arr, low, i - 1);\n    quicksort(arr, j + 1, high);\n}\n\nvoid adaptive_sort(std::vector<int>& arr) {\n    if (arr.size() > 1) quicksort(arr, 0, static_cast<int>(arr.size()) - 1);\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00113315, 0.010705, 0.00057551, 0.00646753, 0.00032125, 0.00360678, 0.00028632, 0.00513927, 0.00107183, 0.0109537], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}