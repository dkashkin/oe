{"id": "5884ca12-d8b0-4fba-b3d8-1e0840c4a356", "code": "\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a force-directed physics simulation with a cooling\nschedule and a robust randomized greedy expansion to maximize the sum of radii.\n\"\"\"\n\nimport numpy as np\n\n\ndef compute_max_radii(centers, trials=250):\n    \"\"\"\n    Maximize the sum of radii for fixed center positions by solving the\n    overlap and boundary constraints using multiple randomized greedy trials.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates.\n        trials: Number of randomized greedy expansion trials.\n\n    Returns:\n        np.array of shape (n) with the radius of each circle.\n    \"\"\"\n    n = centers.shape[0]\n    # Precompute pairwise distances and boundary limits.\n    # diffs shape: (n, n, 2)\n    diffs = centers[:, np.newaxis, :] - centers[None, :, :]\n    # dists shape: (n, n)\n    dists = np.sqrt(np.sum(diffs**2, axis=2))\n    # Set diagonal to a large value to prevent circles from constraining themselves.\n    np.fill_diagonal(dists, 1e9)\n    # Boundary limits shape: (n,)\n    b_limits = np.min(np.concatenate([centers, 1.0 - centers], axis=1), axis=1)\n\n    best_sum = 0\n    best_radii = np.zeros(n)\n\n    for _ in range(trials):\n        radii = np.zeros(n)\n        order = np.random.permutation(n)\n        \n        # Initial greedy pass: grow each circle as much as possible in chosen order.\n        for i in order:\n            # r_i must be <= dist to border and <= (dist_ij - r_j) for all j.\n            r_limit = min(b_limits[i], np.min(dists[i] - radii))\n            radii[i] = max(0.0, r_limit)\n        \n        # Refinement passes: allow circles placed earlier to reclaim space.\n        for _ in range(2):\n            for i in reversed(order):\n                radii[i] = 0.0\n                r_limit = min(b_limits[i], np.min(dists[i] - radii))\n                radii[i] = max(0.0, r_limit)\n\n        current_sum = np.sum(radii)\n        if current_sum > best_sum:\n            best_sum = current_sum\n            best_radii = radii.copy()\n\n    return best_radii\n\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Utilizes a vectorized force-directed simulation to optimize center positions,\n    followed by an iterative greedy expansion to maximize the sum of radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    np.random.seed(42)\n\n    # Initial layout: 5x5 grid plus one extra circle in the center.\n    # This provides a dense starting configuration near the global optimum.\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.1 + 0.2 * i, 0.1 + 0.2 * j]\n    \n    centers[25] = [0.5, 0.5]\n    \n    # Break symmetry with jitter to allow the simulation to find better packings.\n    centers += np.random.normal(0, 0.015, (n, 2))\n    centers = np.clip(centers, 0.0, 1.0)\n\n    # Force-directed simulation parameters.\n    # target_dist corresponds to an average diameter for a high sum-of-radii.\n    iters = 1800\n    target_dist = 0.205\n    target_radius = target_dist / 2.0\n    initial_dt = 0.05\n\n    # Physics simulation loop\n    for k in range(iters):\n        # 1. Vectorized pairwise repulsion forces.\n        # diff: (n, n, 2), dists: (n, n)\n        diff = centers[:, np.newaxis, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff**2, axis=2)) + 1e-9\n        \n        # Identify circles that are overlapping or closer than target distance.\n        mask = (dists < target_dist) & (~np.eye(n, dtype=bool))\n        \n        # Linear repulsion force magnitude.\n        f_mag = np.zeros_like(dists)\n        f_mag[mask] = (target_dist - dists[mask]) / dists[mask]\n        \n        # Resulting forces: (n, 2)\n        forces = np.sum(f_mag[:, :, np.newaxis] * diff, axis=1)\n        \n        # 2. Boundary repulsion forces (keeping centers within safe radius).\n        b_forces = np.zeros((n, 2))\n        low_x = centers[:, 0] < target_radius\n        b_forces[low_x, 0] += (target_radius - centers[low_x, 0])\n        high_x = centers[:, 0] > 1.0 - target_radius\n        b_forces[high_x, 0] -= (centers[high_x, 0] - (1.0 - target_radius))\n        \n        low_y = centers[:, 1] < target_radius\n        b_forces[low_y, 1] += (target_radius - centers[low_y, 1])\n        high_y = centers[:, 1] > 1.0 - target_radius\n        b_forces[high_y, 1] -= (centers[high_y, 1] - (1.0 - target_radius))\n        \n        # 3. Apply cooling schedule to step size (dt) for convergence.\n        dt = initial_dt * (1.0 - 0.9 * k / iters)\n        centers += (forces + b_forces) * dt\n        \n        # 4. Stochastic noise (annealing) to escape local minima.\n        if k < iters * 0.5:\n            noise_mag = 0.0015 * (1.0 - k / (iters * 0.5))\n            centers += np.random.normal(0, noise_mag, (n, 2))\n            \n        # Keep centers within the unit square.\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Calculate optimal radii for final center positions.\n    radii = compute_max_radii(centers, trials=300)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef run_packing():\n    \"\"\"\n    Main entry point to execute the packing algorithm and return results.\n    \"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")", "changes_description": null, "language": "python", "parent_id": "5fc12833-4f32-4d8f-bdfe-3a64d9ce8a4e", "generation": 2, "timestamp": 1771869461.1381304, "iteration_found": 27, "metrics": {"validity": 1.0, "sum_radii": 2.4466160256847154, "target_ratio": 0.9285070306203854, "combined_score": 0.9285070306203854, "radius_variance": 0.0005937235893543555, "spatial_spread": 0.18991002477762292, "eval_time": 0.4141831398010254}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.4556500167967377, "target_ratio": 0.9319354902454413, "combined_score": 0.9319354902454413, "radius_variance": 0.00035241760346652073, "spatial_spread": 0.19480923773191144, "eval_time": 0.5164246559143066}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. \nYour task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. \nTry to improve the results reported in the AlphaEvolve paper which achieved a sum of 2.635 for n=26.\n\nKey insights:\n- The optimization routine is critically important - simple physics-based models with carefully tuned parameters\n- Consider strategic placement of circles at square corners and edges\n- Adjusting the pattern to place larger circles at the center and smaller at the edges\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nMake sure that your codecod is syntactically perfect, strictly adheres to PEP 8 indentation, and contains no ellipses, comments-as-placeholders, or unclosed structures.\n", "user": "# Current Program Information\n- Fitness: 0.9319\n- Feature coordinates: \n- Focus areas: - Fitness improved: 0.8312 \u2192 0.9319\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### execution_time\n```\n0.52s\n```\n\n### stage\n```\nquick_validation\n```\n\n### packing_summary\n```\nSum of radii: 2.455650/2.635 = 0.9319\n```\n\n### validation_report\n```\nValid: True, Violations: 0 boundary, 0 overlaps\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1901, target_ratio: 0.8312, combined_score: 0.8312, radius_variance: 0.0217, spatial_spread: 0.1892, eval_time: 3.9997\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4195, target_ratio: 0.9182, combined_score: 0.9182, radius_variance: 0.0012, spatial_spread: 0.1906, eval_time: 0.5332\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.4557, target_ratio: 0.9319, combined_score: 0.9319, radius_variance: 0.0004, spatial_spread: 0.1948, eval_time: 0.5164\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9319)\n```python\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii using a force-directed\n    simulation and a greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Seed for reproducibility\n    np.random.seed(42)\n\n    # Initial layout: 5x5 grid plus one extra circle\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.2 * i + 0.1, 0.2 * j + 0.1]\n    \n    # Place the 26th circle in the center and add jitter to break symmetry\n    centers[25] = [0.5, 0.5]\n    centers += np.random.uniform(-0.02, 0.02, (n, 2))\n\n    # Force-directed simulation parameters\n    # A target distance of ~0.203 aims for a sum of radii around 2.639\n    iters = 1200\n    dt = 0.05\n    target_dist = 0.203\n    target_radius = target_dist / 2.0\n\n    # Physics simulation loop\n    for _ in range(iters):\n        forces = np.zeros((n, 2))\n        # Pairwise repulsion between circles\n        for i in range(n):\n            for j in range(i + 1, n):\n                dx = centers[i, 0] - centers[j, 0]\n                dy = centers[i, 1] - centers[j, 1]\n                dist = (dx * dx + dy * dy)**0.5 + 1e-9\n                if dist < target_dist:\n                    f = (target_dist - dist) / dist\n                    forces[i, 0] += dx * f\n                    forces[i, 1] += dy * f\n                    forces[j, 0] -= dx * f\n                    forces[j, 1] -= dy * f\n        \n        # Repulsion from square boundaries\n        for i in range(n):\n            # Distance to left wall\n            if centers[i, 0] < target_radius:\n                forces[i, 0] += (target_radius - centers[i, 0])\n            # Distance to right wall\n            if centers[i, 0] > 1.0 - target_radius:\n                forces[i, 0] -= (centers[i, 0] - (1.0 - target_radius))\n            # Distance to bottom wall\n            if centers[i, 1] < target_radius:\n                forces[i, 1] += (target_radius - centers[i, 1])\n            # Distance to top wall\n            if centers[i, 1] > 1.0 - target_radius:\n                forces[i, 1] -= (centers[i, 1] - (1.0 - target_radius))\n        \n        # Update centers and constrain within unit square\n        centers += forces * dt\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Compute final maximized radii for these center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximize the sum of radii for fixed centers by solving the\n    overlap and boundary constraints iteratively.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n\n    # 1. Initialize radii to the distance to the closest boundary\n    for i in range(n):\n        radii[i] = min(centers[i, 0], centers[i, 1], \n                       1.0 - centers[i, 0], 1.0 - centers[i, 1])\n\n    # 2. Proportionally scale down radii to eliminate overlaps\n    for i in range(n):\n        for j in range(i + 1, n):\n            dx = centers[i, 0] - centers[j, 0]\n            dy = centers[i, 1] - centers[j, 1]\n            dist = (dx * dx + dy * dy)**0.5\n            if radii[i] + radii[j] > dist:\n                scale = dist / (radii[i] + radii[j] + 1e-12)\n                radii[i] *= scale\n                radii[j] *= scale\n\n    # 3. Iteratively expand circles greedily to maximize the sum of radii\n    # We alternate the order to ensure fair space distribution\n    for it in range(60):\n        order = range(n) if it % 2 == 0 else range(n - 1, -1, -1)\n        for i in order:\n            # Maximum radius is limited by distance to boundaries\n            r_limit = min(centers[i, 0], centers[i, 1], \n                          1.0 - centers[i, 0], 1.0 - centers[i, 1])\n            # And distance to every other already-sized circle\n            for j in range(n):\n                if i != j:\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    dist = (dx * dx + dy * dy)**0.5\n                    r_limit = min(r_limit, dist - radii[j])\n            radii[i] = max(0.0, r_limit)\n\n    return radii\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4557), Performs well on target_ratio (0.9319), Performs well on combined_score (0.9319), Performs well on radius_variance (0.0004), Performs well on spatial_spread (0.1948), Performs well on eval_time (0.5164)\n\n### Program 2 (Score: 0.9182)\n```python\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii using a force-directed\n    simulation and a greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Seed for reproducibility\n    np.random.seed(42)\n\n    # Initial layout: Start with a 5x5 grid and an extra circle in the center.\n    # The simulation will distribute them to accommodate the 26th circle.\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.2 * i + 0.1, 0.2 * j + 0.1]\n    \n    # Place the 26th circle in the center and add jitter to break symmetry\n    centers[25] = [0.5, 0.5]\n    centers += np.random.uniform(-0.02, 0.02, (n, 2))\n\n    # Force-directed simulation parameters\n    # A target distance of ~0.208 aims for a sum of radii around 2.7\n    # which provides enough compression to maximize space utilization.\n    iters = 1500\n    target_dist = 0.208\n    target_dist_sq = target_dist**2\n    r_target = target_dist / 2.0\n\n    # Physics simulation loop\n    for k in range(iters):\n        forces = np.zeros((n, 2))\n        \n        # Pairwise repulsion between circles\n        for i in range(n):\n            xi, yi = centers[i]\n            for j in range(i + 1, n):\n                dx = xi - centers[j, 0]\n                dy = yi - centers[j, 1]\n                dist_sq = dx * dx + dy * dy\n                if dist_sq < target_dist_sq:\n                    dist = dist_sq**0.5 + 1e-9\n                    f = (target_dist - dist) / dist\n                    fx, fy = dx * f, dy * f\n                    forces[i, 0] += fx\n                    forces[i, 1] += fy\n                    forces[j, 0] -= fx\n                    forces[j, 1] -= fy\n        \n        # Repulsion from square boundaries\n        for i in range(n):\n            if centers[i, 0] < r_target:\n                forces[i, 0] += (r_target - centers[i, 0])\n            if centers[i, 0] > 1.0 - r_target:\n                forces[i, 0] -= (centers[i, 0] - (1.0 - r_target))\n            if centers[i, 1] < r_target:\n                forces[i, 1] += (r_target - centers[i, 1])\n            if centers[i, 1] > 1.0 - r_target:\n                forces[i, 1] -= (centers[i, 1] - (1.0 - r_target))\n        \n        # Update centers with cooling step size\n        dt = 0.05 * (1.0 - 0.9 * k / iters)\n        centers += forces * dt\n        \n        # Add small jitter that cools down over time to help escape local minima\n        if k < iters * 0.7:\n            jitter_mag = 0.0015 * (1.0 - k / (iters * 0.7))\n            centers += np.random.normal(0, jitter_mag, (n, 2))\n            \n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Compute final maximized radii for these center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximize the sum of radii for fixed centers by solving the\n    overlap and boundary constraints iteratively.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n\n    # Iteratively expand circles greedily to maximize the sum of radii.\n    # We alternate the order to ensure space is distributed fairly.\n    # 80 iterations ensure convergence of the radii.\n    for it in range(80):\n        order = range(n) if it % 2 == 0 else range(n - 1, -1, -1)\n        for i in order:\n            xi, yi = centers[i]\n            # Maximum radius is limited by distance to boundaries\n            r_limit = min(xi, yi, 1.0 - xi, 1.0 - yi)\n            # And distance to every other circle's current radius\n            for j in range(n):\n                if i != j:\n                    dx = xi - centers[j, 0]\n                    dy = yi - centers[j, 1]\n                    dist = (dx * dx + dy * dy)**0.5\n                    r_limit = min(r_limit, dist - radii[j])\n            radii[i] = max(0.0, r_limit)\n\n    return radii\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.4195), Performs well on target_ratio (0.9182), Performs well on combined_score (0.9182), Performs well on radius_variance (0.0012), Performs well on spatial_spread (0.1906), Performs well on eval_time (0.5332)\n\n### Program 3 (Score: 0.8312)\n```python\nimport numpy as np\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Computes the maximum possible radii for each circle position such that\n    no circles overlap and all stay within the unit square. Uses a greedy\n    iterative approach to maximize the sum of radii.\n    \"\"\"\n    n = centers.shape[0]\n    # Maximum radius for each circle restricted only by the square boundaries\n    boundary_limits = np.array([min(c[0], c[1], 1.0 - c[0], 1.0 - c[1]) for c in centers])\n    \n    best_radii = np.zeros(n)\n    best_sum = 0\n    \n    # Run multiple trials with different random greedy expansion orders\n    for trial in range(15):\n        radii = np.zeros(n)\n        order = np.random.permutation(n)\n        \n        # Iteratively maximize each radius given the others\n        for _ in range(25):\n            for i in order:\n                # Radius must satisfy r_i <= boundary_limit and r_i + r_j <= dist_ij\n                limit = boundary_limits[i]\n                for j in range(n):\n                    if i != j:\n                        dist = np.linalg.norm(centers[i] - centers[j])\n                        # Candidate radius given fixed neighbor j\n                        limit = min(limit, dist - radii[j])\n                radii[i] = max(0.0, limit)\n        \n        current_sum = np.sum(radii)\n        if current_sum > best_sum:\n            best_sum = current_sum\n            best_radii = radii.copy()\n            \n    return best_radii\n\n\ndef construct_packing():\n    \"\"\"\n    Constructs a packing of 26 circles in a unit square to maximize \n    the sum of radii using a force-directed physics simulation.\n    \"\"\"\n    n = 26\n    np.random.seed(42)\n    \n    # Initialize with a structured pattern: 4 rows of 5 and 1 middle row of 6\n    centers_list = []\n    # Top and bottom rows (y=0.1, 0.3, 0.7, 0.9)\n    for y in [0.1, 0.3, 0.7, 0.9]:\n        for x in [0.1, 0.3, 0.5, 0.7, 0.9]:\n            centers_list.append([x, y])\n    # Middle row (y=0.5) with 6 circles\n    for x in np.linspace(0.08, 0.92, 6):\n        centers_list.append([x, 0.5])\n        \n    centers = np.array(centers_list)\n    \n    # Add small jitter to break symmetry\n    centers += np.random.normal(0, 0.005, (n, 2))\n    \n    # Physics parameters: target separation distance and time step\n    dt = 0.1\n    target_dist = 0.203\n    \n    # Simulation loop to optimize center positions\n    for _ in range(3000):\n        forces = np.zeros((n, 2))\n        for i in range(n):\n            # Inter-circle repulsion\n            for j in range(i + 1, n):\n                d_vec = centers[i] - centers[j]\n                d = np.linalg.norm(d_vec)\n                if d < target_dist:\n                    # Prevent division by zero\n                    if d < 1e-9:\n                        d_vec = np.array([0.001, 0.0])\n                        d = 0.001\n                    # Magnitude of repulsion proportional to overlap\n                    f_mag = (target_dist - d) / d\n                    forces[i] += d_vec * f_mag\n                    forces[j] -= d_vec * f_mag\n            \n            # Boundary repulsion: pushes centers away from square edges\n            r_est = target_dist / 2.0\n            if centers[i, 0] < r_est:\n                forces[i, 0] += (r_est - centers[i, 0])\n            elif centers[i, 0] > 1.0 - r_est:\n                forces[i, 0] -= (centers[i, 0] - (1.0 - r_est))\n                \n            if centers[i, 1] < r_est:\n                forces[i, 1] += (r_est - centers[i, 1])\n            elif centers[i, 1] > 1.0 - r_est:\n                forces[i, 1] -= (centers[i, 1] - (1.0 - r_est))\n        \n        # Apply force updates and constrain centers to the unit square\n        centers += forces * dt\n        centers = np.clip(centers, 0.0, 1.0)\n        \n        # Annealing the step size\n        dt *= 0.999\n        \n    # Calculate optimal radii for final center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n    \n    return centers, radii, sum_radii\n\n\ndef run_packing():\n    \"\"\"\n    Main entry point for evaluating the packing of n=26 circles.\n    Returns (centers, radii, sum_radii).\n    \"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1901), Performs well on target_ratio (0.8312), Performs well on combined_score (0.8312), Performs well on radius_variance (0.0217), Performs well on spatial_spread (0.1892), Performs well on eval_time (3.9997)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.8113)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii using a \n    force-directed simulation and greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Initial layout: 5x5 grid plus one circle in the middle\n    centers = np.zeros((n, 2))\n    for i in range(25):\n        centers[i] = [0.1 + (i % 5) * 0.2, 0.1 + (i // 5) * 0.2]\n    centers[25] = [0.5, 0.5]\n\n    # Add small jitter to break symmetry for better packing exploration\n    np.random.seed(42)\n    centers += np.random.normal(0, 0.01, (n, 2))\n\n    # Parameters for the physics-based repulsion simulation\n    # Target distance corresponds to an average radius of ~0.1015\n    iters = 500\n    dt = 0.08\n    target_dist = 0.203\n\n    # Simulation loop: push overlapping circles apart\n    for _ in range(iters):\n        forces = np.zeros((n, 2))\n        for i in range(n):\n            for j in range(i + 1, n):\n                d = centers[i] - centers[j]\n                dist = np.sqrt(d[0]**2 + d[1]**2)\n                if dist < target_dist:\n                    # Prevent division by zero with small epsilon\n                    if dist < 1e-8:\n                        d = np.array([0.01, 0.0])\n                        dist = 0.01\n                    # Repulsion force proportional to overlap\n                    f = (target_dist - dist) / dist\n                    forces[i] += d * f\n                    forces[j] -= d * f\n        \n        centers += forces * dt\n        # Constrain centers to the unit square\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Compute final optimized radii for these center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    using proportional scaling followed by iterative greedy refinement.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n\n    # 1. Initialize radii based on distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # 2. Proportionally scale radii to remove any circle-circle overlaps\n    for i in range(n):\n        for j in range(i + 1, n):\n            dx = centers[i, 0] - centers[j, 0]\n            dy = centers[i, 1] - centers[j, 1]\n            dist = np.sqrt(dx*dx + dy*dy)\n            if radii[i] + radii[j] > dist:\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    # 3. Greedy refinement to maximize the sum of radii\n    # In each step, maximize r_i given all other fixed radii\n    for _ in range(20):\n        for i in range(n):\n            # Distance to boundaries\n            m = min(centers[i, 0], centers[i, 1], 1 - centers[i, 0], 1 - centers[i, 1])\n            # Distance to all other circles\n            for j in range(n):\n                if i != j:\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    dist = np.sqrt(dx*dx + dy*dy)\n                    if dist - radii[j] < m:\n                        m = dist - radii[j]\n            radii[i] = max(0.0, m)\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```python\n```python\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a force-directed physics simulation with annealing\nand a randomized greedy optimization to maximize the sum of radii.\n\"\"\"\n\nimport numpy as np\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximizes the sum of radii for fixed center positions by using multiple\n    trials of randomized greedy expansion.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates.\n\n    Returns:\n        np.array of shape (n) with the radius of each circle.\n    \"\"\"\n    n = centers.shape[0]\n    # Precompute pairwise distances and distance-to-boundary limits\n    d_mat = np.sqrt(np.sum((centers[:, None, :] - centers[None, :, :])**2, axis=-1))\n    b_lim = np.array([min(c[0], c[1], 1.0 - c[0], 1.0 - c[1]) for c in centers])\n\n    best_radii = np.zeros(n)\n    best_sum = 0\n\n    # Try different expansion orders to explore the feasible space of radii\n    for _ in range(500):\n        order = np.random.permutation(n)\n        radii = np.zeros(n)\n        # Greedily expand each circle to its maximum available space\n        for i in order:\n            r = b_lim[i]\n            # Must satisfy r_i + r_j <= dist_ij for all circles\n            for j in range(n):\n                if i != j:\n                    r = min(r, d_mat[i, j] - radii[j])\n            radii[i] = max(0.0, r)\n\n        current_sum = np.sum(radii)\n        if current_sum > best_sum:\n            best_sum = current_sum\n            best_radii = radii.copy()\n\n    return best_radii\n\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square using a\n    vectorized force-directed simulation to maximize the sum of radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    # Seed for reproducibility\n    np.random.seed(42)\n\n    # Initial layout: 5x5 grid plus one circle in the center\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.1 + i * 0.2, 0.1 + j * 0.2]\n    centers[25] = [0.5, 0.5]\n\n    # Jitter to break initial grid symmetry\n    centers += np.random.normal(0, 0.01, (n, 2))\n    centers = np.clip(centers, 0.0, 1.0)\n\n    # Simulation parameters\n    dt = 0.05\n    iters = 3000\n    target_dist = 0.204\n    r_proxy = target_dist / 2.0\n\n    # Force-directed simulation loop\n    for step in range(iters):\n        # Calculate pairwise displacement vectors and distances\n        diff = centers[:, None, :] - centers[None, :, :]\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9182, Type: High-Performer)\n```python\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii using a force-directed\n    simulation and a greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Seed for reproducibility\n    np.random.seed(42)\n\n    # Initial layout: Start with a 5x5 grid and an extra circle in the center.\n    # The simulation will distribute them to accommodate the 26th circle.\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.2 * i + 0.1, 0.2 * j + 0.1]\n    \n    # Place the 26th circle in the center and add jitter to break symmetry\n    centers[25] = [0.5, 0.5]\n    centers += np.random.uniform(-0.02, 0.02, (n, 2))\n\n    # Force-directed simulation parameters\n    # A target distance of ~0.208 aims for a sum of radii around 2.7\n    # which provides enough compression to maximize space utilization.\n    iters = 1500\n    target_dist = 0.208\n    target_dist_sq = target_dist**2\n    r_target = target_dist / 2.0\n\n    # Physics simulation loop\n    for k in range(iters):\n        forces = np.zeros((n, 2))\n        \n        # Pairwise repulsion between circles\n        for i in range(n):\n            xi, yi = centers[i]\n            for j in range(i + 1, n):\n                dx = xi - centers[j, 0]\n                dy = yi - centers[j, 1]\n                dist_sq = dx * dx + dy * dy\n                if dist_sq < target_dist_sq:\n                    dist = dist_sq**0.5 + 1e-9\n                    f = (target_dist - dist) / dist\n                    fx, fy = dx * f, dy * f\n                    forces[i, 0] += fx\n                    forces[i, 1] += fy\n                    forces[j, 0] -= fx\n                    forces[j, 1] -= fy\n        \n        # Repulsion from square boundaries\n        for i in range(n):\n            if centers[i, 0] < r_target:\n                forces[i, 0] += (r_target - centers[i, 0])\n            if centers[i, 0] > 1.0 - r_target:\n                forces[i, 0] -= (centers[i, 0] - (1.0 - r_target))\n            if centers[i, 1] < r_target:\n                forces[i, 1] += (r_target - centers[i, 1])\n            if centers[i, 1] > 1.0 - r_target:\n                forces[i, 1] -= (centers[i, 1] - (1.0 - r_target))\n        \n        # Update centers with cooling step size\n        dt = 0.05 * (1.0 - 0.9 * k / iters)\n        centers += forces * dt\n        \n        # Add small jitter that cools down over time to help escape local minima\n        if k < iters * 0.7:\n            jitter_mag = 0.0015 * (1.0 - k / (iters * 0.7))\n            centers += np.random.normal(0, jitter_mag, (n, 2))\n            \n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Compute final maximized radii for these center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximize the sum of radii for fixed centers by solving the\n    overlap and boundary constraints iteratively.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n\n    # Iteratively expand circles greedily to maximize the sum of radii.\n    # We alternate the order to ensure space is distributed fairly.\n    # 80 iterations ensure convergence of the radii.\n    for it in range(80):\n        order = range(n) if it % 2 == 0 else range(n - 1, -1, -1)\n        for i in order:\n            xi, yi = centers[i]\n            # Maximum radius is limited by distance to boundaries\n            r_limit = min(xi, yi, 1.0 - xi, 1.0 - yi)\n            # And distance to every other circle's current radius\n            for j in range(n):\n                if i != j:\n                    dx = xi - centers[j, 0]\n                    dy = yi - centers[j, 1]\n                    dist = (dx * dx + dy * dy)**0.5\n                    r_limit = min(r_limit, dist - radii[j])\n            radii[i] = max(0.0, r_limit)\n\n    return radii\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```python\ndef compute_max_radii(centers):\n        n = centers.shape[0]\n        radii = np.zeros(n)\n        for i in range(n):\n            x, y = centers[i]\n            radii[i] = min(x, y, 1 - x, 1 - y)\n        for _ in range(100):\n            for i in range(n):\n                for j in range(i + 1, n):\n                    d = np.linalg.norm(centers[i] - centers[j])\n                    if radii[i] + radii[j] > d:\n                        scale = d / (radii[i] + radii[j])\n                        radii[i] *= scale\n                        radii[j] *= scale\n        return radii\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Constructor-based circle packing for n=26 circles in a unit square\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii using a force-directed\n    simulation and a greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Seed for reproducibility\n    np.random.seed(42)\n\n    # Initial layout: 5x5 grid plus one extra circle\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.2 * i + 0.1, 0.2 * j + 0.1]\n    \n    # Place the 26th circle in the center and add jitter to break symmetry\n    centers[25] = [0.5, 0.5]\n    centers += np.random.uniform(-0.02, 0.02, (n, 2))\n\n    # Force-directed simulation parameters\n    # A target distance of ~0.203 aims for a sum of radii around 2.639\n    iters = 1200\n    dt = 0.05\n    target_dist = 0.203\n    target_radius = target_dist / 2.0\n\n    # Physics simulation loop\n    for _ in range(iters):\n        forces = np.zeros((n, 2))\n        # Pairwise repulsion between circles\n        for i in range(n):\n            for j in range(i + 1, n):\n                dx = centers[i, 0] - centers[j, 0]\n                dy = centers[i, 1] - centers[j, 1]\n                dist = (dx * dx + dy * dy)**0.5 + 1e-9\n                if dist < target_dist:\n                    f = (target_dist - dist) / dist\n                    forces[i, 0] += dx * f\n                    forces[i, 1] += dy * f\n                    forces[j, 0] -= dx * f\n                    forces[j, 1] -= dy * f\n        \n        # Repulsion from square boundaries\n        for i in range(n):\n            # Distance to left wall\n            if centers[i, 0] < target_radius:\n                forces[i, 0] += (target_radius - centers[i, 0])\n            # Distance to right wall\n            if centers[i, 0] > 1.0 - target_radius:\n                forces[i, 0] -= (centers[i, 0] - (1.0 - target_radius))\n            # Distance to bottom wall\n            if centers[i, 1] < target_radius:\n                forces[i, 1] += (target_radius - centers[i, 1])\n            # Distance to top wall\n            if centers[i, 1] > 1.0 - target_radius:\n                forces[i, 1] -= (centers[i, 1] - (1.0 - target_radius))\n        \n        # Update centers and constrain within unit square\n        centers += forces * dt\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Compute final maximized radii for these center positions\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximize the sum of radii for fixed centers by solving the\n    overlap and boundary constraints iteratively.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.zeros(n)\n\n    # 1. Initialize radii to the distance to the closest boundary\n    for i in range(n):\n        radii[i] = min(centers[i, 0], centers[i, 1], \n                       1.0 - centers[i, 0], 1.0 - centers[i, 1])\n\n    # 2. Proportionally scale down radii to eliminate overlaps\n    for i in range(n):\n        for j in range(i + 1, n):\n            dx = centers[i, 0] - centers[j, 0]\n            dy = centers[i, 1] - centers[j, 1]\n            dist = (dx * dx + dy * dy)**0.5\n            if radii[i] + radii[j] > dist:\n                scale = dist / (radii[i] + radii[j] + 1e-12)\n                radii[i] *= scale\n                radii[j] *= scale\n\n    # 3. Iteratively expand circles greedily to maximize the sum of radii\n    # We alternate the order to ensure fair space distribution\n    for it in range(60):\n        order = range(n) if it % 2 == 0 else range(n - 1, -1, -1)\n        for i in order:\n            # Maximum radius is limited by distance to boundaries\n            r_limit = min(centers[i, 0], centers[i, 1], \n                          1.0 - centers[i, 0], 1.0 - centers[i, 1])\n            # And distance to every other already-sized circle\n            for j in range(n):\n                if i != j:\n                    dx = centers[i, 0] - centers[j, 0]\n                    dy = centers[i, 1] - centers[j, 1]\n                    dist = (dx * dx + dy * dy)**0.5\n                    r_limit = min(r_limit, dist - radii[j])\n            radii[i] = max(0.0, r_limit)\n\n    return radii\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a force-directed physics simulation with a cooling\nschedule and a robust randomized greedy expansion to maximize the sum of radii.\n\"\"\"\n\nimport numpy as np\n\n\ndef compute_max_radii(centers, trials=250):\n    \"\"\"\n    Maximize the sum of radii for fixed center positions by solving the\n    overlap and boundary constraints using multiple randomized greedy trials.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates.\n        trials: Number of randomized greedy expansion trials.\n\n    Returns:\n        np.array of shape (n) with the radius of each circle.\n    \"\"\"\n    n = centers.shape[0]\n    # Precompute pairwise distances and boundary limits.\n    # diffs shape: (n, n, 2)\n    diffs = centers[:, np.newaxis, :] - centers[None, :, :]\n    # dists shape: (n, n)\n    dists = np.sqrt(np.sum(diffs**2, axis=2))\n    # Set diagonal to a large value to prevent circles from constraining themselves.\n    np.fill_diagonal(dists, 1e9)\n    # Boundary limits shape: (n,)\n    b_limits = np.min(np.concatenate([centers, 1.0 - centers], axis=1), axis=1)\n\n    best_sum = 0\n    best_radii = np.zeros(n)\n\n    for _ in range(trials):\n        radii = np.zeros(n)\n        order = np.random.permutation(n)\n        \n        # Initial greedy pass: grow each circle as much as possible in chosen order.\n        for i in order:\n            # r_i must be <= dist to border and <= (dist_ij - r_j) for all j.\n            r_limit = min(b_limits[i], np.min(dists[i] - radii))\n            radii[i] = max(0.0, r_limit)\n        \n        # Refinement passes: allow circles placed earlier to reclaim space.\n        for _ in range(2):\n            for i in reversed(order):\n                radii[i] = 0.0\n                r_limit = min(b_limits[i], np.min(dists[i] - radii))\n                radii[i] = max(0.0, r_limit)\n\n        current_sum = np.sum(radii)\n        if current_sum > best_sum:\n            best_sum = current_sum\n            best_radii = radii.copy()\n\n    return best_radii\n\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Utilizes a vectorized force-directed simulation to optimize center positions,\n    followed by an iterative greedy expansion to maximize the sum of radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n    \"\"\"\n    n = 26\n    np.random.seed(42)\n\n    # Initial layout: 5x5 grid plus one extra circle in the center.\n    # This provides a dense starting configuration near the global optimum.\n    centers = np.zeros((n, 2))\n    for i in range(5):\n        for j in range(5):\n            centers[i * 5 + j] = [0.1 + 0.2 * i, 0.1 + 0.2 * j]\n    \n    centers[25] = [0.5, 0.5]\n    \n    # Break symmetry with jitter to allow the simulation to find better packings.\n    centers += np.random.normal(0, 0.015, (n, 2))\n    centers = np.clip(centers, 0.0, 1.0)\n\n    # Force-directed simulation parameters.\n    # target_dist corresponds to an average diameter for a high sum-of-radii.\n    iters = 1800\n    target_dist = 0.205\n    target_radius = target_dist / 2.0\n    initial_dt = 0.05\n\n    # Physics simulation loop\n    for k in range(iters):\n        # 1. Vectorized pairwise repulsion forces.\n        # diff: (n, n, 2), dists: (n, n)\n        diff = centers[:, np.newaxis, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diff**2, axis=2)) + 1e-9\n        \n        # Identify circles that are overlapping or closer than target distance.\n        mask = (dists < target_dist) & (~np.eye(n, dtype=bool))\n        \n        # Linear repulsion force magnitude.\n        f_mag = np.zeros_like(dists)\n        f_mag[mask] = (target_dist - dists[mask]) / dists[mask]\n        \n        # Resulting forces: (n, 2)\n        forces = np.sum(f_mag[:, :, np.newaxis] * diff, axis=1)\n        \n        # 2. Boundary repulsion forces (keeping centers within safe radius).\n        b_forces = np.zeros((n, 2))\n        low_x = centers[:, 0] < target_radius\n        b_forces[low_x, 0] += (target_radius - centers[low_x, 0])\n        high_x = centers[:, 0] > 1.0 - target_radius\n        b_forces[high_x, 0] -= (centers[high_x, 0] - (1.0 - target_radius))\n        \n        low_y = centers[:, 1] < target_radius\n        b_forces[low_y, 1] += (target_radius - centers[low_y, 1])\n        high_y = centers[:, 1] > 1.0 - target_radius\n        b_forces[high_y, 1] -= (centers[high_y, 1] - (1.0 - target_radius))\n        \n        # 3. Apply cooling schedule to step size (dt) for convergence.\n        dt = initial_dt * (1.0 - 0.9 * k / iters)\n        centers += (forces + b_forces) * dt\n        \n        # 4. Stochastic noise (annealing) to escape local minima.\n        if k < iters * 0.5:\n            noise_mag = 0.0015 * (1.0 - k / (iters * 0.5))\n            centers += np.random.normal(0, noise_mag, (n, 2))\n            \n        # Keep centers within the unit square.\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # Calculate optimal radii for final center positions.\n    radii = compute_max_radii(centers, trials=300)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef run_packing():\n    \"\"\"\n    Main entry point to execute the packing algorithm and return results.\n    \"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii:.6f}\")\n```"]}}, "artifacts_json": "{\"execution_time\": \"0.41s\", \"stage\": \"quick_validation\", \"packing_summary\": \"Sum of radii: 2.446616/2.635 = 0.9285\", \"validation_report\": \"Valid: True, Violations: 0 boundary, 0 overlaps\"}", "artifact_dir": null, "embedding": null}