{"id": "97370a68-784d-4e14-a749-a4567c3c2fb3", "code": "n)) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((batch_size, n, 2))\n    v_radii = np.zeros((batch_size, n))\n    \n    eye_n = np.eye(n, dtype=bool)[np.newaxis, :, :]\n\n    for step in range(iters):\n        progress = step / iters\n\n        # Tune optimization parameters: decaying learning rate\n        lr_pos = 0.18 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.04 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, :, np.newaxis, :] - centers[:, np.newaxis, :, :]\n        dist = np.linalg.norm(diff, axis=-1)\n        \n        # Safely avoid division by zero\n        dist_safe = dist + np.where(eye_n, 1.0, 0.0)\n        dist_safe = np.where(dist_safe < 1e-8, 1e-8, dist_safe)\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(batch_size, n, 2))\n\n        # Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=-1)\n        grad_radii = 0.09 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, :, np.newaxis] + radii[:, np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = (overlap > 0) & (~eye_n)\n        \n        force = np.where(mask, overlap * 2.8, 0.0)\n        dir_vec = diff / dist_safe[..., np.newaxis]\n\n        # Accumulate forces\n        grad_centers += np.sum(dir_vec * force[..., np.newaxis], axis=2)\n        grad_radii -= np.sum(force, axis=2) * 1.5\n\n        # Boundary constraints\n        overlap_x0 = radii - centers[:, :, 0]\n        overlap_x1 = radii - (1.0 - centers[:, :, 0])\n        overlap_y0 = radii - centers[:, :, 1]\n        overlap_y1 = radii - (1.0 - centers[:, :, 1])\n\n        grad_centers[:, :, 0] += np.where(overlap_x0 > 0, overlap_x0 * 3.5, 0.0)\n        grad_radii -= np.where(overlap_x0 > 0, overlap_x0 * 2.0, 0.0)\n        \n        grad_centers[:, :, 0] -= np.where(overlap_x1 > 0, overlap_x1 * 3.5, 0.0)\n        grad_radii -= np.where(overlap_x1 > 0, overlap_x1 * 2.0, 0.0)\n        \n        grad_centers[:, :, 1] += np.where(overlap_y0 > 0, overlap_y0 * 3.5", "changes_description": null, "language": "python", "parent_id": "ade7c82e-0635-4ed6-a736-1d466f4991f0", "generation": 3, "timestamp": 1771877763.1433132, "iteration_found": 89, "metrics": {"validity": 0.0, "combined_score": 0.0, "radius_variance": 0.0, "spatial_spread": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.5889053829195814, "target_ratio": 0.9825067866867482, "combined_score": 0.9825067866867482, "radius_variance": 0.005218013000303681, "spatial_spread": 0.1980294795448683, "eval_time": 7.5192835330963135}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician and Python developer specializing in computational geometry and circle packing optimization.\nYour task is to improve the given Python algorithm for finding a highly optimized arrangement of 26 circles within a 1x1 unit square. The objective is to maximize the sum of their radii without any of the circles overlapping or extending outside the boundaries of the square.\nInstead of hardcoding a direct arrangement, implement an iterative optimization algorithm to find the solution.\nWhen designing the optimization routine, incorporate the following geometric heuristics:\n* Seed initial positions strategically: Bias initial placements toward corners and edges to maximize space utilization.\n* Break perfect symmetry: Introduce slight random perturbations during the optimization to escape local maxima caused by edge constraints.\n* Tune optimization parameters: Ensure your physics model uses a decaying learning rate or simulated annealing approach to smoothly settle into the tightest possible packing.\n* Size placement: Bias the initialization to push larger circles toward the center and smaller circles into the corners and interstitial gaps.\n\nCode Requirements:\n* Completeness: You MUST provide a fully runnable script. Do not use ellipses (`...`), comments-as-placeholders, or unclosed structures.\n* Style: Ensure the code is syntactically perfect and strictly adheres to PEP 8 formatting.\n* Code review: doublecheck your code to make sure it will not bump into any runtime exceptions.\n* Response Format: response in plain text, without wrapping the code into a Markdown codeblock.\n", "user": "# Current Program Information\n- Fitness: 0.9825\n- Feature coordinates: \n- Focus areas: - Fitness improved: 0.9697 \u2192 0.9825\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### execution_time\n```\n7.52s\n```\n\n### stage\n```\nquick_validation\n```\n\n### packing_summary\n```\nSum of radii: 2.588905/2.635 = 0.9825\n```\n\n### validation_report\n```\nValid: True, Violations: 0 boundary, 0 overlaps\n```\n\n### stdout\n```\nExcellent packing! Achieved 98.3% of target value\n```\n\n### radius_stats\n```\nMin: 0.072685, Max: 0.135305, Avg: 0.099573\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.5551, target_ratio: 0.9697, combined_score: 0.9697, radius_variance: 0.0049, spatial_spread: 0.1891, eval_time: 1.9768\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.5661, target_ratio: 0.9739, combined_score: 0.9739, radius_variance: 0.0087, spatial_spread: 0.1950, eval_time: 2.1733\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.5889, target_ratio: 0.9825, combined_score: 0.9825, radius_variance: 0.0052, spatial_spread: 0.1980, eval_time: 7.5193\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9825)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n    Uses a multi-start strategy with different geometric seeds.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    configs = get_initial_configs(n)\n    \n    best_centers = None\n    best_radii = None\n    best_sum = -1\n\n    for i, c_init in enumerate(configs):\n        centers = optimize_centers(c_init, iters=3500, seed=42 + i * 7)\n        radii = compute_max_radii(centers)\n        sum_radii = np.sum(radii)\n        \n        if sum_radii > best_sum:\n            best_sum = sum_radii\n            best_centers = centers\n            best_radii = radii\n\n    return best_centers, best_radii, best_sum\n\n\ndef get_initial_configs(n):\n    \"\"\"Generate strategic initial seed positions for optimization\"\"\"\n    configs = []\n    \n    # Config 1: Center, corners, edges, inner ring, outer ring\n    c1 = np.zeros((n, 2))\n    c1[0] = [0.5, 0.5]\n    c1[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    c1[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        c1[i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        c1[i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n    configs.append(c1)\n    \n    # Config 2: Hexagonal-like layers\n    c2 = np.zeros((n, 2))\n    c2[0] = [0.5, 0.5]\n    for i in range(6):\n        angle = 2 * np.pi * i / 6\n        c2[i + 1] = [0.5 + 0.18 * np.cos(angle), 0.5 + 0.18 * np.sin(angle)]\n    for i in range(11):\n        angle = 2 * np.pi * i / 11 + np.pi / 11\n        c2[i + 7] = [0.5 + 0.34 * np.cos(angle), 0.5 + 0.34 * np.sin(angle)]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        c2[i + 18] = [0.5 + 0.48 * np.cos(angle), 0.5 + 0.48 * np.sin(angle)]\n    configs.append(c2)\n    \n    # Config 3: Fibonacci spiral\n    c3 = np.zeros((n, 2))\n    phi = (1 + np.sqrt(5)) / 2\n    for i in range(n):\n        r = np.sqrt(i + 0.5) / np.sqrt(n) * 0.48\n        theta = 2 * np.pi * i / phi\n        c3[i] = [0.5 + r * np.cos(theta), 0.5 + r * np.sin(theta)]\n    configs.append(c3)\n    \n    # Config 4: Concentric rings (1, 5, 9, 11)\n    c4 = np.zeros((n, 2))\n    c4[0] = [0.5, 0.5]\n    for i in range(5):\n        angle = 2 * np.pi * i / 5\n        c4[i + 1] = [0.5 + 0.16 * np.cos(angle), 0.5 + 0.16 * np.sin(angle)]\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        c4[i + 6] = [0.5 + 0.32 * np.cos(angle), 0.5 + 0.32 * np.sin(angle)]\n    for i in range(11):\n        angle = 2 * np.pi * i / 11\n        c4[i + 15] = [0.5 + 0.46 * np.cos(angle), 0.5 + 0.46 * np.sin(angle)]\n    configs.append(c4)\n    \n    # Config 5: Random scatter with corner anchors\n    np.random.seed(123)\n    c5 = np.random.uniform(0.1, 0.9, (n, 2))\n    c5[0:4] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    c5[4] = [0.5, 0.5]\n    configs.append(c5)\n    \n    return configs\n\n\ndef optimize_centers(c_init, iters, seed):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    n = c_init.shape[0]\n    np.random.seed(seed)\n    \n    centers = c_init.copy()\n    # Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # Tune optimization parameters: decaying learning rate\n        lr_pos = 0.15 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.03 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(n, 2))\n\n        # Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.08 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.8\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 1.5\n            grad_radii[v] -= force * 1.5\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.5\n                grad_radii[u] -= overlap_x0[u] * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.5\n                grad_radii[u] -= overlap_x1[u] * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.5\n                grad_radii[u] -= overlap_y0[u] * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.5\n                grad_radii[u] -= overlap_y1[u] * 2.0\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(1500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.5889), Performs well on target_ratio (0.9825), Performs well on combined_score (0.9825), Performs well on radius_variance (0.0052), Performs well on spatial_spread (0.1980), Performs well on eval_time (7.5193)\n\n### Program 2 (Score: 0.9739)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=4500)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]  # Corners\n    centers[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]  # Edges\n\n    for i in range(8):       # Inner ring\n        angle = 2 * np.pi * i / 8\n        centers[i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n\n    for i in range(9):       # Outer ring\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        centers[i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate\n        lr_pos = 0.15 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.03 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(n, 2))\n        \n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.08 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.8\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 1.5\n            grad_radii[v] -= force * 1.5\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.5\n                grad_radii[u] -= overlap_x0[u] * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.5\n                grad_radii[u] -= overlap_x1[u] * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.5\n                grad_radii[u] -= overlap_y0[u] * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.5\n                grad_radii[u] -= overlap_y1[u] * 2.0\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(1500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.5661), Performs well on target_ratio (0.9739), Performs well on combined_score (0.9739), Performs well on radius_variance (0.0087), Performs well on spatial_spread (0.1950), Performs well on eval_time (2.1733)\n\n### Program 3 (Score: 0.9697)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=4000)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1] = [0.1, 0.1]  # Corners\n    centers[2] = [0.1, 0.9]\n    centers[3] = [0.9, 0.1]\n    centers[4] = [0.9, 0.9]\n\n    for i in range(7):       # Inner ring\n        angle = 2 * np.pi * i / 7\n        centers[i + 5] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n\n    for i in range(14):      # Outer ring\n        angle = 2 * np.pi * i / 14 + np.pi / 14\n        centers[i + 12] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.01, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate\n        lr_pos = 0.2 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.05 * (1.0 - progress) + 0.001\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        grad_centers = np.zeros((n, 2))\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**2\n        grad_centers += np.random.normal(0, temp, size=(n, 2))\n\n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.1 + 0.05 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.5\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force\n            grad_radii[v] -= force\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.0\n                grad_radii[u] -= overlap_x0[u] * 1.5\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.0\n                grad_radii[u] -= overlap_x1[u] * 1.5\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.0\n                grad_radii[u] -= overlap_y0[u] * 1.5\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.0\n                grad_radii[u] -= overlap_y1[u] * 1.5\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.5551), Performs well on target_ratio (0.9697), Performs well on combined_score (0.9697), Performs well on radius_variance (0.0049), Performs well on spatial_spread (0.1891), Performs well on eval_time (1.9768)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.0000)\n```python\nidx = np.arange(n)\n        overlap[:, idx, idx] = -1.0\n```\nKey features: Alternative approach to validity, Alternative approach to combined_score\n\n### Program D2 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.0000, Type: Exploratory)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using fully vectorized physics-based relaxation followed by linear\n    programming to strictly maximize the sum of their radii without any overlaps.\n    Uses a batched multi-start strategy to explore many geometric seeds simultaneously.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    batch_size = 40\n    iters = 6000\n    \n    configs = get_initial_configs_batch(n, batch_size)\n    \n    # Process all initial configs entirely in parallel using vectorization\n    final_centers_batch = optimize_centers_batch(configs, iters)\n    \n    best_centers = None\n    best_radii = None\n    best_sum = -1\n\n    for b in range(batch_size):\n        centers = final_centers_batch[b]\n        radii = compute_max_radii(centers)\n        sum_radii = np.sum(radii)\n        \n        if sum_radii > best_sum:\n            best_sum = sum_radii\n            best_centers = centers\n            best_radii = radii\n\n    return best_centers, best_radii, best_sum\n\n\ndef get_initial_configs_batch(n, batch_size=40):\n    \"\"\"Generate diverse strategic initial seed positions for parallel optimization\"\"\"\n    configs = np.zeros((batch_size, n, 2))\n    np.random.seed(42)\n    \n    for b in range(batch_size):\n        cx, cy = 0.5 + np.random.uniform(-0.02, 0.02), 0.5 + np.random.uniform(-0.02, 0.02)\n        configs[b, 0] = [cx, cy]\n        \n        n_layers = np.random.choice([2, 3])\n        rem = n - 1\n        \n        if n_layers == 2:\n            l1 = np.random.randint(6, 11)\n            layers = [l1, rem - l1]\n        else:\n            l1 = np.random.randint(4, 9)\n            l2 = np.random.randint(7, 13)\n            l3 = rem - l1 - l2\n            if l3 < 1:\n                 layers = [l1, rem - l1]\n            else:\n                 layers = [l1, l2, l3]\n                 \n        idx = 1\n        for j, count in enumerate(layers):\n            r = 0.15 + 0.35 * (j / max(1, len(layers)-1)) + np.random.uniform(-0.02, 0.02)\n            offset = np.random.uniform(0, 2 * np.pi)\n            for i in range(count):\n                angle = 2 * np.pi * i / count + offset\n                configs[b, idx] = [cx + r * np.cos(angle), cy + r * np.sin(angle)]\n                idx += 1\n                \n    # Manually inject known highly-performant seeds to guarantee baseline quality\n    \n    # Seed 0: Top performer concentric layers\n    configs[0, 0] = [0.5, 0.5]\n    configs[0, 1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    configs[0, 5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        configs[0, i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        configs[0, i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    # Seed 1: Hexagonal-like layers\n    configs[1, 0] = [0.5, 0.5]\n    for i in range(6):\n        angle = 2 * np.pi * i / 6\n        configs[1, i + 1] = [0.5 + 0.18 * np.cos(angle), 0.5 + 0.18 * np.sin(angle)]\n    for i in range(11):\n        angle = 2 * np.pi * i / 11 + np.pi / 11\n        configs[1, i + 7] = [0.5 + 0.34 * np.cos(angle), 0.5 + 0.34 * np.sin(angle)]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        configs[1, i + 18] = [0.5 + 0.48 * np.cos(angle), 0.5 + 0.48 * np.sin(angle)]\n        \n    # Seed 2: Fibonacci spiral\n    phi = (1 + np.sqrt(5)) / 2\n    for i in range(n):\n        r = np.sqrt(i + 0.5) / np.sqrt(n) * 0.48\n        theta = 2 * np.pi * i / phi\n        configs[2, i] = [0.5 + r * np.cos(theta), 0.5 + r * np.sin(theta)]\n        \n    # Seed 3: Grid-like\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation with batched optimization across multiple \n    structural seeds, followed by linear programming to strictly maximize \n    the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    batch_size = 8\n    iters = 8000\n    \n    # 1. Optimize multiple configurations in parallel\n    batched_centers = optimize_centers(n, iters, batch_size)\n    \n    best_sum = -1\n    best_centers = None\n    best_radii = None\n    \n    # 2. Evaluate each using the strict LP solver\n    for b in range(batch_size):\n        centers = batched_centers[b]\n        radii = compute_max_radii(centers)\n        sum_r = np.sum(radii)\n        \n        if sum_r > best_sum:\n            best_sum = sum_r\n            best_centers = centers\n            best_radii = radii\n            \n    return best_centers, best_radii, best_sum\n\n\ndef generate_seeds(batch_size, n):\n    \"\"\"Generate diverse geometric starting arrangements\"\"\"\n    centers = np.zeros((batch_size, n, 2))\n    \n    # Seed 0: The historically best performing seed structure\n    centers[0, 0] = [0.5, 0.5]\n    centers[0, 1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    centers[0, 5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]\n    for i in range(8):\n        a = 2 * np.pi * i / 8\n        centers[0, i + 9] = [0.5 + 0.22 * np.cos(a), 0.5 + 0.22 * np.sin(a)]\n    for i in range(9):\n        a = 2 * np.pi * i / 9 + np.pi / 9\n        centers[0, i + 17] = [0.5 + 0.42 * np.cos(a), 0.5 + 0.42 * np.sin(a)]\n        \n    # Seed 1: 1 center, 6 inner, 11 middle, 8 outer\n    centers[1, 0] = [0.5, 0.5]\n    for i in range(6):\n        a = 2 * np.pi * i / 6\n        centers[1, i + 1] = [0.5 + 0.18 * np.cos(a), 0.5 + 0.18 * np.sin(a)]\n    for i in range(11):\n        a = 2 * np.pi * i / 11 + np.pi / 11\n        centers[1, i + 7] = [0.5 + 0.34 * np.cos(a), 0.5 + 0.34 * np.sin(a)]\n    for i in range(8):\n        a = 2 * np.pi * i / 8\n        centers[1, i + 18] = [0.5 + 0.46 * np.cos(a), 0.5 + 0.46 * np.sin(a)]\n        \n    # Seed 2: 1 center, 7 inner, 18 outer\n    centers[2, 0] = [0.5, 0.5]\n    for i in range(7):\n        a = 2 * np.pi * i / 7\n        centers[2, i + 1] = [0.5 + 0.2 * np.cos(a), 0.5 + 0.2 * np.sin(a)]\n    for i in range(18):\n        a = 2 * np.pi * i / 18 + np.pi / 18\n        centers[2, i + 8] = [0.5 + 0.42 * np.cos(a), 0.5 + 0.42 * np.sin(a)]\n        \n    # Seed 3: 4 corners, 1 center, 7 inner, 14 outer\n    centers[3, 0] = [0.5, 0.5]\n    centers[3, 1:5] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n    for i in range(7):\n        a = 2 * np.pi * i / 7\n        centers[3, i + 5] = [0.5 + 0.22 * np.cos(a), 0.5 + 0.22 * np.sin(a)]\n    for i in range(14):\n        a = 2 * np.pi * i / 14 + np.pi / 14\n        centers[3, i + 12] = [0.5 + 0.42 * np.cos(a), 0.5 + 0.42 * np.sin(a)]\n        \n    # Seed 4: 5 inner,\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n    Uses a multi-start strategy with different geometric seeds.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    configs = get_initial_configs(n)\n    \n    best_centers = None\n    best_radii = None\n    best_sum = -1\n\n    for i, c_init in enumerate(configs):\n        centers = optimize_centers(c_init, iters=3500, seed=42 + i * 7)\n        radii = compute_max_radii(centers)\n        sum_radii = np.sum(radii)\n        \n        if sum_radii > best_sum:\n            best_sum = sum_radii\n            best_centers = centers\n            best_radii = radii\n\n    return best_centers, best_radii, best_sum\n\n\ndef get_initial_configs(n):\n    \"\"\"Generate strategic initial seed positions for optimization\"\"\"\n    configs = []\n    \n    # Config 1: Center, corners, edges, inner ring, outer ring\n    c1 = np.zeros((n, 2))\n    c1[0] = [0.5, 0.5]\n    c1[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    c1[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        c1[i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        c1[i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n    configs.append(c1)\n    \n    # Config 2: Hexagonal-like layers\n    c2 = np.zeros((n, 2))\n    c2[0] = [0.5, 0.5]\n    for i in range(6):\n        angle = 2 * np.pi * i / 6\n        c2[i + 1] = [0.5 + 0.18 * np.cos(angle), 0.5 + 0.18 * np.sin(angle)]\n    for i in range(11):\n        angle = 2 * np.pi * i / 11 + np.pi / 11\n        c2[i + 7] = [0.5 + 0.34 * np.cos(angle), 0.5 + 0.34 * np.sin(angle)]\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        c2[i + 18] = [0.5 + 0.48 * np.cos(angle), 0.5 + 0.48 * np.sin(angle)]\n    configs.append(c2)\n    \n    # Config 3: Fibonacci spiral\n    c3 = np.zeros((n, 2))\n    phi = (1 + np.sqrt(5)) / 2\n    for i in range(n):\n        r = np.sqrt(i + 0.5) / np.sqrt(n) * 0.48\n        theta = 2 * np.pi * i / phi\n        c3[i] = [0.5 + r * np.cos(theta), 0.5 + r * np.sin(theta)]\n    configs.append(c3)\n    \n    # Config 4: Concentric rings (1, 5, 9, 11)\n    c4 = np.zeros((n, 2))\n    c4[0] = [0.5, 0.5]\n    for i in range(5):\n        angle = 2 * np.pi * i / 5\n        c4[i + 1] = [0.5 + 0.16 * np.cos(angle), 0.5 + 0.16 * np.sin(angle)]\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        c4[i + 6] = [0.5 + 0.32 * np.cos(angle), 0.5 + 0.32 * np.sin(angle)]\n    for i in range(11):\n        angle = 2 * np.pi * i / 11\n        c4[i + 15] = [0.5 + 0.46 * np.cos(angle), 0.5 + 0.46 * np.sin(angle)]\n    configs.append(c4)\n    \n    # Config 5: Random scatter with corner anchors\n    np.random.seed(123)\n    c5 = np.random.uniform(0.1, 0.9, (n, 2))\n    c5[0:4] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    c5[4] = [0.5, 0.5]\n    configs.append(c5)\n    \n    return configs\n\n\ndef optimize_centers(c_init, iters, seed):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    n = c_init.shape[0]\n    np.random.seed(seed)\n    \n    centers = c_init.copy()\n    # Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # Tune optimization parameters: decaying learning rate\n        lr_pos = 0.15 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.03 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(n, 2))\n\n        # Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.08 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.8\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 1.5\n            grad_radii[v] -= force * 1.5\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.5\n                grad_radii[u] -= overlap_x0[u] * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.5\n                grad_radii[u] -= overlap_x1[u] * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.5\n                grad_radii[u] -= overlap_y0[u] * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.5\n                grad_radii[u] -= overlap_y1[u] * 2.0\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(1500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["n)) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((batch_size, n, 2))\n    v_radii = np.zeros((batch_size, n))\n    \n    eye_n = np.eye(n, dtype=bool)[np.newaxis, :, :]\n\n    for step in range(iters):\n        progress = step / iters\n\n        # Tune optimization parameters: decaying learning rate\n        lr_pos = 0.18 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.04 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, :, np.newaxis, :] - centers[:, np.newaxis, :, :]\n        dist = np.linalg.norm(diff, axis=-1)\n        \n        # Safely avoid division by zero\n        dist_safe = dist + np.where(eye_n, 1.0, 0.0)\n        dist_safe = np.where(dist_safe < 1e-8, 1e-8, dist_safe)\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(batch_size, n, 2))\n\n        # Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=-1)\n        grad_radii = 0.09 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, :, np.newaxis] + radii[:, np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = (overlap > 0) & (~eye_n)\n        \n        force = np.where(mask, overlap * 2.8, 0.0)\n        dir_vec = diff / dist_safe[..., np.newaxis]\n\n        # Accumulate forces\n        grad_centers += np.sum(dir_vec * force[..., np.newaxis], axis=2)\n        grad_radii -= np.sum(force, axis=2) * 1.5\n\n        # Boundary constraints\n        overlap_x0 = radii - centers[:, :, 0]\n        overlap_x1 = radii - (1.0 - centers[:, :, 0])\n        overlap_y0 = radii - centers[:, :, 1]\n        overlap_y1 = radii - (1.0 - centers[:, :, 1])\n\n        grad_centers[:, :, 0] += np.where(overlap_x0 > 0, overlap_x0 * 3.5, 0.0)\n        grad_radii -= np.where(overlap_x0 > 0, overlap_x0 * 2.0, 0.0)\n        \n        grad_centers[:, :, 0] -= np.where(overlap_x1 > 0, overlap_x1 * 3.5, 0.0)\n        grad_radii -= np.where(overlap_x1 > 0, overlap_x1 * 2.0, 0.0)\n        \n        grad_centers[:, :, 1] += np.where(overlap_y0 > 0, overlap_y0 * 3.5"]}}, "artifacts_json": "{\"stderr\": \"Stage 1 evaluation failed: Program execution failed: unmatched ')' (tmp0am1bzux.py, line 1)\", \"traceback\": \"Traceback (most recent call last):\\n  File \\\"/home/jupyter/Code/oe/dennis/run2_improved_si/evaluator.py\\\", line 384, in evaluate_stage1\\n    centers, radii, sum_radii = run_with_timeout(program_path, timeout_seconds=600)\\n  File \\\"/home/jupyter/Code/oe/dennis/run2_improved_si/evaluator.py\\\", line 172, in run_with_timeout\\n    raise RuntimeError(f\\\"Program execution failed: {results['error']}\\\")\\nRuntimeError: Program execution failed: unmatched ')' (tmp0am1bzux.py, line 1)\\n\", \"failure_stage\": \"stage1_execution\", \"suggestion\": \"Check basic syntax and imports before attempting full evaluation\"}", "artifact_dir": null, "embedding": null}