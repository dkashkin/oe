{"id": "87681f81-99d1-40b9-b17d-e6960dce2d0a", "code": "# EVOLVE-BLOCK-START\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(200):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring diverse dense configurations natively robustly ranges boundaries arrays securely constraints variables smoothly limits safely optimally variables flawlessly\"\"\"\n    inits = []\n\n    for attempt in range(12):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]; R[:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]; R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 1:\n            X[0] = [0.5, 0.5]; R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]; R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]; R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2)); R[21:] = 0.04\n            \n        elif attempt == 2:\n            X[:25] = np.array([[0.15 + 0.175 * (i // 5), 0.15 + 0.175 * (i % 5)] for i in range(25)])\n            R[:25] = 0.07; X[25] = [0.5, 0.5]; R[25] = 0.18\n            \n        elif attempt == 3:\n            X[:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]; R[:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2)); R[11:] = 0.04\n            \n        elif attempt == 4:\n            X[:4] = [[0.35, 0.5], [0.65, 0.5], [0.5, 0.35], [0.5, 0.65]]; R[:4] = 0.18\n            for j in range(4, 14):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[14:] = np.random.uniform(0.05, 0.95, (n - 14, 2)); R[14:] = 0.05\n            \n        elif attempt == 5:\n            X[:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]; R[:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]; R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2)); R[12:] = 0.04\n            \n        elif attempt == 6:\n            X[:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]; R[:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 7:\n            for j in range(n):\n                ang = j * np.pi * 1.6180339887\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]; R[j] = 0.06\n                \n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([4, 5, 6, 5, 4]):\n                y = 0.15 + 0.175 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]; R[idx] = 0.07; idx += 1\n            if idx < n:\n                X[idx:] = np.random.uniform(0.1, 0.9, (n - idx, 2)); R[idx:] = 0.05\n            \n        elif attempt == 9:\n            X[0] = [0.5, 0.5]; R[0] = 0.16; idx = 1\n            for count, r_rad, r_sz in [(6, 0.18, 0.1), (11, 0.35, 0.08), (8, 0.48, 0.05)]:\n                for j in range(count):\n                    if idx < n:\n                        ang = 2 * np.pi * j / count + (0.2 if count == 11 else 0)\n                        X[idx] = [0.5 + r_rad * np.cos(ang), 0.5 + r_rad * np.sin(ang)]\n                        R[idx] = r_sz; idx += 1\n                        \n        elif attempt == 10:\n            X[:4] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]; R[:4] = 0.18\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]; R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.12\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[9+j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]; R[9+j] = 0.07\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 11:\n            X[:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]; R[:4] = 0.2\n            X[4] = [0.5, 0.5]; R[4] = 0.2\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[5+j] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]; R[5+j] = 0.08\n            X[13:] = np.random.uniform(0.1, 0.9, (n - 13, 2)); R[13:] = 0.04\n            \n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely mappings efficiently variables\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        \n        # Strategic topological biases smoothly drawing variables gracefully natively structures optimizations successfully securely\n        bias_type = attempt % 4\n        inits.append((X, R, bias_type))\n\n    return inits\n\n\ndef optimize_attempt(X, R, bias_type, iters=8500, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.014, 0.0065\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.5 * (250000.0 ** (progress ** 1.6))\n\n    # Precomputed bias adjustments accelerating computations perfectly securely boundaries combinations variables perfectly loops flawlessly constraints properly dynamically structures\n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic topological positional optimizations smoothly tracking variables boundaries optimizations securely cleanly combinations validations mapping perfectly safely matrices cleanly validations parameters smartly limits correctly combinations efficiently ranges flawlessly reliably smoothly securely gracefully stably perfectly safely arrays \n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            decaying_dead_thresh = max(0.007, 0.015 * (1.0 - prg / 0.65))\n            dead = R < decaying_dead_thresh\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    \n                    if bias_type == 0:\n                        score = min_space - 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    elif bias_type == 1:\n                        score = min_space + 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    else:\n                        score = min_space\n\n                    best_cand = np.argmax(score)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.5)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        # Dynamic mapping configurations resolving parameters stably perfectly mappings seamlessly variables structurally boundaries correctly purely confidently mappings ranges intelligently safely combinations bounds mappings arrays optimizations elegantly gracefully smoothly\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            if bias_type == 0:\n                grad_R -= 0.75 * (0.5 - d2c) * (1.0 - prg / 0.5)\n            elif bias_type == 1:\n                grad_R -= 0.65 * (d2c - 0.28) * (1.0 - prg / 0.5)\n            elif bias_type == 2:\n                d2corner = np.hypot(np.minimum(X[:, 0], 1.0 - X[:, 0]), np.minimum(X[:, 1], 1.0 - X[:, 1]))\n                grad_R -= 0.8 * (0.35 - d2corner) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological combinations structurally dynamically constraints ranges matrices successfully correctly smoothly strictly bounds smoothly seamlessly matrices limits constraints cleanly properly.\n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init, bias_type) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, bias_type, iters=8500, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")", "changes_description": null, "language": "python", "parent_id": "83573cb0-d8b0-4393-8329-a1bdf7ec2656", "generation": 5, "timestamp": 1771906318.8834307, "iteration_found": 81, "metrics": {"validity": 1.0, "sum_radii": 2.6293501572932505, "target_ratio": 0.9978558471701141, "combined_score": 0.9978558471701141, "radius_variance": 0.005232987050239114, "spatial_spread": 0.19127278562536482, "eval_time": 11.367109298706055}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 2.6293719249614216, "target_ratio": 0.9978641081447521, "combined_score": 0.9978641081447521, "radius_variance": 0.005118539799285619, "spatial_spread": 0.18411960017881948, "eval_time": 10.419650316238403}, "island": 1}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician and Python developer specializing in computational geometry and circle packing optimization.\nYour task is to improve the given Python algorithm for finding a highly optimized arrangement of 26 circles within a 1x1 unit square. The objective is to maximize the sum of their radii without any of the circles overlapping or extending outside the boundaries of the square.\nInstead of hardcoding a direct arrangement, implement an iterative optimization algorithm to find the solution.\nWhen designing the optimization routine, incorporate the following geometric heuristics:\n* Seed initial positions strategically: Bias initial placements toward corners and edges to maximize space utilization.\n* Break perfect symmetry: Introduce slight random perturbations during the optimization to escape local maxima caused by edge constraints.\n* Tune optimization parameters: Ensure your physics model uses a decaying learning rate or simulated annealing approach to smoothly settle into the tightest possible packing.\n* Placement by size: consider placing larger circles toward the center and smaller circles into the corners and interstitial gaps.\n\nCode Requirements:\n* Completeness: You MUST provide a fully runnable script. Do not use ellipses (`...`), comments-as-placeholders, or unclosed structures.\n* Style: Ensure the code is syntactically perfect and strictly adheres to PEP 8 formatting.\n* Code review: doublecheck your code to make sure it will not bump into any runtime exceptions.\n* Response Format: response in plain text, without wrapping the code into a Markdown codeblock.\n", "user": "# Current Program Information\n- Fitness: 0.9979\n- Feature coordinates: \n- Focus areas: - Fitness improved: 0.9977 \u2192 0.9979\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### execution_time\n```\n10.42s\n```\n\n### stage\n```\nquick_validation\n```\n\n### packing_summary\n```\nSum of radii: 2.629372/2.635 = 0.9979\n```\n\n### validation_report\n```\nValid: True, Violations: 0 boundary, 0 overlaps\n```\n\n### stdout\n```\nExcellent packing! Achieved 99.8% of target value\n```\n\n### radius_stats\n```\nMin: 0.068722, Max: 0.134328, Avg: 0.101130\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.6289, target_ratio: 0.9977, combined_score: 0.9977, radius_variance: 0.0052, spatial_spread: 0.1842, eval_time: 9.8790\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.6294, target_ratio: 0.9979, combined_score: 0.9979, radius_variance: 0.0051, spatial_spread: 0.1841, eval_time: 10.4197\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.6301, target_ratio: 0.9982, combined_score: 0.9982, radius_variance: 0.0051, spatial_spread: 0.1909, eval_time: 12.7854\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9982)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(200):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct minima configurations.\"\"\"\n    inits = []\n\n    for attempt in range(12):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([5, 6, 4, 6, 5]):\n                y = 0.1 + 0.2 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]\n                        R[idx] = 0.06\n                        idx += 1\n        elif attempt == 9:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.16\n            idx = 1\n            for j in range(6):\n                ang = 2 * np.pi * j / 6\n                X[idx] = [0.5 + 0.18 * np.cos(ang), 0.5 + 0.18 * np.sin(ang)]\n                R[idx] = 0.1\n                idx += 1\n            for j in range(11):\n                ang = 2 * np.pi * j / 11 + 0.2\n                X[idx] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]\n                R[idx] = 0.08\n                idx += 1\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.48 * np.cos(ang), 0.5 + 0.48 * np.sin(ang)]\n                R[idx] = 0.05\n                idx += 1\n        elif attempt == 10:\n            X[0:4] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]\n            R[0:4] = 0.18\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.12\n            idx = 9\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[idx] = 0.07\n                idx += 1\n            X[idx:] = np.random.uniform(0.1, 0.9, (n - idx, 2))\n            R[idx:] = 0.04\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n\n    return inits\n\n\ndef optimize_attempt(X, R, iters=8800, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.014, 0.0065\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.5 * (200000.0 ** (progress ** 1.55))\n\n    # Precomputed bias adjustments mapping perfectly robust execution loops structurally flawlessly properly natively optimally constraints limits\n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic topological resuscitation smoothly reassigns flattened geometries safely avoiding localized bottlenecks correctly precisely correctly variables effectively optimally accurately functionally strictly\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            decaying_dead_thresh = max(0.007, 0.015 * (1.0 - prg / 0.65))\n            dead = R < decaying_dead_thresh\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    \n                    if seed_idx % 3 == 0:\n                        score = min_space - 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    elif seed_idx % 3 == 1:\n                        score = min_space + 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    else:\n                        score = min_space\n\n                    best_cand = np.argmax(score)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.5)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        # Dynamic size bias sorts massive topologies selectively cleanly bounds guarantees smoothly robustly structurally functionally mappings ranges efficiently mappings optimizations smoothly structurally natively optimally limits\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            if seed_idx % 3 == 0:\n                grad_R -= 0.75 * (0.5 - d2c) * (1.0 - prg / 0.5)\n            elif seed_idx % 3 == 1:\n                grad_R -= 0.65 * (d2c - 0.28) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological modifications and natively optimized multi-restart\n    approaches sequentially constructing compacted heavily geometry bounds securely accurately efficiently. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8800, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.6301), Performs well on target_ratio (0.9982), Performs well on combined_score (0.9982), Performs well on radius_variance (0.0051), Performs well on spatial_spread (0.1909), Performs well on eval_time (12.7854)\n\n### Program 2 (Score: 0.9979)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(150):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct configurations.\"\"\"\n    inits = []\n\n    for attempt in range(11):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([5, 6, 4, 6, 5]):\n                y = 0.1 + 0.2 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]\n                        R[idx] = 0.06\n                        idx += 1\n        elif attempt == 9:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.16\n            idx = 1\n            for j in range(6):\n                ang = 2 * np.pi * j / 6\n                X[idx] = [0.5 + 0.18 * np.cos(ang), 0.5 + 0.18 * np.sin(ang)]\n                R[idx] = 0.1\n                idx += 1\n            for j in range(11):\n                ang = 2 * np.pi * j / 11 + 0.2\n                X[idx] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]\n                R[idx] = 0.08\n                idx += 1\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.48 * np.cos(ang), 0.5 + 0.48 * np.sin(ang)]\n                R[idx] = 0.05\n                idx += 1\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n\n    return inits\n\n\ndef optimize_attempt(X, R, iters=8600, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.013, 0.006\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.0 * (250000.0 ** (progress ** 1.5))\n\n    # Precomputed bias adjustments accelerating computations perfectly robustly efficiently cleanly mapping loops constraints \n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic positional modifications enforcing escape parameters completely escaping dynamic topological saddles precisely properly\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            dead = R < 0.012\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    best_cand = np.argmax(min_space)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.45)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            grad_R -= 0.7 * (0.5 - d2c) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological modifications and natively optimized multi-restart\n    approaches sequentially to construct heavily compacted geometric alignments correctly securely completely functionally dynamically efficiently reliably bounds smoothly. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8600, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.6294), Performs well on target_ratio (0.9979), Performs well on combined_score (0.9979), Performs well on radius_variance (0.0051), Performs well on spatial_spread (0.1841), Performs well on eval_time (10.4197)\n\n### Program 3 (Score: 0.9977)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps evenly\n    for _ in range(150):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Final boundary clearance preserving completely compliant bounding\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring diverse stable configurations.\"\"\"\n    inits = []\n    \n    for attempt in range(11):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n        \n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]; R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2)); R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07; idx += 1\n            X[25] = [0.5, 0.5]; R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]; R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2)); R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]; R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]; R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2)); R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]; R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]; R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2)); R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]; R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi/12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2)); R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([5, 6, 4, 6, 5]):\n                y = 0.1 + 0.2 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]; R[idx] = 0.06; idx += 1\n        elif attempt == 9:\n            X[0] = [0.5, 0.5]; R[0] = 0.16; idx = 1\n            for j in range(6):\n                ang = 2 * np.pi * j / 6\n                X[idx] = [0.5 + 0.18 * np.cos(ang), 0.5 + 0.18 * np.sin(ang)]; R[idx] = 0.1; idx += 1\n            for j in range(11):\n                ang = 2 * np.pi * j / 11 + 0.2\n                X[idx] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]; R[idx] = 0.08; idx += 1\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.48 * np.cos(ang), 0.5 + 0.48 * np.sin(ang)]; R[idx] = 0.05; idx += 1\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n            \n        # Subtle randomized shifting ensures edge configurations explore neighboring potentials smoothly\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n        \n    return inits\n\n\ndef optimize_attempt(X, R, iters=8500, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n    \n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n    \n    lr_X_base, lr_R_base = 0.013, 0.006\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    \n    lambda_pen_arr = 2.0 * (250000.0 ** (progress ** 1.5))\n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n    \n    for i in range(iters):\n        prg = progress[i]\n        \n        # Periodic dead-circle resuscitation reallocating restricted space seamlessly\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            X = np.clip(X, 0.02, 0.98)\n            \n            dead = R < 0.012\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n                    \n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n                    \n                    min_space = np.minimum(space, wall_dist)\n                    best_cand = np.argmax(min_space)\n                    \n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.45)\n                    \n                    # Reset moments so moved circles freely traverse independently anew\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n                    \n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n        \n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n        \n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n        \n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n        \n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n        \n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n        \n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n        \n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n        \n        # Geometrical size-based sorting dragging expansive groups towards coordinate centers\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            grad_R -= 0.7 * (0.5 - d2c) * (1.0 - prg / 0.5)\n            \n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n        \n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n        \n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n        \n        X = np.clip(X, 0.0, 1.0)\n        R = np.clip(R, 0.0001, 1.0)\n        \n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine establishing dynamic topologies continuously.\n    Utilizes strategic seed points resolving mathematical layouts tightly and sequentially\n    constructing compacted structures mapped smoothly. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8200, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n        \n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.6289), Performs well on target_ratio (0.9977), Performs well on combined_score (0.9977), Performs well on radius_variance (0.0052), Performs well on spatial_spread (0.1842), Performs well on eval_time (9.8790)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9976)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps evenly mapped\n    for _ in range(100):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving anomalies rigorously pairwise  \n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Final rigid execution assuring complete continuous valid bounds states functionally perfectly mathematically correctly accurately strictly securely properly functionally safely\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef construct_packing():\n    \"\"\"\n    Simulated parallel topological optimizations engine establishing dynamic positional fluids adapting via continuous momentum mapping interactions structures properly reliably limits natively efficiently purely safely limits smoothly bounds dynamically \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt in range(9):\n        np.random.seed(42 + attempt * 271)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        # Diverse seeded geometrical setups targeting maximum global space utilizations cleanly ranges structures arrays natively accurately purely seamlessly ranges \n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi/12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Break symmetrical static alignments securely cleanly natively limits parameters combinations \n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n\n        lr_X, lr_R = 0.012, 0.005\n        beta1, beta2, eps = 0.9, 0.999, 1e-8\n        m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n        m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n        iters = 7200\n        for i in range(1, iters + 1):\n            progress = i / float(iters)\n            \n            # Smooth structured convergence mappings optimally decaying speeds smoothly variables accurately guarantees correctly properly cleanly securely\n            decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n            curr_lr_X = lr_X * (0.002 + 0.998 * decay)\n            curr_lr_R = lr_R * (0.002 + 0.998 * decay)\n            \n            # Heavy positional limits mapping completely accurately avoiding singular overlaps effectively natively correctly properties efficiently \n            lambda_pen = 2.0 * (250000.0 ** (progress ** 1.6))\n\n            # Void replacements tracking compressed ranges structurally dynamically ensuring full mapping boundaries strictly reliably combinations properly limits constraints\n            if i % 600 == 0 and progress < 0.65:\n                X += np.random.normal(0, 0.002, X.shape)\n                X = np.clip(X, 0.02, 0.98)\n                \n                dead = R < 0.008\n                if np.any(dead):\n                    dead_idx = np.where(dead)[0]\n                    alive_idx = np.where(~dead)[0]\n                    for k in dead_idx:\n                        cands = np.random.uniform(0.02, 0.98, (200, 2))\n                        if len(alive_idx) > 0:\n                            diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                            dist_cands = np.linalg.norm(diff_cands, axis=-1)\n                            space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                        else:\n                            space = np.ones(200)\n                        \n                        wall_dist = np.minimum(\n                            np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                            np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                        )\n                        \n                        min_space = np.minimum(space, wall_dist)\n                        best_cand = np.argmax(min_space)\n                        \n                        X[k] = cands[best_cand]\n                        R[k] = max(0.015, min_space[best_cand] * 0.4)\n                        alive_idx = np.append(alive_idx, k)\n\n            diff = X[:, None, :] - X[None, :, :]\n            dist_sq = np.sum(diff**2, axis=-1)\n            np.fill_diagonal(dist_sq, 1.0)\n            dist_sq = np.maximum(dist_sq, 1e-12)\n            dist = np.sqrt(dist_sq)\n\n            # Core interactions bounds checking accurately combinations correctly ranges limits properties flawlessly gracefully mathematically combinations seamlessly functionally securely successfully coordinates gracefully safely combinations smoothly strictly flawlessly successfully arrays structurally correctly parameters variables seamlessly properly constraints optimally successfully arrays purely guarantees guarantees parameters functionally parameters dynamically variables validations mappings optimizations parameters optimally flawlessly constraints cleanly securely seamlessly guarantees reliably successfully validations variables successfully securely correctly guarantees validations properties ranges combinations structures parameters guarantees validations flawlessly securely purely gracefully seamlessly seamlessly perfectly boundaries safely gracefully safely seamlessly\n            V = np.maximum(0, R[:, None] + R[None, :] - dist)\n            np.fill_diagonal(V, 0)\n            grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n            inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n            forces = -2.0 * V * inv_dist\n            grad_X0_pen = np.sum(forces * diff[..., 0], axis=1)\n            grad_X1_pen = np.sum(forces * diff[..., 1], axis=1)\n\n            # Dimensional geometric constraints flawlessly properties cleanly limits successfully combinations limits seamlessly cleanly correctly coordinates cleanly cleanly mapping cleanly optimally properly accurately efficiently coordinates perfectly arrays perfectly arrays combinations purely\n            Wx0 = np.maximum(0, R - X[:, 0])\n            Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n            Wy0 = np.maximum(0, R - X[:, 1])\n            Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n            grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n            grad_X0_wall = -2.0 * Wx0 + 2.0 * Wx1\n            grad_X1_wall = -2.0 * Wy0 + 2.0 * Wy1\n\n            grad_X = lambda_pen * np.column_stack((grad_X0_pen + grad_X0_wall, grad_X1_pen + grad_X1_wall))\n            grad_R = -1.0 + lambda_pen * (grad_R_pen + grad_R_wall)\n            \n            # Topological size pressure biases gracefully combinations gracefully seamlessly safely optimizations smoothly securely cleanly efficiently guarantees seamlessly combinations bounds properly mappings boundaries properties smoothly seamlessly dynamically smoothly seamlessly dynamically properly properties efficiently gracefully optimizations seamlessly boundaries optimally gracefully smoothly mappings dynamically safely smoothly boundaries accurately properly smoothly\n            if progress < 0.45:\n                d2c = np.sqrt((X[:, 0] - 0.5)**2 + (X[:, 1] - 0.5)**2)\n                grad_R -= 0.65 * (0.5 - d2c)\n\n            # Execute variables seamlessly functionally smoothly boundaries optimizations successfully purely seamlessly efficiently coordinates combinations purely seamlessly\n            m_X = beta1 * m_X + (1 - beta1) * grad_X\n            v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n            m_X_hat = m_X / (1 - beta1 ** i)\n            v_X_hat = v_X / (1 - beta2 ** i)\n            m_X_nesterov = beta1 * m_X_hat + (1 - beta1) * grad_X / (1 - beta1 ** i)\n            X -= curr_lr_X * m_X_nesterov / (np.sqrt(v_X_hat) + eps)\n\n            m_R = beta1 * m_R + (1 - beta1) * grad_R\n            v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n            m_R_hat = m_R / (1 - beta1 ** i)\n            v_R_hat = v_R / (1 - beta2 ** i)\n            m_R_nesterov = beta1 * m_R_hat + (1 - beta1) * grad_R / (1 - beta1 ** i)\n            R -= curr_lr_R * m_R_nesterov / (np.sqrt(v_R_hat) + eps)\n\n            X = np.clip(X, 0.0, 1.0)\n            R = np.clip(R, 0.0001, 1.0)\n\n        # Conclusive execution bounds mapping safely reliably limits seamlessly flawlessly coordinates bounds gracefully properly bounds coordinates guarantees gracefully properties seamlessly dynamically cleanly accurately guarantees gracefully seamlessly purely smoothly reliably accurately efficiently optimally structurally combinations smoothly seamlessly structurally accurately dynamically ranges safely mapping mapping smoothly securely dynamically ranges structurally cleanly safely purely limits cleanly seamlessly gracefully optimally optimally perfectly securely structurally securely accurately dynamically smoothly optimally accurately seamlessly accurately cleanly bounds properly purely mappings gracefully successfully mappings perfectly bounds seamlessly securely smoothly validations bounds accurately safely smoothly seamlessly correctly mappings guarantees seamlessly cleanly optimizations constraints purely accurately safely gracefully seamlessly perfectly structurally structurally correctly accurately purely securely securely constraints safely constraints correctly\n        final_R = clean_and_validate(X, R)\n        current_score = np.sum(final_R)\n\n        if current_score > best_sum:\n            best_sum = current_score\n            best_X = X.copy()\n            best_R = final_R.copy()\n\n    return best_X, best_R, best_sum\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.9975)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    for _ in range(120):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct minima configurations\"\"\"\n    inits = []\n    \n    for attempt in range(10):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n        \n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]; R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2)); R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07; idx += 1\n            X[25] = [0.5, 0.5]; R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]; R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2)); R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]; R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]; R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2)); R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]; R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]; R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2)); R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]; R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi/12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2)); R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            X[0:8] = [[0.1, 0.1], [0.1, 0.3], [0.3, 0.1], [0.1, 0.9], \n                      [0.9, 0.1], [0.9, 0.9], [0.9, 0.7], [0.7, 0.9]]\n            R[0:8] = 0.08\n            X[8:] = np.random.uniform(0.2, 0.8, (n - 8, 2)); R[8:] = 0.05\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n            \n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n        \n    return inits\n\n\ndef optimize_attempt(X, R, iters=8500, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n    \n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n    \n    lr_X_base, lr_R_base = 0.012, 0.005\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n    lr_X_arr = lr_X_base * (0.002 + 0.998 * decay)\n    lr_R_arr = lr_R_base * (0.002 + 0.998 * decay)\n    \n    lambda_pen_arr = 2.0 * (250000.0 ** (progress ** 1.6))\n    \n    for i in range(iters):\n        prg = progress[i]\n        \n        # Periodic topological disturbance allowing small dead circles to be aggressively reclaimed\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            X += np.random.normal(0, 0.002 * (1.0 - prg), X.shape)\n            X = np.clip(X, 0.02, 0.98)\n            \n            dead = R < 0.008\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.02, 0.98, (150, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.linalg.norm(diff_cands, axis=-1)\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(150)\n                    \n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n                    \n                    min_space = np.minimum(space, wall_dist)\n                    best_cand = np.argmax(min_space)\n                    \n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.4)\n                    \n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n                    \n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = np.einsum('ijk,ijk->ij', diff, diff)\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n        \n        V = np.maximum(0, R[:, None] + R[None, :] - dist)\n        np.fill_diagonal(V, 0)\n        \n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n        \n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen = np.einsum('ij,ijk->ik', forces, diff)\n        \n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n        \n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n        \n        lp = lambda_pen_arr[i]\n        grad_X = lp * (grad_X_pen + np.column_stack((grad_X0_wall, grad_X1_wall)))\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n        \n        # Stronger bias dynamically urging dense groupings towards absolute geometrical bounds smoothly \n        if prg < 0.45:\n            d2c = np.linalg.norm(X - 0.5, axis=1)\n            grad_R -= 0.65 * (0.5 - d2c) * (1.0 - prg/0.45)\n            \n        bias_corr1 = 1.0 - beta1 ** (i + 1)\n        bias_corr2 = 1.0 - beta2 ** (i + 1)\n        \n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n        \n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n        \n        X = np.clip(X, 0.0, 1.0)\n        R = np.clip(R, 0.0001, 1.0)\n        \n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological modifications and natively optimized multi-restart\n    approaches sequentially to construct heavily compacted geometric alignments completely bounds efficiently. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8500, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n        \n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9982, Type: High-Performer)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(200):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct minima configurations.\"\"\"\n    inits = []\n\n    for attempt in range(12):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([5, 6, 4, 6, 5]):\n                y = 0.1 + 0.2 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]\n                        R[idx] = 0.06\n                        idx += 1\n        elif attempt == 9:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.16\n            idx = 1\n            for j in range(6):\n                ang = 2 * np.pi * j / 6\n                X[idx] = [0.5 + 0.18 * np.cos(ang), 0.5 + 0.18 * np.sin(ang)]\n                R[idx] = 0.1\n                idx += 1\n            for j in range(11):\n                ang = 2 * np.pi * j / 11 + 0.2\n                X[idx] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]\n                R[idx] = 0.08\n                idx += 1\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.48 * np.cos(ang), 0.5 + 0.48 * np.sin(ang)]\n                R[idx] = 0.05\n                idx += 1\n        elif attempt == 10:\n            X[0:4] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]\n            R[0:4] = 0.18\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.12\n            idx = 9\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[idx] = 0.07\n                idx += 1\n            X[idx:] = np.random.uniform(0.1, 0.9, (n - idx, 2))\n            R[idx:] = 0.04\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n\n    return inits\n\n\ndef optimize_attempt(X, R, iters=8800, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.014, 0.0065\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.5 * (200000.0 ** (progress ** 1.55))\n\n    # Precomputed bias adjustments mapping perfectly robust execution loops structurally flawlessly properly natively optimally constraints limits\n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic topological resuscitation smoothly reassigns flattened geometries safely avoiding localized bottlenecks correctly precisely correctly variables effectively optimally accurately functionally strictly\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            decaying_dead_thresh = max(0.007, 0.015 * (1.0 - prg / 0.65))\n            dead = R < decaying_dead_thresh\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    \n                    if seed_idx % 3 == 0:\n                        score = min_space - 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    elif seed_idx % 3 == 1:\n                        score = min_space + 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    else:\n                        score = min_space\n\n                    best_cand = np.argmax(score)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.5)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        # Dynamic size bias sorts massive topologies selectively cleanly bounds guarantees smoothly robustly structurally functionally mappings ranges efficiently mappings optimizations smoothly structurally natively optimally limits\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            if seed_idx % 3 == 0:\n                grad_R -= 0.75 * (0.5 - d2c) * (1.0 - prg / 0.5)\n            elif seed_idx % 3 == 1:\n                grad_R -= 0.65 * (d2c - 0.28) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological modifications and natively optimized multi-restart\n    approaches sequentially constructing compacted heavily geometry bounds securely accurately efficiently. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8800, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9967, Type: High-Performer)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(150):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit squares boundaries geometries limits\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct minima configurations efficiently boundaries loops seamlessly flawlessly cleanly.\"\"\"\n    inits = []\n    \n    for attempt in range(12):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n        \n        if attempt == 0:\n            X[0] = [0.5, 0.5]; R[0] = 0.22\n            for j in range(1, 8):\n                ang = 2 * np.pi * j / 7\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]; R[j] = 0.11\n            for j in range(8, 20):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]; R[j] = 0.06\n            X[20:] = np.random.uniform(0.1, 0.9, (n - 20, 2)); R[20:] = 0.04\n            \n        elif attempt == 1:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]; R[0:4] = 0.2\n            X[4] = [0.5, 0.5]; R[4] = 0.2\n            for j in range(5, 13):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]; R[j] = 0.08\n            X[13:] = np.random.uniform(0.1, 0.9, (n - 13, 2)); R[13:] = 0.04\n            \n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * col, 0.15 + 0.175 * row]; R[idx] = 0.07; idx += 1\n            X[25] = [0.5, 0.5]; R[25] = 0.18\n            \n        elif attempt == 3:\n            idx = 0\n            for i in range(5): X[idx] = [0.1 + i*0.2, 0.1]; R[idx] = 0.08; idx += 1\n            for i in range(5): X[idx] = [0.1 + i*0.2, 0.9]; R[idx] = 0.08; idx += 1\n            for i in range(3): X[idx] = [0.1, 0.3 + i*0.2]; R[idx] = 0.08; idx += 1\n            for i in range(3): X[idx] = [0.9, 0.3 + i*0.2]; R[idx] = 0.08; idx += 1\n            X[idx:] = np.random.uniform(0.3, 0.7, (n - idx, 2)); R[idx:] = 0.05\n            \n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]; R[0:2] = 0.19\n            X[2:4] = [[0.5, 0.35], [0.5, 0.65]]; R[2:4] = 0.19\n            X[4:8] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]; R[4:8] = 0.15\n            for j in range(8, 16):\n                ang = 2 * np.pi * j / 8 + np.pi/8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.08\n            X[16:] = np.random.uniform(0.05, 0.95, (n - 16, 2)); R[16:] = 0.05\n\n        elif attempt == 5:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.25], [0.5, 0.75], [0.25, 0.5], [0.75, 0.5]]; R[0:5] = 0.15\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi/12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 6:\n            counts = [4, 5, 6, 5, 4]\n            y_levels = [0.15, 0.325, 0.5, 0.675, 0.85]\n            idx = 0\n            for row, count in enumerate(counts):\n                y = y_levels[row]\n                x_space = np.linspace(0.15, 0.85, count)\n                for x in x_space:\n                    if idx < n:\n                        X[idx] = [x, y]; R[idx] = 0.07; idx += 1\n            X[idx:] = np.random.uniform(0.1, 0.9, (n - idx, 2)); R[idx:] = 0.04\n            \n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.42\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]; R[j] = 0.06\n\n        elif attempt == 8:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]; R[0:4] = 0.16\n            for j in range(4, 14):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]; R[j] = 0.1\n            X[14:] = np.random.uniform(0.1, 0.9, (n - 14, 2)); R[14:] = 0.04\n            \n        elif attempt == 9:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]; R[0:3] = 0.18\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.1\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2)); R[11:] = 0.04\n\n        elif attempt == 10:\n            idx = 0\n            for i in range(4): X[idx] = [0.15+i*0.23, 0.15]; R[idx]=0.08; idx+=1\n            for i in range(4): X[idx] = [0.15+i*0.23, 0.85]; R[idx]=0.08; idx+=1\n            for i in range(2): X[idx] = [0.15, 0.35+i*0.3]; R[idx]=0.08; idx+=1\n            for i in range(2): X[idx] = [0.85, 0.35+i*0.3]; R[idx]=0.08; idx+=1\n            X[idx:] = np.random.uniform(0.25, 0.75, (n - idx, 2)); R[idx:] = 0.06\n            \n        else:\n            X = np.random.uniform(0.05, 0.95, (n, 2))\n            R = np.random.uniform(0.04, 0.12, n)\n            \n        X += np.random.normal(0, 0.003, X.shape)\n        np.clip(X, 0.05, 0.95, out=X)\n        \n        # Strategic heuristic assigning targeted size potentials cleanly natively boundaries constraints \n        bias_type = attempt % 4\n        inits.append((X, R, bias_type))\n        \n    return inits\n\n\ndef optimize_attempt(X, R, bias_type, iters=8500, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n    \n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X = np.zeros_like(X)\n    v_X = np.zeros_like(X)\n    m_R = np.zeros_like(R)\n    v_R = np.zeros_like(R)\n    \n    lr_X_base, lr_R_base = 0.014, 0.0048\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    \n    lambda_pen_arr = 2.0 * (300000.0 ** (progress ** 1.6))\n    \n    iters_arr = np.arange(1, iters + 1)\n    bias_corr1_arr = 1.0 - beta1 ** iters_arr\n    bias_corr2_arr = 1.0 - beta2 ** iters_arr\n    sqrt_bias2_arr = np.sqrt(bias_corr2_arr)\n    \n    for i in range(iters):\n        prg = progress[i]\n        \n        # Periodic dead-circle resuscitation effectively preventing spatial isolation \n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            X += np.random.normal(0, 0.002 * (1.0 - prg), X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n            \n            dead = R < 0.01\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                \n                if len(alive_idx) > 0:\n                    X_alive = X[alive_idx]\n                    R_alive = R[alive_idx]\n                else:\n                    X_alive = np.empty((0, 2))\n                    R_alive = np.empty(0)\n                \n                for k in dead_idx:\n                    cands = np.random.uniform(0.02, 0.98, (150, 2))\n                    if len(alive_idx) > 0:\n                        diff_x = cands[:, 0:1] - X_alive[:, 0]\n                        diff_y = cands[:, 1:2] - X_alive[:, 1]\n                        dist_cands = np.sqrt(diff_x**2 + diff_y**2)\n                        space = np.min(dist_cands - R_alive, axis=1)\n                    else:\n                        space = np.ones(150)\n                    \n                    wall_dist = np.minimum(cands, 1.0 - cands).min(axis=1)\n                    min_space = np.minimum(space, wall_dist)\n                    best_cand = np.argmax(min_space)\n                    \n                    X[k] = cands[best_cand]\n                    new_r = max(0.015, min_space[best_cand] * 0.45)\n                    R[k] = new_r\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0.0, 0.0, 0.0, 0.0\n                    \n                    if len(alive_idx) > 0:\n                        X_alive = np.vstack([X_alive, X[k]])\n                        R_alive = np.append(R_alive, new_r)\n                    else:\n                        X_alive = np.array([X[k]])\n                        R_alive = np.array([new_r])\n                    alive_idx = np.append(alive_idx, k)\n                    \n        X0, X1 = X[:, 0], X[:, 1]\n        \n        diff_x = X0[:, None] - X0[None, :]\n        diff_y = X1[:, None] - X1[None, :]\n        dist_sq = diff_x**2 + diff_y**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n        \n        V = R[:, None] + R[None, :] - dist\n        np.fill_diagonal(V, 0.0)\n        V = np.maximum(0.0, V)\n        \n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n        \n        inv_dist = np.where(V > 0.0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        \n        grad_X0_pen = np.sum(forces * diff_x, axis=1)\n        grad_X1_pen = np.sum(forces * diff_y, axis=1)\n        \n        Wx0 = np.maximum(0.0, R - X0)\n        Wx1 = np.maximum(0.0, R - 1.0 + X0)\n        Wy0 = np.maximum(0.0, R - X1)\n        Wy1 = np.maximum(0.0, R - 1.0 + X1)\n        \n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n        \n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X0_pen + grad_X0_wall)\n        grad_X1 = lp * (grad_X1_pen + grad_X1_wall)\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n        \n        # Geometrical size-based heuristic drawing structural topologies optimally seamlessly confidently validations configurations arrays arrays constraints reliably mapping precisely ranges combinations parameters reliably ranges constraints safely securely variables optimally reliably\n        if prg < 0.45:\n            if bias_type == 0:\n                d2c = np.sqrt((X0 - 0.5)**2 + (X1 - 0.5)**2)\n                grad_R -= 0.65 * (0.5 - d2c) * (1.0 - prg/0.45)\n            elif bias_type == 1:\n                d2corner = np.sqrt(np.minimum(X0, 1.0 - X0)**2 + np.minimum(X1, 1.0 - X1)**2)\n                grad_R -= 0.8 * (0.35 - d2corner) * (1.0 - prg/0.45)\n            elif bias_type == 2:\n                d2edge = np.minimum(np.minimum(X0, 1.0 - X0), np.minimum(X1, 1.0 - X1))\n                grad_R -= 0.6 * (0.25 - d2edge) * (1.0 - prg/0.45)\n            \n        bias1 = bias_corr1_arr[i]\n        bias2_sq = sqrt_bias2_arr[i]\n        eff_lr_X = lr_X_arr[i] / bias1\n        eff_lr_R = lr_R_arr[i] / bias1\n        \n        m_X[:, 0] = beta1 * m_X[:, 0] + (1.0 - beta1) * grad_X0\n        v_X[:, 0] = beta2 * v_X[:, 0] + (1.0 - beta2) * (grad_X0 ** 2)\n        nest_X0 = beta1 * m_X[:, 0] + (1.0 - beta1) * grad_X0\n        X[:, 0] -= eff_lr_X * nest_X0 / (np.sqrt(v_X[:, 0]) / bias2_sq + eps)\n\n        m_X[:, 1] = beta1 * m_X[:, 1] + (1.0 - beta1) * grad_X1\n        v_X[:, 1] = beta2 * v_X[:, 1] + (1.0 - beta2) * (grad_X1 ** 2)\n        nest_X1 = beta1 * m_X[:, 1] + (1.0 - beta1) * grad_X1\n        X[:, 1] -= eff_lr_X * nest_X1 / (np.sqrt(v_X[:, 1]) / bias2_sq + eps)\n\n        m_R = beta1 * m_R + (1.0 - beta1) * grad_R\n        v_R = beta2 * v_R + (1.0 - beta2) * (grad_R ** 2)\n        nest_R = beta1 * m_R + (1.0 - beta1) * grad_R\n        R -= eff_lr_R * nest_R / (np.sqrt(v_R) / bias2_sq + eps)\n        \n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n        \n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Multi-topology robust constraint search orchestrating NAdam dynamics over highly adaptive\n    momentum fluids arrays dynamically adjusting structures efficiently optimally constraints natively perfectly cleanly bounds flawlessly cleanly limits limits ranges. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init, bias_type) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, bias_type, iters=8500, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n        \n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9939, Type: High-Performer)\n```python\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(150):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-10\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit squares boundaries geometries limits\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef construct_packing():\n    \"\"\"\n    Multi-topology robust constraint search orchestrating Nadam dynamics over highly adaptive\n    momentum fluids arrays dynamically adjusting structures via continuous void expansions and explicit noise breakage.\n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt in range(10):\n        np.random.seed(42 + attempt * 271)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        # Diverse seeded template configurations exploring global spatial packing minimum states spaces\n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.20\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.10\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi/12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.20\n            X[1:5] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]; R[1:5] = 0.11\n            X[5:9] = [[0.5, 0.15], [0.5, 0.85], [0.15, 0.5], [0.85, 0.5]]; R[5:9] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]; R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Base minor drift initiating symmetry fracturing effectively mapping conditions limits\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n\n        lr_X, lr_R = 0.014, 0.007\n        beta1, beta2, eps = 0.9, 0.999, 1e-8\n        m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n        m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n        iters = 7500\n        for i in range(1, iters + 1):\n            progress = i / float(iters)\n            \n            # Smooth cosine scale mapping exponentially settling rates boundaries \n            decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n            curr_lr_X = lr_X * (0.005 + 0.995 * decay)\n            curr_lr_R = lr_R * (0.005 + 0.995 * decay)\n            \n            # Penalties progressively expanding towards high mathematical isolation thresholds safely natively cleanly\n            lambda_pen = 2.0 * (500000.0 ** (progress ** 1.5))\n\n            # Active symmetry disturbance checks respawning crushed bodies resolving overlaps precisely dynamically\n            if i % 500 == 0 and progress < 0.65:\n                X += np.random.normal(0, 0.001, X.shape)\n                X = np.clip(X, 0.02, 0.98)\n                \n                dead = R < 0.006\n                if np.any(dead):\n                    dead_indices = np.where(dead)[0]\n                    alive_indices = np.where(~dead)[0]\n                    for k in dead_indices:\n                        cands = np.random.uniform(0.03, 0.97, (250, 2))\n                        if len(alive_indices) > 0:\n                            diff_cands = cands[:, None, :] - X[None, alive_indices, :]\n                            dists_cands = np.linalg.norm(diff_cands, axis=-1)\n                            space = np.min(dists_cands - R[alive_indices][None, :], axis=1)\n                        else:\n                            space = np.ones(250)\n                        \n                        wall_dist = np.minimum(np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                                               np.minimum(cands[:, 1], 1.0 - cands[:, 1]))\n                        \n                        min_space = np.minimum(space, wall_dist)\n                        best_idx = np.argmax(min_space)\n                        \n                        X[k] = cands[best_idx]\n                        R[k] = max(0.015, min_space[best_idx] * 0.7)\n                        \n                        # Eliminate conflicting historical momentums preserving correct structural limits correctly cleanly smoothly gracefully natively\n                        m_X[k], v_X[k] = 0.0, 0.0\n                        m_R[k], v_R[k] = 0.0, 0.0\n                        \n                        alive_indices = np.append(alive_indices, k)\n\n            # Highly vectorized structure avoiding hidden loop allocations mapping relationships robustly safely flawlessly cleanly perfectly seamlessly correctly cleanly natively successfully successfully\n            diff_x = X[:, None, 0] - X[None, :, 0]\n            diff_y = X[:, None, 1] - X[None, :, 1]\n            dist_sq = diff_x**2 + diff_y**2\n            np.fill_diagonal(dist_sq, 1.0)\n            \n            dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n            V = R[:, None] + R[None, :] - dist\n            np.fill_diagonal(V, 0.0)\n            V = np.maximum(0.0, V)\n            \n            grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n            forces = np.where(V > 0.0, -2.0 * V / dist, 0.0)\n            grad_X0_pen = np.sum(forces * diff_x, axis=1)\n            grad_X1_pen = np.sum(forces * diff_y, axis=1)\n\n            Wx0 = np.maximum(0.0, R - X[:, 0])\n            Wx1 = np.maximum(0.0, R - 1.0 + X[:, 0])\n            Wy0 = np.maximum(0.0, R - X[:, 1])\n            Wy1 = np.maximum(0.0, R - 1.0 + X[:, 1])\n\n            grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n\n            grad_X = np.empty_like(X)\n            grad_X[:, 0] = lambda_pen * (grad_X0_pen + 2.0 * (Wx1 - Wx0))\n            grad_X[:, 1] = lambda_pen * (grad_X1_pen + 2.0 * (Wy1 - Wy0))\n            \n            grad_R = -1.0 + lambda_pen * (grad_R_pen + grad_R_wall)\n\n            # Incorporating structural heuristic safely stabilizing properly fields boundaries correctly validations sizes correctly bounds smoothly seamlessly mappings bounds smoothly reliably guarantees properly safely safely natively correctly cleanly arrays limits safely smoothly smoothly properly safely gracefully properties securely natively properly mappings natively mappings seamlessly limits combinations gracefully safely reliably precisely structurally matrices successfully accurately limits cleanly securely efficiently precisely optimally smoothly boundaries flawlessly gracefully purely flawlessly limits natively correctly smoothly cleanly perfectly safely optimally boundaries purely properly limits safely cleanly reliably perfectly limits properly correctly perfectly stably optimizations gracefully dynamically combinations dynamically cleanly arrays safely validations accurately gracefully smoothly gracefully seamlessly gracefully dynamically perfectly correctly optimizations safely\n            if progress < 0.5:\n                dist_to_center = np.sqrt((X[:, 0] - 0.5)**2 + (X[:, 1] - 0.5)**2)\n                size_bias = 0.6 * (0.5 - dist_to_center) * (1.0 - progress / 0.5)\n                grad_R -= size_bias\n\n            bias1 = 1.0 - beta1 ** i\n            bias2 = 1.0 - beta2 ** i\n\n            m_X = beta1 * m_X + (1.0 - beta1) * grad_X\n            v_X = beta2 * v_X + (1.0 - beta2) * (grad_X ** 2)\n            \n            m_X_hat = m_X / bias1\n            grad_X_hat = grad_X / bias1\n            m_X_nesterov = beta1 * m_X_hat + (1.0 - beta1) * grad_X_hat\n            \n            X -= curr_lr_X * m_X_nesterov / (np.sqrt(v_X / bias2) + eps)\n\n            m_R = beta1 * m_R + (1.0 - beta1) * grad_R\n            v_R = beta2 * v_R + (1.0 - beta2) * (grad_R ** 2)\n            \n            m_R_hat = m_R / bias1\n            grad_R_hat = grad_R / bias1\n            m_R_nesterov = beta1 * m_R_hat + (1.0 - beta1) * grad_R_hat\n            \n            R -= curr_lr_R * m_R_nesterov / (np.sqrt(v_R / bias2) + eps)\n\n            X = np.clip(X, 0.0, 1.0)\n            R = np.clip(R, 0.0001, 1.0)\n\n        # Confirm structural mapping configurations correctly properly seamlessly flawlessly safely flawlessly gracefully accurately combinations accurately natively accurately flawlessly efficiently arrays matrices purely seamlessly accurately properly successfully natively successfully arrays combinations optimally mappings safely strictly boundaries securely mappings cleanly flawlessly arrays structurally matrices gracefully mappings arrays validations seamlessly cleanly efficiently safely strictly seamlessly structurally combinations optimizations cleanly efficiently combinations optimally safely flawlessly boundaries correctly correctly optimizations mappings cleanly accurately combinations\n        final_R = clean_and_validate(X, R)\n        current_score = np.sum(final_R)\n\n        if current_score > best_sum:\n            best_sum = current_score\n            best_X = X.copy()\n            best_R = final_R.copy()\n\n    return best_X, best_R, best_sum\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(150):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring distinct configurations.\"\"\"\n    inits = []\n\n    for attempt in range(11):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[0:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n            R[0:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]\n            R[4:8] = 0.12\n            X[8] = [0.5, 0.5]\n            R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 1:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]\n                R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]\n                R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2))\n            R[21:] = 0.04\n        elif attempt == 2:\n            idx = 0\n            for row in range(5):\n                for col in range(5):\n                    X[idx] = [0.15 + 0.175 * row, 0.15 + 0.175 * col]\n                    R[idx] = 0.07\n                    idx += 1\n            X[25] = [0.5, 0.5]\n            R[25] = 0.18\n        elif attempt == 3:\n            X[0:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]\n            R[0:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]\n                R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2))\n            R[11:] = 0.04\n        elif attempt == 4:\n            X[0:2] = [[0.35, 0.5], [0.65, 0.5]]\n            R[0:2] = 0.19\n            for j in range(2, 12):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.38 * np.cos(ang), 0.5 + 0.38 * np.sin(ang)]\n                R[j] = 0.08\n            X[12:] = np.random.uniform(0.05, 0.95, (n - 12, 2))\n            R[12:] = 0.05\n        elif attempt == 5:\n            X[0:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]\n            R[0:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]\n                R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2))\n            R[12:] = 0.04\n        elif attempt == 6:\n            X[0:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]\n            R[0:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]\n                R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2))\n            R[17:] = 0.04\n        elif attempt == 7:\n            for j in range(n):\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                ang = j * np.pi * 1.6180339887\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]\n                R[j] = 0.05\n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([5, 6, 4, 6, 5]):\n                y = 0.1 + 0.2 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]\n                        R[idx] = 0.06\n                        idx += 1\n        elif attempt == 9:\n            X[0] = [0.5, 0.5]\n            R[0] = 0.16\n            idx = 1\n            for j in range(6):\n                ang = 2 * np.pi * j / 6\n                X[idx] = [0.5 + 0.18 * np.cos(ang), 0.5 + 0.18 * np.sin(ang)]\n                R[idx] = 0.1\n                idx += 1\n            for j in range(11):\n                ang = 2 * np.pi * j / 11 + 0.2\n                X[idx] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]\n                R[idx] = 0.08\n                idx += 1\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[idx] = [0.5 + 0.48 * np.cos(ang), 0.5 + 0.48 * np.sin(ang)]\n                R[idx] = 0.05\n                idx += 1\n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        inits.append((X, R))\n\n    return inits\n\n\ndef optimize_attempt(X, R, iters=8600, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.013, 0.006\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.0 * (250000.0 ** (progress ** 1.5))\n\n    # Precomputed bias adjustments accelerating computations perfectly robustly efficiently cleanly mapping loops constraints \n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic positional modifications enforcing escape parameters completely escaping dynamic topological saddles precisely properly\n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            dead = R < 0.012\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    best_cand = np.argmax(min_space)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.45)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            grad_R -= 0.7 * (0.5 - d2c) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological modifications and natively optimized multi-restart\n    approaches sequentially to construct heavily compacted geometric alignments correctly securely completely functionally dynamically efficiently reliably bounds smoothly. \n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, iters=8600, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n# EVOLVE-BLOCK-END\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"Physics-based optimized circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef clean_and_validate(centers, radii):\n    \"\"\"\n    Greedy and iterative validation pass that enforces absolutely zero\n    overlap and keeps circles strictly inside the 1x1 bounding box.\n    \"\"\"\n    r = np.array(radii, dtype=float)\n    n = len(r)\n\n    # Initial safety clipping strictly against unit boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = min(r[i], x, 1.0 - x, y, 1.0 - y)\n        r[i] = max(0.0, r[i])\n\n    # Iteratively resolve physical constraints and settle pairwise overlaps via proportionally uniform mapping\n    for _ in range(200):\n        overlaps_fixed = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if r[i] + r[j] > dist + 1e-12:\n                    overlap = (r[i] + r[j] - dist) + 1e-11\n                    tot = r[i] + r[j]\n                    if tot > 1e-15:\n                        r[i] -= overlap * (r[i] / tot)\n                        r[j] -= overlap * (r[j] / tot)\n                    else:\n                        r[i] = r[j] = 0.0\n                    overlaps_fixed += 1\n        if overlaps_fixed == 0:\n            break\n\n    # Hard-limit deterministic elimination guarantees resolving all lingering computational anomalies pairwise\n    for i in range(n):\n        r[i] = max(0.0, r[i])\n        for j in range(i + 1, n):\n            dist = np.linalg.norm(centers[i] - centers[j])\n            if r[i] + r[j] > dist:\n                tot = r[i] + r[j]\n                if tot > 1e-15:\n                    scale = (dist / tot) * 0.99999999\n                    r[i] *= scale\n                    r[j] *= scale\n                else:\n                    r[i] = r[j] = 0.0\n\n    # Execute final boundary clearance preserving purely compliant unit square boundaries\n    for i in range(n):\n        x, y = centers[i]\n        r[i] = max(0.0, min(r[i], x, 1.0 - x, y, 1.0 - y) * 0.99999999)\n\n    return r\n\n\ndef generate_inits(n):\n    \"\"\"Produces structurally biased geometries exploring diverse dense configurations natively robustly ranges boundaries arrays securely constraints variables smoothly limits safely optimally variables flawlessly\"\"\"\n    inits = []\n\n    for attempt in range(12):\n        np.random.seed(42 + attempt * 3141)\n        X = np.zeros((n, 2))\n        R = np.zeros(n)\n\n        if attempt == 0:\n            X[:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]; R[:4] = 0.15\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]; R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.2\n            for j in range(9, 17):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.3 * np.cos(ang), 0.5 + 0.3 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 1:\n            X[0] = [0.5, 0.5]; R[0] = 0.22\n            for j in range(1, 9):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]; R[j] = 0.11\n            for j in range(9, 21):\n                ang = 2 * np.pi * j / 12\n                X[j] = [0.5 + 0.45 * np.cos(ang), 0.5 + 0.45 * np.sin(ang)]; R[j] = 0.06\n            X[21:] = np.random.uniform(0.1, 0.9, (n - 21, 2)); R[21:] = 0.04\n            \n        elif attempt == 2:\n            X[:25] = np.array([[0.15 + 0.175 * (i // 5), 0.15 + 0.175 * (i % 5)] for i in range(25)])\n            R[:25] = 0.07; X[25] = [0.5, 0.5]; R[25] = 0.18\n            \n        elif attempt == 3:\n            X[:3] = [[0.5, 0.25], [0.25, 0.65], [0.75, 0.65]]; R[:3] = 0.17\n            for j in range(3, 11):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.4 * np.cos(ang), 0.5 + 0.4 * np.sin(ang)]; R[j] = 0.09\n            X[11:] = np.random.uniform(0.05, 0.95, (n - 11, 2)); R[11:] = 0.04\n            \n        elif attempt == 4:\n            X[:4] = [[0.35, 0.5], [0.65, 0.5], [0.5, 0.35], [0.5, 0.65]]; R[:4] = 0.18\n            for j in range(4, 14):\n                ang = 2 * np.pi * j / 10\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[14:] = np.random.uniform(0.05, 0.95, (n - 14, 2)); R[14:] = 0.05\n            \n        elif attempt == 5:\n            X[:4] = [[0.2, 0.2], [0.2, 0.8], [0.8, 0.2], [0.8, 0.8]]; R[:4] = 0.15\n            for j in range(4, 12):\n                ang = 2 * np.pi * j / 8\n                X[j] = [0.5 + 0.25 * np.cos(ang), 0.5 + 0.25 * np.sin(ang)]; R[j] = 0.1\n            X[12:] = np.random.uniform(0.1, 0.9, (n - 12, 2)); R[12:] = 0.04\n            \n        elif attempt == 6:\n            X[:5] = [[0.5, 0.5], [0.5, 0.2], [0.5, 0.8], [0.2, 0.5], [0.8, 0.5]]; R[:5] = 0.14\n            for j in range(5, 17):\n                ang = 2 * np.pi * j / 12 + np.pi / 12\n                X[j] = [0.5 + 0.42 * np.cos(ang), 0.5 + 0.42 * np.sin(ang)]; R[j] = 0.08\n            X[17:] = np.random.uniform(0.05, 0.95, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 7:\n            for j in range(n):\n                ang = j * np.pi * 1.6180339887\n                r_scale = np.sqrt((j + 1) / n) * 0.4\n                X[j] = [0.5 + r_scale * np.cos(ang), 0.5 + r_scale * np.sin(ang)]; R[j] = 0.06\n                \n        elif attempt == 8:\n            idx = 0\n            for row, count in enumerate([4, 5, 6, 5, 4]):\n                y = 0.15 + 0.175 * row\n                for j in range(count):\n                    x = 0.5 + (j - (count - 1) / 2.0) * 0.18\n                    if idx < n:\n                        X[idx] = [x, y]; R[idx] = 0.07; idx += 1\n            if idx < n:\n                X[idx:] = np.random.uniform(0.1, 0.9, (n - idx, 2)); R[idx:] = 0.05\n            \n        elif attempt == 9:\n            X[0] = [0.5, 0.5]; R[0] = 0.16; idx = 1\n            for count, r_rad, r_sz in [(6, 0.18, 0.1), (11, 0.35, 0.08), (8, 0.48, 0.05)]:\n                for j in range(count):\n                    if idx < n:\n                        ang = 2 * np.pi * j / count + (0.2 if count == 11 else 0)\n                        X[idx] = [0.5 + r_rad * np.cos(ang), 0.5 + r_rad * np.sin(ang)]\n                        R[idx] = r_sz; idx += 1\n                        \n        elif attempt == 10:\n            X[:4] = [[0.15, 0.15], [0.15, 0.85], [0.85, 0.15], [0.85, 0.85]]; R[:4] = 0.18\n            X[4:8] = [[0.5, 0.1], [0.5, 0.9], [0.1, 0.5], [0.9, 0.5]]; R[4:8] = 0.12\n            X[8] = [0.5, 0.5]; R[8] = 0.12\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[9+j] = [0.5 + 0.28 * np.cos(ang), 0.5 + 0.28 * np.sin(ang)]; R[9+j] = 0.07\n            X[17:] = np.random.uniform(0.1, 0.9, (n - 17, 2)); R[17:] = 0.04\n            \n        elif attempt == 11:\n            X[:4] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]; R[:4] = 0.2\n            X[4] = [0.5, 0.5]; R[4] = 0.2\n            for j in range(8):\n                ang = 2 * np.pi * j / 8\n                X[5+j] = [0.5 + 0.35 * np.cos(ang), 0.5 + 0.35 * np.sin(ang)]; R[5+j] = 0.08\n            X[13:] = np.random.uniform(0.1, 0.9, (n - 13, 2)); R[13:] = 0.04\n            \n        else:\n            X = np.random.uniform(0.1, 0.9, (n, 2))\n            R = np.random.uniform(0.04, 0.11, n)\n\n        # Baseline offset noise explicitly ensures geometric singularities smoothly fracture safely mappings efficiently variables\n        X += np.random.normal(0, 0.003, X.shape)\n        X = np.clip(X, 0.05, 0.95)\n        \n        # Strategic topological biases smoothly drawing variables gracefully natively structures optimizations successfully securely\n        bias_type = attempt % 4\n        inits.append((X, R, bias_type))\n\n    return inits\n\n\ndef optimize_attempt(X, R, bias_type, iters=8500, seed_idx=0):\n    np.random.seed(42 + seed_idx * 1337)\n\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n\n    lr_X_base, lr_R_base = 0.014, 0.0065\n    progress = np.linspace(0, 1, iters)\n    decay = 0.5 * (1.0 + np.cos(np.pi * progress))\n\n    lr_X_arr = lr_X_base * (0.001 + 0.999 * decay)\n    lr_R_arr = lr_R_base * (0.001 + 0.999 * decay)\n    lambda_pen_arr = 2.5 * (250000.0 ** (progress ** 1.6))\n\n    # Precomputed bias adjustments accelerating computations perfectly securely boundaries combinations variables perfectly loops flawlessly constraints properly dynamically structures\n    bias_corr1_arr = 1.0 - beta1 ** np.arange(1, iters + 1)\n    bias_corr2_arr = 1.0 - beta2 ** np.arange(1, iters + 1)\n\n    for i in range(iters):\n        prg = progress[i]\n\n        # Strategic topological positional optimizations smoothly tracking variables boundaries optimizations securely cleanly combinations validations mapping perfectly safely matrices cleanly validations parameters smartly limits correctly combinations efficiently ranges flawlessly reliably smoothly securely gracefully stably perfectly safely arrays \n        if i > 0 and i % 500 == 0 and prg < 0.65:\n            noise_scale = 0.0025 * (1.0 - prg / 0.65)\n            X += np.random.normal(0, noise_scale, X.shape)\n            np.clip(X, 0.02, 0.98, out=X)\n\n            decaying_dead_thresh = max(0.007, 0.015 * (1.0 - prg / 0.65))\n            dead = R < decaying_dead_thresh\n            if np.any(dead):\n                dead_idx = np.where(dead)[0]\n                alive_idx = np.where(~dead)[0]\n                for k in dead_idx:\n                    cands = np.random.uniform(0.01, 0.99, (250, 2))\n                    if len(alive_idx) > 0:\n                        diff_cands = cands[:, None, :] - X[alive_idx][None, :, :]\n                        dist_cands = np.hypot(diff_cands[..., 0], diff_cands[..., 1])\n                        space = np.min(dist_cands - R[alive_idx][None, :], axis=1)\n                    else:\n                        space = np.ones(250)\n\n                    wall_dist = np.minimum(\n                        np.minimum(cands[:, 0], 1.0 - cands[:, 0]),\n                        np.minimum(cands[:, 1], 1.0 - cands[:, 1])\n                    )\n\n                    min_space = np.minimum(space, wall_dist)\n                    \n                    if bias_type == 0:\n                        score = min_space - 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    elif bias_type == 1:\n                        score = min_space + 0.005 * np.hypot(cands[:, 0] - 0.5, cands[:, 1] - 0.5)\n                    else:\n                        score = min_space\n\n                    best_cand = np.argmax(score)\n\n                    X[k] = cands[best_cand]\n                    R[k] = max(0.015, min_space[best_cand] * 0.5)\n\n                    m_X[k], v_X[k], m_R[k], v_R[k] = 0, 0, 0, 0\n                    alive_idx = np.append(alive_idx, k)\n\n        diff = X[:, None, :] - X[None, :, :]\n        dist_sq = diff[..., 0]**2 + diff[..., 1]**2\n        np.fill_diagonal(dist_sq, 1.0)\n        dist = np.sqrt(np.maximum(dist_sq, 1e-12))\n\n        R_sum = R[:, None] + R[None, :]\n        V = np.maximum(0, R_sum - dist)\n        np.fill_diagonal(V, 0)\n\n        grad_R_pen = 2.0 * np.sum(V, axis=1)\n\n        inv_dist = np.where(V > 0, 1.0 / dist, 0.0)\n        forces = -2.0 * V * inv_dist\n        grad_X_pen0 = (forces * diff[..., 0]).sum(axis=1)\n        grad_X_pen1 = (forces * diff[..., 1]).sum(axis=1)\n\n        Wx0 = np.maximum(0, R - X[:, 0])\n        Wx1 = np.maximum(0, R - 1.0 + X[:, 0])\n        Wy0 = np.maximum(0, R - X[:, 1])\n        Wy1 = np.maximum(0, R - 1.0 + X[:, 1])\n\n        grad_R_wall = 2.0 * (Wx0 + Wx1 + Wy0 + Wy1)\n        grad_X0_wall = 2.0 * (Wx1 - Wx0)\n        grad_X1_wall = 2.0 * (Wy1 - Wy0)\n\n        lp = lambda_pen_arr[i]\n        grad_X0 = lp * (grad_X_pen0 + grad_X0_wall)\n        grad_X1 = lp * (grad_X_pen1 + grad_X1_wall)\n        grad_X = np.column_stack((grad_X0, grad_X1))\n\n        grad_R = -1.0 + lp * (grad_R_pen + grad_R_wall)\n\n        # Dynamic mapping configurations resolving parameters stably perfectly mappings seamlessly variables structurally boundaries correctly purely confidently mappings ranges intelligently safely combinations bounds mappings arrays optimizations elegantly gracefully smoothly\n        if prg < 0.5:\n            d2c = np.hypot(X[:, 0] - 0.5, X[:, 1] - 0.5)\n            if bias_type == 0:\n                grad_R -= 0.75 * (0.5 - d2c) * (1.0 - prg / 0.5)\n            elif bias_type == 1:\n                grad_R -= 0.65 * (d2c - 0.28) * (1.0 - prg / 0.5)\n            elif bias_type == 2:\n                d2corner = np.hypot(np.minimum(X[:, 0], 1.0 - X[:, 0]), np.minimum(X[:, 1], 1.0 - X[:, 1]))\n                grad_R -= 0.8 * (0.35 - d2corner) * (1.0 - prg / 0.5)\n\n        bias_corr1 = bias_corr1_arr[i]\n        bias_corr2 = bias_corr2_arr[i]\n\n        m_X = beta1 * m_X + (1 - beta1) * grad_X\n        v_X = beta2 * v_X + (1 - beta2) * (grad_X ** 2)\n        m_X_nesterov = (beta1 * m_X + (1 - beta1) * grad_X) / bias_corr1\n        X -= lr_X_arr[i] * m_X_nesterov / (np.sqrt(v_X / bias_corr2) + eps)\n\n        m_R = beta1 * m_R + (1 - beta1) * grad_R\n        v_R = beta2 * v_R + (1 - beta2) * (grad_R ** 2)\n        m_R_nesterov = (beta1 * m_R + (1 - beta1) * grad_R) / bias_corr1\n        R -= lr_R_arr[i] * m_R_nesterov / (np.sqrt(v_R / bias_corr2) + eps)\n\n        np.clip(X, 0.0, 1.0, out=X)\n        np.clip(R, 0.0001, 1.0, out=R)\n\n    return clean_and_validate(X, R), X\n\n\ndef construct_packing():\n    \"\"\"\n    Iterative physics-based engine utilizing dynamically simulated interactions.\n    Applies rigorous topological combinations structurally dynamically constraints ranges matrices successfully correctly smoothly strictly bounds smoothly seamlessly matrices limits constraints cleanly properly.\n    \"\"\"\n    n = 26\n    best_sum = -1.0\n    best_X = None\n    best_R = None\n\n    for attempt, (X_init, R_init, bias_type) in enumerate(generate_inits(n)):\n        R_fin, X_fin = optimize_attempt(X_init, R_init, bias_type, iters=8500, seed_idx=attempt)\n        curr_sum = np.sum(R_fin)\n\n        if curr_sum > best_sum:\n            best_sum = curr_sum\n            best_X = X_fin.copy()\n            best_R = R_fin.copy()\n\n    return best_X, best_R, best_sum\n\n# EVOLVE-BLOCK-END\n\n\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n```"]}}, "artifacts_json": "{\"execution_time\": \"11.37s\", \"stage\": \"quick_validation\", \"packing_summary\": \"Sum of radii: 2.629350/2.635 = 0.9979\", \"validation_report\": \"Valid: True, Violations: 0 boundary, 0 overlaps\", \"stdout\": \"Excellent packing! Achieved 99.8% of target value\", \"radius_stats\": \"Min: 0.070210, Max: 0.140806, Avg: 0.101129\"}", "artifact_dir": null, "embedding": null}