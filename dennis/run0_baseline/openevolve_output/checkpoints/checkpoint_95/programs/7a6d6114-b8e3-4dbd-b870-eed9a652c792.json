{"id": "7a6d6114-b8e3-4dbd-b870-eed9a652c792", "code": "# EVOLVE-BLOCK-START\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a Fibonacci Lattice (Golden Spiral) initialization \nfollowed by a force-directed relaxation to maximize minimum separation, \nand finally solves for the maximum sum of radii using coordinate descent.\n\"\"\"\nimport numpy as np\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximizes the sum of radii for fixed centers using iterative coordinate descent.\n    Constraints: \n      1) r_i + r_j <= distance(c_i, c_j) for all i, j\n      2) r_i <= distance(c_i, boundary) for all i\n    Starting with a balanced initial radius helps the greedy solver find a higher total sum.\n    \"\"\"\n    n = centers.shape[0]\n    # Calculate pairwise distances between all centers\n    dists = np.linalg.norm(centers[:, np.newaxis, :] - centers[np.newaxis, :, :], axis=2)\n    # Set diagonal to a large value to ignore self-distance in the minimization\n    np.fill_diagonal(dists, 1e6)\n    \n    # Calculate distance to the four boundaries of the unit square\n    b_limit = np.min(np.hstack([centers, 1.0 - centers]), axis=1)\n    \n    # Balanced initialization (0.05 is approx half the target radius for n=26)\n    radii = np.full(n, 0.05)\n    \n    # Coordinate descent iterations\n    for _ in range(250):\n        for i in range(n):\n            # The radius of circle i is limited by its neighbors: r_i <= dist(i, j) - r_j\n            neighbor_limit = np.min(dists[i] - radii)\n            # Combine neighbor limit with boundary limit\n            radii[i] = max(0.0, min(b_limit[i], neighbor_limit))\n            \n    return radii\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Strategy:\n    1. Fibonacci Spiral Initialization: Distributes points uniformly in a square domain.\n    2. Force-Directed Relaxation: Pushes circles apart using a cooling repulsive force.\n    3. Radius Optimization: Finalizes radii to maximize the total sum for the fixed centers.\n    \"\"\"\n    n = 26\n    np.random.seed(42)\n    \n    # 1. Initialization: Fibonacci Lattice (Golden Spiral mapping)\n    phi = (1 + 5**0.5) / 2\n    indices = np.arange(n)\n    # The golden angle 2*pi / phi^2 provides optimal density for circular spirals\n    theta = 2 * np.pi * indices / (phi**2)\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    # Map the spiral into the central region of the unit square\n    centers = 0.5 + 0.48 * r_vals[:, np.newaxis] * np.column_stack([np.cos(theta), np.sin(theta)])\n    \n    # 2. Force-Directed Relaxation\n    # Target diameter slightly larger than the optimal equal-circle diameter (~0.2027)\n    target_d = 0.203\n    for i in range(1600):\n        # Linear cooling schedule for the simulation step size\n        dt = 0.03 * (1.0 - i / 1600)\n        \n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.linalg.norm(diffs, axis=2) + 1e-12\n        np.fill_diagonal(dists, 1.0)\n        \n        # Calculate repulsion forces proportional to overlap depth\n        overlap = np.maximum(0, target_d - dists)\n        forces = np.sum(diffs * (overlap / dists)[:, :, np.newaxis], axis=1)\n        \n        # Boundary wall repulsion: pushes centers toward a margin of target_d/2\n        m = target_d / 2\n        forces[:, 0] += np.maximum(0, m - centers[:, 0]) - np.maximum(0, centers[:, 0] - (1.0 - m))\n        forces[:, 1] += np.maximum(0, m - centers[:, 1]) - np.maximum(0, centers[:, 1] - (1.0 - m))\n        \n        # Update center positions and strictly clip to unit square\n        centers = np.clip(centers + forces * dt, 0.0, 1.0)\n        \n    # 3. Radius Optimization\n    # Solve for the set of radii that maximize the sum for these refined coordinates.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator.\"\"\"\n    return construct_packing()\n\n# EVOLVE-BLOCK-END", "changes_description": null, "language": "python", "parent_id": "1e9fa8af-3703-4bcd-a4e6-b2accd4b74c1", "generation": 4, "timestamp": 1771624611.98585, "iteration_found": 75, "metrics": {"validity": 1.0, "sum_radii": 2.13678390582788, "target_ratio": 0.8109236834261405, "combined_score": 0.8109236834261405, "radius_variance": 0.013088711227051627, "spatial_spread": 0.17997397748078162, "eval_time": 0.2581307888031006}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 1.0, "sum_radii": 1.7331731729131474, "target_ratio": 0.6577507297583103, "combined_score": 0.6577507297583103, "radius_variance": 0.04734396141562881, "spatial_spread": 0.18437100537985876, "eval_time": 0.26618170738220215}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.\n\nKey geometric insights:\n- Circle packings often follow hexagonal patterns in the densest regions\n- Maximum density for infinite circle packing is pi/(2*sqrt(3)) \u2248 0.9069\n- Edge effects make square container packing harder than infinite packing\n- Circles can be placed in layers or shells when confined to a square\n- Similar radius circles often form regular patterns, while varied radii allow better space utilization\n- Perfect symmetry may not yield the optimal packing due to edge effects\n\nFocus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.\n", "user": "# Current Program Information\n- Fitness: 0.6578\n- Feature coordinates: \n- Focus areas: - Fitness declined: 0.8306 \u2192 0.6578. Consider revising recent changes.\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### execution_time\n```\n0.27s\n```\n\n### stage\n```\nquick_validation\n```\n\n### packing_summary\n```\nSum of radii: 1.733173/2.635 = 0.6578\n```\n\n### validation_report\n```\nValid: True, Violations: 0 boundary, 0 overlaps\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.1885, target_ratio: 0.8306, combined_score: 0.8306, radius_variance: 0.0173, spatial_spread: 0.1908, eval_time: 0.6214\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2495, target_ratio: 0.8537, combined_score: 0.8537, radius_variance: 0.0083, spatial_spread: 0.1882, eval_time: 0.1766\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.2545, target_ratio: 0.8556, combined_score: 0.8556, radius_variance: 0.0081, spatial_spread: 0.1843, eval_time: 0.2394\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.8556)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Construct an arrangement of 26 circles in a unit square aiming to maximize the sum of radii.\n    The target sum for n=26 is approximately 2.635, which corresponds to an average radius\n    of ~0.10135. This is achieved through a specific hexagonal-like arrangement \n    found via force-directed relaxation starting from a Fibonacci lattice.\n    \"\"\"\n    n = 26\n    np.random.seed(42)  # For reproducibility\n\n    # 1. Initialization using a Golden Spiral (Fibonacci Lattice)\n    # This provides a dense, near-uniform distribution that serves as a \n    # superior starting point for square packing compared to a simple grid.\n    phi = (1 + np.sqrt(5)) / 2\n    indices = np.arange(n)\n    theta = 2 * np.pi * indices / (phi ** 2)\n    # Spreading points according to area density\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    centers = np.zeros((n, 2))\n    # Map the circular spiral distribution into the unit square\n    centers[:, 0] = 0.5 + 0.49 * r_vals * np.cos(theta)\n    centers[:, 1] = 0.5 + 0.49 * r_vals * np.sin(theta)\n\n    # Add a tiny amount of noise to break potential symmetry traps during relaxation\n    centers += (np.random.rand(n, 2) - 0.5) * 1e-3\n\n    # 2. Force-Directed Relaxation (Physics-based refinement)\n    # Target diameter derived from the known optimal packing for n=26 equal circles.\n    target_dist = 0.202692  # 2 * 0.101346\n    dt = 0.02\n    \n    for i in range(1500):\n        # Calculate pairwise differences and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.sqrt(np.sum(diffs**2, axis=-1)) + 1e-12\n        np.fill_diagonal(dists, 1.0)\n        \n        # Calculate repulsion forces for overlapping circles\n        overlap = np.maximum(0, target_dist - dists)\n        # Force magnitude proportional to overlap\n        force_mag = overlap / dists\n        forces = np.sum(diffs * force_mag[:, :, np.newaxis], axis=1)\n        \n        # Update center positions\n        centers += dt * forces\n        \n        # Boundary constraints: Keep centers at least target_radius away from the edge\n        # This prevents circles from being squeezed out and losing their radius.\n        target_r = target_dist / 2.0\n        centers = np.clip(centers, target_r, 1.0 - target_r)\n        \n        # Optional: very slight step size decay for convergence stability\n        if i > 1000:\n            dt *= 0.995\n\n    # 3. Radius Optimization (Maximize Sum of Radii)\n    # Once centers are placed, the problem of maximizing the sum of radii is a \n    # Linear Programming problem. Coordinate descent is an effective way to solve it.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Calculates the maximum possible radii for fixed centers by solving \n    the optimization problem: Maximize \u03a3 r_i subject to:\n    1) r_i + r_j <= distance(c_i, c_j) for all i, j\n    2) r_i <= distance(c_i, boundary) for all i\n    \"\"\"\n    n = centers.shape[0]\n    # Pairwise distance matrix\n    dists = np.sqrt(np.sum((centers[:, np.newaxis, :] - centers[np.newaxis, :, :])**2, axis=-1))\n    np.fill_diagonal(dists, 1e6)  # Ignore self-distance\n    \n    # Pre-calculate boundary constraints for each center\n    b_dist = np.min(np.hstack([centers, 1 - centers]), axis=1)\n    \n    # Iterative coordinate descent to maximize the sum\n    # Initializing with a small radius allows for a balanced growth\n    radii = np.ones(n) * 0.05\n    for _ in range(300):\n        for i in range(n):\n            # The radius of circle i is limited by its nearest neighbor and the boundary\n            r_limit_neighbors = np.min(dists[i] - radii)\n            radii[i] = max(0.0, min(b_dist[i], r_limit_neighbors))\n            \n    return radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Total sum of radii: {s:.6f}\")\n    print(f\"Average radius: {s/26:.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2545), Performs well on target_ratio (0.8556), Performs well on combined_score (0.8556), Performs well on radius_variance (0.0081), Performs well on spatial_spread (0.1843), Performs well on eval_time (0.2394)\n\n### Program 2 (Score: 0.8537)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    using a relaxation-based constructor followed by a greedy radius optimization.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2)\n        radii: np.array of shape (26)\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # 1. Initialize with a 5x5 grid plus one circle slightly offset from the center\n    # This starting point breaks symmetry and provides a good dense foundation.\n    grid_points = np.linspace(0.1, 0.9, 5)\n    centers = np.array([[x, y] for x in grid_points for y in grid_points] + [[0.51, 0.51]])\n    \n    # 2. Force-directed relaxation (repulsion)\n    # This pushes circles apart to maximize the minimum distance between them.\n    # We use a target diameter slightly over 0.2 to encourage radii ~0.1.\n    for _ in range(800):\n        # Compute pairwise distance vectors\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        # Compute distances (adding epsilon to avoid division by zero)\n        dists = np.sqrt(np.sum(diffs**2, axis=-1)) + 1e-10\n        \n        # Calculate repulsion: push only if circles are within the target distance\n        # Target distance 0.203 is chosen based on n=26 optimal packing studies\n        overlap = np.maximum(0, 0.203 - dists)\n        forces = np.sum(diffs * (overlap[:, :, np.newaxis] / dists[:, :, np.newaxis]), axis=1)\n        \n        # Update centers and keep them within the unit square\n        centers += forces * 0.05\n        centers = np.clip(centers, 0.01, 0.99)\n    \n    # 3. Compute maximum radii for the final positions\n    # We use a coordinate descent approach to maximize the sum of radii.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Greedy coordinate descent to maximize the sum of radii for fixed centers.\n    Linear objective (sum) subject to r_i + r_j <= d_ij and boundary constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # Pre-calculate pairwise distances\n    dists = np.sqrt(np.sum((centers[:, np.newaxis, :] - centers[np.newaxis, :, :])**2, axis=-1))\n    np.fill_diagonal(dists, 1e6) # Distance to self is irrelevant\n    \n    # Initialize radii at zero\n    radii = np.zeros(n)\n    \n    # Iterate to converge on a set of radii that maximize the sum\n    # Updating one radius at a time given current others\n    for _ in range(150):\n        for i in range(n):\n            # Limit by boundary distance\n            r_bound = min(centers[i, 0], centers[i, 1], 1 - centers[i, 0], 1 - centers[i, 1])\n            # Limit by all other neighbors: r_i <= d_ij - r_j\n            r_neighbors = np.min(dists[i] - radii)\n            # Update to the largest possible valid value\n            radii[i] = max(0.0, min(r_bound, r_neighbors))\n            \n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# Fixed helper functions\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"Visualize the circle packing\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5, color='blue')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Target AlphaEvolve sum: 2.635\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.2495), Performs well on target_ratio (0.8537), Performs well on combined_score (0.8537), Performs well on radius_variance (0.0083), Performs well on spatial_spread (0.1882), Performs well on eval_time (0.1766)\n\n### Program 3 (Score: 0.8306)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a multi-start strategy with two distinct initializations:\n1. Fibonacci Lattice (Golden Spiral) for uniform, non-grid distribution.\n2. Staggered Hexagonal Rows for dense geometric packing.\nPositions are refined via force-directed relaxation with a cooling schedule,\nand radii are optimized using iterative coordinate descent to maximize their sum.\n\"\"\"\nimport numpy as np\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximizes the sum of radii for a fixed set of centers using iterative coordinate descent.\n    Constraints: \n      1) r_i + r_j <= distance(c_i, c_j) for all i, j\n      2) r_i <= distance(c_i, boundary) for all i\n    This is a Linear Program (LP), and Gauss-Seidel updates converge to a valid maximum.\n    \"\"\"\n    n = centers.shape[0]\n    # Pairwise distance matrix between centers\n    dists = np.sqrt(np.sum((centers[:, None, :] - centers[None, :, :])**2, axis=2))\n    np.fill_diagonal(dists, 1e6)\n    # Distance to boundaries: min(x, y, 1-x, 1-y)\n    b_dist = np.min(np.hstack([centers, 1 - centers]), axis=1)\n    \n    best_sum, best_r = -1, None\n    # Use different update orders to improve the chance of hitting a better sum\n    for seed in [0, 42, 123]:\n        rng = np.random.default_rng(seed)\n        radii, order = np.zeros(n), rng.permutation(n)\n        for _ in range(150):\n            for i in order:\n                # r_i <= dist(i, j) - r_j for all j\n                r_neighbor = np.min(dists[i] - radii)\n                # Combine neighbor limit with boundary limit\n                radii[i] = max(0.0, min(b_dist[i], r_neighbor))\n        \n        cur_sum = np.sum(radii)\n        if cur_sum > best_sum:\n            best_sum, best_r = cur_sum, radii.copy()\n    return best_r\n\ndef relax(centers, target_d, iterations=1500):\n    \"\"\"\n    Refines center positions through a force-directed (repulsive) simulation.\n    Includes boundary forces and a linear cooling schedule for step size.\n    \"\"\"\n    n = len(centers)\n    rng = np.random.default_rng(42)\n    # Subtle jitter to break potential symmetries\n    centers += rng.standard_normal(centers.shape) * 1e-4\n    \n    for i in range(iterations):\n        # Linearly cooling step size (dt)\n        dt = 0.04 * (1.0 - i / iterations)\n        \n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, None, :] - centers[None, :, :]\n        dists = np.sqrt(np.sum(diffs**2, axis=2)) + 1e-9\n        np.fill_diagonal(dists, 1.0)\n        \n        # Repulsive force if circles are closer than the target diameter\n        overlap = np.maximum(0, target_d - dists)\n        f_mag = overlap / dists\n        forces = np.sum(diffs * f_mag[:, :, None], axis=1)\n        \n        # Repulsion from the four square boundaries\n        margin = target_d / 2\n        b_forces = np.zeros_like(centers)\n        b_forces[:, 0] += np.maximum(0, margin - centers[:, 0])\n        b_forces[:, 0] -= np.maximum(0, centers[:, 0] - (1.0 - margin))\n        b_forces[:, 1] += np.maximum(0, margin - centers[:, 1])\n        b_forces[:, 1] -= np.maximum(0, centers[:, 1] - (1.0 - margin))\n        \n        # Update centers and constrain them to the unit square\n        centers += (forces + b_forces) * dt\n        centers = np.clip(centers, 0, 1)\n        \n    return centers\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Strategy: Try multiple initialization strategies, relax them, and pick the best.\n    Target sum for n=26 is ~2.635.\n    \"\"\"\n    n = 26\n    candidates = []\n    \n    # Initialization A: Golden Spiral (Fibonacci Lattice)\n    phi = (1 + 5**0.5) / 2\n    indices = np.arange(n)\n    theta = 2 * np.pi * indices / (phi ** 2)\n    rv = np.sqrt(indices + 0.5) / np.sqrt(n)\n    c_fib = 0.5 + 0.47 * np.column_stack([rv * np.cos(theta), rv * np.sin(theta)])\n    candidates.append((c_fib, 0.203))\n    \n    # Initialization B: Staggered Hexagonal Rows (5-4-5-4-5-3 distribution)\n    c_hex = []\n    row_counts = [5, 4, 5, 4, 5, 3]\n    for i, count in enumerate(row_counts):\n        # Distribute rows vertically (y) and shift x for staggering\n        y = 0.1 + i * 0.17\n        x_start = 0.1 if i % 2 == 0 else 0.2\n        for j in range(count):\n            c_hex.append([x_start + j * 0.2, y])\n    candidates.append((np.array(c_hex), 0.205))\n    \n    # Initialization C: Modified 5x5 Grid\n    x_grid = np.linspace(0.1, 0.9, 5)\n    xv, yv = np.meshgrid(x_grid, x_grid)\n    c_grid = np.vstack([np.column_stack([xv.ravel(), yv.ravel()]), [0.5, 0.5]])\n    candidates.append((c_grid, 0.207))\n\n    best_centers, best_radii, best_sum = None, None, -1\n    \n    for c_init, t_d in candidates:\n        # Perform physical relaxation\n        c_relaxed = relax(c_init, t_d)\n        # Find optimal radii for the resulting positions\n        radii = compute_max_radii(c_relaxed)\n        total_sum = np.sum(radii)\n        \n        if total_sum > best_sum:\n            best_sum = total_sum\n            best_centers = c_relaxed\n            best_radii = radii\n            \n    return best_centers, best_radii, best_sum\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n# EVOLVE-BLOCK-END\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Total sum of radii: {s:.6f}\")\n    print(f\"Average radius: {s/len(r):.6f}\")\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.1885), Performs well on target_ratio (0.8306), Performs well on combined_score (0.8306), Performs well on radius_variance (0.0173), Performs well on spatial_spread (0.1908), Performs well on eval_time (0.6214)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8265)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Greedy coordinate descent to maximize the sum of radii for fixed centers.\n    Linear objective (sum) subject to r_i + r_j <= d_ij and boundary constraints.\n    \"\"\"\n    n = centers.shape[0]\n    # Pre-calculate pairwise distances\n    dist_mat = np.sqrt(np.sum((centers[:, np.newaxis, :] - centers[np.newaxis, :, :])**2, axis=2))\n    # Distance to self is not a constraint\n    np.fill_diagonal(dist_mat, 10.0)\n    \n    # Initialize radii\n    radii = np.zeros(n)\n    \n    # Iterate to converge on a set of radii that maximize the sum\n    # Updating one radius at a time given current others\n    for _ in range(250):\n        for i in range(n):\n            # Limit by distance to the four boundaries of the unit square\n            r_bound = min(centers[i, 0], centers[i, 1], 1.0 - centers[i, 0], 1.0 - centers[i, 1])\n            # Limit by distance to all other circles: r_i <= d_ij - r_j\n            # This is efficient using NumPy's min over the distance matrix row\n            r_neighbor = np.min(dist_mat[i] - radii)\n            # Update to the largest possible valid value\n            radii[i] = max(0.0, min(r_bound, r_neighbor))\n            \n    return radii\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a dense arrangement of 26 circles in a unit square using \n    force-directed relaxation followed by a coordinate-descent radius optimizer.\n    \n    The strategy involves starting from a 5x5 grid (optimal for n=25) \n    and inserting a 26th circle, then allowing the system to relax into a \n    more efficient configuration that moves beyond simple square grid packing.\n    \"\"\"\n    n = 26\n    \n    # 1. Initialize with a 5x5 grid plus a 26th circle in one of the gaps\n    grid_coords = np.linspace(0.1, 0.9, 5)\n    centers = np.array([[x, y] for x in grid_coords for y in grid_coords] + [[0.2, 0.2]], dtype=float)\n    \n    # Seed for reproducibility and apply small jitter to break grid symmetry\n    np.random.seed(42)\n    centers += np.random.uniform(-0.01, 0.01, size=centers.shape)\n    \n    # 2. Force-directed relaxation (Repulsion)\n    # We use a target distance that encourages radii slightly above 0.1 (diameter 0.2)\n    target_dist = 0.204\n    iterations = 1000\n    \n    for i in range(iterations):\n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.sqrt(np.sum(diffs**2, axis=-1)) + 1e-9\n        \n        # Calculate repulsion: push only if circles are within the target distance\n        overlap = np.maximum(0, target_dist - dists)\n        np.fill_diagonal(overlap, 0)\n        \n        # Aggregate forces on each circle and update positions\n        # Cooling schedule applied to step size to ensure convergence\n        eta = 0.05 * (1.0 - i / iterations)\n        repulsion_forces = np.sum(overlap[:, :, np.newaxis] * (diffs / dists[:, :, np.newaxis]), axis=1)\n        centers += repulsion_forces * eta\n        \n        # Boundary wall repulsion: pushes circles away from the edges\n        tr = target_dist / 2\n        centers[:, 0] += np.maximum(0, tr - centers[:, 0]) * eta\n        centers[:, 0] -= np.maximum(0, centers[:, 0] - (1.0 - tr)) * eta\n        centers[:, 1] += np.maximum(0, tr - centers[:, 1]) * eta\n        centers[:, 1] -= np.maximum(0, centers[:, 1] - (1.0 - tr)) * eta\n        \n        # Enforce boundary constraints (keep center inside the unit square)\n        centers = np.clip(centers, 0, 1)\n    \n    # 3. Radius Optimization (Coordinate Descent)\n    # Solve for the maximum possible radii given the final center positions.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# Fixed helper functions\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"Visualize the circle packing\"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5, color='blue', ec='black')\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\", fontsize=8)\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Target AlphaEvolve sum for n=26: 2.635\n    # visualize(centers, radii)\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n### Program D2 (Score: 0.7388)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Solves for the maximum possible radii for a fixed set of centers by maximizing\n    the sum of radii subject to non-overlap and boundary constraints.\n    Uses iterative coordinate descent (Gauss-Seidel approach).\n    \"\"\"\n    n = centers.shape[0]\n    # Pairwise distance matrix between all centers\n    dists = np.linalg.norm(centers[:, np.newaxis, :] - centers[np.newaxis, :, :], axis=2)\n    # Ignore self-distance constraints by setting diagonal to a large value\n    np.fill_diagonal(dists, 1e6)\n    \n    # Distance from each center to the nearest square boundary\n    # Constraints: r_i <= x_i, r_i <= y_i, r_i <= 1-x_i, r_i <= 1-y_i\n    b_dist = np.min(np.hstack([centers, 1.0 - centers]), axis=1)\n    \n    # Initialize radii with a small value to ensure a valid starting point\n    radii = np.zeros(n)\n    \n    # Iteratively update each radius to its maximum valid value given current neighbors\n    # This converges to a local (and for this LP, global) maximum for the sum of radii.\n    for _ in range(400):\n        for i in range(n):\n            # Constraint: r_i + r_j <= distance(i, j) => r_i <= distance(i, j) - r_j\n            r_neighbor_limit = np.min(dists[i] - radii)\n            # Combine neighbor and boundary constraints\n            radii[i] = max(0.0, min(b_dist[i], r_neighbor_limit))\n            \n    return radii\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Strategy:\n    1. Initialize centers using a Golden Spiral (Fibonacci Lattice) for uniform density.\n    2. Refine positions via force-directed relaxation to maximize minimum spacing.\n    3. Optimize individual radii to maximize the total sum.\n    \"\"\"\n    n = 26\n    np.random.seed(42) # Ensure deterministic output for the constructor\n\n    # 1. Initialization: Golden Spiral (Fibonacci Lattice)\n    # Provides an excellent density foundation for non-grid circle counts.\n    phi = (1 + 5**0.5) / 2\n    indices = np.arange(n)\n    theta = 2 * np.pi * indices / (phi**2) # Golden angle\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    # Map the spiral into the central region of the unit square\n    centers = 0.5 + 0.47 * np.column_stack([r_vals * np.cos(theta), r_vals * np.sin(theta)])\n    # Subtle jitter to break potential symmetry traps during physical relaxation\n    centers += (np.random.rand(n, 2) - 0.5) * 1e-3\n\n    # 2. Force-Directed Relaxation\n    # We apply a repulsive force to push circles away from each other and toward boundaries.\n    # target_d is chosen based on the known equal-circle optimal packing for n=26.\n    target_d = 0.2027 \n    dt = 0.02 # Euler integration step size\n    \n    for i in range(1500):\n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.linalg.norm(diffs, axis=2) + 1e-12\n        np.fill_diagonal(dists, 1.0)\n        \n        # Repulsive force is proportional to the depth of overlap\n        overlap = np.maximum(0, target_d - dists)\n        force_mag = overlap / dists\n        forces = np.sum(diffs * force_mag[:, :, np.newaxis], axis=1)\n        \n        # Update center positions\n        centers += dt * forces\n        \n        # Constrain centers to keep circles within the boundary\n        # A margin of target_d/2 encourages the formation of a dense shell.\n        margin = target_d / 2.0\n        centers = np.clip(centers, margin, 1.0 - margin)\n        \n        # Annealing: Reduce step size in later iterations for stable convergence\n        if i > 1000:\n            dt *= 0.992\n\n    # 3. Radius Optimization\n    # Finalize by maximizing the sum of radii for the produced center coordinates.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator.\"\"\"\n    return construct_packing()\n\n# EVOLVE-BLOCK-END\n\nif __name__ == \"__main__\":\n    c, r, s = run_packing()\n    print(f\"Total sum of radii: {s:.6f}\")\n    print(f\"Average radius: {s/26:.6f}\")\n```\nKey features: Alternative approach to validity, Alternative approach to sum_radii\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii by using a grid-based \n    constructor with an additional circle in a natural gap.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Create a 5x5 grid as the primary structure (25 circles)\n    # Spacing of 0.2 provides room for radii of approximately 0.1\n    grid_coords = [0.1, 0.3, 0.5, 0.7, 0.9]\n    centers_list = [[x, y] for x in grid_coords for y in grid_coords]\n    \n    # Add the 26th circle into one of the largest gaps in the 5x5 grid\n    # A gap exists at the center of any 2x2 group of grid circles\n    centers_list.append([0.2, 0.2])\n    \n    centers = np.array(centers_list)\n\n    # Compute maximum valid radii for this configuration\n    # To maximize the sum of radii (a linear objective) subject to linear constraints:\n    # r_i + r_j <= distance(c_i, c_j) and r_i <= distance(c_i, boundaries).\n    # We use a coordinate descent approach to grow the radii greedily.\n    radii = np.zeros(n)\n    \n    # Run coordinate descent iterations to converge on an optimal set of radii\n    # for these fixed centers.\n    for _ in range(150):\n        for i in range(n):\n            # Limit by distance to the four boundaries of the unit square\n            r_max = min(centers[i, 0], centers[i, 1], 1.0 - centers[i, 0], 1.0 - centers[i, 1])\n            \n            # Limit by distance to all other circles\n            for j in range(n):\n                if i != j:\n                    dist = np.sqrt(np.sum((centers[i] - centers[j])**2))\n                    r_max = min(r_max, dist - radii[j])\n            \n            # Update radius for circle i; ensure it remains non-negative\n            radii[i] = max(0.0, r_max)\n\n    # Calculate the total sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.7239, Type: Alternative)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a Fibonacci Lattice initialization for optimal density distribution,\nfollowed by a force-directed relaxation to refine positions, and finally solves for \nthe maximum possible sum of radii using iterative coordinate descent.\nTarget sum for n=26 is ~2.635.\n\"\"\"\nimport numpy as np\n\ndef construct_packing():\n    \"\"\"\n    Main constructor for the 26-circle packing.\n    Returns (centers, radii, sum_radii).\n    \"\"\"\n    n = 26\n    np.random.seed(42)  # For deterministic behavior\n\n    # 1. Initialization: Fibonacci Lattice (Golden Spiral)\n    # This provides a more uniform distribution than a standard grid for n=26.\n    phi = (1 + np.sqrt(5)) / 2\n    indices = np.arange(n)\n    theta = 2 * np.pi * indices / (phi ** 2)\n    # Distribute points radially to cover the square area\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    centers = np.zeros((n, 2))\n    # Map the spiral into the unit square central region\n    centers[:, 0] = 0.5 + 0.48 * r_vals * np.cos(theta)\n    centers[:, 1] = 0.5 + 0.48 * r_vals * np.sin(theta)\n    \n    # Break symmetry slightly to allow better relaxation\n    centers += (np.random.rand(n, 2) - 0.5) * 1e-3\n\n    # 2. Force-Directed Relaxation\n    # We simulate a repulsive physical system where circles push each other away.\n    # The target diameter for n=26 equal circles is roughly 0.2027.\n    target_d = 0.2027\n    iterations = 2000\n    dt = 0.04\n    \n    for i in range(iterations):\n        # Cooling schedule for the step size\n        cooling_dt = dt * (1.0 - i / iterations)\n        \n        # Calculate pairwise differences and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        # Square distances to avoid unnecessary sqrt in early phase\n        dist_sq = np.sum(diffs**2, axis=-1)\n        dists = np.sqrt(dist_sq + 1e-12)\n        np.fill_diagonal(dists, 1.0)\n        \n        # Repulsion force: only if circles are closer than the target diameter\n        overlap = np.maximum(0, target_d - dists)\n        # Normalize force by distance\n        f_mag = overlap / dists\n        forces = np.sum(diffs * f_mag[:, :, np.newaxis], axis=1)\n        \n        # Boundary constraints: push centers at least target_d/2 away from edges\n        margin = target_d / 2.0\n        b_forces = np.zeros_like(centers)\n        b_forces[:, 0] += np.maximum(0, margin - centers[:, 0])\n        b_forces[:, 0] -= np.maximum(0, centers[:, 0] - (1.0 - margin))\n        b_forces[:, 1] += np.maximum(0, margin - centers[:, 1])\n        b_forces[:, 1] -= np.maximum(0, centers[:, 1] - (1.0 - margin))\n        \n        # Update positions\n        centers += (forces + b_forces * 1.5) * cooling_dt\n        \n        # Ensure centers remain strictly within the unit square\n        centers = np.clip(centers, 0.0, 1.0)\n\n    # 3. Radius Optimization\n    # Given the fixed centers, maximize Sum(r_i) subject to r_i + r_j <= d_ij.\n    # This is a Linear Program; coordinate descent converges to the optimum.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Finds the maximum possible radii for the given centers using coordinate descent.\n    Objective: Maximize sum(radii)\n    Constraints: \n    1. r_i + r_j <= dist(c_i, c_j)\n    2. r_i <= dist(c_i, boundary)\n    \"\"\"\n    n = centers.shape[0]\n    # Pairwise distances\n    dists = np.sqrt(np.sum((centers[:, np.newaxis, :] - centers[np.newaxis, :, :])**2, axis=-1))\n    np.fill_diagonal(dists, 10.0)\n    \n    # Pre-calculate boundary constraints\n    b_limit = np.min(np.hstack([centers, 1.0 - centers]), axis=1)\n    \n    # Initialize radii to zero\n    radii = np.zeros(n)\n    \n    # Iteratively update each radius to its maximum permissible value\n    for _ in range(300):\n        # We process circles in order; we could randomize but fixed is deterministic\n        for i in range(n):\n            # Constraint: r_i <= d_ij - r_j for all j\n            neighbor_limit = np.min(dists[i] - radii)\n            # Take the minimum of boundary and neighbor constraints\n            radii[i] = max(0.0, min(b_limit[i], neighbor_limit))\n            \n    return radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator.\"\"\"\n    return construct_packing()\n\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Solves for the maximum possible radii for a fixed set of centers by maximizing\n    the sum of radii subject to non-overlap and boundary constraints.\n    Uses iterative coordinate descent (Gauss-Seidel approach) for optimization.\n    \"\"\"\n    n = centers.shape[0]\n    # Pairwise distance matrix between all centers\n    dists = np.linalg.norm(centers[:, np.newaxis, :] - centers[np.newaxis, :, :], axis=2)\n    # Ignore self-distance constraints by setting diagonal to a large value\n    np.fill_diagonal(dists, 1e6)\n    \n    # Distance from each center to the nearest square boundary\n    b_dist = np.min(np.hstack([centers, 1.0 - centers]), axis=1)\n    \n    # Initialize radii with zeros\n    radii = np.zeros(n)\n    \n    # Iteratively update each radius to its maximum valid value given current neighbors.\n    # Shuffling the update order helps avoid local bias in the greedy maximization.\n    indices = np.arange(n)\n    for _ in range(400):\n        np.random.shuffle(indices)\n        for i in indices:\n            # Constraint: r_i <= dist(i, j) - r_j\n            r_neighbor_limit = np.min(dists[i] - radii)\n            # Combine neighbor and boundary constraints\n            radii[i] = max(0.0, min(b_dist[i], r_neighbor_limit))\n            \n    return radii\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Strategy:\n    1. Initialize centers using a Golden Spiral (Fibonacci Lattice) mapping to the square.\n    2. Refine positions via force-directed relaxation targeting the optimal equal-circle radius.\n    3. Finalize by maximizing the total sum of radii for the resulting configuration.\n    \"\"\"\n    n = 26\n    np.random.seed(42) # Ensure deterministic output for the constructor\n\n    # 1. Initialization: Fibonacci Spiral\n    # This provides an excellent uniform distribution for packing in circular and square domains.\n    phi = (1 + 5**0.5) / 2\n    indices = np.arange(n)\n    theta = 2 * np.pi * indices / (phi**2)\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    # Map the spiral into the unit square [0.01, 0.99]\n    centers = 0.5 + 0.49 * r_vals[:, np.newaxis] * np.column_stack([np.cos(theta), np.sin(theta)])\n    # Break potential symmetry traps\n    centers += (np.random.rand(n, 2) - 0.5) * 1e-3\n\n    # 2. Force-Directed Relaxation (Physics-based refinement)\n    # Target radius for n=26 equal circles in a unit square is approx 0.101346\n    target_d = 0.202692\n    target_r = target_d / 2.0\n    dt = 0.02\n    \n    for i in range(1600):\n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.linalg.norm(diffs, axis=2) + 1e-12\n        np.fill_diagonal(dists, 1.0)\n        \n        # Repulsive force is proportional to the depth of overlap\n        overlap = np.maximum(0, target_d - dists)\n        force_mag = overlap / dists\n        forces = np.sum(diffs * force_mag[:, :, np.newaxis], axis=1)\n        \n        # Update center positions\n        centers += dt * forces\n        \n        # Constrain centers: stay target_r away from edges to facilitate dense packing\n        centers = np.clip(centers, target_r, 1.0 - target_r)\n        \n        # Annealing: Reduce step size in later iterations for stable convergence\n        if i > 1100:\n            dt *= 0.99\n\n    # 3. Radius Optimization\n    # Solve for the set of radii that maximize the sum of radii for these coordinates.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator.\"\"\"\n    return construct_packing()\n\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n# EVOLVE-BLOCK-START\n\"\"\"\nConstructor-based circle packing for n=26 circles in a unit square.\nThis implementation uses a Fibonacci Lattice (Golden Spiral) initialization \nfollowed by a force-directed relaxation to maximize minimum separation, \nand finally solves for the maximum sum of radii using coordinate descent.\n\"\"\"\nimport numpy as np\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Maximizes the sum of radii for fixed centers using iterative coordinate descent.\n    Constraints: \n      1) r_i + r_j <= distance(c_i, c_j) for all i, j\n      2) r_i <= distance(c_i, boundary) for all i\n    Starting with a balanced initial radius helps the greedy solver find a higher total sum.\n    \"\"\"\n    n = centers.shape[0]\n    # Calculate pairwise distances between all centers\n    dists = np.linalg.norm(centers[:, np.newaxis, :] - centers[np.newaxis, :, :], axis=2)\n    # Set diagonal to a large value to ignore self-distance in the minimization\n    np.fill_diagonal(dists, 1e6)\n    \n    # Calculate distance to the four boundaries of the unit square\n    b_limit = np.min(np.hstack([centers, 1.0 - centers]), axis=1)\n    \n    # Balanced initialization (0.05 is approx half the target radius for n=26)\n    radii = np.full(n, 0.05)\n    \n    # Coordinate descent iterations\n    for _ in range(250):\n        for i in range(n):\n            # The radius of circle i is limited by its neighbors: r_i <= dist(i, j) - r_j\n            neighbor_limit = np.min(dists[i] - radii)\n            # Combine neighbor limit with boundary limit\n            radii[i] = max(0.0, min(b_limit[i], neighbor_limit))\n            \n    return radii\n\ndef construct_packing():\n    \"\"\"\n    Constructs an arrangement of 26 circles in a unit square.\n    Strategy:\n    1. Fibonacci Spiral Initialization: Distributes points uniformly in a square domain.\n    2. Force-Directed Relaxation: Pushes circles apart using a cooling repulsive force.\n    3. Radius Optimization: Finalizes radii to maximize the total sum for the fixed centers.\n    \"\"\"\n    n = 26\n    np.random.seed(42)\n    \n    # 1. Initialization: Fibonacci Lattice (Golden Spiral mapping)\n    phi = (1 + 5**0.5) / 2\n    indices = np.arange(n)\n    # The golden angle 2*pi / phi^2 provides optimal density for circular spirals\n    theta = 2 * np.pi * indices / (phi**2)\n    r_vals = np.sqrt(indices + 0.5) / np.sqrt(n)\n    \n    # Map the spiral into the central region of the unit square\n    centers = 0.5 + 0.48 * r_vals[:, np.newaxis] * np.column_stack([np.cos(theta), np.sin(theta)])\n    \n    # 2. Force-Directed Relaxation\n    # Target diameter slightly larger than the optimal equal-circle diameter (~0.2027)\n    target_d = 0.203\n    for i in range(1600):\n        # Linear cooling schedule for the simulation step size\n        dt = 0.03 * (1.0 - i / 1600)\n        \n        # Calculate pairwise displacement vectors and distances\n        diffs = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dists = np.linalg.norm(diffs, axis=2) + 1e-12\n        np.fill_diagonal(dists, 1.0)\n        \n        # Calculate repulsion forces proportional to overlap depth\n        overlap = np.maximum(0, target_d - dists)\n        forces = np.sum(diffs * (overlap / dists)[:, :, np.newaxis], axis=1)\n        \n        # Boundary wall repulsion: pushes centers toward a margin of target_d/2\n        m = target_d / 2\n        forces[:, 0] += np.maximum(0, m - centers[:, 0]) - np.maximum(0, centers[:, 0] - (1.0 - m))\n        forces[:, 1] += np.maximum(0, m - centers[:, 1]) - np.maximum(0, centers[:, 1] - (1.0 - m))\n        \n        # Update center positions and strictly clip to unit square\n        centers = np.clip(centers + forces * dt, 0.0, 1.0)\n        \n    # 3. Radius Optimization\n    # Solve for the set of radii that maximize the sum for these refined coordinates.\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\ndef run_packing():\n    \"\"\"Execution entry point for the evaluator.\"\"\"\n    return construct_packing()\n\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": "{\"execution_time\": \"0.26s\", \"stage\": \"quick_validation\", \"packing_summary\": \"Sum of radii: 2.136784/2.635 = 0.8109\", \"validation_report\": \"Valid: True, Violations: 0 boundary, 0 overlaps\"}", "artifact_dir": null, "embedding": null}