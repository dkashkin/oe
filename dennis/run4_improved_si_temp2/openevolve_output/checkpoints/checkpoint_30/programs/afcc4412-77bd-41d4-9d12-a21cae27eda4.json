{"id": "afcc4412-77bd-41d4-9d12-a21cae27eda4", "code": "# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=4500)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]  # Corners\n    centers[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]  # Edges\n\n    for i in range(8):       # Inner ring\n        angle = 2 * np.pi * i / 8\n        centers[i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n\n    for i in range(9):       # Outer ring\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        centers[i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate\n        lr_pos = 0.15 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.03 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(n, 2))\n        \n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.08 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.8\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 1.5\n            grad_radii[v] -= force * 1.5\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.5\n                grad_radii[u] -= overlap_x0[u] * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.5\n                grad_radii[u] -= overlap_x1[u] * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.5\n                grad_radii[u] -= overlap_y0[u] * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.5\n                grad_radii[u] -= overlap_y1[u] * 2.0\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(1500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)", "changes_description": null, "language": "python", "parent_id": "96c52a67-6335-4f3d-9800-bbe8557ca124", "generation": 2, "timestamp": 1771874262.2352414, "iteration_found": 27, "metrics": {"validity": 1.0, "sum_radii": 2.566135105076863, "target_ratio": 0.9738653150196823, "combined_score": 0.9738653150196823, "radius_variance": 0.00869066920972063, "spatial_spread": 0.1950026091225906, "eval_time": 2.1732845306396484}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"validity": 0.0, "combined_score": 0.0, "radius_variance": 0.0, "spatial_spread": 0.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert mathematician and Python developer specializing in computational geometry and circle packing optimization.\nYour task is to improve the given Python algorithm for finding a highly optimized arrangement of 26 circles within a 1x1 unit square. The objective is to maximize the sum of their radii without any of the circles overlapping or extending outside the boundaries of the square.\nInstead of hardcoding a direct arrangement, implement an iterative optimization algorithm to find the solution.\nWhen designing the optimization routine, incorporate the following geometric heuristics:\n* Seed initial positions strategically: Bias initial placements toward corners and edges to maximize space utilization.\n* Break perfect symmetry: Introduce slight random perturbations during the optimization to escape local maxima caused by edge constraints.\n* Tune optimization parameters: Ensure your physics model uses a decaying learning rate or simulated annealing approach to smoothly settle into the tightest possible packing.\n* Size placement: Bias the initialization to push larger circles toward the center and smaller circles into the corners and interstitial gaps.\n\nCode Requirements:\n* Completeness: You MUST provide a fully runnable script. Do not use ellipses (`...`), comments-as-placeholders, or unclosed structures.\n* Style: Ensure the code is syntactically perfect and strictly adheres to PEP 8 formatting.\n* Code review: doublecheck your code to make sure it will not bump into any runtime exceptions.\n* Response Format: response in plain text, without wrapping the code into a Markdown codeblock.\n", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at 0.0000\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### stderr\n```\nStage 1 evaluation failed: Program execution failed: invalid syntax (tmpx34fu5hu.py, line 1)\n```\n\n### traceback\n```\nTraceback (most recent call last):\n  File \"/home/jupyter/Code/oe/dennis/run2_improved_si/evaluator.py\", line 384, in evaluate_stage1\n    centers, radii, sum_radii = run_with_timeout(program_path, timeout_seconds=600)\n  File \"/home/jupyter/Code/oe/dennis/run2_improved_si/evaluator.py\", line 172, in run_with_timeout\n    raise RuntimeError(f\"Program execution failed: {results['error']}\")\nRuntimeError: Program execution failed: invalid syntax (tmpx34fu5hu.py, line 1)\n\n```\n\n### failure_stage\n```\nstage1_execution\n```\n\n### suggestion\n```\nCheck basic syntax and imports before attempting full evaluation\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: validity: 0.0000, combined_score: 0.0000, radius_variance: 0.0000, spatial_spread: 0.0000\n- Outcome: Regression in all metrics\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: validity: 1.0000, sum_radii: 0.9598, target_ratio: 0.3642, combined_score: 0.3642, radius_variance: 0.0266, spatial_spread: 0.2381\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: validity: 1.0000, sum_radii: 2.5551, target_ratio: 0.9697, combined_score: 0.9697, radius_variance: 0.0049, spatial_spread: 0.1891, eval_time: 1.9768\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9697)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=4000)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1] = [0.1, 0.1]  # Corners\n    centers[2] = [0.1, 0.9]\n    centers[3] = [0.9, 0.1]\n    centers[4] = [0.9, 0.9]\n\n    for i in range(7):       # Inner ring\n        angle = 2 * np.pi * i / 7\n        centers[i + 5] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n\n    for i in range(14):      # Outer ring\n        angle = 2 * np.pi * i / 14 + np.pi / 14\n        centers[i + 12] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.01, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate\n        lr_pos = 0.2 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.05 * (1.0 - progress) + 0.001\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        grad_centers = np.zeros((n, 2))\n        \n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**2\n        grad_centers += np.random.normal(0, temp, size=(n, 2))\n\n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.1 + 0.05 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.5\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force\n            grad_radii[v] -= force\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.0\n                grad_radii[u] -= overlap_x0[u] * 1.5\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.0\n                grad_radii[u] -= overlap_x1[u] * 1.5\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.0\n                grad_radii[u] -= overlap_y0[u] * 1.5\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.0\n                grad_radii[u] -= overlap_y1[u] * 1.5\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (2.5551), Performs well on target_ratio (0.9697), Performs well on combined_score (0.9697), Performs well on radius_variance (0.0049), Performs well on spatial_spread (0.1891), Performs well on eval_time (1.9768)\n\n### Program 2 (Score: 0.3642)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nKey features: Performs well on validity (1.0000), Performs well on sum_radii (0.9598), Performs well on target_ratio (0.3642), Performs well on combined_score (0.3642), Performs well on radius_variance (0.0266), Performs well on spatial_spread (0.2381)\n\n### Program 3 (Score: 0.0000)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation with Adam optimization across multiple \n    structural seeds, followed by linear programming to strictly maximize \n    the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    centers, radii, sum_radii = optimize_packing(n=26, iters=3000, batch_size=8)\n    return centers, radii, sum_radii\n\n\ndef optimize_packing(n, iters, batch_size):\n    \"\"\"\n    Simulate a batched force-directed physical model to find optimal circle centers.\n    Runs multiple initialized configurations in parallel to find the global optimum.\n    \"\"\"\n    np.random.seed(42)\n    centers = np.zeros((batch_size, n, 2))\n\n    # Seed 1: 1 center, 4 corners, 7 inner, 14 outer\n    base_1 = np.zeros((n, 2))\n    base_1[0] = [0.5, 0.5]\n    base_1[1:5] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n    for i in range(7):\n        a = 2 * np.pi * i / 7\n        base_1[i + 5] = [0.5 + 0.22 * np.cos(a), 0.5 + 0.22 * np.sin(a)]\n    for i in range(14):\n        a = 2 * np.pi * i / 14 + np.pi / 14\n        base_1[i + 12] = [0.5 + 0.42 * np.cos(a), 0.5 + 0.42 * np.sin(a)]\n\n    # Seed 2: 1 center, 6 inner, 11 middle, 8 outer\n    base_2 = np.zeros((n, 2))\n    base_2[0] = [0.5, 0.5]\n    for i in range(6):\n        a = 2 * np.pi * i / 6\n        base_2[i + 1] = [0.5 + 0.18 * np.cos(a), 0.5 + 0.18 * np.sin(a)]\n    for i in range(11):\n        a = 2 * np.pi * i / 11 + np.pi / 11\n        base_2[i + 7] = [0.5 + 0.34 * np.cos(a), 0.5 + 0.34 * np.sin(a)]\n    for i in range(8):\n        a = 2 * np.pi * i / 8\n        base_2[i + 18] =\n```\nKey features: Performs well on validity (0.0000), Performs well on combined_score (0.0000), Performs well on radius_variance (0.0000), Performs well on spatial_spread (0.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.0000)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a physics-based approach\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Optimize centers and radii using iterative physical relaxation\n    centers, radii = optimize_packing(n, iters=3000)\n\n    # Strictly compute maximum valid radii to guarantee zero overlaps\n    radii = compute_max_radii(centers, radii)\n\n    # Calculate the exact valid sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_packing(n=26, iters=3000):\n    \"\"\"\n    Iteratively grow and repel circles to find an optimal dense packing.\n    \"\"\"\n    # Deterministic execution\n    np.random.seed(42)\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization mapping to geometric heuristics\n    centers[0] = [0.5, 0.5]  # Center\n    centers[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]  # Corners\n    centers[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]  # Edges\n\n    # Inner ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[9 + i] = [0.5 + 0.25 * np.cos(angle), 0.5 + 0.25 * np.sin(angle)]\n\n    # Interstitial / outer ring\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        centers[17 + i] = [0.5 + 0.43 * np.cos(angle), 0.5 + 0.43 * np.sin(angle)]\n\n    # 2. Break perfect symmetry to prevent gradient gridlocks\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.02, 0.98)\n\n    radii = np.ones(n) * 0.01\n\n    # 3. Size placement bias: center grows larger, corners settle smaller\n    growth_rates = np.ones(n)\n    growth_rates[0] = 1.5\n    growth_rates[1:5] = 0.7\n    growth_rates[5:9] = 0.9\n    growth_rates[9:17] = 1.2\n    growth_rates[17:\n```\nKey features: Alternative approach to validity, Alternative approach to combined_score\n\n### Program D2 (Score: 0.0000)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct an optimized arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii using a physics-based approach.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    np.random.seed(42)  # Fixed seed for deterministic evaluation\n    \n    # 1. Initialize Centers (X) and Radii (R)\n    X = np.zeros((n, 2))\n    \n    # Heuristic Seeding Strategies:\n    # Corners\n    X[0:4] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]\n    # Center\n    X[4] = [0.5, 0.5]\n    # Edges\n    X[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]\n    # Ring around center\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        X[9+i] = [0.5 + 0.25 * np.cos(angle), 0.5 + 0.25 * np.sin(angle)]\n    # Randomly fill the rest in interstitial gaps\n    X[17:] = np.random.uniform(0.1, 0.9, (9, 2))\n    \n    # Break initial perfect symmetry\n    X += np.random.normal(0, 0.005, X.shape)\n    X = np.clip(X, 0.01, 0.99)\n    \n    # Size Placement Bias: Push larger circles toward center, smaller to corners\n    R = np.ones(n) * 0.02\n    R[4] = 0.15      # Large center\n    R[9:17] = 0.08   # Medium inner ring\n    R[5:9] = 0.05    # Edges\n    R[0:4] = 0.03    # Small corners\n    \n    # 2. Iterative Optimization using Adam with Penalty Method\n    max_steps = 10000\n    beta1, beta2, eps = 0.9, 0.99, 1e-8\n    \n    m_X, v_X = np.zeros_like(X), np.zeros_like(X)\n    m_R, v_R = np.zeros_like(R), np.zeros_like(R)\n    \n    for step in range(1, max_steps + 1):\n        progress = step / max_steps\n        \n        # Simulated annealing parameter schedules\n        curr_lambda = 100.0 * (10000.0 ** progress)\n        \n```\nKey features: Alternative approach to validity, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.3642, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a specific arrangement of 26 circles in a unit square\n    that attempts to maximize the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    # Initialize arrays for 26 circles\n    n = 26\n    centers = np.zeros((n, 2))\n\n    # Place circles in a structured pattern\n    # This is a simple pattern - evolution will improve this\n\n    # First, place a large circle in the center\n    centers[0] = [0.5, 0.5]\n\n    # Place 8 circles around it in a ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[i + 1] = [0.5 + 0.3 * np.cos(angle), 0.5 + 0.3 * np.sin(angle)]\n\n    # Place 16 more circles in an outer ring\n    for i in range(16):\n        angle = 2 * np.pi * i / 16\n        centers[i + 9] = [0.5 + 0.7 * np.cos(angle), 0.5 + 0.7 * np.sin(angle)]\n\n    # Additional positioning adjustment to make sure all circles\n    # are inside the square and don't overlap\n    # Clip to ensure everything is inside the unit square\n    centers = np.clip(centers, 0.01, 0.99)\n\n    # Compute maximum valid radii for this configuration\n    radii = compute_max_radii(centers)\n\n    # Calculate the sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n\n    Returns:\n        np.array of shape (n) with radius of each circle\n    \"\"\"\n    n = centers.shape[0]\n    radii = np.ones(n)\n\n    # First, limit by distance to square borders\n    for i in range(n):\n        x, y = centers[i]\n        # Distance to borders\n        radii[i] = min(x, y, 1 - x, 1 - y)\n\n    # Then, limit by distance to other circles\n    # Each pair of circles with centers at distance d can have\n    # sum of radii at most d to avoid overlap\n    for i in range(n):\n        for j in range(i + 1, n):\n            dist = np.sqrt(np.sum((centers[i] - centers[j]) ** 2))\n\n            # If current radii would cause overlap\n            if radii[i] + radii[j] > dist:\n                # Scale both radii proportionally\n                scale = dist / (radii[i] + radii[j])\n                radii[i] *= scale\n                radii[j] *= scale\n\n    return radii\n\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        circle = Circle(center, radius, alpha=0.5)\n        ax.add_patch(circle)\n        ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # AlphaEvolve improved this to 2.635\n\n    # Uncomment to visualize:\n    visualize(centers, radii)\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation with Adam optimization across multiple \n    structural seeds, followed by linear programming to strictly maximize \n    the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    centers, radii, sum_radii = optimize_packing(n=26, iters=3000, batch_size=8)\n    return centers, radii, sum_radii\n\n\ndef optimize_packing(n, iters, batch_size):\n    \"\"\"\n    Simulate a batched force-directed physical model to find optimal circle centers.\n    Runs multiple initialized configurations in parallel to find the global optimum.\n    \"\"\"\n    np.random.seed(42)\n    centers = np.zeros((batch_size, n, 2))\n\n    # Seed 1: 1 center, 4 corners, 7 inner, 14 outer\n    base_1 = np.zeros((n, 2))\n    base_1[0] = [0.5, 0.5]\n    base_1[1:5] = [[0.1, 0.1], [0.1, 0.9], [0.9, 0.1], [0.9, 0.9]]\n    for i in range(7):\n        a = 2 * np.pi * i / 7\n        base_1[i + 5] = [0.5 + 0.22 * np.cos(a), 0.5 + 0.22 * np.sin(a)]\n    for i in range(14):\n        a = 2 * np.pi * i / 14 + np.pi / 14\n        base_1[i + 12] = [0.5 + 0.42 * np.cos(a), 0.5 + 0.42 * np.sin(a)]\n\n    # Seed 2: 1 center, 6 inner, 11 middle, 8 outer\n    base_2 = np.zeros((n, 2))\n    base_2[0] = [0.5, 0.5]\n    for i in range(6):\n        a = 2 * np.pi * i / 6\n        base_2[i + 1] = [0.5 + 0.18 * np.cos(a), 0.5 + 0.18 * np.sin(a)]\n    for i in range(11):\n        a = 2 * np.pi * i / 11 + np.pi / 11\n        base_2[i + 7] = [0.5 + 0.34 * np.cos(a), 0.5 + 0.34 * np.sin(a)]\n    for i in range(8):\n        a = 2 * np.pi * i / 8\n        base_2[i + 18] =\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\ntry:\n    from scipy.optimize import linprog\n    HAS_SCIPY = True\nexcept ImportError:\n    HAS_SCIPY = False\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=2500)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1] = [0.1, 0.1]  # Corners\n    centers[2] = [0.1, 0.9]\n    centers[3] = [0.9, 0.1]\n    centers[4] = [0.9, 0.9]\n\n    for i in range(7):       # Inner ring\n        angle = 2 * np.pi * i / 7\n        centers[i + 5] = [0.5 + 0.2 * np.cos(angle), 0.5 + 0.2 * np.sin(angle)]\n\n    for i in range(14):      # Outer ring\n        angle = 2 * np.pi * i / 14 + np.pi / 14\n        centers[i + 12] = [0.5 + 0.4 * np.cos(angle), 0.5 + 0.4 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.01, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate and annealing\n        lr_pos = 0.04 * (1.0 - progress) + 0.001\n        lr_rad = 0.01 * (1.0 - progress) + 0.001\n        temp = np.exp(-6.0 * progress)\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        grad_centers = np.zeros((n, 2))\n\n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.05 + 0.1 * (1.0 - dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        repulsion_mult = 20.0\n        wall_force_mult = 20.0\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            if dist[u, v] > 1e-6:\n                dir_vec = diff[u, v] / dist[u, v]\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * repulsion_mult\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 2.0\n            grad_radii[v] -= force * 2.0\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * wall_force_mult\n                grad_radii[u] -= overlap_x0[u] * wall_force_mult * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * wall_force_mult\n                grad_radii[u] -= overlap_x1[u] * wall_force_mult * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * wall_force_mult\n                grad_radii[u] -= overlap_y0[u] * wall_force_mult * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * wall_force_mult\n                grad_radii[u] -= overlap_y1[u] * wall_force_mult * 2.0\n\n        # Apply gradients with momentum\n        grad_centers = np.clip(grad_centers, -10.0, 10.0)\n        grad_radii = np.clip(\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n```python\n# EVOLVE-BLOCK-START\n\"\"\"Constructor-based circle packing for n=26 circles using a physics-based approach\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    that maximizes the sum of their radii.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    # Optimize centers and radii using iterative physical relaxation\n    centers, radii = optimize_packing(n, iters=3000)\n\n    # Strictly compute maximum valid radii to guarantee zero overlaps\n    radii = compute_max_radii(centers, radii)\n\n    # Calculate the exact valid sum of radii\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_packing(n=26, iters=3000):\n    \"\"\"\n    Iteratively grow and repel circles to find an optimal dense packing.\n    \"\"\"\n    # Deterministic execution\n    np.random.seed(42)\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization mapping to geometric heuristics\n    centers[0] = [0.5, 0.5]  # Center\n    centers[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]  # Corners\n    centers[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]  # Edges\n\n    # Inner ring\n    for i in range(8):\n        angle = 2 * np.pi * i / 8\n        centers[9 + i] = [0.5 + 0.25 * np.cos(angle), 0.5 + 0.25 * np.sin(angle)]\n\n    # Interstitial / outer ring\n    for i in range(9):\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        centers[17 + i] = [0.5 + 0.43 * np.cos(angle), 0.5 + 0.43 * np.sin(angle)]\n\n    # 2. Break perfect symmetry to prevent gradient gridlocks\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.02, 0.98)\n\n    radii = np.ones(n) * 0.01\n\n    # 3. Size placement bias: center grows larger, corners settle smaller\n    growth_rates = np.ones(n)\n    growth_rates[0] = 1.5\n    growth_rates[1:5] = 0.7\n    growth_rates[5:9] = 0.9\n    growth_rates[9:17] = 1.2\n    growth_rates[17:\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["# EVOLVE-BLOCK-START\n\"\"\"Iterative optimization-based circle packing for n=26 circles\"\"\"\nimport numpy as np\n\n\ndef construct_packing():\n    \"\"\"\n    Construct a highly optimized arrangement of 26 circles in a unit square\n    using physics-based relaxation followed by linear programming to\n    strictly maximize the sum of their radii without any overlaps.\n\n    Returns:\n        Tuple of (centers, radii, sum_of_radii)\n        centers: np.array of shape (26, 2) with (x, y) coordinates\n        radii: np.array of shape (26) with radius of each circle\n        sum_of_radii: Sum of all radii\n    \"\"\"\n    n = 26\n    centers = optimize_centers(n, iters=4500)\n    radii = compute_max_radii(centers)\n    sum_radii = np.sum(radii)\n\n    return centers, radii, sum_radii\n\n\ndef optimize_centers(n, iters):\n    \"\"\"\n    Simulate a force-directed physical model to find optimal circle centers.\n    Incorporates simulated annealing and momentum for smooth convergence.\n    \"\"\"\n    centers = np.zeros((n, 2))\n\n    # 1. Strategic initialization\n    # Push larger circles to center, smaller to corners\n    centers[0] = [0.5, 0.5]  # Center\n\n    centers[1:5] = [[0.05, 0.05], [0.95, 0.05], [0.05, 0.95], [0.95, 0.95]]  # Corners\n    centers[5:9] = [[0.5, 0.05], [0.5, 0.95], [0.05, 0.5], [0.95, 0.5]]  # Edges\n\n    for i in range(8):       # Inner ring\n        angle = 2 * np.pi * i / 8\n        centers[i + 9] = [0.5 + 0.22 * np.cos(angle), 0.5 + 0.22 * np.sin(angle)]\n\n    for i in range(9):       # Outer ring\n        angle = 2 * np.pi * i / 9 + np.pi / 9\n        centers[i + 17] = [0.5 + 0.42 * np.cos(angle), 0.5 + 0.42 * np.sin(angle)]\n\n    np.random.seed(42)\n    # 2. Add slight initial random perturbations to break perfect symmetry\n    centers += np.random.normal(0, 0.005, size=(n, 2))\n    centers = np.clip(centers, 0.01, 0.99)\n\n    radii = np.ones(n) * 0.02\n    \n    # Velocity variables for Adam-like momentum\n    v_centers = np.zeros((n, 2))\n    v_radii = np.zeros(n)\n\n    for step in range(iters):\n        progress = step / iters\n\n        # 3. Tune optimization parameters: decaying learning rate\n        lr_pos = 0.15 * (1.0 - progress)**2 + 0.001\n        lr_rad = 0.03 * (1.0 - progress) + 0.0005\n\n        diff = centers[:, np.newaxis, :] - centers[np.newaxis, :, :]\n        dist = np.sqrt(np.sum(diff**2, axis=-1))\n        np.fill_diagonal(dist, np.inf)\n\n        # Add simulated annealing noise to escape local minima\n        temp = 0.005 * (1.0 - progress)**3\n        grad_centers = np.random.normal(0, temp, size=(n, 2))\n        \n        # 4. Size placement: Bias center circles to grow larger\n        dist_to_center = np.linalg.norm(centers - 0.5, axis=1)\n        grad_radii = 0.08 + 0.06 * (1.0 - 2 * dist_to_center)\n\n        radii_sum = radii[:, np.newaxis] + radii[np.newaxis, :]\n        overlap = radii_sum - dist\n\n        mask = overlap > 0\n        i_idx, j_idx = np.where(mask)\n\n        for u, v in zip(i_idx, j_idx):\n            if u >= v:\n                continue\n\n            d = dist[u, v]\n            if d > 1e-8:\n                dir_vec = diff[u, v] / d\n            else:\n                dir_vec = np.random.randn(2)\n                dir_vec /= np.linalg.norm(dir_vec)\n\n            force = overlap[u, v] * 2.8\n\n            grad_centers[u] += dir_vec * force\n            grad_centers[v] -= dir_vec * force\n            grad_radii[u] -= force * 1.5\n            grad_radii[v] -= force * 1.5\n\n        overlap_x0 = radii - centers[:, 0]\n        overlap_x1 = radii - (1.0 - centers[:, 0])\n        overlap_y0 = radii - centers[:, 1]\n        overlap_y1 = radii - (1.0 - centers[:, 1])\n\n        for u in range(n):\n            if overlap_x0[u] > 0:\n                grad_centers[u, 0] += overlap_x0[u] * 3.5\n                grad_radii[u] -= overlap_x0[u] * 2.0\n            if overlap_x1[u] > 0:\n                grad_centers[u, 0] -= overlap_x1[u] * 3.5\n                grad_radii[u] -= overlap_x1[u] * 2.0\n            if overlap_y0[u] > 0:\n                grad_centers[u, 1] += overlap_y0[u] * 3.5\n                grad_radii[u] -= overlap_y0[u] * 2.0\n            if overlap_y1[u] > 0:\n                grad_centers[u, 1] -= overlap_y1[u] * 3.5\n                grad_radii[u] -= overlap_y1[u] * 2.0\n\n        # Apply momentum\n        v_centers = 0.9 * v_centers + 0.1 * grad_centers\n        v_radii = 0.9 * v_radii + 0.1 * grad_radii\n\n        # Apply gradients\n        centers += lr_pos * v_centers\n        radii += lr_rad * v_radii\n        \n        centers = np.clip(centers, 0.001, 0.999)\n        radii = np.clip(radii, 0.001, 0.5)\n\n    return centers\n\n\ndef compute_max_radii(centers):\n    \"\"\"\n    Compute the maximum possible radii for each circle position\n    such that they don't overlap and stay within the unit square.\n    Uses linear programming for optimal assignment, with a fallback.\n    \"\"\"\n    n = centers.shape[0]\n    \n    try:\n        from scipy.optimize import linprog\n        c = -np.ones(n)\n        A_ub = []\n        b_ub = []\n        \n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                row = np.zeros(n)\n                row[i] = 1\n                row[j] = 1\n                A_ub.append(row)\n                b_ub.append(dist)\n                \n        bounds = []\n        for i in range(n):\n            x, y = centers[i]\n            max_r = min(x, y, 1 - x, 1 - y)\n            bounds.append((0, max_r))\n            \n        res = linprog(c, A_ub=A_ub, b_ub=b_ub, bounds=bounds, method='highs')\n        if res.success:\n            return np.maximum(res.x, 0.0)\n    except Exception:\n        pass\n        \n    # Fallback if LP fails or scipy is not available\n    radii = np.zeros(n)\n    for i in range(n):\n        x, y = centers[i]\n        radii[i] = min(x, y, 1 - x, 1 - y)\n        \n    for _ in range(1500):\n        changed = False\n        for i in range(n):\n            for j in range(i + 1, n):\n                dist = np.linalg.norm(centers[i] - centers[j])\n                if radii[i] + radii[j] > dist + 1e-9:\n                    diff = radii[i] + radii[j] - dist\n                    if radii[i] > 0 and radii[j] > 0:\n                        ratio = radii[i] / (radii[i] + radii[j])\n                        radii[i] -= diff * ratio\n                        radii[j] -= diff * (1.0 - ratio)\n                    elif radii[i] > 0:\n                        radii[i] -= diff\n                    elif radii[j] > 0:\n                        radii[j] -= diff\n                    changed = True\n        if not changed:\n            break\n            \n    return np.maximum(radii, 0.0)\n\n# EVOLVE-BLOCK-END\n\n\n# This part remains fixed (not evolved)\ndef run_packing():\n    \"\"\"Run the circle packing constructor for n=26\"\"\"\n    centers, radii, sum_radii = construct_packing()\n    return centers, radii, sum_radii\n\n\ndef visualize(centers, radii):\n    \"\"\"\n    Visualize the circle packing\n\n    Args:\n        centers: np.array of shape (n, 2) with (x, y) coordinates\n        radii: np.array of shape (n) with radius of each circle\n    \"\"\"\n    import matplotlib.pyplot as plt\n    from matplotlib.patches import Circle\n\n    fig, ax = plt.subplots(figsize=(8, 8))\n\n    # Draw unit square\n    ax.set_xlim(0, 1)\n    ax.set_ylim(0, 1)\n    ax.set_aspect(\"equal\")\n    ax.grid(True)\n\n    # Draw circles\n    for i, (center, radius) in enumerate(zip(centers, radii)):\n        if radius > 0:\n            circle = Circle(center, radius, alpha=0.5)\n            ax.add_patch(circle)\n            ax.text(center[0], center[1], str(i), ha=\"center\", va=\"center\")\n\n    plt.title(f\"Circle Packing (n={len(centers)}, sum={sum(radii):.6f})\")\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    centers, radii, sum_radii = run_packing()\n    print(f\"Sum of radii: {sum_radii}\")\n    # Uncomment to visualize:\n    # visualize(centers, radii)"]}}, "artifacts_json": "{\"execution_time\": \"2.17s\", \"stage\": \"quick_validation\", \"packing_summary\": \"Sum of radii: 2.566135/2.635 = 0.9739\", \"validation_report\": \"Valid: True, Violations: 0 boundary, 0 overlaps\", \"stdout\": \"Excellent packing! Achieved 97.4% of target value\", \"radius_stats\": \"Min: 0.040133, Max: 0.147649, Avg: 0.098698\"}", "artifact_dir": null, "embedding": null}