{"id": "5f266afe-d3a6-4e1c-83e7-340ab2d9827a", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n    if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n    if arr.windows(2).all(|w| w[0] >= w[1]) { arr.reverse(); return; }\n    let (m, r) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[r] < arr[0] { arr.swap(0, r); }\n    if arr[r] < arr[m] { arr.swap(m, r); }\n    let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n    while i < g {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; arr.swap(i, g); }\n            Ordering::Equal => i += 1,\n        }\n    }\n    adaptive_sort(&mut arr[..l]);\n    adaptive_sort(&mut arr[g..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "132eff76-fbe5-480e-a049-64efefe70382", "generation": 4, "timestamp": 1772732873.773744, "iteration_found": 22, "metrics": {"score": 0.9988438990802275, "combined_score": 0.9988438990802275, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9982099988497526, "adaptability_score": 0.9997947494259397, "avg_time": 0.00017932109999999998, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n      } else {\n          if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n          let (m, r) = (n / 2, n - 1);\n          if arr[m] < arr[0] { arr.swap(0, m); }\n          if arr[r] < arr[0] { arr.swap(0, r); }\n          if arr[r] < arr[m] { arr.swap(m, r); }\n          arr.swap(m, r);\n          let (mut i, p) = (1, arr[r].clone());\n          for j in 1..r {\n              if arr[j] <= p {\n                  arr.swap(i, j);\n                  i += 1;\n              }\n          }\n          arr.swap(i, r);\n          let (lt, rt) = arr.split_at_mut(i);\n          adaptive_sort(lt);\n          adaptive_sort(&mut rt[1..]);\n      }\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n          }\n          return;\n      }\n      if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n      if arr.windows(2).all(|w| w[0] >= w[1]) { arr.reverse(); return; }\n      let (m, r) = (n / 2, n - 1);\n      if arr[m] < arr[0] { arr.swap(0, m); }\n      if arr[r] < arr[0] { arr.swap(0, r); }\n      if arr[r] < arr[m] { arr.swap(m, r); }\n      let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n      while i < g {\n          match arr[i].cmp(&p) {\n              Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n              Ordering::Greater => { g -= 1; arr.swap(i, g); }\n              Ordering::Equal => i += 1,\n          }\n      }\n      adaptive_sort(&mut arr[..l]);\n      adaptive_sort(&mut arr[g..]);\n  }", "parent_metrics": {"score": 0.9989699539473877, "combined_score": 0.9989699539473877, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9983868713883575, "adaptability_score": 0.9998445777859332, "avg_time": 0.00016157350000000001, "memory_safe": 1.0}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9990\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9987 \u2192 0.9990\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[3.544e-05, 0.000454611, 2.2669e-05, 0.000196771, 1.4611e-05, 0.00018096, 1.966e-05, 0.000300251, 2.8511e-05, 0.000362251]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      if arr.len() < 20 {\n          insertion_sort(arr);\n      } else {\n          quicksort(arr, 0, arr.len() - 1);\n      }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          if high - low < 20 {\n              insertion_sort(&mut arr[low..=high]);\n              return;\n          }\n          let pivot_index = partition(arr, low, high);\n          if pivot_index > 0 {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let m = low + (high - low) / 2;\n      if arr[m] < arr[low] { arr.swap(low, m); }\n      if arr[high] < arr[low] { arr.swap(low, high); }\n      if arr[high] < arr[m] { arr.swap(m, high); }\n      arr.swap(m, high);\n  \n      let (mut i, pivot) = (low, arr[high].clone());\n  ... (19 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 20 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n          return;\n      }\n      let m = n / 2;\n      if arr[m] < arr[0] { arr.swap(0, m); }\n      if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n      if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n      let (pivot, mut lt, mut i, mut gt) = (arr[m].clone(), 0, 0, n);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(i, lt);\n              lt += 1;\n              i += 1;\n          } else if arr[i] > pivot {\n              gt -= 1;\n              arr.swap(i, gt);\n          } else {\n              i += 1;\n          }\n      }\n      adaptive_sort(&mut arr[..lt]);\n  ... (2 more lines)\n- Metrics: score: 0.9987, combined_score: 0.9987, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9979, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 20 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n          return;\n      }\n      let m = n / 2;\n      if arr[m] < arr[0] { arr.swap(0, m); }\n      if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n      if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n      let (pivot, mut lt, mut i, mut gt) = (arr[m].clone(), 0, 0, n);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(i, lt);\n              lt += 1;\n              i += 1;\n          } else if arr[i] > pivot {\n              gt -= 1;\n              arr.swap(i, gt);\n          } else {\n              i += 1;\n          }\n      }\n      adaptive_sort(&mut arr[..lt]);\n  ... (2 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n          }\n          return;\n      }\n      if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n      let m = n / 2;\n      if arr[m] < arr[0] { arr.swap(0, m); }\n      if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n      if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n      let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n      while i < g {\n          match arr[i].cmp(&p) {\n              Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n              Ordering::Greater => { g -= 1; arr.swap(i, g); }\n              Ordering::Equal => i += 1,\n          }\n      }\n      adaptive_sort(&mut arr[..l]);\n      adaptive_sort(&mut arr[g..]);\n  }\n- Metrics: score: 0.9987, combined_score: 0.9987, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9979, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 20 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n          return;\n      }\n      let m = n / 2;\n      if arr[m] < arr[0] { arr.swap(0, m); }\n      if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n      if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n      let (pivot, mut lt, mut i, mut gt) = (arr[m].clone(), 0, 0, n);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(i, lt);\n              lt += 1;\n              i += 1;\n          } else if arr[i] > pivot {\n              gt -= 1;\n              arr.swap(i, gt);\n          } else {\n              i += 1;\n          }\n      }\n      adaptive_sort(&mut arr[..lt]);\n  ... (2 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n      } else {\n          if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n          let (m, r) = (n / 2, n - 1);\n          if arr[m] < arr[0] { arr.swap(0, m); }\n          if arr[r] < arr[0] { arr.swap(0, r); }\n          if arr[r] < arr[m] { arr.swap(m, r); }\n          arr.swap(m, r);\n          let (mut i, p) = (1, arr[r].clone());\n          for j in 1..r {\n              if arr[j] <= p {\n                  arr.swap(i, j);\n                  i += 1;\n              }\n          }\n          arr.swap(i, r);\n          let (lt, rt) = arr.split_at_mut(i);\n          adaptive_sort(lt);\n          adaptive_sort(&mut rt[1..]);\n      }\n  }\n- Metrics: score: 0.9990, combined_score: 0.9990, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9984, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9990)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n    } else {\n        if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n        let (m, r) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[r] < arr[0] { arr.swap(0, r); }\n        if arr[r] < arr[m] { arr.swap(m, r); }\n        arr.swap(m, r);\n        let (mut i, p) = (1, arr[r].clone());\n        for j in 1..r {\n            if arr[j] <= p {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, r);\n        let (lt, rt) = arr.split_at_mut(i);\n        adaptive_sort(lt);\n        adaptive_sort(&mut rt[1..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9990), Performs well on combined_score (0.9990), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9984), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9987)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n    if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n    let m = n / 2;\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n    if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n    let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n    while i < g {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; arr.swap(i, g); }\n            Ordering::Equal => i += 1,\n        }\n    }\n    adaptive_sort(&mut arr[..l]);\n    adaptive_sort(&mut arr[g..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9987), Performs well on combined_score (0.9987), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9979), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9987)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 20 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n        return;\n    }\n    let m = n / 2;\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n    if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n    let (pivot, mut lt, mut i, mut gt) = (arr[m].clone(), 0, 0, n);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(i, lt);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    adaptive_sort(&mut arr[..lt]);\n    adaptive_sort(&mut arr[gt..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9987), Performs well on combined_score (0.9987), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9979), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9987, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 20 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n        return;\n    }\n    let m = n / 2;\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n    if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n    let (pivot, mut lt, mut i, mut gt) = (arr[m].clone(), 0, 0, n);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(i, lt);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    adaptive_sort(&mut arr[..lt]);\n    adaptive_sort(&mut arr[gt..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9987, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n    if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n    let m = n / 2;\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[n - 1] < arr[0] { arr.swap(0, n - 1); }\n    if arr[n - 1] < arr[m] { arr.swap(m, n - 1); }\n    let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n    while i < g {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; arr.swap(i, g); }\n            Ordering::Equal => i += 1,\n        }\n    }\n    adaptive_sort(&mut arr[..l]);\n    adaptive_sort(&mut arr[g..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n    } else {\n        if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n        let (m, r) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[r] < arr[0] { arr.swap(0, r); }\n        if arr[r] < arr[m] { arr.swap(m, r); }\n        arr.swap(m, r);\n        let (mut i, p) = (1, arr[r].clone());\n        for j in 1..r {\n            if arr[j] <= p {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, r);\n        let (lt, rt) = arr.split_at_mut(i);\n        adaptive_sort(lt);\n        adaptive_sort(&mut rt[1..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a hybrid approach with a Lomuto-style quicksort and insertion sort. To improve both the fitness score and the adaptability (especially to data with many duplicates or reversed patterns), I will refine the algorithm by:\n1.  **Adding a reverse-sorted check**: This significantly improves performance on reverse-sorted data.\n2.  **Using a 3-way partition (Dutch National Flag algorithm)**: This handles duplicate keys much more efficiently than Lomuto partitioning, turning $O(N^2)$ cases with many duplicates into $O(N)$ cases.\n3.  **Retaining the Median-of-Three pivot selection**: This helps prevent worst-case performance on common patterns.\n4.  **Simplifying the control flow**: Removing nested `else` blocks and using `return` statements to reduce indentation and improve code clarity, which also addresses the request to simplify the code (aiming for under 500 characters of logic).\n\nThe combination of median-of-three for pivot stability and 3-way partitioning for duplicate resistance creates a highly adaptive sorting algorithm that should improve both performance and adaptability scores.\n\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n    } else {\n        if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n        let (m, r) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[r] < arr[0] { arr.swap(0, r); }\n        if arr[r] < arr[m] { arr.swap(m, r); }\n        arr.swap(m, r);\n        let (mut i, p) = (1, arr[r].clone());\n        for j in 1..r {\n            if arr[j] <= p {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, r);\n        let (lt, rt) = arr.split_at_mut(i);\n        adaptive_sort(lt);\n        adaptive_sort(&mut rt[1..]);\n    }\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n    if arr.windows(2).all(|w| w[0] <= w[1]) { return; }\n    if arr.windows(2).all(|w| w[0] >= w[1]) { arr.reverse(); return; }\n    let (m, r) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[r] < arr[0] { arr.swap(0, r); }\n    if arr[r] < arr[m] { arr.swap(m, r); }\n    let (p, mut l, mut i, mut g) = (arr[m].clone(), 0, 0, n);\n    while i < g {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; arr.swap(i, g); }\n            Ordering::Equal => i += 1,\n        }\n    }\n    adaptive_sort(&mut arr[..l]);\n    adaptive_sort(&mut arr[g..]);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [4.0111e-05, 0.00053192, 2.4411e-05, 0.000316849, 6.8e-07, 5.92e-06, 1.682e-05, 0.00031154, 3.6251e-05, 0.000508709], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}