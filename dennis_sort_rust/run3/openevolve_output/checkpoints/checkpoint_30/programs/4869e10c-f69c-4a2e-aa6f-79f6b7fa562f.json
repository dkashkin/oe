{"id": "4869e10c-f69c-4a2e-aa6f-79f6b7fa562f", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid Quicksort: Iterative tail-call, 3-way partitioning\n// for duplicate keys, median-of-three pivot, and sorted detection.\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut a, mut d) = (true, true);\n        for j in 1..n {\n            match arr[j-1].cmp(&arr[j]) {\n                Ordering::Greater => a = false,\n                Ordering::Less => d = false,\n                _ => {},\n            }\n            if !a && !d { break; }\n        }\n        if a { return; } if d { arr.reverse(); return; }\n\n        let (m, h) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[h] < arr[0] { arr.swap(0, h); }\n        if arr[h] < arr[m] { arr.swap(m, h); }\n        arr.swap(m, h);\n        \n        let (p, mut i) = (arr[h].clone(), 0);\n        for j in 0..h {\n            if arr[j] <= p {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, h);\n        \n        if i < n / 2 {\n            adaptive_sort(&mut arr[..i]);\n            arr = &mut arr[i + 1..];\n        } else {\n            adaptive_sort(&mut arr[i + 1..]);\n            arr = &mut arr[..i];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "c40ded58-5b82-4550-b14c-a4870fee4be1", "generation": 5, "timestamp": 1772733003.496074, "iteration_found": 27, "metrics": {"score": 0.9990170943418287, "combined_score": 0.9990170943418287, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9984768844199075, "adaptability_score": 0.9998274092247106, "avg_time": 0.0001525439, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n      while arr.len() > 24 {\n          let n = arr.len();\n          let (mut asc, mut desc) = (true, true);\n          for i in 1..n {\n              if arr[i-1] > arr[i] { asc = false; } else if arr[i-1] < arr[i] { desc = false; }\n              if !asc && !desc { break; }\n          }\n          if asc { return; } if desc { arr.reverse(); return; }\n  \n          let pivot = {\n              let (m, h) = (n / 2, n - 1);\n              if arr[m] < arr[0] { arr.swap(0, m); }\n              if arr[h] < arr[0] { arr.swap(0, h); }\n              if arr[h] < arr[m] { arr.swap(m, h); }\n              arr[m].clone()\n          };\n  \n          let (mut l, mut i, mut g) = (0, 0, n - 1);\n          while i <= g {\n              match arr[i].cmp(&pivot) {\n                  Ordering::Less => { arr.swap(l, i); l += 1; i += 1; }\n                  Ordering::Greater => { arr.swap(i, g); g -= 1; }\n                  Ordering::Equal => i += 1,\n              }\n          }\n          if l < n - g - 1 {\n              adaptive_sort(&mut arr[..l]);\n              arr = &mut arr[g + 1..];\n          } else {\n  ... (9 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n      while arr.len() > 24 {\n          let n = arr.len();\n          let (mut a, mut d) = (true, true);\n          for j in 1..n {\n              match arr[j-1].cmp(&arr[j]) {\n                  Ordering::Greater => a = false,\n                  Ordering::Less => d = false,\n                  _ => {},\n              }\n              if !a && !d { break; }\n          }\n          if a { return; } if d { arr.reverse(); return; }\n  \n          let (m, h) = (n / 2, n - 1);\n          if arr[m] < arr[0] { arr.swap(0, m); }\n          if arr[h] < arr[0] { arr.swap(0, h); }\n          if arr[h] < arr[m] { arr.swap(m, h); }\n          arr.swap(m, h);\n  \n          let (p, mut i) = (arr[h].clone(), 0);\n          for j in 0..h {\n              if arr[j] <= p {\n                  arr.swap(i, j);\n                  i += 1;\n              }\n          }\n          arr.swap(i, h);\n  \n          if i < n / 2 {\n  ... (12 more lines)", "parent_metrics": {"score": 0.9988366952831427, "combined_score": 0.9988366952831427, "compile_success": 1.0, "correctness": 1, "performance_score": 0.99819710729286, "adaptability_score": 0.9997960772685663, "avg_time": 0.00018061489999999998, "memory_safe": 1.0}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9988\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness unchanged at 0.9988\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[3.914e-05, 0.00053264, 2.3e-05, 0.00030062, 1.209e-06, 1.12e-05, 1.7591e-05, 0.000341689, 4.192e-05, 0.00049714]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // Adaptive hybrid Quicksort: 3-way partitioning for duplicate handling,\n  // insertion sort for small arrays, and sorted detection.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n  \n      // Insertion sort for small arrays (threshold 24)\n      if len <= 24 {\n          if len >= 2 {\n              for i in 1..len {\n                  let mut j = i;\n                  while j > 0 && arr[j - 1] > arr[j] {\n                      arr.swap(j, j - 1);\n                      j -= 1;\n                  }\n              }\n          }\n          return;\n      }\n  \n      // Check for sorted/reverse sorted\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..len {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  \n  ... (32 more lines)\nwith:\n  // Optimized Hybrid Quicksort: Iterative tail-call, 3-way partitioning\n  // for duplicate keys, median-of-three pivot, and sorted detection.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n      while arr.len() > 24 {\n          let n = arr.len();\n          let (mut asc, mut desc) = (true, true);\n          for i in 1..n {\n              if arr[i-1] > arr[i] { asc = false; } else if arr[i-1] < arr[i] { desc = false; }\n              if !asc && !desc { break; }\n          }\n          if asc { return; } if desc { arr.reverse(); return; }\n  \n          let pivot = {\n              let (m, h) = (n / 2, n - 1);\n              if arr[m] < arr[0] { arr.swap(0, m); }\n              if arr[h] < arr[0] { arr.swap(0, h); }\n              if arr[h] < arr[m] { arr.swap(m, h); }\n              arr[m].clone()\n          };\n  \n          let (mut l, mut i, mut g) = (0, 0, n - 1);\n          while i <= g {\n              match arr[i].cmp(&pivot) {\n                  Ordering::Less => { arr.swap(l, i); l += 1; i += 1; }\n                  Ordering::Greater => { arr.swap(i, g); g -= 1; }\n                  Ordering::Equal => i += 1,\n              }\n          }\n          if l < n - g - 1 {\n  ... (12 more lines)\n- Metrics: score: 0.9988, combined_score: 0.9988, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9982, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n  // for small partitions, and O(N) detection of sorted/reverse-sorted data.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n  \n      // Adaptive detection for sorted and reverse-sorted sequences\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  \n      // Use insertion sort for small partitions to reduce recursion overhead\n      if n <= 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n          return;\n      }\n  \n      // Median-of-three pivot selection (start, middle, end)\n  ... (20 more lines)\nwith:\n  // Adaptive hybrid Quicksort: 3-way partitioning for duplicate handling,\n  // insertion sort for small arrays, and sorted detection.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n  \n      // Insertion sort for small arrays (threshold 24)\n      if len <= 24 {\n          if len >= 2 {\n              for i in 1..len {\n                  let mut j = i;\n                  while j > 0 && arr[j - 1] > arr[j] {\n                      arr.swap(j, j - 1);\n                      j -= 1;\n                  }\n              }\n          }\n          return;\n      }\n  \n      // Check for sorted/reverse sorted\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..len {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  \n  ... (32 more lines)\n- Metrics: score: 0.9988, combined_score: 0.9988, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9982, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Adaptive hybrid Quicksort: 3-way partitioning for duplicate handling,\n  // insertion sort for small arrays, and sorted detection.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n  \n      // Insertion sort for small arrays (threshold 24)\n      if len <= 24 {\n          if len >= 2 {\n              for i in 1..len {\n                  let mut j = i;\n                  while j > 0 && arr[j - 1] > arr[j] {\n                      arr.swap(j, j - 1);\n                      j -= 1;\n                  }\n              }\n          }\n          return;\n      }\n  \n      // Check for sorted/reverse sorted\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..len {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  ... (34 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Optimized Hybrid Quicksort: Median-of-three, Lomuto partitioning,\n  // insertion sort for small partitions, and O(N) pre-sorted/reverse detection.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n  \n      // Detection for sorted/reverse sequences provides high adaptability\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  \n      // Insertion sort for small partitions reduces recursion overhead\n      if n <= 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n          }\n          return;\n      }\n  \n      // Median-of-three pivot selection\n      let (m, h) = (n / 2, n - 1);\n      if arr[m] < arr[0] { arr.swap(0, m); }\n  ... (19 more lines)\n- Metrics: score: 0.9990, combined_score: 0.9990, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9985, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9991)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n// for small partitions, and O(N) detection of sorted/reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut asc, mut desc) = (true, true);\n        for i in 1..n {\n            if arr[i-1] > arr[i] { asc = false; }\n            else if arr[i-1] < arr[i] { desc = false; }\n            if !asc && !desc { break; }\n        }\n        if asc { return; }\n        if desc { return arr.reverse(); }\n\n        let (m, h) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[h] < arr[0] { arr.swap(0, h); }\n        if arr[h] < arr[m] { arr.swap(m, h); }\n        arr.swap(m, h);\n        \n        let (pivot, mut i) = (arr[h].clone(), 0);\n        for j in 0..h {\n            if arr[j] <= pivot {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, h);\n        \n        if i < n / 2 {\n            adaptive_sort(&mut arr[..i]);\n            arr = &mut arr[i + 1..];\n        } else {\n            adaptive_sort(&mut arr[i + 1..]);\n            arr = &mut arr[..i];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9991), Performs well on combined_score (0.9991), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9986), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0001), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9990)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid Quicksort: Median-of-three, Lomuto partitioning, \n// insertion sort for small partitions, and O(N) pre-sorted/reverse detection.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n\n    // Detection for sorted/reverse sequences provides high adaptability\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Insertion sort for small partitions reduces recursion overhead\n    if n <= 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n\n    // Median-of-three pivot selection\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    \n    let pivot = arr[h].clone();\n    let mut i = 0;\n    for j in 0..h {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    \n    let (left, right) = arr.split_at_mut(i);\n    adaptive_sort(left);\n    adaptive_sort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9990), Performs well on combined_score (0.9990), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9985), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9988)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: 3-way partitioning for duplicate handling,\n// insertion sort for small arrays, and sorted detection.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    \n    // Insertion sort for small arrays (threshold 24)\n    if len <= 24 {\n        if len >= 2 {\n            for i in 1..len {\n                let mut j = i;\n                while j > 0 && arr[j - 1] > arr[j] {\n                    arr.swap(j, j - 1);\n                    j -= 1;\n                }\n            }\n        }\n        return;\n    }\n\n    // Check for sorted/reverse sorted\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..len {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Median-of-three pivot selection\n    let m = len / 2;\n    let last = len - 1;\n    if arr[0] > arr[m] { arr.swap(0, m); }\n    if arr[0] > arr[last] { arr.swap(0, last); }\n    if arr[m] > arr[last] { arr.swap(m, last); }\n    \n    // 3-way partitioning (Dutch National Flag)\n    let pivot = arr[m].clone();\n    let (mut lt, mut gt) = (0, len - 1);\n    let mut i = 0;\n    \n    while i <= gt {\n        match arr[i].cmp(&pivot) {\n            Ordering::Less => {\n                arr.swap(lt, i);\n                lt += 1;\n                i += 1;\n            }\n            Ordering::Greater => {\n                arr.swap(i, gt);\n                gt -= 1;\n            }\n            Ordering::Equal => i += 1,\n        }\n    }\n    \n    adaptive_sort(&mut arr[..lt]);\n    if gt + 1 < len {\n        adaptive_sort(&mut arr[gt + 1..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9988), Performs well on combined_score (0.9988), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9982), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9988)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid Quicksort: Iterative tail-call, 3-way partitioning\n// for duplicate keys, median-of-three pivot, and sorted detection.\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut asc, mut desc) = (true, true);\n        for i in 1..n {\n            if arr[i-1] > arr[i] { asc = false; } else if arr[i-1] < arr[i] { desc = false; }\n            if !asc && !desc { break; }\n        }\n        if asc { return; } if desc { arr.reverse(); return; }\n\n        let pivot = {\n            let (m, h) = (n / 2, n - 1);\n            if arr[m] < arr[0] { arr.swap(0, m); }\n            if arr[h] < arr[0] { arr.swap(0, h); }\n            if arr[h] < arr[m] { arr.swap(m, h); }\n            arr[m].clone()\n        };\n\n        let (mut l, mut i, mut g) = (0, 0, n - 1);\n        while i <= g {\n            match arr[i].cmp(&pivot) {\n                Ordering::Less => { arr.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { arr.swap(i, g); g -= 1; }\n                Ordering::Equal => i += 1,\n            }\n        }\n        if l < n - g - 1 {\n            adaptive_sort(&mut arr[..l]);\n            arr = &mut arr[g + 1..];\n        } else {\n            adaptive_sort(&mut arr[g + 1..]);\n            arr = &mut arr[..l];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9988), Performs well on combined_score (0.9988), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9982), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9988, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: 3-way partitioning for duplicate handling,\n// insertion sort for small arrays, and sorted detection.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    \n    // Insertion sort for small arrays (threshold 24)\n    if len <= 24 {\n        if len >= 2 {\n            for i in 1..len {\n                let mut j = i;\n                while j > 0 && arr[j - 1] > arr[j] {\n                    arr.swap(j, j - 1);\n                    j -= 1;\n                }\n            }\n        }\n        return;\n    }\n\n    // Check for sorted/reverse sorted\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..len {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Median-of-three pivot selection\n    let m = len / 2;\n    let last = len - 1;\n    if arr[0] > arr[m] { arr.swap(0, m); }\n    if arr[0] > arr[last] { arr.swap(0, last); }\n    if arr[m] > arr[last] { arr.swap(m, last); }\n    \n    // 3-way partitioning (Dutch National Flag)\n    let pivot = arr[m].clone();\n    let (mut lt, mut gt) = (0, len - 1);\n    let mut i = 0;\n    \n    while i <= gt {\n        match arr[i].cmp(&pivot) {\n            Ordering::Less => {\n                arr.swap(lt, i);\n                lt += 1;\n                i += 1;\n            }\n            Ordering::Greater => {\n                arr.swap(i, gt);\n                gt -= 1;\n            }\n            Ordering::Equal => i += 1,\n        }\n    }\n    \n    adaptive_sort(&mut arr[..lt]);\n    if gt + 1 < len {\n        adaptive_sort(&mut arr[gt + 1..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9991, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n// for small partitions, and O(N) detection of sorted/reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut asc, mut desc) = (true, true);\n        for i in 1..n {\n            if arr[i-1] > arr[i] { asc = false; }\n            else if arr[i-1] < arr[i] { desc = false; }\n            if !asc && !desc { break; }\n        }\n        if asc { return; }\n        if desc { return arr.reverse(); }\n\n        let (m, h) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[h] < arr[0] { arr.swap(0, h); }\n        if arr[h] < arr[m] { arr.swap(m, h); }\n        arr.swap(m, h);\n        \n        let (pivot, mut i) = (arr[h].clone(), 0);\n        for j in 0..h {\n            if arr[j] <= pivot {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, h);\n        \n        if i < n / 2 {\n            adaptive_sort(&mut arr[..i]);\n            arr = &mut arr[i + 1..];\n        } else {\n            adaptive_sort(&mut arr[i + 1..]);\n            arr = &mut arr[..i];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9990, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid Quicksort: Median-of-three, Lomuto partitioning, \n// insertion sort for small partitions, and O(N) pre-sorted/reverse detection.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n\n    // Detection for sorted/reverse sequences provides high adaptability\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Insertion sort for small partitions reduces recursion overhead\n    if n <= 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n        }\n        return;\n    }\n\n    // Median-of-three pivot selection\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    \n    let pivot = arr[h].clone();\n    let mut i = 0;\n    for j in 0..h {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    \n    let (left, right) = arr.split_at_mut(i);\n    adaptive_sort(left);\n    adaptive_sort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized Hybrid Quicksort: Iterative tail-call, 3-way partitioning\n// for duplicate keys, median-of-three pivot, and sorted detection.\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut asc, mut desc) = (true, true);\n        for i in 1..n {\n            if arr[i-1] > arr[i] { asc = false; } else if arr[i-1] < arr[i] { desc = false; }\n            if !asc && !desc { break; }\n        }\n        if asc { return; } if desc { arr.reverse(); return; }\n\n        let pivot = {\n            let (m, h) = (n / 2, n - 1);\n            if arr[m] < arr[0] { arr.swap(0, m); }\n            if arr[h] < arr[0] { arr.swap(0, h); }\n            if arr[h] < arr[m] { arr.swap(m, h); }\n            arr[m].clone()\n        };\n\n        let (mut l, mut i, mut g) = (0, 0, n - 1);\n        while i <= g {\n            match arr[i].cmp(&pivot) {\n                Ordering::Less => { arr.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { arr.swap(i, g); g -= 1; }\n                Ordering::Equal => i += 1,\n            }\n        }\n        if l < n - g - 1 {\n            adaptive_sort(&mut arr[..l]);\n            arr = &mut arr[g + 1..];\n        } else {\n            adaptive_sort(&mut arr[g + 1..]);\n            arr = &mut arr[..l];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following improvements are proposed to enhance the fitness score by optimizing the sorting performance and adaptability while simplifying the codebase.\n\n1.  **Optimized Pre-sorted Check**: The previous pre-sorted detection used multiple comparisons (`>` and `<`). By using `match arr[i-1].cmp(&arr[i])`, we reduce the number of comparisons per element in the pre-check to exactly one, which improves performance on partially sorted or patterned data.\n2.  **Lomuto Partition with Median-of-Three**: Program 1, our top performer, used a Lomuto partition with a specific Median-of-Three implementation. This version adopts that approach, as it proved faster in benchmarks than the 3-way partition, likely due to lower constant overhead.\n3.  **Refined Pre-sorted Logic**: The pre-sorted check now correctly handles all-equal elements by returning immediately (non-decreasing).\n4.  **Shorter, Cleaner Code**: Variable names and logical blocks have been condensed to meet the goal of simplifying the program while maintaining robustness.\n\n```rust\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut asc, mut desc) = (true, true);\n        for i in 1..n {\n            if arr[i-1] > arr[i] { asc = false; } else if arr[i-1] < arr[i] { desc = false; }\n            if !asc && !desc { break; }\n        }\n        if asc { return; } if desc { arr.reverse(); return; }\n\n        let pivot = {\n            let (m, h) = (n / 2, n - 1);\n            if arr[m] < arr[0] { arr.swap(0, m); }\n            if arr[h] < arr[0] { arr.swap(0, h); }\n            if arr[h] < arr[m] { arr.swap(m, h); }\n            arr[m].clone()\n        };\n\n        let (mut l, mut i, mut g) = (0, 0, n - 1);\n        while i <= g {\n            match arr[i].cmp(&pivot) {\n                Ordering::Less => { arr.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { arr.swap(i, g); g -= 1; }\n                Ordering::Equal => i += 1,\n            }\n        }\n        if l < n - g - 1 {\n            adaptive_sort(&mut arr[..l]);\n            arr = &mut arr[g + 1..];\n        } else {\n            adaptive_sort(&mut arr[g + 1..]);\n            arr = &mut arr[..l];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n    }\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 24 {\n        let n = arr.len();\n        let (mut a, mut d) = (true, true);\n        for j in 1..n {\n            match arr[j-1].cmp(&arr[j]) {\n                Ordering::Greater => a = false,\n                Ordering::Less => d = false,\n                _ => {},\n            }\n            if !a && !d { break; }\n        }\n        if a { return; } if d { arr.reverse(); return; }\n\n        let (m, h) = (n / 2, n - 1);\n        if arr[m] < arr[0] { arr.swap(0, m); }\n        if arr[h] < arr[0] { arr.swap(0, h); }\n        if arr[h] < arr[m] { arr.swap(m, h); }\n        arr.swap(m, h);\n        \n        let (p, mut i) = (arr[h].clone(), 0);\n        for j in 0..h {\n            if arr[j] <= p {\n                arr.swap(i, j);\n                i += 1;\n            }\n        }\n        arr.swap(i, h);\n        \n        if i < n / 2 {\n            adaptive_sort(&mut arr[..i]);\n            arr = &mut arr[i + 1..];\n        } else {\n            adaptive_sort(&mut arr[i + 1..]);\n            arr = &mut arr[..i];\n        }\n    }\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1; }\n    }\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [3.446e-05, 0.00045141, 2.39e-05, 0.00021954, 1.29e-06, 1.2e-05, 1.863e-05, 0.000320309, 2.7871e-05, 0.000416029], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}