{"id": "c217d139-040a-4152-b96a-60bb3d72a5ae", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n// for small partitions, and O(N) detection of sorted/reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n\n    // Adaptive detection for sorted and reverse-sorted sequences\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Use insertion sort for small partitions to reduce recursion overhead\n    if n <= 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n        return;\n    }\n\n    // Median-of-three pivot selection (start, middle, end)\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    \n    let pivot = arr[h].clone();\n    let mut i = 0;\n    for j in 0..h {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, n - 1);\n    \n    let (left, right) = arr.split_at_mut(i);\n    adaptive_sort(left);\n    adaptive_sort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "fb9e6388-0ebf-4fbf-9308-05b3e7b55a91", "generation": 2, "timestamp": 1772732222.9785838, "iteration_found": 8, "metrics": {"score": 0.9990683181482525, "combined_score": 0.9990683181482525, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9985552204082478, "adaptability_score": 0.9998379647582593, "avg_time": 0.00014468699999999998, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Optimized implementation: Hybrid Quicksort with Insertion Sort for small partitions,\n  // Median-of-Three pivot, and early exit for sorted data.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len <= 1 {\n          return;\n      }\n  \n      // Check if already sorted (O(N) worst case, O(1) average for random)\n      // This provides high adaptability to pre-sorted data\n      let mut sorted = true;\n      for i in 0..len-1 {\n          if arr[i] > arr[i+1] {\n              sorted = false;\n              break;\n          }\n      }\n      if sorted {\n          return;\n      }\n  \n      // Use insertion sort for small arrays\n      if len <= 20 {\n          insertion_sort(arr);\n          return;\n      }\n  \n      quicksort(arr, 0, len - 1);\n  ... (52 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n  // for small partitions, and O(N) detection of sorted/reverse-sorted data.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n  \n      // Adaptive detection for sorted and reverse-sorted sequences\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          else if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n  \n      // Use insertion sort for small partitions to reduce recursion overhead\n      if n <= 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] {\n                  arr.swap(j, j - 1);\n                  j -= 1;\n              }\n          }\n          return;\n      }\n  \n  ... (22 more lines)", "parent_metrics": {"score": 0.9987547824858535, "combined_score": 0.9987547824858535, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9980511255282035, "adaptability_score": 0.9998102679223283, "avg_time": 0.00019526800000000001, "memory_safe": 1.0}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9988\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness unchanged at 0.9988\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[3.409e-05, 0.00046093, 1.636e-05, 0.00020314, 1.537e-05, 0.00023715, 4.754e-05, 0.00053566, 2.857e-05, 0.00037387]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      if arr.len() <= 1 {\n          return;\n      }\n  \n      // Use quicksort as the base implementation\n      quicksort(arr, 0, arr.len() - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          let pivot_index = partition(arr, low, high);\n  \n          // Recursively sort elements before and after partition\n          if pivot_index > 0 {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n  ... (49 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Optimized implementation: Hybrid Quicksort with Insertion Sort for small partitions,\n  // Median-of-Three pivot, and early exit for sorted data.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len <= 1 {\n          return;\n      }\n  \n      // Check if already sorted (O(N) worst case, O(1) average for random)\n      // This provides high adaptability to pre-sorted data\n      let mut sorted = true;\n      for i in 0..len-1 {\n          if arr[i] > arr[i+1] {\n              sorted = false;\n              break;\n          }\n      }\n      if sorted {\n          return;\n      }\n  \n      // Use insertion sort for small arrays\n      if len <= 20 {\n          insertion_sort(arr);\n          return;\n      }\n  \n      quicksort(arr, 0, len - 1);\n  ... (52 more lines)\n- Metrics: score: 0.9988, combined_score: 0.9988, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9981, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9988)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized implementation: Hybrid Quicksort with Insertion Sort for small partitions,\n// Median-of-Three pivot, and early exit for sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len <= 1 {\n        return;\n    }\n    \n    // Check if already sorted (O(N) worst case, O(1) average for random)\n    // This provides high adaptability to pre-sorted data\n    let mut sorted = true;\n    for i in 0..len-1 {\n        if arr[i] > arr[i+1] {\n            sorted = false;\n            break;\n        }\n    }\n    if sorted {\n        return;\n    }\n    \n    // Use insertion sort for small arrays\n    if len <= 20 {\n        insertion_sort(arr);\n        return;\n    }\n    \n    quicksort(arr, 0, len - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        // Hybrid: Switch to insertion sort for small partitions\n        if high - low < 20 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n\n        let pivot_index = partition(arr, low, high);\n        \n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection to handle sorted/reverse-sorted data efficiently\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[high] < arr[mid] { arr.swap(mid, high); }\n    \n    // Place pivot (median) at high\n    arr.swap(mid, high);\n    let pivot = arr[high].clone();\n    \n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9988), Performs well on combined_score (0.9988), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9981), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized implementation: Hybrid Quicksort with Insertion Sort for small partitions,\n// Median-of-Three pivot, and early exit for sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len <= 1 {\n        return;\n    }\n    \n    // Check if already sorted (O(N) worst case, O(1) average for random)\n    // This provides high adaptability to pre-sorted data\n    let mut sorted = true;\n    for i in 0..len-1 {\n        if arr[i] > arr[i+1] {\n            sorted = false;\n            break;\n        }\n    }\n    if sorted {\n        return;\n    }\n    \n    // Use insertion sort for small arrays\n    if len <= 20 {\n        insertion_sort(arr);\n        return;\n    }\n    \n    quicksort(arr, 0, len - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        // Hybrid: Switch to insertion sort for small partitions\n        if high - low < 20 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n\n        let pivot_index = partition(arr, low, high);\n        \n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection to handle sorted/reverse-sorted data efficiently\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[high] < arr[mid] { arr.swap(mid, high); }\n    \n    // Place pivot (median) at high\n    arr.swap(mid, high);\n    let pivot = arr[high].clone();\n    \n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The sorting algorithm is improved by consolidating the hybrid logic into a more concise recursive function, adding support for reverse-sorted data detection, and using more idiomatic Rust features like `split_at_mut`. These changes simplify the codebase while enhancing its adaptability to different data patterns.\n\n<<<<<<< SEARCH\n// EVOLVE-BLOCK-START\n// Optimized implementation: Hybrid Quicksort with Insertion Sort for small partitions,\n// Median-of-Three pivot, and early exit for sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len <= 1 {\n        return;\n    }\n    \n    // Check if already sorted (O(N) worst case, O(1) average for random)\n    // This provides high adaptability to pre-sorted data\n    let mut sorted = true;\n    for i in 0..len-1 {\n        if arr[i] > arr[i+1] {\n            sorted = false;\n            break;\n        }\n    }\n    if sorted {\n        return;\n    }\n    \n    // Use insertion sort for small arrays\n    if len <= 20 {\n        insertion_sort(arr);\n        return;\n    }\n    \n    quicksort(arr, 0, len - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        // Hybrid: Switch to insertion sort for small partitions\n        if high - low < 20 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n\n        let pivot_index = partition(arr, low, high);\n        \n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection to handle sorted/reverse-sorted data efficiently\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[high] < arr[mid] { arr.swap(mid, high); }\n    \n    // Place pivot (median) at high\n    arr.swap(mid, high);\n    let pivot = arr[high].clone();\n    \n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n=======\n// EVOLVE-BLOCK-START\n// Adaptive hybrid Quicksort: Median-of-three pivot selection, insertion sort\n// for small partitions, and O(N) detection of sorted/reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n\n    // Adaptive detection for sorted and reverse-sorted sequences\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        else if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n\n    // Use insertion sort for small partitions to reduce recursion overhead\n    if n <= 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n        return;\n    }\n\n    // Median-of-three pivot selection (start, middle, end)\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    \n    let pivot = arr[h].clone();\n    let mut i = 0;\n    for j in 0..h {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, n - 1);\n    \n    let (left, right) = arr.split_at_mut(i);\n    adaptive_sort(left);\n    adaptive_sort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [3.337e-05, 0.00042599, 1.81e-05, 0.00022194, 1.13e-06, 1.07e-05, 1.965e-05, 0.00030121, 3.052e-05, 0.00038426], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}