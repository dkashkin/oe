{"id": "4dad76cb-141e-4bc0-861d-50b7295e712f", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "315ad859-6169-41a7-a1e5-4c099bc18381", "generation": 7, "timestamp": 1772738402.157095, "iteration_found": 35, "metrics": {"score": 0.9883611163548485, "combined_score": 0.9883611163548485, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9820856595971801, "adaptability_score": 0.997774301491351, "avg_time": 0.0018241118, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 96 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false; }\n              if a[i-1] < a[i] { dsc = false; }\n              if !asc && !dsc { break; }\n          }\n          if asc { return; }\n          if dsc { a.reverse(); return; }\n      }\n      while a.len() > 32 {\n          let (l, g) = partition(a);\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n      }\n      for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n      let (m, r) = (a.len() / 2, a.len() - 1);\n      if a[0] > a[m] { a.swap(0, m); }\n      if a[0] > a[r] { a.swap(0, r); }\n      if a[m] > a[r] { a.swap(m, r); }\n      let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n      while i < g {\n          match a[i].cmp(&p) {\n  ... (11 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 96 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false }\n              if a[i-1] < a[i] { dsc = false }\n              if !asc && !dsc { break }\n          }\n          if asc { return }\n          if dsc { a.reverse(); return }\n      }\n      while a.len() > 32 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n          let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n          while i < g {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                  Ordering::Greater => {\n                      g -= 1;\n                      while g > i && a[g] > p { g -= 1 }\n                      a.swap(i, g)\n                  }\n                  _ => i += 1,\n              }\n          }\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n  ... (6 more lines)", "parent_metrics": {"score": 0.9883061622295283, "combined_score": 0.9883061622295283, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9820202328024694, "adaptability_score": 0.9977350563701164, "avg_time": 0.0018308957999999998, "memory_safe": 1.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9883\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.4101 \u2192 0.9883\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00050664, 0.00616357, 0.00014714, 0.00188526, 9.05e-06, 8.943e-05, 0.00016893, 0.003028759, 0.000498649, 0.00581153]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Unknown changes\n- Metrics: score: 0.4101, combined_score: 0.4101, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.2915, adaptability_score: 0.5880, avg_time: 0.2431, memory_safe: 1.0000\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 96 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false; }\n              if a[i-1] < a[i] { dsc = false; }\n              if !asc && !dsc { break; }\n          }\n          if asc { return; }\n          if dsc { a.reverse(); return; }\n      }\n      while a.len() > 32 {\n          let (l, g) = partition(a);\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n      }\n      for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n      let (m, r) = (a.len() / 2, a.len() - 1);\n      if a[0] > a[m] { a.swap(0, m); }\n      if a[0] > a[r] { a.swap(0, r); }\n      if a[m] > a[r] { a.swap(m, r); }\n      let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n      while i < g {\n          match a[i].cmp(&p) {\n  ... (11 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 64 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false }\n              if a[i-1] < a[i] { dsc = false }\n              if !asc && !dsc { break }\n          }\n          if asc { return }\n          if dsc { a.reverse(); return }\n      }\n      while a.len() > 24 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n          let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n          while i < g {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                  Ordering::Greater => {\n                      g -= 1;\n                      while g > i && a[g] > p { g -= 1 }\n                      a.swap(i, g)\n                  }\n                  _ => i += 1,\n              }\n          }\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n  ... (6 more lines)\n- Metrics: score: 0.9881, combined_score: 0.9881, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9816, adaptability_score: 0.9977, avg_time: 0.0019, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  if a.len() > 64 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false; }\n              if a[i-1] < a[i] { dsc = false; }\n              if !asc && !dsc { break; }\n          }\n          if asc { return; }\n          if dsc { a.reverse(); return; }\n      }\n      while a.len() > 24 {\n          let (l, g) = partition(a);\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n      }\nwith:\n  if a.len() > 96 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false; }\n              if a[i-1] < a[i] { dsc = false; }\n              if !asc && !dsc { break; }\n          }\n          if asc { return; }\n          if dsc { a.reverse(); return; }\n      }\n      while a.len() > 32 {\n          let (l, g) = partition(a);\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n      }\nChange 2: Replace:\n  let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n      while i < g {\n          match a[i].cmp(&p) {\n              Ordering::Less => { a.swap(i, l); l += 1; i += 1; }\n              Ordering::Greater => { g -= 1; a.swap(i, g); }\n              _ => i += 1,\n          }\n      }\nwith:\n  let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n      while i < g {\n          match a[i].cmp(&p) {\n              Ordering::Less => { a.swap(i, l); l += 1; i += 1; }\n              Ordering::Greater => {\n                  g -= 1;\n                  while g > i && a[g] > p { g -= 1; }\n                  a.swap(i, g);\n              }\n              _ => i += 1,\n          }\n      }\n- Metrics: score: 0.9883, combined_score: 0.9883, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9820, adaptability_score: 0.9977, avg_time: 0.0018, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9883)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false; }\n            if a[i-1] < a[i] { dsc = false; }\n            if !asc && !dsc { break; }\n        }\n        if asc { return; }\n        if dsc { a.reverse(); return; }\n    }\n    while a.len() > 32 {\n        let (l, g) = partition(a);\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n    }\n}\n\nfn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n    let (m, r) = (a.len() / 2, a.len() - 1);\n    if a[0] > a[m] { a.swap(0, m); }\n    if a[0] > a[r] { a.swap(0, r); }\n    if a[m] > a[r] { a.swap(m, r); }\n    let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => {\n                g -= 1;\n                while g > i && a[g] > p { g -= 1; }\n                a.swap(i, g);\n            }\n            _ => i += 1,\n        }\n    }\n    (l, g)\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9883), Performs well on combined_score (0.9883), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9820), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9881)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 64 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 24 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9881), Performs well on combined_score (0.9881), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9816), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0019), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.4101)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    if arr.len() <= 1 {\n        return;\n    }\n    \n    // Use quicksort as the base implementation\n    quicksort(arr, 0, arr.len() - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        let pivot_index = partition(arr, low, high);\n        \n        // Recursively sort elements before and after partition\n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Choose the last element as pivot (can be evolved to use better strategies)\n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Helper function to detect if array is nearly sorted\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    if arr.len() <= 1 {\n        return true;\n    }\n    \n    let mut inversions = 0;\n    let max_inversions = ((arr.len() * (arr.len() - 1)) / 2) as f64 * threshold;\n    \n    for i in 0..arr.len() - 1 {\n        for j in i + 1..arr.len() {\n            if arr[i] > arr[j] {\n                inversions += 1;\n                if inversions as f64 > max_inversions {\n                    return false;\n                }\n            }\n        }\n    }\n    \n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.4101), Performs well on combined_score (0.4101), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.2915), Performs well on adaptability_score (0.5880), Performs well on avg_time (0.2431), Performs well on memory_safe (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4101, Type: Experimental)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    if arr.len() <= 1 {\n        return;\n    }\n    \n    // Use quicksort as the base implementation\n    quicksort(arr, 0, arr.len() - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        let pivot_index = partition(arr, low, high);\n        \n        // Recursively sort elements before and after partition\n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Choose the last element as pivot (can be evolved to use better strategies)\n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Helper function to detect if array is nearly sorted\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    if arr.len() <= 1 {\n        return true;\n    }\n    \n    let mut inversions = 0;\n    let max_inversions = ((arr.len() * (arr.len() - 1)) / 2) as f64 * threshold;\n    \n    for i in 0..arr.len() - 1 {\n        for j in i + 1..arr.len() {\n            if arr[i] > arr[j] {\n                inversions += 1;\n                if inversions as f64 > max_inversions {\n                    return false;\n                }\n            }\n        }\n    }\n    \n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9881, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 64 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 24 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false; }\n            if a[i-1] < a[i] { dsc = false; }\n            if !asc && !dsc { break; }\n        }\n        if asc { return; }\n        if dsc { a.reverse(); return; }\n    }\n    while a.len() > 32 {\n        let (l, g) = partition(a);\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n    }\n}\n\nfn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n    let (m, r) = (a.len() / 2, a.len() - 1);\n    if a[0] > a[m] { a.swap(0, m); }\n    if a[0] > a[r] { a.swap(0, r); }\n    if a[m] > a[r] { a.swap(m, r); }\n    let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => {\n                g -= 1;\n                while g > i && a[g] > p { g -= 1; }\n                a.swap(i, g);\n            }\n            _ => i += 1,\n        }\n    }\n    (l, g)\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current sorting algorithm is a high-performing hybrid sort combining a pre-sortedness check, 3-way quicksort (Dijkstra's Dutch National Flag partitioning), and insertion sort. It achieves a high fitness score (0.9883) by being highly adaptive to various data patterns (sorted, reversed, many duplicates, random).\n\nThe following improvements focus on:\n1.  **Inlining the Partitioning Logic**: Consolidating the `partition` function into `adaptive_sort` reduces function call overhead and helps address the suggestion to simplify the code length while maintaining the proven high-performance logic.\n2.  **Threshold Preservation**: Retaining the 96/32 thresholds which were found to be more effective for overall fitness than the 64/24 alternatives explored in previous attempts.\n3.  **Code Compactness**: Cleaning up the syntax slightly to improve readability and reduce character count without changing the algorithmic complexity.\n4.  **Adaptability**: Maintaining the 3-way partition ensures the algorithm remains robust against datasets with many duplicate elements, a key factor in its high adaptability score.\n\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false; }\n            if a[i-1] < a[i] { dsc = false; }\n            if !asc && !dsc { break; }\n        }\n        if asc { return; }\n        if dsc { a.reverse(); return; }\n    }\n    while a.len() > 32 {\n        let (l, g) = partition(a);\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..]; }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l]; }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n    }\n}\n\nfn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n    let (m, r) = (a.len() / 2, a.len() - 1);\n    if a[0] > a[m] { a.swap(0, m); }\n    if a[0] > a[r] { a.swap(0, r); }\n    if a[m] > a[r] { a.swap(m, r); }\n    let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(i, l); l += 1; i += 1; }\n            Ordering::Greater => {\n                g -= 1;\n                while g > i && a[g] > p { g -= 1; }\n                a.swap(i, g);\n            }\n            _ => i += 1,\n        }\n    }\n    (l, g)\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00051246, 0.006110609, 0.00015573, 0.00208695, 8.64e-06, 8.563e-05, 0.00016273, 0.003020549, 0.00047002, 0.0056278], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}