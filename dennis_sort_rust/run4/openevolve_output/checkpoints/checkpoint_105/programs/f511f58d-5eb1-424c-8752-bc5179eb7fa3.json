{"id": "f511f58d-5eb1-424c-8752-bc5179eb7fa3", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "83019e6d-1177-4412-947f-07831b1234c9", "generation": 6, "timestamp": 1772738403.7096138, "iteration_found": 37, "metrics": {"score": 0.98713805869309, "combined_score": 0.98713805869309, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9801228460099946, "adaptability_score": 0.9976608777177333, "avg_time": 0.0020280268, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          else if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      if n < 24 { insertion_sort(arr); return; }\n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i-1] {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j-1] > v {\n                  arr[j] = arr[j-1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }", "parent_metrics": {"score": 0.9868979898175253, "combined_score": 0.9868979898175253, "compile_success": 1.0, "correctness": 1, "performance_score": 0.979765340459535, "adaptability_score": 0.9975969638545106, "avg_time": 0.0020652557, "memory_safe": 1.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9869\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9870 \u2192 0.9869. Consider revising recent changes.\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00060663, 0.006504819, 0.00030079, 0.003595289, 7.04e-06, 7.044e-05, 0.00014926, 0.002925969, 0.00047883, 0.00601349]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      if n < 24 { insertion_sort(arr); return; }\n  \n      let (mut sorted, mut reverse) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { sorted = false; }\n          if arr[i-1] < arr[i] { reverse = false; }\n          if !sorted && !reverse { break; }\n      }\n      if sorted { return; }\n      if reverse { arr.reverse(); return; }\n  \n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n      while low < high {\n          if high - low < 16 {\n              insertion_sort(&mut arr[low..=high]);\n              return;\n  ... (50 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\n  \n  fn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n  ... (26 more lines)\n- Metrics: score: 0.9870, combined_score: 0.9870, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9800, adaptability_score: 0.9976, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n  \n      let (mut sorted, mut reverse) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { sorted = false; }\n          if arr[i-1] < arr[i] { reverse = false; }\n          if !sorted && !reverse { break; }\n      }\n      if sorted { return; }\n      if reverse { arr.reverse(); return; }\n  \n      quicksort(arr, 0, n - 1);\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\nChange 2: Replace:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              if gt < h { quicksort(arr, gt + 1, h); }\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nwith:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nChange 3: Replace:\n  // Helper function for insertion sort (useful for small arrays)\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i - 1] {\n              let val = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > val {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = val;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\n- Metrics: score: 0.9870, combined_score: 0.9870, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9799, adaptability_score: 0.9976, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 32 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          else if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 32 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nChange 2: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i-1] {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > v {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n      }\n  }\n- Metrics: score: 0.9872, combined_score: 0.9872, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9803, adaptability_score: 0.9977, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9873)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9873), Performs well on combined_score (0.9873), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9804), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9872)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 32 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 32 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9872), Performs well on combined_score (0.9872), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9803), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9870)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9870), Performs well on combined_score (0.9870), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9799), Performs well on adaptability_score (0.9976), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9870)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9870), Performs well on combined_score (0.9870), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9800), Performs well on adaptability_score (0.9976), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9867)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    \n    let (mut sorted, mut reverse) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { sorted = false; }\n        if arr[i-1] < arr[i] { reverse = false; }\n        if !sorted && !reverse { break; }\n    }\n    if sorted { return; }\n    if reverse { arr.reverse(); return; }\n    \n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            if gt < h { quicksort(arr, gt + 1, h); }\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let val = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > val {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = val;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9869)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    if n < 24 { insertion_sort(arr); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j-1] > v {\n                arr[j] = arr[j-1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9759)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    if n < 24 { insertion_sort(arr); return; }\n    \n    let (mut sorted, mut reverse) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { sorted = false; }\n        if arr[i-1] < arr[i] { reverse = false; }\n        if !sorted && !reverse { break; }\n    }\n    if sorted { return; }\n    if reverse { arr.reverse(); return; }\n    \n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n        let pivot_index = partition(arr, low, high);\n        \n        let left_len = if pivot_index > low { pivot_index - low } else { 0 };\n        let right_len = if high > pivot_index { high - pivot_index } else { 0 };\n        \n        if left_len < right_len {\n            if left_len > 0 { quicksort(arr, low, pivot_index - 1); }\n            low = pivot_index + 1;\n        } else {\n            if right_len > 0 { quicksort(arr, pivot_index + 1, high); }\n            if pivot_index > 0 { high = pivot_index - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(mid, low); }\n    if arr[high] < arr[low] { arr.swap(high, low); }\n    if arr[high] < arr[mid] { arr.swap(high, mid); }\n    let pivot = arr[mid].clone();\n    arr.swap(mid, high);\n    \n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let val = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > val {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = val;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9867, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    \n    let (mut sorted, mut reverse) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { sorted = false; }\n        if arr[i-1] < arr[i] { reverse = false; }\n        if !sorted && !reverse { break; }\n    }\n    if sorted { return; }\n    if reverse { arr.reverse(); return; }\n    \n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            if gt < h { quicksort(arr, gt + 1, h); }\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let val = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > val {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = val;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9759, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    if n < 24 { insertion_sort(arr); return; }\n    \n    let (mut sorted, mut reverse) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { sorted = false; }\n        if arr[i-1] < arr[i] { reverse = false; }\n        if !sorted && !reverse { break; }\n    }\n    if sorted { return; }\n    if reverse { arr.reverse(); return; }\n    \n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n        let pivot_index = partition(arr, low, high);\n        \n        let left_len = if pivot_index > low { pivot_index - low } else { 0 };\n        let right_len = if high > pivot_index { high - pivot_index } else { 0 };\n        \n        if left_len < right_len {\n            if left_len > 0 { quicksort(arr, low, pivot_index - 1); }\n            low = pivot_index + 1;\n        } else {\n            if right_len > 0 { quicksort(arr, pivot_index + 1, high); }\n            if pivot_index > 0 { high = pivot_index - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(mid, low); }\n    if arr[high] < arr[low] { arr.swap(high, low); }\n    if arr[high] < arr[mid] { arr.swap(high, mid); }\n    let pivot = arr[mid].clone();\n    arr.swap(mid, high);\n    \n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let val = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > val {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = val;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9873, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9872, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 32 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 32 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    if n < 24 { insertion_sort(arr); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j-1] > v {\n                arr[j] = arr[j-1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will revert to the highest-performing architecture identified in the program history (Program 1), which achieved a fitness of 0.9873. Recent iterations introduced slight variations in the order of checks in `adaptive_sort` and switched the `insertion_sort` implementation to a shifting-based one, which resulted in a slight fitness decline (0.9870 \u2192 0.9869). \n\nThe key changes include:\n1.  **Reordering `adaptive_sort` checks**: Moving the small-array threshold check (`n < 24`) to the very beginning of the function to avoid redundant O(n) sorted scans for very small arrays.\n2.  **Optimizing `insertion_sort`**: Reverting to the swap-based implementation. While shifting can sometimes be faster, in this specific Rust environment and with the provided benchmark data, the optimized `swap` mechanism has consistently outperformed the manual shifting with `clone()`.\n3.  **Removing Redundancy**: Removing the explicit `n < 2` guard, as the `n < 24` threshold already covers all cases where the array is too small to sort.\n\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    if n < 24 { insertion_sort(arr); return; }\n    quicksort(arr, 0, n - 1);\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j-1] > v {\n                arr[j] = arr[j-1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n=======\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00052496, 0.006147859, 0.00030729, 0.00354754, 8.61e-06, 9.176e-05, 0.00015548, 0.00299729, 0.000491889, 0.00600759], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}