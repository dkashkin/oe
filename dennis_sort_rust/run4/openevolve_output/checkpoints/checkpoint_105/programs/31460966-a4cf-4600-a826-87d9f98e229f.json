{"id": "31460966-a4cf-4600-a826-87d9f98e229f", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    if n < 24 { insertion_sort(arr); return }\n    quicksort(arr, 0, n - 1)\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1) }\n            l = gt + 1\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1 } else { break }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "4ed4c09f-b654-4abd-b907-ebb548ab09c6", "generation": 6, "timestamp": 1772739823.8935409, "iteration_found": 69, "metrics": {"score": 0.9869693880015957, "combined_score": 0.9869693880015957, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9798790652855207, "adaptability_score": 0.9976048720757081, "avg_time": 0.00205341, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          match arr[i-1].cmp(&arr[i]) {\n              Ordering::Greater => s = false,\n              Ordering::Less => r = false,\n              _ => {}\n          }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { arr.reverse(); return }\n      quicksort(arr, 0, n - 1)\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false }\n          else if arr[i-1] < arr[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { arr.reverse(); return }\n      if n < 24 { insertion_sort(arr); return }\n      quicksort(arr, 0, n - 1)\n  }\nChange 2: Replace:\n  fn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n      let m = l + (h - l) / 2;\n      if arr[m] < arr[l] { arr.swap(m, l) }\n      if arr[h] < arr[l] { arr.swap(h, l) }\n      if arr[h] < arr[m] { arr.swap(h, m) }\n      let p = arr[m].clone();\n      let (mut lt, mut i, mut gt) = (l, l, h);\n      while i <= gt {\n          match arr[i].cmp(&p) {\n              Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n              Ordering::Greater => { if i < gt { arr.swap(i, gt) } if gt == 0 { break } gt -= 1 }\n              _ => i += 1,\n          }\n      }\n      (lt, gt)\n  }\nwith:\n  fn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n      let m = l + (h - l) / 2;\n      if arr[m] < arr[l] { arr.swap(m, l) }\n      if arr[h] < arr[l] { arr.swap(h, l) }\n      if arr[h] < arr[m] { arr.swap(h, m) }\n      let p = arr[m].clone();\n      let (mut lt, mut i, mut gt) = (l, l, h);\n      while i <= gt {\n          match arr[i].cmp(&p) {\n              Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n              Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n              _ => i += 1,\n          }\n      }\n      (lt, gt)\n  }\nChange 3: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i - 1] {\n              let mut j = i;\n              while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1 }\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i - 1] {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > v {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n      }\n  }", "parent_metrics": {"score": 0.9864822101653529, "combined_score": 0.9864822101653529, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9791128901966565, "adaptability_score": 0.9975361901183973, "avg_time": 0.0021332688, "memory_safe": 1.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9865\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9872 \u2192 0.9865. Consider revising recent changes.\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00057679, 0.006458539, 0.00031122, 0.00396248, 1.148e-05, 0.00011469, 0.00016449, 0.00288172, 0.00052271, 0.006328569]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 28 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          else if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\nChange 2: Replace:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\nwith:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 28 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\nChange 3: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i-1] {\n              let mut j = i;\n              while j > 0 && arr[j-1] > arr[j] {\n                  arr.swap(j, j-1);\n                  j -= 1;\n              }\n          }\n      }\n  }\n- Metrics: score: 0.9872, combined_score: 0.9872, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9802, adaptability_score: 0.9976, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 32 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          else if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 32 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\nChange 2: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          if arr[i] < arr[i-1] {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > v {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n      }\n  }\n- Metrics: score: 0.9872, combined_score: 0.9872, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9803, adaptability_score: 0.9977, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return; }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false; }\n          if arr[i-1] < arr[i] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1); }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1; } else { break; }\n          }\n      }\n  }\n  \n  fn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n      let m = l + (h - l) / 2;\n  ... (14 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 { insertion_sort(arr); return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { s = false }\n          else if arr[i-1] < arr[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { arr.reverse(); return }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n      while l < h {\n          if h - l < 24 { insertion_sort(&mut arr[l..=h]); return }\n          let (lt, gt) = partition_3way(arr, l, h);\n          if lt - l < h - gt {\n              if lt > l { quicksort(arr, l, lt - 1) }\n              l = gt + 1;\n          } else {\n              quicksort(arr, gt + 1, h);\n              if lt > l { h = lt - 1 } else { break }\n          }\n      }\n  }\n  \n  fn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n      let m = l + (h - l) / 2;\n  ... (14 more lines)\nChange 2: Replace:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\nwith:\n  fn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n      for i in 1..arr.len() {\n          let mut j = i;\n          while j > 0 && arr[j - 1] > arr[j] {\n              arr.swap(j, j - 1);\n              j -= 1;\n          }\n      }\n  }\n- Metrics: score: 0.9873, combined_score: 0.9873, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9803, adaptability_score: 0.9977, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9873)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    if n < 24 { insertion_sort(arr); return }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1) }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1 } else { break }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j-1] > v {\n                arr[j] = arr[j-1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9873), Performs well on combined_score (0.9873), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9804), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9873)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1) }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1 } else { break }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9873), Performs well on combined_score (0.9873), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9803), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9872)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 32 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 32 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9872), Performs well on combined_score (0.9872), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9803), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9872)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 28 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 28 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let mut j = i;\n            while j > 0 && arr[j-1] > arr[j] {\n                arr.swap(j, j-1);\n                j -= 1;\n            }\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9872), Performs well on combined_score (0.9872), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9802), Performs well on adaptability_score (0.9976), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9870)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9872)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9871)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9872, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9872, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 28 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 28 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let mut j = i;\n            while j > 0 && arr[j-1] > arr[j] {\n                arr.swap(j, j-1);\n                j -= 1;\n            }\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9870, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9869, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false; }\n        else if arr[i-1] < arr[i] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    if n < 24 { insertion_sort(arr); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return; }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1); }\n            l = gt + 1;\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1; } else { break; }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l); }\n    if arr[h] < arr[l] { arr.swap(h, l); }\n    if arr[h] < arr[m] { arr.swap(h, m); }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { arr.swap(lt, i); lt += 1; i += 1; }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break; } gt -= 1; }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i-1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j-1] > v {\n                arr[j] = arr[j-1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        match arr[i-1].cmp(&arr[i]) {\n            Ordering::Greater => s = false,\n            Ordering::Less => r = false,\n            _ => {}\n        }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    quicksort(arr, 0, n - 1)\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut l: usize, mut h: usize) {\n    while l < h {\n        if h - l < 24 { insertion_sort(&mut arr[l..=h]); return }\n        let (lt, gt) = partition_3way(arr, l, h);\n        if lt - l < h - gt {\n            if lt > l { quicksort(arr, l, lt - 1) }\n            l = gt + 1\n        } else {\n            quicksort(arr, gt + 1, h);\n            if lt > l { h = lt - 1 } else { break }\n        }\n    }\n}\n\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { if i < gt { arr.swap(i, gt) } if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1 }\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, we will refine the adaptive sorting logic and the base case implementations. \n\nThe primary changes are:\n1.  **Adaptive Scan Optimization**: In `adaptive_sort`, the linear scan for sorted or reverse-sorted data is moved before the small-array threshold check. This ensures that even small arrays that are already sorted or reversed are handled in $O(n)$ time. Additionally, the scan loop is simplified from a `match` statement to more efficient `if-else` comparisons.\n2.  **Insertion Sort Enhancement**: The `insertion_sort` implementation is updated to a more efficient clone-and-shift pattern, which reduces the number of moves per element compared to repeated `swap` calls.\n3.  **Code Consolidation**: Minor refactorings in `partition_3way` and `adaptive_sort` to improve execution speed and maintain the program's adaptability.\n\nThese changes aim to restore and exceed the previous high score of 0.9873 by combining the best-performing elements from earlier iterations.\n\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 { insertion_sort(arr); return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        match arr[i-1].cmp(&arr[i]) {\n            Ordering::Greater => s = false,\n            Ordering::Less => r = false,\n            _ => {}\n        }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    quicksort(arr, 0, n - 1)\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { s = false }\n        else if arr[i-1] < arr[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { arr.reverse(); return }\n    if n < 24 { insertion_sort(arr); return }\n    quicksort(arr, 0, n - 1)\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { if i < gt { arr.swap(i, gt) } if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n=======\nfn partition_3way<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> (usize, usize) {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(m, l) }\n    if arr[h] < arr[l] { arr.swap(h, l) }\n    if arr[h] < arr[m] { arr.swap(h, m) }\n    let p = arr[m].clone();\n    let (mut lt, mut i, mut gt) = (l, l, h);\n    while i <= gt {\n        match arr[i].cmp(&p) {\n            Ordering::Less => { if lt < i { arr.swap(lt, i) } lt += 1; i += 1 }\n            Ordering::Greater => { arr.swap(i, gt); if gt == 0 { break } gt -= 1 }\n            _ => i += 1,\n        }\n    }\n    (lt, gt)\n}\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] { arr.swap(j, j - 1); j -= 1 }\n        }\n    }\n}\n=======\nfn insertion_sort<T: Ord + Clone>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        if arr[i] < arr[i - 1] {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00052803, 0.00614635, 0.0002825, 0.00349136, 1.016e-05, 0.00010073, 0.00015453, 0.00298213, 0.0004889, 0.00634941], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}