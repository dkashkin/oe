{"id": "6ca1818c-1776-492f-bb0d-8507d958c7ff", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 24 {\n        insertion_sort(arr);\n        return;\n    }\n    let mid = len / 2;\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n    \n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(lt, i);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    let (left, rest) = arr.split_at_mut(lt);\n    quicksort(left);\n    quicksort(&mut rest[gt-lt..]);\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "b70d6ebf-d467-41b3-8fe1-06d3b7abc1bf", "generation": 3, "timestamp": 1772737331.537415, "iteration_found": 10, "metrics": {"score": 0.9887543589608268, "combined_score": 0.9887543589608268, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9826211191958644, "adaptability_score": 0.9979542186082704, "avg_time": 0.0017686248000000002, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 16 {\n          insertion_sort(arr);\n          return;\n      }\n      let p = partition(arr);\n      let (left, right) = arr.split_at_mut(p);\n      quicksort(left);\n      quicksort(&mut right[1..]);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n      let len = arr.len();\n      let m = len / 2;\n      if arr[0] > arr[m] { arr.swap(0, m); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[m] > arr[len-1] { arr.swap(m, len-1); }\n      arr.swap(m, len-1);\n      let pivot = arr[len-1].clone();\n      let mut i = 0;\n      for j in 0..len-1 {\n          if arr[j] <= pivot {\n              arr.swap(i, j);\n              i += 1;\n          }\n      }\n      arr.swap(i, len-1);\n      i\n  }\nwith:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 24 {\n          insertion_sort(arr);\n          return;\n      }\n      let mid = len / 2;\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n  \n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(lt, i);\n              lt += 1;\n              i += 1;\n          } else if arr[i] > pivot {\n              gt -= 1;\n              arr.swap(i, gt);\n          } else {\n              i += 1;\n          }\n      }\n      let (left, rest) = arr.split_at_mut(lt);\n      quicksort(left);\n      quicksort(&mut rest[gt-lt..]);\n  }", "parent_metrics": {"score": 0.9765033814597588, "combined_score": 0.9765033814597588, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9646500636827728, "adaptability_score": 0.9942833581252376, "avg_time": 0.0036645346999999995, "memory_safe": 1.0}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9765\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=0.99\n- Focus areas: - Fitness unchanged at 0.9765\n- Exploring score=0.98, performance_score=0.96, adaptability_score=0.99 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00045936, 0.0056885, 0.00021153, 0.00250894, 8.72e-06, 8.708e-05, 0.002228829, 0.019843268, 0.00040816, 0.00520096]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          if arr[i-1] < arr[i] { desc = false; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          if high - low < 16 {\n              insertion_sort(&mut arr[low..=high]);\n              return;\n          }\n          let p = partition(arr, low, high);\n          if p > 0 { quicksort(arr, low, p - 1); }\n          quicksort(arr, p + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let m = low + (high - low) / 2;\n      if arr[low] > arr[m] { arr.swap(low, m); }\n      if arr[low] > arr[high] { arr.swap(low, high); }\n      if arr[m] > arr[high] { arr.swap(m, high); }\n  ... (22 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n      quicksort(arr);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 16 {\n          insertion_sort(arr);\n          return;\n      }\n      let p = partition(arr);\n      let (left, right) = arr.split_at_mut(p);\n      quicksort(left);\n      quicksort(&mut right[1..]);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n      let len = arr.len();\n      let m = len / 2;\n      if arr[0] > arr[m] { arr.swap(0, m); }\n  ... (24 more lines)\n- Metrics: score: 0.9765, combined_score: 0.9765, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9647, adaptability_score: 0.9943, avg_time: 0.0037, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9765)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 16 {\n        insertion_sort(arr);\n        return;\n    }\n    let p = partition(arr);\n    let (left, right) = arr.split_at_mut(p);\n    quicksort(left);\n    quicksort(&mut right[1..]);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n    let len = arr.len();\n    let m = len / 2;\n    if arr[0] > arr[m] { arr.swap(0, m); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[m] > arr[len-1] { arr.swap(m, len-1); }\n    arr.swap(m, len-1);\n    let pivot = arr[len-1].clone();\n    let mut i = 0;\n    for j in 0..len-1 {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, len-1);\n    i\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9765), Performs well on combined_score (0.9765), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9647), Performs well on adaptability_score (0.9943), Performs well on avg_time (0.0037), Performs well on memory_safe (1.0000)\n\n\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 16 {\n        insertion_sort(arr);\n        return;\n    }\n    let p = partition(arr);\n    let (left, right) = arr.split_at_mut(p);\n    quicksort(left);\n    quicksort(&mut right[1..]);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n    let len = arr.len();\n    let m = len / 2;\n    if arr[0] > arr[m] { arr.swap(0, m); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[m] > arr[len-1] { arr.swap(m, len-1); }\n    arr.swap(m, len-1);\n    let pivot = arr[len-1].clone();\n    let mut i = 0;\n    for j in 0..len-1 {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, len-1);\n    i\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["To improve the fitness score, I will implement a robust 3-way partitioning (Dutch National Flag) in the Quicksort implementation. This handles duplicate elements much more efficiently (O(N) vs O(N^2) for Lomuto partition in worst case) and simplifies the code by removing the separate `partition` function.\n\nReasons for change:\n1.  **Adaptability**: The 3-way partition is much more consistent across different data distributions, especially those with many duplicate keys, improving the adaptability score by reducing execution time variance.\n2.  **Performance**: Handling duplicates efficiently avoids the worst-case O(N^2) behavior of the previous Lomuto partition scheme.\n3.  **Simplicity**: Merging the partitioning logic into `quicksort` reduces the overall lines of code and function call overhead, addressing the \"simplify\" suggestion.\n4.  **Robustness**: Maintaining the median-of-three pivot selection ensures protection against sorted/reverse-sorted inputs (in addition to the adaptive check).\n\nHere is the replacement:\n\n<<<<<<< SEARCH\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 16 {\n        insertion_sort(arr);\n        return;\n    }\n    let p = partition(arr);\n    let (left, right) = arr.split_at_mut(p);\n    quicksort(left);\n    quicksort(&mut right[1..]);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n    let len = arr.len();\n    let m = len / 2;\n    if arr[0] > arr[m] { arr.swap(0, m); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[m] > arr[len-1] { arr.swap(m, len-1); }\n    arr.swap(m, len-1);\n    let pivot = arr[len-1].clone();\n    let mut i = 0;\n    for j in 0..len-1 {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, len-1);\n    i\n}\n=======\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 24 {\n        insertion_sort(arr);\n        return;\n    }\n    let mid = len / 2;\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n    \n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(lt, i);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    let (left, rest) = arr.split_at_mut(lt);\n    quicksort(left);\n    quicksort(&mut rest[gt-lt..]);\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00046175, 0.005356459, 0.00025101, 0.00295773, 8.64e-06, 8.578e-05, 0.00014645, 0.00265697, 0.00044119, 0.005320269], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}