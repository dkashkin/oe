{"id": "9ce6e5ba-b081-423e-b774-4f23dd033df9", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\n// Adaptive sorting using a hybrid Quicksort/Insertion sort strategy.\n// Features a fast heuristic scan for nearly-sorted or nearly-reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { if n > 1 { insertion_sort(arr); } return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p > 0 { quicksort(arr, low, p - 1); }\n        quicksort(arr, p + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let (pivot, mut i) = (arr[high].clone(), low);\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "2230e4f6-6108-495a-9398-827cd9a7c534", "generation": 5, "timestamp": 1772738289.3927503, "iteration_found": 36, "metrics": {"score": 0.9030960033996369, "combined_score": 0.9030960033996369, "compile_success": 1.0, "correctness": 1, "performance_score": 0.8620643765107544, "adaptability_score": 0.9646434437329605, "avg_time": 0.0160006175, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  use std::cmp::Ordering;\n  \n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n <= 16 { insertion_sort(arr); return; }\n      let (mut inv, mut rev, lim) = (0, 0, n / 6);\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n          if inv > lim && rev > lim { break; }\n      }\n      if inv <= lim { insertion_sort(arr); }\n      else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n      else { quicksort(arr, 0, n - 1); }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n      while low < high {\n          if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n          let p = partition(arr, low, high);\n          if p - low < high - p {\n              if p > low { quicksort(arr, low, p - 1); }\n              low = p + 1;\n  ... (22 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Adaptive sorting using a hybrid Quicksort/Insertion sort strategy.\n  // Features a fast heuristic scan for nearly-sorted or nearly-reverse-sorted data.\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n <= 16 { if n > 1 { insertion_sort(arr); } return; }\n      let (mut inv, mut rev, lim) = (0, 0, n / 7);\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n          if inv > lim && rev > lim { break; }\n      }\n      if inv <= lim { insertion_sort(arr); }\n      else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n      else { quicksort(arr, 0, n - 1); }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n          let p = partition(arr, low, high);\n          if p > 0 { quicksort(arr, low, p - 1); }\n          quicksort(arr, p + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let mid = low + (high - low) / 2;\n      if arr[mid] < arr[low] { arr.swap(low, mid); }\n      if arr[high] < arr[low] { arr.swap(low, high); }\n  ... (11 more lines)", "parent_metrics": {"score": 0.9023776158391187, "combined_score": 0.9023776158391187, "compile_success": 1.0, "correctness": 1, "performance_score": 0.8608561349933436, "adaptability_score": 0.9646598371077814, "avg_time": 0.016163428400000002, "memory_safe": 1.0}, "island": 3}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9024\n- Feature coordinates: score=0.90, performance_score=0.86, adaptability_score=0.96\n- Focus areas: - Fitness unchanged at 0.9024\n- Exploring score=0.90, performance_score=0.86, adaptability_score=0.96 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00045862, 0.005508919, 0.00152978, 0.124438948, 1.455e-05, 0.00014347, 0.00224928, 0.022074578, 0.00039892, 0.004817219]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  let (mut inv, mut rev, lim) = (0, 0, n / 7);\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n          if inv > lim && rev > lim { break; }\n      }\nwith:\n  let (mut inv, mut rev, lim) = (0, 0, n / 6);\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n          if inv > lim && rev > lim { break; }\n      }\nChange 2: Replace:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n          let p = partition(arr, low, high);\n          if p > 0 { quicksort(arr, low, p - 1); }\n          quicksort(arr, p + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let mid = low + (high - low) / 2;\n      if arr[mid] < arr[low] { arr.swap(low, mid); }\n      if arr[high] < arr[low] { arr.swap(low, high); }\n      if arr[mid] < arr[high] { arr.swap(mid, high); }\n      let pivot = arr[high].clone();\n      let mut i = low;\n      for j in low..high {\n          if arr[j] <= pivot {\n              arr.swap(i, j);\n              i += 1;\n          }\n      }\n      arr.swap(i, high);\n      i\n  }\nwith:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n      while low < high {\n          if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n          let p = partition(arr, low, high);\n          if p - low < high - p {\n              if p > low { quicksort(arr, low, p - 1); }\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              if p > 0 { high = p - 1; } else { break; }\n          }\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let mid = low + (high - low) / 2;\n      if arr[mid] < arr[low] { arr.swap(low, mid); }\n      if arr[high] < arr[low] { arr.swap(low, high); }\n      if arr[mid] < arr[high] { arr.swap(mid, high); }\n      let mut i = low;\n      for j in low..high {\n          if arr[j] <= arr[high] {\n              arr.swap(i, j);\n              i += 1;\n          }\n      }\n      arr.swap(i, high);\n      i\n  }\n- Metrics: score: 0.9024, combined_score: 0.9024, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8609, adaptability_score: 0.9647, avg_time: 0.0162, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len <= 1 {\n          return;\n      }\n  \n      // Hybrid strategy: Insertion sort for small or nearly sorted arrays\n      if len <= 16 || is_nearly_sorted(arr, 0.15) {\n          insertion_sort(arr);\n          return;\n      }\n  \n      quicksort(arr, 0, len - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          // Small partition optimization\n          if high - low < 16 {\n              insertion_sort(&mut arr[low..=high]);\n              return;\n          }\n  \n          let pivot_index = partition(arr, low, high);\n  \n          if pivot_index > 0 {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  ... (38 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n <= 24 {\n          if n > 1 { insertion_sort(arr); }\n          return;\n      }\n  \n      let mut inv = 0;\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; }\n      }\n  \n      if inv <= n / 6 {\n          insertion_sort(arr);\n      } else if inv >= n - n / 6 {\n          arr.reverse();\n          insertion_sort(arr);\n      } else {\n          quicksort(arr, 0, n - 1);\n      }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n      while low < high {\n          if high - low < 24 {\n              insertion_sort(&mut arr[low..=high]);\n              break;\n          }\n  \n          let p = partition(arr, low, high);\n  ... (30 more lines)\n- Metrics: score: 0.9037, combined_score: 0.9037, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8631, adaptability_score: 0.9648, avg_time: 0.0159, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n <= 16 { insertion_sort(arr); return; }\n  \n      let mut inv = 0;\n      for w in arr.windows(2) { if w[0] > w[1] { inv += 1; } }\n  \n      if inv <= n / 7 {\n          insertion_sort(arr);\n      } else if inv >= n.saturating_sub(n / 7 + 1) {\n          arr.reverse();\n          insertion_sort(arr);\n      } else {\n          quicksort(arr, 0, n - 1);\n      }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n      while low < high {\n          if high - low < 16 {\n              insertion_sort(&mut arr[low..=high]);\n              break;\n          }\n          let p = partition(arr, low, high);\n          if p - low < high - p {\n              if p > 0 { quicksort(arr, low, p - 1); }\n              low = p + 1;\n          } else {\n              quicksort(arr, p + 1, high);\n              if p > 0 { high = p - 1; } else { break; }\n  ... (24 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n <= 16 { insertion_sort(arr); return; }\n      let (mut inv, mut rev, lim) = (0, 0, n / 7);\n      for w in arr.windows(2) {\n          if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n          if inv > lim && rev > lim { break; }\n      }\n      if inv <= lim { insertion_sort(arr); }\n      else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n      else { quicksort(arr, 0, n - 1); }\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n          let p = partition(arr, low, high);\n          if p > 0 { quicksort(arr, low, p - 1); }\n          quicksort(arr, p + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n      let mid = low + (high - low) / 2;\n      if arr[mid] < arr[low] { arr.swap(low, mid); }\n      if arr[high] < arr[low] { arr.swap(low, high); }\n      if arr[mid] < arr[high] { arr.swap(mid, high); }\n      let pivot = arr[high].clone();\n      let mut i = low;\n      for j in low..high {\n  ... (8 more lines)\n- Metrics: score: 0.9038, combined_score: 0.9038, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.8631, adaptability_score: 0.9649, avg_time: 0.0159, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9040)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len <= 1 {\n        return;\n    }\n    \n    // Hybrid strategy: Insertion sort for small or nearly sorted arrays\n    if len <= 16 || is_nearly_sorted(arr, 0.15) {\n        insertion_sort(arr);\n        return;\n    }\n    \n    quicksort(arr, 0, len - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        // Small partition optimization\n        if high - low < 16 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n\n        let pivot_index = partition(arr, low, high);\n        \n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    \n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Optimized O(N) heuristic for nearly sorted check\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    let mut inversions = 0;\n    let limit = (arr.len() as f64 * threshold) as usize;\n    \n    for w in arr.windows(2) {\n        if w[0] > w[1] {\n            inversions += 1;\n            if inversions > limit {\n                return false;\n            }\n        }\n    }\n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9040), Performs well on combined_score (0.9040), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8629), Performs well on adaptability_score (0.9655), Performs well on avg_time (0.0159), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9038)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p > 0 { quicksort(arr, low, p - 1); }\n        quicksort(arr, p + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let pivot = arr[high].clone();\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9038), Performs well on combined_score (0.9038), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8631), Performs well on adaptability_score (0.9649), Performs well on avg_time (0.0159), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9037)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 24 {\n        if n > 1 { insertion_sort(arr); }\n        return;\n    }\n    \n    let mut inv = 0;\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; }\n    }\n    \n    if inv <= n / 6 {\n        insertion_sort(arr);\n    } else if inv >= n - n / 6 {\n        arr.reverse();\n        insertion_sort(arr);\n    } else {\n        quicksort(arr, 0, n - 1);\n    }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 24 {\n            insertion_sort(&mut arr[low..=high]);\n            break;\n        }\n        \n        let p = partition(arr, low, high);\n        \n        if p.saturating_sub(low) < high.saturating_sub(p) {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            if p < high { quicksort(arr, p + 1, high); }\n            if p > low { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    \n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9037), Performs well on combined_score (0.9037), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8631), Performs well on adaptability_score (0.9648), Performs well on avg_time (0.0159), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9024)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 6);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= arr[high] {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9024), Performs well on combined_score (0.9024), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.8609), Performs well on adaptability_score (0.9647), Performs well on avg_time (0.0162), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.8831)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    \n    let mut inv = 0;\n    for w in arr.windows(2) { if w[0] > w[1] { inv += 1; } }\n    \n    if inv <= n / 7 {\n        insertion_sort(arr);\n    } else if inv >= n.saturating_sub(n / 7 + 1) {\n        arr.reverse();\n        insertion_sort(arr);\n    } else {\n        quicksort(arr, 0, n - 1);\n    }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 {\n            insertion_sort(&mut arr[low..=high]);\n            break;\n        }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > 0 { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    \n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.8827)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 24 { if n > 1 { insertion_sort(arr); } return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 24 { insertion_sort(&mut arr[low..=high]); break; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let pivot = arr[high].clone();\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot { arr.swap(i, j); i += 1; }\n    }\n    arr.swap(i, high);\n    i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.8914)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    if n <= 24 { insertion_sort(arr); return; }\n\n    let (mut inv, mut r_inv, limit) = (0, 0, n / 10);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; }\n        if w[0] < w[1] { r_inv += 1; }\n        if inv > limit && r_inv > limit { break; }\n    }\n\n    if inv <= limit { insertion_sort(arr); }\n    else if r_inv <= limit { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 24 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> usize {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(l, m); }\n    if arr[h] < arr[l] { arr.swap(l, h); }\n    if arr[m] < arr[h] { arr.swap(m, h); }\n    let (pivot, mut i, mut j) = (arr[m].clone(), l as isize - 1, h as isize + 1);\n    loop {\n        loop { i += 1; if arr[i as usize] >= pivot { break; } }\n        loop { j -= 1; if arr[j as usize] <= pivot { break; } }\n        if i >= j { return j as usize; }\n        arr.swap(i as usize, j as usize);\n    }\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9040, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len <= 1 {\n        return;\n    }\n    \n    // Hybrid strategy: Insertion sort for small or nearly sorted arrays\n    if len <= 16 || is_nearly_sorted(arr, 0.15) {\n        insertion_sort(arr);\n        return;\n    }\n    \n    quicksort(arr, 0, len - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        // Small partition optimization\n        if high - low < 16 {\n            insertion_sort(&mut arr[low..=high]);\n            return;\n        }\n\n        let pivot_index = partition(arr, low, high);\n        \n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Median-of-three pivot selection\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    \n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Optimized O(N) heuristic for nearly sorted check\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    let mut inversions = 0;\n    let limit = (arr.len() as f64 * threshold) as usize;\n    \n    for w in arr.windows(2) {\n        if w[0] > w[1] {\n            inversions += 1;\n            if inversions > limit {\n                return false;\n            }\n        }\n    }\n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9038, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p > 0 { quicksort(arr, low, p - 1); }\n        quicksort(arr, p + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let pivot = arr[high].clone();\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.8827, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 24 { if n > 1 { insertion_sort(arr); } return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 24 { insertion_sort(&mut arr[low..=high]); break; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let pivot = arr[high].clone();\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= pivot { arr.swap(i, j); i += 1; }\n    }\n    arr.swap(i, high);\n    i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.8914, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    if n <= 24 { insertion_sort(arr); return; }\n\n    let (mut inv, mut r_inv, limit) = (0, 0, n / 10);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; }\n        if w[0] < w[1] { r_inv += 1; }\n        if inv > limit && r_inv > limit { break; }\n    }\n\n    if inv <= limit { insertion_sort(arr); }\n    else if r_inv <= limit { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 24 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            quicksort(arr, low, p);\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            high = p;\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> usize {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(l, m); }\n    if arr[h] < arr[l] { arr.swap(l, h); }\n    if arr[m] < arr[h] { arr.swap(m, h); }\n    let (pivot, mut i, mut j) = (arr[m].clone(), l as isize - 1, h as isize + 1);\n    loop {\n        loop { i += 1; if arr[i as usize] >= pivot { break; } }\n        loop { j -= 1; if arr[j as usize] <= pivot { break; } }\n        if i >= j { return j as usize; }\n        arr.swap(i as usize, j as usize);\n    }\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 6);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= arr[high] {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n\n\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program (Attempt 4) has slightly lower performance than previous iterations. Analysis of the top-performing programs (Program 1 and 2) suggests that a recursive quicksort implementation and a more balanced inversion/reversal threshold ($n/7 \\approx 14.2\\%$) are more effective for the benchmarked data patterns. \n\nAdditionally, using a local `pivot` variable in the `partition` function (instead of repeatedly accessing `arr[high]`) can reduce array access overhead, and simplifying the quicksort structure often allows the compiler to perform better optimizations.\n\nHere are the suggested improvements:\n\n1.  **Refine `adaptive_sort`**: Use a $n/7$ limit for the inversion/reversal scan, which is closer to the $0.15$ threshold used by the top-performing Program 1.\n2.  **Simplify `quicksort`**: Switch from the iterative tail-call version back to a simple recursive version. While tail-call optimization is theoretically safer for recursion depth, quicksort with median-of-three pivot selection and a small-array threshold has a guaranteed logarithmic average stack depth, and the simpler structure is often faster in practice.\n3.  **Optimize `partition`**: Use a local `pivot` variable to avoid repeated indexing of the array within the inner loop.\n4.  **Clean up**: Remove the unused `Ordering` import to reduce code clutter.\n\n<<<<<<< SEARCH\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { insertion_sort(arr); return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 6);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], mut low: usize, mut high: usize) {\n    while low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p - low < high - p {\n            if p > low { quicksort(arr, low, p - 1); }\n            low = p + 1;\n        } else {\n            quicksort(arr, p + 1, high);\n            if p > 0 { high = p - 1; } else { break; }\n        }\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let mut i = low;\n    for j in low..high {\n        if arr[j] <= arr[high] {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n=======\n// EVOLVE-BLOCK-START\n// Adaptive sorting using a hybrid Quicksort/Insertion sort strategy.\n// Features a fast heuristic scan for nearly-sorted or nearly-reverse-sorted data.\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n <= 16 { if n > 1 { insertion_sort(arr); } return; }\n    let (mut inv, mut rev, lim) = (0, 0, n / 7);\n    for w in arr.windows(2) {\n        if w[0] > w[1] { inv += 1; } else if w[0] < w[1] { rev += 1; }\n        if inv > lim && rev > lim { break; }\n    }\n    if inv <= lim { insertion_sort(arr); }\n    else if rev <= lim { arr.reverse(); insertion_sort(arr); }\n    else { quicksort(arr, 0, n - 1); }\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        if high - low < 16 { insertion_sort(&mut arr[low..=high]); return; }\n        let p = partition(arr, low, high);\n        if p > 0 { quicksort(arr, low, p - 1); }\n        quicksort(arr, p + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    let mid = low + (high - low) / 2;\n    if arr[mid] < arr[low] { arr.swap(low, mid); }\n    if arr[high] < arr[low] { arr.swap(low, high); }\n    if arr[mid] < arr[high] { arr.swap(mid, high); }\n    let (pivot, mut i) = (arr[high].clone(), low);\n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, high);\n    i\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00049155, 0.005539699, 0.001804599, 0.124691729, 1.46e-05, 0.0001439, 0.001986129, 0.019433238, 0.000402111, 0.00549862], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}