{"id": "9dd896fb-937c-4307-934f-ec515b8ab3af", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (l, g) = partition(a);\n        if l < a.len() - g {\n            adaptive_sort(&mut a[..l]);\n            a = &mut a[g..];\n        } else {\n            adaptive_sort(&mut a[g..]);\n            a = &mut a[..l];\n        }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n    }\n}\n\nfn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n    let (m, r) = (a.len() / 2, a.len() - 1);\n    if a[0] > a[m] { a.swap(0, m); }\n    if a[0] > a[r] { a.swap(0, r); }\n    if a[m] > a[r] { a.swap(m, r); }\n    let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { if i != l { a.swap(i, l); } l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; a.swap(i, g); }\n            _ => i += 1,\n        }\n    }\n    (l, g)\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "259123a4-728f-430a-97bb-f69b10f88518", "generation": 3, "timestamp": 1772737549.5436852, "iteration_found": 18, "metrics": {"score": 0.9767895743100711, "combined_score": 0.9767895743100711, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9644083218578066, "adaptability_score": 0.995361452988468, "avg_time": 0.0036905196, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  pub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n      while arr.len() > 20 {\n          let (lt, gt) = partition(arr);\n          if lt < arr.len() - gt {\n              adaptive_sort(&mut arr[..lt]);\n              arr = &mut arr[gt..];\n          } else {\n              adaptive_sort(&mut arr[gt..]);\n              arr = &mut arr[..lt];\n          }\n      }\n      insertion_sort(arr);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n      let len = arr.len();\n      let (mid, last) = (len / 2, len - 1);\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[last] { arr.swap(0, last); }\n      if arr[mid] > arr[last] { arr.swap(mid, last); }\n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n  ... (28 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let (l, g) = partition(a);\n          if l < a.len() - g {\n              adaptive_sort(&mut a[..l]);\n              a = &mut a[g..];\n          } else {\n              adaptive_sort(&mut a[g..]);\n              a = &mut a[..l];\n          }\n      }\n      for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n      let (m, r) = (a.len() / 2, a.len() - 1);\n      if a[0] > a[m] { a.swap(0, m); }\n      if a[0] > a[r] { a.swap(0, r); }\n      if a[m] > a[r] { a.swap(m, r); }\n      let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n      while i < g {\n          match a[i].cmp(&p) {\n              Ordering::Less => { if i != l { a.swap(i, l); } l += 1; i += 1; }\n              Ordering::Greater => { g -= 1; a.swap(i, g); }\n              _ => i += 1,\n          }\n  ... (4 more lines)", "parent_metrics": {"score": 0.9767187634726945, "combined_score": 0.9767187634726945, "compile_success": 1.0, "correctness": 1, "performance_score": 0.96426994344416, "adaptability_score": 0.9953919935154965, "avg_time": 0.0037053998000000006, "memory_safe": 1.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9767\n- Feature coordinates: score=0.98, performance_score=0.96, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.4101 \u2192 0.9767\n- Exploring score=0.98, performance_score=0.96, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.000534, 0.006301269, 0.00031814, 0.00356243, 0.00056148, 0.015824999, 0.00016336, 0.00312261, 0.0004969, 0.00616881]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: score: 0.4101, combined_score: 0.4101, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.2915, adaptability_score: 0.5880, avg_time: 0.2431, memory_safe: 1.0000\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len <= 16 {\n          insertion_sort(arr);\n          return;\n      }\n  \n      let (lt, gt) = partition(arr);\n      adaptive_sort(&mut arr[0..lt]);\n      adaptive_sort(&mut arr[gt..]);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n      let len = arr.len();\n      let mid = len / 2;\n  \n      // Median of 3 pivot selection\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[len - 1] { arr.swap(0, len - 1); }\n      if arr[mid] > arr[len - 1] { arr.swap(mid, len - 1); }\n  \n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n  \n      while i < gt {\n          match arr[i].cmp(&pivot) {\n              Ordering::Less => {\n                  arr.swap(i, lt);\n                  lt += 1;\n                  i += 1;\n  ... (10 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n      while arr.len() > 20 {\n          let (lt, gt) = partition(arr);\n          if lt < arr.len() - gt {\n              adaptive_sort(&mut arr[..lt]);\n              arr = &mut arr[gt..];\n          } else {\n              adaptive_sort(&mut arr[gt..]);\n              arr = &mut arr[..lt];\n          }\n      }\n      insertion_sort(arr);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n      let len = arr.len();\n      let (mid, last) = (len / 2, len - 1);\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[last] { arr.swap(0, last); }\n      if arr[mid] > arr[last] { arr.swap(mid, last); }\n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n      while i < gt {\n          match arr[i].cmp(&pivot) {\n              Ordering::Less => {\n                  if i != lt { arr.swap(i, lt); }\n                  lt += 1;\n                  i += 1;\n              }\n              Ordering::Greater => {\n  ... (8 more lines)\n- Metrics: score: 0.9767, combined_score: 0.9767, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9643, adaptability_score: 0.9954, avg_time: 0.0037, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9767)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 20 {\n        let (lt, gt) = partition(arr);\n        if lt < arr.len() - gt {\n            adaptive_sort(&mut arr[..lt]);\n            arr = &mut arr[gt..];\n        } else {\n            adaptive_sort(&mut arr[gt..]);\n            arr = &mut arr[..lt];\n        }\n    }\n    insertion_sort(arr);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n    let len = arr.len();\n    let (mid, last) = (len / 2, len - 1);\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[last] { arr.swap(0, last); }\n    if arr[mid] > arr[last] { arr.swap(mid, last); }\n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        match arr[i].cmp(&pivot) {\n            Ordering::Less => {\n                if i != lt { arr.swap(i, lt); }\n                lt += 1;\n                i += 1;\n            }\n            Ordering::Greater => {\n                gt -= 1;\n                arr.swap(i, gt);\n            }\n            Ordering::Equal => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9767), Performs well on combined_score (0.9767), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9643), Performs well on adaptability_score (0.9954), Performs well on avg_time (0.0037), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.4101)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    if arr.len() <= 1 {\n        return;\n    }\n    \n    // Use quicksort as the base implementation\n    quicksort(arr, 0, arr.len() - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        let pivot_index = partition(arr, low, high);\n        \n        // Recursively sort elements before and after partition\n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Choose the last element as pivot (can be evolved to use better strategies)\n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Helper function to detect if array is nearly sorted\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    if arr.len() <= 1 {\n        return true;\n    }\n    \n    let mut inversions = 0;\n    let max_inversions = ((arr.len() * (arr.len() - 1)) / 2) as f64 * threshold;\n    \n    for i in 0..arr.len() - 1 {\n        for j in i + 1..arr.len() {\n            if arr[i] > arr[j] {\n                inversions += 1;\n                if inversions as f64 > max_inversions {\n                    return false;\n                }\n            }\n        }\n    }\n    \n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.4101), Performs well on combined_score (0.4101), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.2915), Performs well on adaptability_score (0.5880), Performs well on avg_time (0.2431), Performs well on memory_safe (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4101, Type: Experimental)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    if arr.len() <= 1 {\n        return;\n    }\n    \n    // Use quicksort as the base implementation\n    quicksort(arr, 0, arr.len() - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n    if low < high {\n        let pivot_index = partition(arr, low, high);\n        \n        // Recursively sort elements before and after partition\n        if pivot_index > 0 {\n            quicksort(arr, low, pivot_index - 1);\n        }\n        quicksort(arr, pivot_index + 1, high);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n    // Choose the last element as pivot (can be evolved to use better strategies)\n    let pivot = arr[high].clone();\n    let mut i = low;\n    \n    for j in low..high {\n        if arr[j] <= pivot {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    \n    arr.swap(i, high);\n    i\n}\n\n// Helper function to detect if array is nearly sorted\nfn is_nearly_sorted<T: Ord>(arr: &[T], threshold: f64) -> bool {\n    if arr.len() <= 1 {\n        return true;\n    }\n    \n    let mut inversions = 0;\n    let max_inversions = ((arr.len() * (arr.len() - 1)) / 2) as f64 * threshold;\n    \n    for i in 0..arr.len() - 1 {\n        for j in i + 1..arr.len() {\n            if arr[i] > arr[j] {\n                inversions += 1;\n                if inversions as f64 > max_inversions {\n                    return false;\n                }\n            }\n        }\n    }\n    \n    true\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 20 {\n        let (lt, gt) = partition(arr);\n        if lt < arr.len() - gt {\n            adaptive_sort(&mut arr[..lt]);\n            arr = &mut arr[gt..];\n        } else {\n            adaptive_sort(&mut arr[gt..]);\n            arr = &mut arr[..lt];\n        }\n    }\n    insertion_sort(arr);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n    let len = arr.len();\n    let (mid, last) = (len / 2, len - 1);\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[last] { arr.swap(0, last); }\n    if arr[mid] > arr[last] { arr.swap(mid, last); }\n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        match arr[i].cmp(&pivot) {\n            Ordering::Less => {\n                if i != lt { arr.swap(i, lt); }\n                lt += 1;\n                i += 1;\n            }\n            Ordering::Greater => {\n                gt -= 1;\n                arr.swap(i, gt);\n            }\n            Ordering::Equal => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The following improvements focus on maximizing the fitness score by refining the sorting logic and simplifying the implementation to meet the code length suggestions.\n\nKey changes:\n1.  **Threshold Optimization**: Increased the threshold for switching to insertion sort from 20 to 24, which typically aligns better with modern CPU cache and branch prediction performance for small segments.\n2.  **Code Simplification**: Inlined the insertion sort and streamlined the partitioning logic to reduce boilerplate and character count, moving toward the 500-character goal.\n3.  **Refined Partitioning**: Maintained the robust 3-way (Dutch National Flag) partitioning and Median-of-Three pivot selection to handle data with many duplicates and partially sorted inputs efficiently, ensuring high adaptability.\n4.  **Tail-Call Optimization**: Retained the tail-recursion optimization in the `while` loop to minimize stack depth and improve performance on large datasets.\n\n```rust\n<<<<<<< SEARCH\n// EVOLVE-BLOCK-START\n// Initial implementation: Simple quicksort\n// This can be evolved to:\n// - Hybrid algorithms (introsort, timsort-like)\n// - Adaptive pivot selection\n// - Special handling for nearly sorted data\n// - Switching to different algorithms based on data characteristics\n\npub fn adaptive_sort<T: Ord + Clone>(mut arr: &mut [T]) {\n    while arr.len() > 20 {\n        let (lt, gt) = partition(arr);\n        if lt < arr.len() - gt {\n            adaptive_sort(&mut arr[..lt]);\n            arr = &mut arr[gt..];\n        } else {\n            adaptive_sort(&mut arr[gt..]);\n            arr = &mut arr[..lt];\n        }\n    }\n    insertion_sort(arr);\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T]) -> (usize, usize) {\n    let len = arr.len();\n    let (mid, last) = (len / 2, len - 1);\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[last] { arr.swap(0, last); }\n    if arr[mid] > arr[last] { arr.swap(mid, last); }\n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        match arr[i].cmp(&pivot) {\n            Ordering::Less => {\n                if i != lt { arr.swap(i, lt); }\n                lt += 1;\n                i += 1;\n            }\n            Ordering::Greater => {\n                gt -= 1;\n                arr.swap(i, gt);\n            }\n            Ordering::Equal => i += 1,\n        }\n    }\n    (lt, gt)\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n=======\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (l, g) = partition(a);\n        if l < a.len() - g {\n            adaptive_sort(&mut a[..l]);\n            a = &mut a[g..];\n        } else {\n            adaptive_sort(&mut a[g..]);\n            a = &mut a[..l];\n        }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1; }\n    }\n}\n\nfn partition<T: Ord + Clone>(a: &mut [T]) -> (usize, usize) {\n    let (m, r) = (a.len() / 2, a.len() - 1);\n    if a[0] > a[m] { a.swap(0, m); }\n    if a[0] > a[r] { a.swap(0, r); }\n    if a[m] > a[r] { a.swap(m, r); }\n    let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { if i != l { a.swap(i, l); } l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; a.swap(i, g); }\n            _ => i += 1,\n        }\n    }\n    (l, g)\n}\n// EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.00053029, 0.006333059, 0.00031758, 0.003590589, 0.00055153, 0.015968499, 0.000146689, 0.00302265, 0.00050472, 0.00593959], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}