{"id": "525ebd72-3ab6-4e48-b30a-64d9a872ea0d", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 64 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let p = a[m].clone();\n        let (mut l, mut i, mut g) = (0, 0, a.len());\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "56484acb-9c6a-427b-bee6-037f65ca2599", "generation": 11, "timestamp": 1772742679.1664274, "iteration_found": 142, "metrics": {"score": 0.9884358659048323, "combined_score": 0.9884358659048323, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9822264551380442, "adaptability_score": 0.9977499820550144, "avg_time": 0.0018095160000000002, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      let mut n = a.len();\n      if n < 2 { return }\n      if n > 96 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..n {\n              if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { return a.reverse() }\n      }\n      while n > 32 {\n          let (m, last) = (n / 2, n - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[last] { a.swap(0, last) }\n          if a[m] > a[last] { a.swap(m, last) }\n          let (p, mut l, mut g, mut i) = (a[m].clone(), 0, n, 0);\n          while i < g {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                  Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                  _ => i += 1,\n              }\n          }\n          if l < n - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n          n = a.len();\n      }\n      for i in 1..n {\n          let mut j = i;\n  ... (3 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() < 2 { return }\n      if a.len() > 64 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { return a.reverse() }\n      }\n      while a.len() > 32 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n          let p = a[m].clone();\n          let (mut l, mut i, mut g) = (0, 0, a.len());\n          while i < g {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                  Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                  _ => i += 1,\n              }\n          }\n          if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n          else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n      }\n      for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n  ... (2 more lines)", "parent_metrics": {"score": 0.9881285463025804, "combined_score": 0.9881285463025804, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9817626995162948, "adaptability_score": 0.9976773164820089, "avg_time": 0.0018576078000000003, "memory_safe": 1.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9881\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness declined: 0.9885 \u2192 0.9881. Consider revising recent changes.\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 96 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { s = false }\n              if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { a.reverse(); return }\n      }\n      while a.len() > 24 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n          let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() < 2 { return }\n      if a.len() > 96 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { s = false }\n              if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { a.reverse(); return }\n      }\n      while a.len() > 32 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n          let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n- Metrics: score: 0.9885, combined_score: 0.9885, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9822, adaptability_score: 0.9978, avg_time: 0.0018, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() <= 1 { return; }\n  \n      if a.len() > 64 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false; }\n              if a[i-1] < a[i] { dsc = false; }\n              if !asc && !dsc { break; }\n          }\n          if asc { return; }\n          if dsc { a.reverse(); return; }\n      }\n  \n      while a.len() > 16 {\n          let (mid, last) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[mid] { a.swap(0, mid); }\n          if a[0] > a[last] { a.swap(0, last); }\n          if a[mid] > a[last] { a.swap(mid, last); }\n  \n          let pivot = a[mid].clone();\n          let (mut l, mut i, mut g) = (0, 0, a.len());\n  \n          while i < g {\n              match a[i].cmp(&pivot) {\n                  Ordering::Less => {\n                      a.swap(i, l);\n                      l += 1;\n                      i += 1;\n                  }\n  ... (9 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() < 2 { return }\n      if a.len() > 96 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { s = false }\n              if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { a.reverse(); return }\n      }\n      while a.len() > 32 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[r] { a.swap(0, r) }\n          if a[m] > a[r] { a.swap(m, r) }\n  \n          let p = a[m].clone();\n          let (mut l, mut i, mut g) = (0, 0, a.len());\n          while i < g {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                  Ordering::Greater => {\n                      g -= 1;\n                      while g > i && a[g] > p { g -= 1 }\n                      a.swap(i, g)\n                  }\n                  _ => i += 1,\n              }\n          }\nChange 2: Replace:\n  for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] {\n              a.swap(j, j-1);\n              j -= 1;\n          }\n      }\n  }\nwith:\n  for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n      }\n  }\n- Metrics: score: 0.9886, combined_score: 0.9886, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9824, adaptability_score: 0.9978, avg_time: 0.0018, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() > 64 {\n          let (mut asc, mut dsc) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { asc = false }\n              if a[i-1] < a[i] { dsc = false }\n              if !asc && !dsc { break }\n          }\n          if asc { return }\n          if dsc { a.reverse(); return }\n      }\n      while a.len() > 24 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n      if a.len() < 2 { return }\n      if a.len() > 96 {\n          let (mut s, mut r) = (true, true);\n          for i in 1..a.len() {\n              if a[i-1] > a[i] { s = false }\n              if a[i-1] < a[i] { r = false }\n              if !s && !r { break }\n          }\n          if s { return } if r { a.reverse(); return }\n      }\n      while a.len() > 32 {\n          let (m, r) = (a.len() / 2, a.len() - 1);\n- Metrics: score: 0.9886, combined_score: 0.9886, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9824, adaptability_score: 0.9978, avg_time: 0.0018, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9886)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false }\n            if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9886), Performs well on combined_score (0.9886), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9825), Performs well on adaptability_score (0.9978), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9886)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false }\n            if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9886), Performs well on combined_score (0.9886), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9824), Performs well on adaptability_score (0.9978), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9886)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false }\n            if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n\n        let p = a[m].clone();\n        let (mut l, mut i, mut g) = (0, 0, a.len());\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n\n        if l < a.len() - g {\n            adaptive_sort(&mut a[..l]);\n            a = &mut a[g..];\n        } else {\n            adaptive_sort(&mut a[g..]);\n            a = &mut a[..l];\n        }\n    }\n\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9886), Performs well on combined_score (0.9886), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9824), Performs well on adaptability_score (0.9978), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9885)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false }\n            if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9885), Performs well on combined_score (0.9885), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9822), Performs well on adaptability_score (0.9978), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9884)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, last) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[last] { a.swap(0, last) }\n        if a[m] > a[last] { a.swap(m, last) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9884)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    if n > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..n {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while a.len() > 32 {\n        let (len, m) = (a.len(), a.len() / 2);\n        let last = len - 1;\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[last] { a.swap(0, last) }\n        if a[m] > a[last] { a.swap(m, last) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, len, 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9884)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9881, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false }\n            if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9881, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 64 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 24 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9884, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    if n > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..n {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while a.len() > 32 {\n        let (len, m) = (a.len(), a.len() / 2);\n        let last = len - 1;\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[last] { a.swap(0, last) }\n        if a[m] > a[last] { a.swap(m, last) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, len, 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9879, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() > 96 {\n        let (mut asc, mut dsc) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { asc = false }\n            else if a[i-1] < a[i] { dsc = false }\n            if !asc && !dsc { break }\n        }\n        if asc { return }\n        if dsc { a.reverse(); return }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, a.len(), 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => {\n                    g -= 1;\n                    while g > i && a[g] > p { g -= 1 }\n                    a.swap(i, g)\n                }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    let mut n = a.len();\n    if n < 2 { return }\n    if n > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..n {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while n > 32 {\n        let (m, last) = (n / 2, n - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[last] { a.swap(0, last) }\n        if a[m] > a[last] { a.swap(m, last) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, n, 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < n - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n        n = a.len();\n    }\n    for i in 1..n {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness recently declined from 0.9886 to 0.9881. This was likely due to the introduction of more verbose variable management and minor logic refactoring that slightly impacted performance. \n\nTo improve the fitness score, I will:\n1.  **Simplify and Condense**: Revert to the structure that previously achieved 0.9886 while making the code more compact to address the length warning.\n2.  **Optimize Thresholds**: Reduce the pre-sort check threshold from 96 to 64 to detect sorted or reverse-sorted patterns earlier.\n3.  **Restore Best Logic**: Re-implement the partitioning and tail-call optimization in the way that showed the highest performance in Attempt 3, which avoids manual length tracking (`n = a.len()`) in favor of direct slice methods that the compiler can often optimize more effectively.\n4.  **Concise Partitioning**: Use a 3-way partition (Dutch National Flag) with an inner loop to skip unnecessary swaps of elements already in the correct relative position.\n\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    let mut n = a.len();\n    if n < 2 { return }\n    if n > 96 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..n {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while n > 32 {\n        let (m, last) = (n / 2, n - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[last] { a.swap(0, last) }\n        if a[m] > a[last] { a.swap(m, last) }\n        let (p, mut l, mut g, mut i) = (a[m].clone(), 0, n, 0);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < n - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n        n = a.len();\n    }\n    for i in 1..n {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(mut a: &mut [T]) {\n    if a.len() < 2 { return }\n    if a.len() > 64 {\n        let (mut s, mut r) = (true, true);\n        for i in 1..a.len() {\n            if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n            if !s && !r { break }\n        }\n        if s { return } if r { return a.reverse() }\n    }\n    while a.len() > 32 {\n        let (m, r) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[r] { a.swap(0, r) }\n        if a[m] > a[r] { a.swap(m, r) }\n        let p = a[m].clone();\n        let (mut l, mut i, mut g) = (0, 0, a.len());\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(i, l); l += 1; i += 1 }\n                Ordering::Greater => { g -= 1; while g > i && a[g] > p { g -= 1 } a.swap(i, g) }\n                _ => i += 1,\n            }\n        }\n        if l < a.len() - g { adaptive_sort(&mut a[..l]); a = &mut a[g..] }\n        else { adaptive_sort(&mut a[g..]); a = &mut a[..l] }\n    }\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.00049695, 0.00615616, 0.00015521, 0.00189522, 7.07e-06, 6.979e-05, 0.00016676, 0.00291878, 0.000479931, 0.005749289], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}