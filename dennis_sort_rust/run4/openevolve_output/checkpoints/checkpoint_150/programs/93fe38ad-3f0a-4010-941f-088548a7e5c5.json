{"id": "93fe38ad-3f0a-4010-941f-088548a7e5c5", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse() } else { qs(a) }\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "0b5c3341-e11c-470c-9020-ca4c385235a2", "generation": 7, "timestamp": 1772742309.4924471, "iteration_found": 134, "metrics": {"score": 0.9892002331370615, "combined_score": 0.9892002331370615, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9833218302542873, "adaptability_score": 0.9980178374612229, "avg_time": 0.0016961049000000002, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\nwith:\n  if s { return }\n      if r { a.reverse() } else { qs(a) }\n  }\nChange 2: Replace:\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 32 {\n          let (n, m, l) = (a.len(), a.len()/2, a.len()-1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rem) = a.split_at_mut(lt);\n          let right = &mut rem[gt-lt..];\n          if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n      }\n      for i in 1..a.len() {\nwith:\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rest) = a.split_at_mut(lt);\n          let right = &mut rest[gt - lt..];\n          if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n      }\n      for i in 1..a.len() {", "parent_metrics": {"score": 0.9894040145842843, "combined_score": 0.9894040145842843, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9836418423837159, "adaptability_score": 0.998047272885137, "avg_time": 0.0016630197, "memory_safe": 1.0}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9894\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9893 \u2192 0.9894\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 32 {\n          let (m, l) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rem) = a.split_at_mut(lt);\n          let right = &mut rem[gt-lt..];\n          if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n      }\n  ... (5 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rem) = a.split_at_mut(lt);\n          let right = &mut rem[gt-lt..];\n          if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n      }\n      for i in 1..a.len() {\n  ... (7 more lines)\n- Metrics: score: 0.9893, combined_score: 0.9893, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9835, adaptability_score: 0.9980, avg_time: 0.0017, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qsort(a);\n  }\n  \n  fn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rest) = a.split_at_mut(lt);\n          let right = &mut rest[gt - lt..];\n          if left.len() < right.len() { qsort(left); a = right; } else { qsort(right); a = left; }\n      }\n  ... (8 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 32 {\n          let (n, m, l) = (a.len(), a.len()/2, a.len()-1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rem) = a.split_at_mut(lt);\n          let right = &mut rem[gt-lt..];\n          if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n      }\n      for i in 1..a.len() {\n  ... (7 more lines)\n- Metrics: score: 0.9894, combined_score: 0.9894, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9836, adaptability_score: 0.9980, avg_time: 0.0017, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  use std::cmp::Ordering;\n  \n  // EVOLVE-BLOCK-START\nwith:\n  // EVOLVE-BLOCK-START\nChange 2: Replace:\n  for i in 1..n {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\nwith:\n  for i in 1..n {\n          if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\nChange 3: Replace:\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 32 {\n          let (m, l) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rem) = a.split_at_mut(lt);\n          let right = &mut rem[gt-lt..];\n          if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n      }\n      for i in 1..a.len() {\n          let mut j = i;\n          while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n      }\n  }\nwith:\n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n          while i < gt {\n              if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n              else if a[i] > p { gt -= 1; a.swap(i, gt) }\n              else { i += 1 }\n          }\n          let (left, rest) = a.split_at_mut(lt);\n          let right = &mut rest[gt - lt..];\n          if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n      }\n      for i in 1..a.len() {\n          if a[i] < a[i-1] {\n              let (v, mut j) = (a[i].clone(), i);\n              while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n              a[j] = v;\n          }\n      }\n  }\n- Metrics: score: 0.9895, combined_score: 0.9895, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9837, adaptability_score: 0.9981, avg_time: 0.0017, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9895)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9895), Performs well on combined_score (0.9895), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9838), Performs well on adaptability_score (0.9981), Performs well on avg_time (0.0016), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9895)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9895), Performs well on combined_score (0.9895), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9837), Performs well on adaptability_score (0.9981), Performs well on avg_time (0.0017), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9894)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 32 {\n        let (n, m, l) = (a.len(), a.len()/2, a.len()-1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9894), Performs well on combined_score (0.9894), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9836), Performs well on adaptability_score (0.9980), Performs well on avg_time (0.0017), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.9893)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9893), Performs well on combined_score (0.9893), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9835), Performs well on adaptability_score (0.9980), Performs well on avg_time (0.0017), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9893)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt - lt..];\n        if left.len() < right.len() { qsort(left); a = right } else { qsort(right); a = left }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let v = a[i].clone();\n            let mut j = i;\n            while j > 0 && a[j-1] > v {\n                a[j] = a[j-1].clone();\n                j -= 1;\n            }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D2 (Score: 0.9893)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = (if a[0] < p { 1 } else { 0 }, if a[l] > p { l } else { n }, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n### Program D3 (Score: 0.9893)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9886, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 { return is(a) }\n    let (m, l) = (n/2, n-1);\n    if a[0] > a[m] { a.swap(0, m) }\n    if a[0] > a[l] { a.swap(0, l) }\n    if a[m] > a[l] { a.swap(m, l) }\n    let p = a[m].clone();\n    let (mut lt, mut gt, mut i) = (0, n, 0);\n    while i < gt {\n        if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n        else if a[i] > p { gt -= 1; a.swap(i, gt) }\n        else { i += 1 }\n    }\n    let (left, rest) = a.split_at_mut(lt);\n    qs(left);\n    qs(&mut rest[gt-lt..]);\n}\nfn is<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9891, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.9873, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    if a.len() < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..a.len() {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let p = {\n            let (m, l) = (a.len() / 2, a.len() - 1);\n            if a[0] > a[m] { a.swap(0, m) }\n            if a[0] > a[l] { a.swap(0, l) }\n            if a[m] > a[l] { a.swap(m, l) }\n            a[m].clone()\n        };\n        let (mut lt, mut i, mut gt) = (0, 0, a.len());\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right }\n        else { qs(right); a = left }\n    }\n    is(a);\n}\n\nfn is<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.9867, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 20 {\n        let (m, l) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt-lt..];\n        if left.len() < right.len() { qsort(left); a = right }\n        else { qsort(right); a = left }\n    }\n    isort(a);\n}\n\nfn isort<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false } else if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 32 {\n        let (n, m, l) = (a.len(), a.len()/2, a.len()-1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n        if a[i] < a[i-1] {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1 }\n            a[j] = v;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a threshold of 32 for transitioning from QuickSort to Insertion Sort and implements a 3-way partition QuickSort with tail-recursion optimization. Based on the evolution history, a threshold of 24 (used in Attempt 2) resulted in a higher performance score. Furthermore, the system has suggested simplifying the code as the length exceeds 500 characters.\n\nThe following changes aim to:\n1.  **Improve Performance**: Reduce the QuickSort threshold from 32 to 24 to better balance recursive partitioning and insertion sort overhead.\n2.  **Simplify Code**: Use more compact syntax and idiomatic Rust to reduce character count while maintaining readability and the effective 3-way partitioning logic.\n3.  **Optimize Adaptive Checks**: Retain the efficient $O(n)$ check for already sorted or reverse-sorted arrays, which is key to the high adaptability score.\n\n### Change 1: Simplify `adaptive_sort` control flow\n\n<<<<<<< SEARCH\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n=======\n    if s { return }\n    if r { a.reverse() } else { qs(a) }\n}\n>>>>>>> REPLACE\n\n### Change 2: Optimize `qs` threshold and compact partition logic\n\n<<<<<<< SEARCH\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 32 {\n        let (n, m, l) = (a.len(), a.len()/2, a.len()-1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right; } else { qs(right); a = left; }\n    }\n    for i in 1..a.len() {\n=======\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let (n, m, l) = (a.len(), a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = ((a[0] < p) as usize, n - (a[l] > p) as usize, 1);\n        while i < gt {\n            if a[i] < p { a.swap(lt, i); lt += 1; i += 1 }\n            else if a[i] > p { gt -= 1; a.swap(i, gt) }\n            else { i += 1 }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt - lt..];\n        if left.len() < right.len() { qs(left); a = right } else { qs(right); a = left }\n    }\n    for i in 1..a.len() {\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"times\": [0.000463769, 0.00528169, 0.00022052, 0.002688109, 7.071e-06, 6.946e-05, 0.000128731, 0.002554829, 0.00043942, 0.00510745], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}