{"id": "f11b4962-67d9-4c48-bdff-c3bc47a7402a", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n        }\n        return\n    }\n    let (m, l) = (n/2, n-1);\n    if a[0] > a[m] { a.swap(0, m) }\n    if a[0] > a[l] { a.swap(0, l) }\n    if a[m] > a[l] { a.swap(m, l) }\n    let p = a[m].clone();\n    let (mut lt, mut gt, mut i) = (0, n, 0);\n    while i < gt {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n            Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n            _ => i += 1\n        }\n    }\n    let (left, right) = a.split_at_mut(gt);\n    qs(&mut left[..lt]);\n    qs(right);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "6ca1818c-1776-492f-bb0d-8507d958c7ff", "generation": 4, "timestamp": 1772737774.2867537, "iteration_found": 23, "metrics": {"score": 0.9871633037626901, "combined_score": 0.9871633037626901, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9801568356205836, "adaptability_score": 0.99767300597585, "avg_time": 0.0020244887000000003, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n      quicksort(arr);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 24 {\n          insertion_sort(arr);\n          return;\n      }\n      let mid = len / 2;\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n  \n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(lt, i);\n  ... (23 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\n  \n  fn qs<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 24 {\n          for i in 1..n {\n              let mut j = i;\n              while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n          }\n          return\n      }\n      let (m, l) = (n/2, n-1);\n      if a[0] > a[m] { a.swap(0, m) }\n      if a[0] > a[l] { a.swap(0, l) }\n      if a[m] > a[l] { a.swap(m, l) }\n      let p = a[m].clone();\n      let (mut lt, mut gt, mut i) = (0, n, 0);\n      while i < gt {\n  ... (10 more lines)", "parent_metrics": {"score": 0.9887543589608268, "combined_score": 0.9887543589608268, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9826211191958644, "adaptability_score": 0.9979542186082704, "avg_time": 0.0017686248000000002, "memory_safe": 1.0}, "island": 2}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.9888\n- Feature coordinates: score=0.99, performance_score=0.98, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.9867 \u2192 0.9888\n- Exploring score=0.99, performance_score=0.98, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[0.00046175, 0.005356459, 0.00025101, 0.00295773, 8.64e-06, 8.578e-05, 0.00014645, 0.00265697, 0.00044119, 0.005320269]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut asc, mut desc) = (true, true);\n      for i in 1..n {\n          if arr[i-1] > arr[i] { asc = false; }\n          if arr[i-1] < arr[i] { desc = false; }\n          if !asc && !desc { break; }\n      }\n      if asc { return; }\n      if desc { arr.reverse(); return; }\n      quicksort(arr);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 24 {\n          insertion_sort(arr);\n          return;\n      }\n      let mid = len / 2;\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n  \n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(lt, i);\n  ... (23 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qsort(a);\n  }\n  \n  fn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 20 {\n          let (m, l) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n          while i < gt {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                  Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                  _ => i += 1\n              }\n          }\n          let (left, rest) = a.split_at_mut(lt);\n  ... (13 more lines)\n- Metrics: score: 0.9867, combined_score: 0.9867, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9795, adaptability_score: 0.9975, avg_time: 0.0021, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..n {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qsort(a);\n  }\n  \n  fn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 20 {\n          let (m, l) = (a.len() / 2, a.len() - 1);\n          if a[0] > a[m] { a.swap(0, m) }\n          if a[0] > a[l] { a.swap(0, l) }\n          if a[m] > a[l] { a.swap(m, l) }\n          let p = a[m].clone();\n          let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n          while i < gt {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                  Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                  _ => i += 1\n              }\n          }\n          let (left, rest) = a.split_at_mut(lt);\n  ... (13 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      if a.len() < 2 { return }\n      let (mut s, mut r) = (true, true);\n      for i in 1..a.len() {\n          if a[i-1] > a[i] { s = false }\n          if a[i-1] < a[i] { r = false }\n          if !s && !r { break }\n      }\n      if s { return }\n      if r { a.reverse(); return }\n      qs(a);\n  }\n  \n  fn qs<T: Ord + Clone>(mut a: &mut [T]) {\n      while a.len() > 24 {\n          let p = {\n              let (m, l) = (a.len() / 2, a.len() - 1);\n              if a[0] > a[m] { a.swap(0, m) }\n              if a[0] > a[l] { a.swap(0, l) }\n              if a[m] > a[l] { a.swap(m, l) }\n              a[m].clone()\n          };\n          let (mut lt, mut i, mut gt) = (0, 0, a.len());\n          while i < gt {\n              match a[i].cmp(&p) {\n                  Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                  Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                  _ => i += 1\n              }\n          }\n  ... (14 more lines)\n- Metrics: score: 0.9873, combined_score: 0.9873, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9804, adaptability_score: 0.9977, avg_time: 0.0020, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 16 {\n          insertion_sort(arr);\n          return;\n      }\n      let p = partition(arr);\n      let (left, right) = arr.split_at_mut(p);\n      quicksort(left);\n      quicksort(&mut right[1..]);\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T]) -> usize {\n      let len = arr.len();\n      let m = len / 2;\n      if arr[0] > arr[m] { arr.swap(0, m); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[m] > arr[len-1] { arr.swap(m, len-1); }\n      arr.swap(m, len-1);\n      let pivot = arr[len-1].clone();\n      let mut i = 0;\n      for j in 0..len-1 {\n          if arr[j] <= pivot {\n              arr.swap(i, j);\n              i += 1;\n          }\n      }\n      arr.swap(i, len-1);\n      i\n  }\nwith:\n  fn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n      let len = arr.len();\n      if len < 24 {\n          insertion_sort(arr);\n          return;\n      }\n      let mid = len / 2;\n      if arr[0] > arr[mid] { arr.swap(0, mid); }\n      if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n      if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n  \n      let pivot = arr[mid].clone();\n      let (mut lt, mut gt, mut i) = (0, len, 0);\n      while i < gt {\n          if arr[i] < pivot {\n              arr.swap(lt, i);\n              lt += 1;\n              i += 1;\n          } else if arr[i] > pivot {\n              gt -= 1;\n              arr.swap(i, gt);\n          } else {\n              i += 1;\n          }\n      }\n      let (left, rest) = arr.split_at_mut(lt);\n      quicksort(left);\n      quicksort(&mut rest[gt-lt..]);\n  }\n- Metrics: score: 0.9888, combined_score: 0.9888, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9826, adaptability_score: 0.9980, avg_time: 0.0018, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.9888)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 24 {\n        insertion_sort(arr);\n        return;\n    }\n    let mid = len / 2;\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n    \n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(lt, i);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    let (left, rest) = arr.split_at_mut(lt);\n    quicksort(left);\n    quicksort(&mut rest[gt-lt..]);\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9888), Performs well on combined_score (0.9888), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9826), Performs well on adaptability_score (0.9980), Performs well on avg_time (0.0018), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.9873)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    if a.len() < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..a.len() {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let p = {\n            let (m, l) = (a.len() / 2, a.len() - 1);\n            if a[0] > a[m] { a.swap(0, m) }\n            if a[0] > a[l] { a.swap(0, l) }\n            if a[m] > a[l] { a.swap(m, l) }\n            a[m].clone()\n        };\n        let (mut lt, mut i, mut gt) = (0, 0, a.len());\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right }\n        else { qs(right); a = left }\n    }\n    is(a);\n}\n\nfn is<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9873), Performs well on combined_score (0.9873), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9804), Performs well on adaptability_score (0.9977), Performs well on avg_time (0.0020), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.9867)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 20 {\n        let (m, l) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt-lt..];\n        if left.len() < right.len() { qsort(left); a = right }\n        else { qsort(right); a = left }\n    }\n    isort(a);\n}\n\nfn isort<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9867), Performs well on combined_score (0.9867), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9795), Performs well on adaptability_score (0.9975), Performs well on avg_time (0.0021), Performs well on memory_safe (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9867, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 20 {\n        let (m, l) = (a.len() / 2, a.len() - 1);\n        if a[0] > a[m] { a.swap(0, m) }\n        if a[0] > a[l] { a.swap(0, l) }\n        if a[m] > a[l] { a.swap(m, l) }\n        let p = a[m].clone();\n        let (mut lt, mut gt, mut i) = (0, a.len(), 0);\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rest) = a.split_at_mut(lt);\n        let right = &mut rest[gt-lt..];\n        if left.len() < right.len() { qsort(left); a = right }\n        else { qsort(right); a = left }\n    }\n    isort(a);\n}\n\nfn isort<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.9873, Type: High-Performer)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    if a.len() < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..a.len() {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(mut a: &mut [T]) {\n    while a.len() > 24 {\n        let p = {\n            let (m, l) = (a.len() / 2, a.len() - 1);\n            if a[0] > a[m] { a.swap(0, m) }\n            if a[0] > a[l] { a.swap(0, l) }\n            if a[m] > a[l] { a.swap(m, l) }\n            a[m].clone()\n        };\n        let (mut lt, mut i, mut gt) = (0, 0, a.len());\n        while i < gt {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n                Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n                _ => i += 1\n            }\n        }\n        let (left, rem) = a.split_at_mut(lt);\n        let right = &mut rem[gt-lt..];\n        if left.len() < right.len() { qs(left); a = right }\n        else { qs(right); a = left }\n    }\n    is(a);\n}\n\nfn is<T: Ord>(a: &mut [T]) {\n    for i in 1..a.len() {\n        let mut j = i;\n        while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 24 {\n        insertion_sort(arr);\n        return;\n    }\n    let mid = len / 2;\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n    \n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(lt, i);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    let (left, rest) = arr.split_at_mut(lt);\n    quicksort(left);\n    quicksort(&mut rest[gt-lt..]);\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current sorting implementation (Program 1) is already highly efficient, but there is room for optimization in both code length and performance. By consolidating the `insertion_sort` into the `quicksort` function (now renamed `qs`), using idiomatic `match` for 3-way partitioning, and refining the adaptive scan, we can reduce boilerplate and potentially improve execution speed.\n\nThe following changes:\n1.  **Consolidate functions**: Moving the insertion sort logic directly into the recursive sort reduces function call overhead and code length.\n2.  **Idiomatic 3-way partitioning**: Switching from `if/else if/else` to `match a[i].cmp(&p)` is more idiomatic in Rust and can be more efficient for complex types.\n3.  **Refined Slicing**: Using more concise slicing and variable names while maintaining the core logic that produced the 0.9888 score.\n4.  **Tail call optimization hint**: The recursion is structured to be cleaner, which helps the compiler optimize the partitioning of large sub-arrays.\n\n```rust\n<<<<<<< SEARCH\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut asc, mut desc) = (true, true);\n    for i in 1..n {\n        if arr[i-1] > arr[i] { asc = false; }\n        if arr[i-1] < arr[i] { desc = false; }\n        if !asc && !desc { break; }\n    }\n    if asc { return; }\n    if desc { arr.reverse(); return; }\n    quicksort(arr);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T]) {\n    let len = arr.len();\n    if len < 24 {\n        insertion_sort(arr);\n        return;\n    }\n    let mid = len / 2;\n    if arr[0] > arr[mid] { arr.swap(0, mid); }\n    if arr[0] > arr[len-1] { arr.swap(0, len-1); }\n    if arr[mid] > arr[len-1] { arr.swap(mid, len-1); }\n    \n    let pivot = arr[mid].clone();\n    let (mut lt, mut gt, mut i) = (0, len, 0);\n    while i < gt {\n        if arr[i] < pivot {\n            arr.swap(lt, i);\n            lt += 1;\n            i += 1;\n        } else if arr[i] > pivot {\n            gt -= 1;\n            arr.swap(i, gt);\n        } else {\n            i += 1;\n        }\n    }\n    let (left, rest) = arr.split_at_mut(lt);\n    quicksort(left);\n    quicksort(&mut rest[gt-lt..]);\n}\n\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n=======\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return }\n    let (mut s, mut r) = (true, true);\n    for i in 1..n {\n        if a[i-1] > a[i] { s = false }\n        if a[i-1] < a[i] { r = false }\n        if !s && !r { break }\n    }\n    if s { return }\n    if r { a.reverse(); return }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && a[j-1] > a[j] { a.swap(j, j-1); j -= 1 }\n        }\n        return\n    }\n    let (m, l) = (n/2, n-1);\n    if a[0] > a[m] { a.swap(0, m) }\n    if a[0] > a[l] { a.swap(0, l) }\n    if a[m] > a[l] { a.swap(m, l) }\n    let p = a[m].clone();\n    let (mut lt, mut gt, mut i) = (0, n, 0);\n    while i < gt {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(lt, i); lt += 1; i += 1 }\n            Ordering::Greater => { gt -= 1; a.swap(i, gt) }\n            _ => i += 1\n        }\n    }\n    let (left, right) = a.split_at_mut(gt);\n    qs(&mut left[..lt]);\n    qs(right);\n}\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [0.000522109, 0.006199819, 0.000294349, 0.00358372, 1.038e-05, 0.000103471, 0.000154571, 0.002997039, 0.00050812, 0.005871309], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}