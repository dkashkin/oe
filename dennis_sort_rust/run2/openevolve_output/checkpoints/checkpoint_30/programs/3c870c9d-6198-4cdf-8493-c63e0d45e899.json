{"id": "3c870c9d-6198-4cdf-8493-c63e0d45e899", "code": "// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Adaptive hybrid sort: O(n) scan, median-of-three 3-way partition, and shifting insertion fallback.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n - 1 {\n        if a[i] > a[i + 1] { s = false; }\n        if a[i] < a[i + 1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (m, h) = (n / 2, n - 1);\n        if a[m] < a[0] { a.swap(0, m); }\n        if a[h] < a[0] { a.swap(0, h); }\n        if a[h] < a[m] { a.swap(m, h); }\n        let (p, mut l, mut i, mut g) = (a[m].clone(), 0, 0, n);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qs(&mut a[..l]); qs(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}", "changes_description": null, "language": "rust", "parent_id": "303b406a-36ea-42f0-b11b-39a42d6329b7", "generation": 2, "timestamp": 1772731048.786963, "iteration_found": 28, "metrics": {"score": 0.9988795474616483, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9982643676571727, "adaptability_score": 0.9998023171683615, "avg_time": 0.00017386500000000002, "memory_safe": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Hybrid sorting: O(n) check for sorted/reverse, median-of-three quicksort, and insertion sort f...\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n-1 {\n          if arr[i] > arr[i+1] { s = false; }\n          if arr[i] < arr[i+1] { r = false; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      quicksort(arr, 0, n - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) {\n      if l + 16 > h {\n          insertion_sort(&mut arr[l..=h]);\n      } else {\n          let p = partition(arr, l, h);\n          if p > 0 { quicksort(arr, l, p - 1); }\n          quicksort(arr, p + 1, h);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> usize {\n      let m = l + (h - l) / 2;\n      if arr[m] < arr[l] { arr.swap(l, m); }\n      if arr[h] < arr[l] { arr.swap(l, h); }\n      if arr[h] < arr[m] { arr.swap(m, h); }\n  ... (19 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Adaptive hybrid sort: O(n) scan, median-of-three 3-way partition, and shifting insertion fallb...\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n - 1 {\n          if a[i] > a[i + 1] { s = false; }\n          if a[i] < a[i + 1] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { a.reverse(); return; }\n      qs(a);\n  }\n  \n  fn qs<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 24 {\n          for i in 1..n {\n              let (v, mut j) = (a[i].clone(), i);\n              while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n              a[j] = v;\n          }\n      } else {\n          let (m, h) = (n / 2, n - 1);\n          if a[m] < a[0] { a.swap(0, m); }\n          if a[h] < a[0] { a.swap(0, h); }\n          if a[h] < a[m] { a.swap(m, h); }\n          let (p, mut l, mut i, mut g) = (a[m].clone(), 0, 0, n);\n  ... (11 more lines)", "parent_metrics": {"score": 0.998789306722758, "compile_success": 1.0, "correctness": 1, "performance_score": 0.9981286983350746, "adaptability_score": 0.9997802193042832, "avg_time": 0.00018748100000000003, "memory_safe": 1.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: 0.7500\n- Feature coordinates: score=1.00, performance_score=1.00, adaptability_score=1.00\n- Focus areas: - Fitness improved: 0.7500 \u2192 0.7500\n- Exploring score=1.00, performance_score=1.00, adaptability_score=1.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### times\n```\n[3.575e-05, 0.00046993, 1.674e-05, 0.00024709, 3.1e-07, 2.6e-06, 5.221e-05, 0.00062783, 3.062e-05, 0.00039173]\n```\n\n### all_correct\n```\nTrue\n```\n\n### build_output\n```\n\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 4\n- Changes: Change 1: Replace:\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n-1 {\n          if arr[i] > arr[i+1] { s = false; }\n          if arr[i] < arr[i+1] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      qsort(arr);\n  }\n  \n  fn qsort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > v {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n          return;\n      }\n      let (m, h) = (n / 2, n - 1);\n      if arr[m] < arr[0] { arr.swap(0, m); }\n  ... (15 more lines)\nwith:\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n - 1 {\n          if a[i] > a[i + 1] { s = false; }\n          else if a[i] < a[i + 1] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { a.reverse(); return; }\n      qsort(a);\n  }\n  \n  fn qsort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 24 {\n          for i in 1..n {\n              let (v, mut j) = (a[i].clone(), i);\n              while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n              a[j] = v;\n          }\n      } else {\n          let (mut l, mut i, mut g, p) = (0, 0, n, {\n              let (m, h) = (n / 2, n - 1);\n              if a[m] < a[0] { a.swap(0, m); }\n              if a[h] < a[0] { a.swap(0, h); }\n              if a[h] < a[m] { a.swap(m, h); }\n              a[m].clone()\n          });\n  ... (10 more lines)\n- Metrics: score: 0.9989, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9982, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  // EVOLVE-BLOCK-START\n  // Initial implementation: Simple quicksort\n  // This can be evolved to:\n  // - Hybrid algorithms (introsort, timsort-like)\n  // - Adaptive pivot selection\n  // - Special handling for nearly sorted data\n  // - Switching to different algorithms based on data characteristics\n  \n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      if arr.len() <= 1 {\n          return;\n      }\n  \n      // Use quicksort as the base implementation\n      quicksort(arr, 0, arr.len() - 1);\n  }\n  \n  fn quicksort<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) {\n      if low < high {\n          let pivot_index = partition(arr, low, high);\n  \n          // Recursively sort elements before and after partition\n          if pivot_index > 0 {\n              quicksort(arr, low, pivot_index - 1);\n          }\n          quicksort(arr, pivot_index + 1, high);\n      }\n  }\n  \n  fn partition<T: Ord + Clone>(arr: &mut [T], low: usize, high: usize) -> usize {\n  ... (49 more lines)\nwith:\n  // EVOLVE-BLOCK-START\n  // Hybrid adaptive sort: O(n) scan, 3-way partition with median-of-three, and insertion fallback.\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n - 1 {\n          if a[i] > a[i + 1] { s = false; }\n          if a[i] < a[i + 1] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { a.reverse(); return; }\n      qs(a);\n  }\n  \n  fn qs<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 24 {\n          for i in 1..n {\n              let (v, mut j) = (a[i].clone(), i);\n              while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n              a[j] = v;\n          }\n      } else {\n          let (m, h) = (n / 2, n - 1);\n          if a[m] < a[0] { a.swap(0, m); }\n          if a[h] < a[0] { a.swap(0, h); }\n          if a[h] < a[m] { a.swap(m, h); }\n          let (p, mut l, mut i, mut g) = (a[m].clone(), 0, 0, n);\n  ... (11 more lines)\n- Metrics: score: 0.9989, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9982, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  // Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\n  pub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n-1 {\n          if arr[i] > arr[i+1] { s = false; }\n          if arr[i] < arr[i+1] { r = false; }\n          if !s && !r { break; }\n      }\n      if s { return; }\n      if r { arr.reverse(); return; }\n      qsort(arr);\n  }\n  \n  fn qsort<T: Ord + Clone>(arr: &mut [T]) {\n      let n = arr.len();\n      if n < 24 {\n          for i in 1..n {\n              let v = arr[i].clone();\n              let mut j = i;\n              while j > 0 && arr[j - 1] > v {\n                  arr[j] = arr[j - 1].clone();\n                  j -= 1;\n              }\n              arr[j] = v;\n          }\n          return;\n      }\n      let (m, h) = (n / 2, n - 1);\n  ... (16 more lines)\nwith:\n  // Hybrid adaptive sort: O(n) scan, 3-way partition with median-of-three, and insertion fallback.\n  pub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 2 { return; }\n      let (mut s, mut r) = (true, true);\n      for i in 0..n-1 {\n          if a[i] > a[i+1] { s = false; }\n          if a[i] < a[i+1] { r = false; }\n          if !(s || r) { break; }\n      }\n      if s { return; }\n      if r { a.reverse(); return; }\n      qs(a);\n  }\n  \n  fn qs<T: Ord + Clone>(a: &mut [T]) {\n      let n = a.len();\n      if n < 24 {\n          for i in 1..n {\n              let v = a[i].clone();\n              let mut j = i;\n              while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1; }\n              a[j] = v;\n          }\n      } else {\n          let (m, h) = (n / 2, n - 1);\n          if a[m] < a[0] { a.swap(0, m); }\n          if a[h] < a[0] { a.swap(0, h); }\n          if a[h] < a[m] { a.swap(m, h); }\n          let (mut l, mut i, mut g, p) = (0, 0, n, a[m].clone());\n  ... (10 more lines)\n- Metrics: score: 0.9989, compile_success: 1.0000, correctness: 1.0000, performance_score: 0.9983, adaptability_score: 0.9998, avg_time: 0.0002, memory_safe: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n        return;\n    }\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), 0);\n    for j in 0..h {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    let (left, right) = arr.split_at_mut(i);\n    qsort(left);\n    qsort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9989), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9983), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 2 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Hybrid adaptive sort: O(n) scan, 3-way partition with median-of-three, and insertion fallback.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if a[i] > a[i+1] { s = false; }\n        if a[i] < a[i+1] { r = false; }\n        if !(s || r) { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = a[i].clone();\n            let mut j = i;\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (m, h) = (n / 2, n - 1);\n        if a[m] < a[0] { a.swap(0, m); }\n        if a[h] < a[0] { a.swap(0, h); }\n        if a[h] < a[m] { a.swap(m, h); }\n        let (mut l, mut i, mut g, p) = (0, 0, n, a[m].clone());\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qs(&mut a[..l]); qs(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9989), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9983), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 3 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Hybrid adaptive sort: O(n) scan, 3-way partition with median-of-three, and insertion fallback.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n - 1 {\n        if a[i] > a[i + 1] { s = false; }\n        if a[i] < a[i + 1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (m, h) = (n / 2, n - 1);\n        if a[m] < a[0] { a.swap(0, m); }\n        if a[h] < a[0] { a.swap(0, h); }\n        if a[h] < a[m] { a.swap(m, h); }\n        let (p, mut l, mut i, mut g) = (a[m].clone(), 0, 0, n);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qs(&mut a[..l]); qs(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9989), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9982), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n### Program 4 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n - 1 {\n        if a[i] > a[i + 1] { s = false; }\n        else if a[i] < a[i + 1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (mut l, mut i, mut g, p) = (0, 0, n, {\n            let (m, h) = (n / 2, n - 1);\n            if a[m] < a[0] { a.swap(0, m); }\n            if a[h] < a[0] { a.swap(0, h); }\n            if a[h] < a[m] { a.swap(m, h); }\n            a[m].clone()\n        });\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qsort(&mut a[..l]); qsort(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Performs well on score (0.9989), Performs well on compile_success (1.0000), Performs well on correctness (1.0000), Performs well on performance_score (0.9982), Performs well on adaptability_score (0.9998), Performs well on avg_time (0.0002), Performs well on memory_safe (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) scan, median-of-three, 3-way partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = a[i].clone();\n            let mut j = i;\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1; }\n            a[j] = v;\n        }\n        return;\n    }\n    let m = n / 2;\n    if a[m] < a[0] { a.swap(0, m); }\n    if a[n-1] < a[0] { a.swap(0, n-1); }\n    if a[n-1] < a[m] { a.swap(m, n-1); }\n    let p = a[m].clone();\n    let (mut l, mut i, mut g) = (0, 0, n);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; a.swap(i, g); }\n            _ => i += 1,\n        }\n    }\n    qsort(&mut a[..l]); qsort(&mut a[g..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to compile_success\n\n### Program D2 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Compact hybrid sort: O(n) adaptive checks + optimized quicksort with insertion fallback\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    sort(arr);\n}\n\nfn sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 17 {\n        for i in 1..n {\n            let mut j = i;\n            while j > 0 && arr[j - 1] > arr[j] {\n                arr.swap(j, j - 1);\n                j -= 1;\n            }\n        }\n        return;\n    }\n    let m = n / 2;\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[n-1] < arr[0] { arr.swap(0, n-1); }\n    if arr[n-1] < arr[m] { arr.swap(m, n-1); }\n    arr.swap(m, n-1);\n    let p = arr[n-1].clone();\n    let mut i = 0;\n    for j in 0..n-1 {\n        if arr[j] <= p {\n            arr.swap(i, j);\n            i += 1;\n        }\n    }\n    arr.swap(i, n-1);\n    let (l, r) = arr.split_at_mut(i);\n    sort(l);\n    sort(&mut r[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to compile_success\n\n### Program D3 (Score: 0.7500)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n        return;\n    }\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), 0);\n    for j in 0..h {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    let (left, right) = arr.split_at_mut(i);\n    qsort(left);\n    qsort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nKey features: Alternative approach to score, Alternative approach to compile_success\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.7500, Type: Alternative)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) scan, median-of-three, 3-way partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = a[i].clone();\n            let mut j = i;\n            while j > 0 && a[j-1] > v { a[j] = a[j-1].clone(); j -= 1; }\n            a[j] = v;\n        }\n        return;\n    }\n    let m = n / 2;\n    if a[m] < a[0] { a.swap(0, m); }\n    if a[n-1] < a[0] { a.swap(0, n-1); }\n    if a[n-1] < a[m] { a.swap(m, n-1); }\n    let p = a[m].clone();\n    let (mut l, mut i, mut g) = (0, 0, n);\n    while i < g {\n        match a[i].cmp(&p) {\n            Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n            Ordering::Greater => { g -= 1; a.swap(i, g); }\n            _ => i += 1,\n        }\n    }\n    qsort(&mut a[..l]); qsort(&mut a[g..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.7500, Type: Alternative)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n        return;\n    }\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), 0);\n    for j in 0..h {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    let (left, right) = arr.split_at_mut(i);\n    qsort(left);\n    qsort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.7500, Type: Alternative)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n - 1 {\n        if a[i] > a[i + 1] { s = false; }\n        else if a[i] < a[i + 1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qsort(a);\n}\n\nfn qsort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (mut l, mut i, mut g, p) = (0, 0, n, {\n            let (m, h) = (n / 2, n - 1);\n            if a[m] < a[0] { a.swap(0, m); }\n            if a[h] < a[0] { a.swap(0, h); }\n            if a[h] < a[m] { a.swap(m, h); }\n            a[m].clone()\n        });\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qsort(&mut a[..l]); qsort(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 4 (Score: 0.7500, Type: Alternative)\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Optimized hybrid sort: O(n) check, median-of-three Lomuto partition, and shifting insertion sort.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    qsort(arr);\n}\n\nfn qsort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 24 {\n        for i in 1..n {\n            let v = arr[i].clone();\n            let mut j = i;\n            while j > 0 && arr[j - 1] > v {\n                arr[j] = arr[j - 1].clone();\n                j -= 1;\n            }\n            arr[j] = v;\n        }\n        return;\n    }\n    let (m, h) = (n / 2, n - 1);\n    if arr[m] < arr[0] { arr.swap(0, m); }\n    if arr[h] < arr[0] { arr.swap(0, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), 0);\n    for j in 0..h {\n        if arr[j] <= pivot {\n            if i != j { arr.swap(i, j); }\n            i += 1;\n        }\n    }\n    arr.swap(i, h);\n    let (left, right) = arr.split_at_mut(i);\n    qsort(left);\n    qsort(&mut right[1..]);\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```rust\n// Adaptive Sorting Algorithm Implementation\n// This program implements a sorting algorithm that can be evolved to adapt to different data patterns\n\nuse std::cmp::Ordering;\n\n// EVOLVE-BLOCK-START\n// Hybrid sorting: O(n) check for sorted/reverse, median-of-three quicksort, and insertion sort for small arrays.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) {\n    if l + 16 > h {\n        insertion_sort(&mut arr[l..=h]);\n    } else {\n        let p = partition(arr, l, h);\n        if p > 0 { quicksort(arr, l, p - 1); }\n        quicksort(arr, p + 1, h);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> usize {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(l, m); }\n    if arr[h] < arr[l] { arr.swap(l, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), l);\n    for j in l..h {\n        if arr[j] <= pivot { arr.swap(i, j); i += 1; }\n    }\n    arr.swap(i, h); i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n\n// Benchmark function to test the sort implementation\npub fn run_benchmark(test_data: Vec<Vec<i32>>) -> BenchmarkResults {\n    let mut results = BenchmarkResults {\n        times: Vec::new(),\n        correctness: Vec::new(),\n        adaptability_score: 0.0,\n    };\n    \n    for data in test_data {\n        let mut arr = data.clone();\n        let start = std::time::Instant::now();\n        \n        adaptive_sort(&mut arr);\n        \n        let elapsed = start.elapsed();\n        results.times.push(elapsed.as_secs_f64());\n        \n        // Check if correctly sorted\n        let is_sorted = arr.windows(2).all(|w| w[0] <= w[1]);\n        results.correctness.push(is_sorted);\n    }\n    \n    // Calculate adaptability score based on performance variance\n    if results.times.len() > 1 {\n        let mean_time: f64 = results.times.iter().sum::<f64>() / results.times.len() as f64;\n        let variance: f64 = results.times.iter()\n            .map(|t| (t - mean_time).powi(2))\n            .sum::<f64>() / results.times.len() as f64;\n        \n        // Lower variance means better adaptability\n        results.adaptability_score = 1.0 / (1.0 + variance.sqrt());\n    }\n    \n    results\n}\n\n#[derive(Debug)]\npub struct BenchmarkResults {\n    pub times: Vec<f64>,\n    pub correctness: Vec<bool>,\n    pub adaptability_score: f64,\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    \n    #[test]\n    fn test_basic_sort() {\n        let mut arr = vec![3, 1, 4, 1, 5, 9, 2, 6];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![1, 1, 2, 3, 4, 5, 6, 9]);\n    }\n    \n    #[test]\n    fn test_empty_array() {\n        let mut arr: Vec<i32> = vec![];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![]);\n    }\n    \n    #[test]\n    fn test_single_element() {\n        let mut arr = vec![42];\n        adaptive_sort(&mut arr);\n        assert_eq!(arr, vec![42]);\n    }\n}\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: score, performance_score, adaptability_score\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a hybrid approach with a Lomuto partition quicksort, which is known to perform poorly on certain datasets (especially those with many duplicate elements or nearly sorted data) and is generally less efficient than 3-way partitioning. Additionally, the implementation uses separate functions and manual index management, which increases code length and potential for errors.\n\nTo improve the **FITNESS SCORE**, especially the **performance_score** and **adaptability_score**, and to simplify the code (as requested by the system), I'm suggesting the following improvements:\n\n1.  **3-Way Partitioning (Dutch National Flag):** This handles arrays with many duplicate elements in $O(n)$ time and is generally more robust against various data distributions.\n2.  **Median-of-Three Pivot Selection:** This helps avoid the $O(n^2)$ worst-case performance of quicksort on already sorted or nearly sorted data.\n3.  **Shifting Insertion Sort:** The current implementation uses `swap` in the insertion sort, which performs three assignments per swap. A shifting insertion sort performs only one assignment per shift, making it significantly faster for the small sub-arrays where it is used.\n4.  **Slice-Based Recursion:** By using Rust slices instead of manual index management (`l`, `h`), the code becomes more idiomatic, easier to read, and reduces the risk of bounds-checking overhead and off-by-one errors.\n5.  **Concise Implementation:** Combining the logic into a more streamlined recursive structure reduces the total code length while maintaining (and improving) functionality.\n\n```rust\n<<<<<<< SEARCH\n// EVOLVE-BLOCK-START\n// Hybrid sorting: O(n) check for sorted/reverse, median-of-three quicksort, and insertion sort for small arrays.\npub fn adaptive_sort<T: Ord + Clone>(arr: &mut [T]) {\n    let n = arr.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n-1 {\n        if arr[i] > arr[i+1] { s = false; }\n        if arr[i] < arr[i+1] { r = false; }\n    }\n    if s { return; }\n    if r { arr.reverse(); return; }\n    quicksort(arr, 0, n - 1);\n}\n\nfn quicksort<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) {\n    if l + 16 > h {\n        insertion_sort(&mut arr[l..=h]);\n    } else {\n        let p = partition(arr, l, h);\n        if p > 0 { quicksort(arr, l, p - 1); }\n        quicksort(arr, p + 1, h);\n    }\n}\n\nfn partition<T: Ord + Clone>(arr: &mut [T], l: usize, h: usize) -> usize {\n    let m = l + (h - l) / 2;\n    if arr[m] < arr[l] { arr.swap(l, m); }\n    if arr[h] < arr[l] { arr.swap(l, h); }\n    if arr[h] < arr[m] { arr.swap(m, h); }\n    arr.swap(m, h);\n    let (pivot, mut i) = (arr[h].clone(), l);\n    for j in l..h {\n        if arr[j] <= pivot { arr.swap(i, j); i += 1; }\n    }\n    arr.swap(i, h); i\n}\n\n// Helper function for insertion sort (useful for small arrays)\nfn insertion_sort<T: Ord>(arr: &mut [T]) {\n    for i in 1..arr.len() {\n        let mut j = i;\n        while j > 0 && arr[j - 1] > arr[j] {\n            arr.swap(j, j - 1);\n            j -= 1;\n        }\n    }\n}\n// EVOLVE-BLOCK-END\n=======\n// EVOLVE-BLOCK-START\n// Adaptive hybrid sort: O(n) scan, median-of-three 3-way partition, and shifting insertion fallback.\npub fn adaptive_sort<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 2 { return; }\n    let (mut s, mut r) = (true, true);\n    for i in 0..n - 1 {\n        if a[i] > a[i + 1] { s = false; }\n        if a[i] < a[i + 1] { r = false; }\n        if !s && !r { break; }\n    }\n    if s { return; }\n    if r { a.reverse(); return; }\n    qs(a);\n}\n\nfn qs<T: Ord + Clone>(a: &mut [T]) {\n    let n = a.len();\n    if n < 24 {\n        for i in 1..n {\n            let (v, mut j) = (a[i].clone(), i);\n            while j > 0 && a[j - 1] > v { a[j] = a[j - 1].clone(); j -= 1; }\n            a[j] = v;\n        }\n    } else {\n        let (m, h) = (n / 2, n - 1);\n        if a[m] < a[0] { a.swap(0, m); }\n        if a[h] < a[0] { a.swap(0, h); }\n        if a[h] < a[m] { a.swap(m, h); }\n        let (p, mut l, mut i, mut g) = (a[m].clone(), 0, 0, n);\n        while i < g {\n            match a[i].cmp(&p) {\n                Ordering::Less => { a.swap(l, i); l += 1; i += 1; }\n                Ordering::Greater => { g -= 1; a.swap(i, g); }\n                _ => i += 1,\n            }\n        }\n        qs(&mut a[..l]); qs(&mut a[g..]);\n    }\n}\n// EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```"]}}, "artifacts_json": "{\"times\": [4.01e-05, 0.00053339, 2.303e-05, 0.00030011, 1.09e-06, 1.021e-05, 1.652e-05, 0.00030818, 3.721e-05, 0.00046881], \"all_correct\": true, \"build_output\": \"\"}", "artifact_dir": null, "embedding": null}